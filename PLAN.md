# 参赛方案：自研薄管线

> 制定时间：2026-09-20。主线决策：**自研薄管线**（用户批准）。
> 相关文档：`README.md`（赛制）、`ACCOUNT.md`（账号）、`raw/`（原始抓取）、`class/`（课程转录稿，含 ASR 错误）。本文件是接下来十天的工作纲领。

---

## 1. 目标与判定

**远期目标：进入初赛 Top 20。近期目标（2026-09-21 重订，口径分工见下）：榜上有分。**

> **为什么要把目标写成两级**：过去 24 小时最大的损失不是代码质量，是**被测的东西选错了**——
> 实际在优化"管线正确"（quickstart 满分），而记分的是"主赛道非零 × 6"。
> 把近期目标明确成**榜上有分**，是为了让**每一个闸门都能回答"它是否让主赛道多了一条通过"**。
> **"榜上有分"同时也是 Top 20 的必要条件**——不在榜上就没有排名可言，所以两级不冲突。
>
> 🔴 **一条要小心的论证（别把它原样继承）**：`reviews/2026-09-21-全局建议-给方案会话.md` R4 的
> **结论**（先追榜上有分）成立，但**论据不成立**——它写"Senior 门槛是 ≥80%，而我们最好 3.1%，
> 所以 Top 20 不现实"。**Senior/Junior 是"展示档"，不是"排名门槛"**：
> 榜单按通过率排名，Junior 档的条目**同样参与排名**。
> 榜单实测（2026-09-20 15:5x，`docs/09 §6`）15 行，前 6 名 82.1–100.0——
> 这说明"名次靠前需要高通过率"（**这才是 Top 20 的现实门槛**），而不是"低于 80% 就进不去"。
> → **所以两级的正确关系是"先上分、再谈名次"，不是"放弃名次"。**
>
> 📌 **分母怎么拿（2026-09-22 补，便宜观测）**：主赛道榜单（`/competition?competition=arc-bench-web`）
> 的**行数会随别人提交而涨**。**每次观测就把行数记进 `STATUS.md`**——
> **每一次观测本身就是一个真实分母**（比问谁都硬），**不需要挑日子**。
> 两条已有观察：① **"聚合分要求跑完 6 个任务"本身就是一道很强的筛子**，
> 大部分队伍未必跑得完 → "榜上有分"对应的名次可能比直觉好；
> ② 9/20 的快照是 **15 行**（那还是初赛开始前）。

判定链条（按优先级，前一项不过后一项无意义）：

1. **跑得完 ∧ 非零通过** — 同一份提交完成主赛道全部 6 个任务，**且至少部分功能可用**。
   平台原文：*A complete leaderboard score is calculated only when one submission has a completed run for every task in this competition.*
   ⚠️ **实测修正（2026-09-20，提交二）**：原先只写"跑得完"，**不够**。
   提交二让平台把每一步都跑通（装依赖 / 构建 / 起服务 3000 可达 / 6 个任务的测试全部执行），
   但 **0 通过 → 六任务各 0.0% → 不在榜上**。所以第一道闸比"跑完"严：**跑完 ∧ 至少部分功能可用**。
   **"0 通过"与"没提交"在榜上是等价的。**

   > 🔴 **"每个任务非零"是我们的保守读法，不是平台事实**（2026-09-21 重订时标注）。
   > 平台原文只说 `a completed run for every task`，**没说每个任务都要有通过**。两种读法：
   > **(a) 按任务**（每个 app 都要 ≥1 条通过）／**(b) 按聚合**（允许个别 0 分，平均 > 0）。
   > **两者的工作量差约 6 倍**，而现有唯一观测（6×0.0% 不在榜）**无法区分**它们。
   > → **本方案按 (a) 排期（保守），但把"1 个主赛道 app 非零"当成两种读法下的共同前提**——
   > 所以它排在关键路径最前，且**与口径之争无关**。口径由第一次主赛道平台跑答掉（见 §7 M3b-1 的 A2）。
2. **上榜** — 榜分两级：**Senior（平均通过率 ≥ 80%）**展示 Avg. Pass Rate / Feature Coverage / Efficiency / Token / Runtime；
   **Junior（< 80%）**只展示排名 / 用户 / 通过率 / 覆盖度。**进 Junior 就是"有分"。**
3. **比性价比** — 通过率达标后按 `pass/CNY` 排序。

**明确的目标函数**：`通过率 / 人民币`。不是通过率，不是速度。参照榜单实测——同样 100% 通过率，Efficiency 从 5.42 到 526 到 7,692,307，**离散五个数量级，唯一变量是 token**。

**因此本方案的第一性约束**：任何设计决策先问"这省不省 token"，再问"这提不提通过率"。
两难时选省 token——但**前提是通过率已经越过地板**（跑完 ∧ 非零通过）。地板以下，token 省得再多也没意义。

> ⚠️ **但"五个数量级"这个说法要用得小心**：榜单前 6 名里有多个队伍 **token ≈ 0 / runtime ≈ 0 却拿到 80–100%**。
> 要么他们的应用不是现场生成的（那会撞"不得预埋答案"的红线），要么**计量口径有我们还没理解的机制**。
> **在搞清之前不要按榜单调整成本策略。** 见 §8 R10。

### 不做什么（写下来，防止收尾时才心疼）

- ❌ 不做 Android / CLI 应用类型（只做 web）
- ❌ 不做可视化面板（不计分，平台自己有）
- ❌ 不追 100% 通过率（追"稳定过线 + 最低 token"）
- ❌ 不通读 ARC 那 12k 行（只读三个文件，其余报错驱动）
- ❌ 不做并行化（单端口 E2E 是硬串行；且省的是墙钟不是 token）
- ❌ 不在初赛期做增量编译（那是决赛的事，初赛只需预留接口）

**2026-09-22 的砍单（依据 `Q15` 的答复；当时初赛只剩 8 天）**

- ❌ **视觉 / 多模态通路：砍建设，保留排查。** 动机例已被证伪（§C1），
  **而排查是零 token 的**（就是"靶子到底有没有送到写代码那次调用"这一类检查）。
  → 它仍留在 M4 的可选项里，但**不再争任何优先级**。
- ❌ **M4 的降本实验：砍掉，只留记账。** R9 已把 reasoning 定为不可优化项，
  剩下的杠杆（闭环形态 / 分块）**已经在关键路径里做掉了**。
- 🔶 **决赛预留接口：不主动维护，但不要破坏。** 正确的形态是"**别为了短期改动
  把架构改成阻碍增量的形状**"——零成本，不需要专门投入。
- ❌ **a11y 第 5 条的精度继续打磨（70% → 更高）：停。**
  **因为刚量出来的瓶颈不在抽取质量，在交付**（`Q19.a`：抽到了、又被降级丢掉）。
  → **先把交付修好，再看精度还剩多少影响。** 这是"不要修不存在的问题"那条纪律的直接应用。

### 待验证项：**哪些会动结构，哪些只动细节**

按"验完会不会改变方案的形状"分两列。**这一列之分就是本节的用处**——
右列的项即便验出坏结果，也只是加工作量；**左列的项验出坏结果会推翻路线**。

| # | 待验证 | 最便宜的验法 | 影响的**是结构**还是**只是细节** |
|---|---|---|---|
| **V-1** | 🔴 **我们的生成能力是否成立**：能不能在一个 app 上拿到**非零通过** | **M3a 能力探针**：`example/ticketbooking-quickstart`（1 个需求）+ Lab04 的 6 条验证测试 | 🔴 **结构**。这是唯一会推翻 §2 路线决策的项：通过 → 自研为主线；**结构性做不出 → 翻到 ARC 主线** |
| V-2 | **规模**：从 1 个需求铺到 86 个 | M3b：6 个 app 都非零 | 只是细节（排期与资源）。结构不变——M3 的闸门无论怎样都是"6 个都非零" |
| V-3 | **C5 / C10 由谁满足**（`db:prepare:e2e`、`ARC_DB_FILE`） | 查平台 Stdout 有没有调这两个；或下一次提交局部验证 | 只是细节。只决定 `pipeline/generate/` 要不要生成那两个脚本 |
| **V-4** | 🔴 **R10 的计量机制**（0-token 却 100%） | **当天可做的可证伪预测**（不必等提问）：我们自己的 run（stage 1 + 拷模板，**零 LLM 调用**）应当显示 `Runtime ≈ 0–2s`、`token 0.00M`。若属实，而榜单前几名同样是 `0s / 0.00M`，则 **`Runtime` 量的是 agent 阶段而不是整跑墙钟**——那些 0-token 高通过率条目就是"**agent 没做生成工作**"。<br>（另可 bbs 提问 + 与 meter 对账。） | 🔴 **结构**。它可能**推翻 M4 的整个前提**（"省 token 是不是有效杠杆"） |
| **V-5** | 🔴 **平台墙钟 / token 配额上限** | bbs 提问，**优先问墙钟上限**，其次配额与"能否分次累积" | 🔴 **可能动结构（改判）**。单次 run 的**墙钟上限直接决定"一次 run 内能塞进多少次生成调用"**，也就决定 V-1 那个"**非零**"够不够得着。够不着 → 不上榜 → 路线不成立。**这不是排期，是结构。** |
| **V-6** | 🔴 **"非零"的判定口径**：<br>(a) **按任务**要求非零（每个任务都要 ≥1 条通过）<br>(b) **按聚合**要求非零（允许个别任务 0 分，只要平均 > 0） | **用现有快照只在一个任务上做到部分通过**（如 keep，32 条过 3–5 条）再跑一次，看：① 该 run 是否被标 `completed`（而不是像 0/86 那样 failed）；② 榜单是否出现条目。<br>**顺带回答"同一快照能否反复跑任务"**——它决定 M3b 的操作模型（能否在同一快照上多轮迭代而不重新上传）。 | 🔴 **结构**。两者的**工作量差很多**：(a) 要求每个 app 都铺开；(b) 允许先集中做一个。**M3b 的目标怎么写取决于这条。** |

**结论：V-1 / V-4 / V-5 / V-6 会动结构，V-2 / V-3 只动细节。**

其中前三项都有现成出口，**只有 V-6 会改 M3b 的目标写法**——所以它要在 M3b 开工前答掉。

- **V-1 的出口**：§2 已写明 ARC 是"零件库 + 防线"，§7 M2 已写明"若自研管线卡死，ARC + 修复壳仍是可提交的备选"。
  **备选路线已经写好了，只是没被排进日程**——所以不需要改写 §2，只需要给探针设一个**分叉点**（见 M3a）。
- **V-4 的出口**：M4 已加前提警告 + "不得在 M3 达成之前启动"。
  **即使验出"省 token 无意义"，受影响的只有 M4**，M1/M2/M3/M5 全不受影响。

→ **所以整个方案不需要大变动，只需要加一道额外步骤（M3a 能力探针）+ 一张分叉表。**
   测完才知道的是**数值**（通过率落在哪个区间），**不是结构**——三种出口现在都有预案。

---

## 2. 路线决策与理由

| 路线 | 结论 | 理由 |
|---|---|---|
| **自研薄管线** | ✅ **主线** | 契约极薄（`main.py` + `requirements.txt`）；事件 SDK 可整包复用（MIT）；token 结构完全可控 |
| ARC 官方实现 | ⚠️ 零件库 + 保底 | 任务理解最深，但 token 结构性劣势：严格串行 DFS、每叶子至少 3 场会话、TDD 最多 15 次往返。官方内置 ARC 在**只有 2 条测试**的 Smoke 赛道烧 9.09M token（对手 0.06M） |
| Octos | ❌ 放弃 | 唯一实测锚点是作者本人 50% 通过率；"内核现装"意味着本地调试与平台运行不是同一份二进制 |

**保底纪律（风险控制的全部）**：**在任何新工作铺开之前，先提交一次保底包**，确保榜上有名。之后只有在同一任务上"通过率不低于保底且 token 更低"时才替换。

> ⚠️ **保底包不能是"原样打包的 ARC"。** 六个赛题里有 2 个的需求文件不是合法 YAML
> （bookstack / keep，`ScannerError`），而 ARC 的加载器就是裸 `yaml.safe_load`
> （`core/files.py:11-23`），**会在这两个文件上直接抛异常**。主赛道计分要求跑完全部
> 6 个任务，2 个任务无完成记录 = **整条榜单分不成立**。所以"原样 ARC"这条保底路线
> 会白丢分。
>
> **保底包必须是「ARC + 输入修复壳」**：在调用 ARC 的加载器之前，先把需求文本里的
> 缩进错误修好（`pipeline/reqcompile/yamlrepair.py` 已实现该思路）。
> 完整推理见 `docs/06-需求契约实测.md` §一。

---

## 3. 架构

### 3.1 提交物形态

```
bundle.zip                     ← 上传到 ARC-Bench
├── main.py                    ← 入口，契约冻结
├── requirements.txt           ← 只增不减
├── arc_runtime/               ← 从 ARC 复制的 arcbench_agent_runtime（MIT）
├── reqcompile/                ← 需求解析与契约抽取
├── design/                    ← 批量设计
├── generate/                  ← 代码生成
├── verify/                    ← 验证与闸门
├── report/                    ← traceability 与事件
└── templates/web-react-express/  ← 从 arc-template 复制的应用骨架
```

**硬性契约**（来自平台 API 文档与 ARC 源码）：

- zip 第一层必须直接是 `main.py`，不能多套一层目录（讲师点名的第一大坑）
- **包根目录**不能有 `package.json` / `index.js` / `index.ts`（会触发 runner 的 Node 入口路径）。
  ⚠️ 这条只管**根层**。`templates/web-react-express/backend/package.json` 这类**子目录**里的
  `package.json` 是必须存在的（模板契约 C4/C5 要求），**不违规**。检查时用
  `unzip -l bundle.zip | grep -E '^[^/]+/(package\.json|index\.[jt]s)$'` 只看根层，不要全仓 grep。
- 不塞 `.env`（bundle 里 `.env` 的加载路径会指向上一级，且平台注入的环境变量优先级更高）
- 必须调 `arcbench_agent_runtime` 写 `.arc/runner-events.jsonl` 与 `.arc/traceability/*.json`；**不得手工构造事件 payload**

### 3.2 生成的应用必须满足的契约

**唯一来源是 [`docs/02-提交契约与打包.md`](docs/02-提交契约与打包.md) §2。** 本文件不再重复 C1–C10 编号——早先两处各有一份编号错位的清单（本文件的 C2 对应 docs/02 的 C2+C3，C3 对应 C4+C5），会让引用"C5"的人指错东西。

违反契约会**静默**导致 E2E 全挂，不会给你明确报错。要点速览（详细定义与源码行号见 docs/02）：

- 目录结构：`frontend/` + `backend/` 两个目录
- 前端构建产物必须是**精确路径** `frontend/dist/index.html`
- `backend/package.json` 必须有 `start` 与 `db:prepare:e2e` 两个 script
- **单端口**：backend 托管前端产物，前端无独立 dev server
- 端口读环境变量，**不硬编码**；后端监听 `0.0.0.0`
- `playwright.config.js` 读 `PLAYWRIGHT_BASE_URL`
- 数据库是 sqlite 文件，支持 `ARC_DB_FILE`；脚手架六个文件放在 `backend/src/database/`

### 3.3 管线六阶段

```
需求树 YAML
  │
  ├─[1] 需求编译 ──────→ 规范化树 + 契约抽取（纯代码，0 LLM 调用）
  │                         · 场景索引（scenarios[].name 即外部测试标题）
  │                         · 可访问名契约（需求文档里显式声明的）
  │                         · 夹具规格（种子账号/数据，自然语言 → 结构化）
  │                         · 依赖图与环检测
  │
  ├─[2] 批量设计 ──────→ 按页面/路由聚类，每簇 1 次调用 → 设计 JSON
  │                         · routes / components / APIs / schema / seed
  │
  ├─[3] 骨架落地 ──────→ 确定性写文件（路由槽 + 空组件 + API stub + schema + seed）
  │
  ├─[4] 实现生成 ──────→ 每簇 1 次调用 → 完整页面 + API 实现
  │
  ├─[5] 验证闸门 ──────→ 三层（见 §4.2）
  │
  └─[6] 定向修复 ──────→ 失败分类 → 只重发相关文件 → 限轮次
```

**与 ARC 的结构性差异**：ARC 每个需求节点跑三场 agent 会话（设计/测试生成/实现），本管线把 **460 个 ATOMIC 模块**（公开快照实测；平台标注 468，属版本差）**按页面聚类后批量处理**，目标是 80–150 次调用而不是上千次。

### 需求树规模（实测，2026-09-20）

| app | 节点总数 | ATOMIC | FOLDER | spec 文件 | test() | YAML |
|---|---:|---:|---:|---:|---:|---|
| 12306 | 143 | 117 | 26 | 117 | **135** | ✅ |
| bookstack | 56 | 34 | 22 | 34 | 34 | ❌ |
| ctrip | 226 | 125 | 101 | 125 | 125 | ✅ |
| keep | 45 | 32 | 13 | 32 | 32 | ❌ |
| prestashop | 109 | 86 | 23 | 86 | 86 | ✅ |
| stackoverflow | 86 | 66 | 20 | 66 | 66 | ✅ |
| **合计** | **665** | **460** | **205** | **460** | **478** | 4/6 |

**与平台标注的逐 app 对照**（审核逐条核对过）：

| app | 平台模块 | 本表 ATOMIC | 平台测试 | 本地 test() |
|---|---:|---:|---:|---:|
| 12306 | 117 | 117 | 138 | 135 |
| bookstack | 34 | 34 | 34 | 34 |
| ctrip | 133 | 133 | 126 | 125 |
| keep | 32 | 32 | 32 | 32 |
| prestashop | 86 | 86 | 87 | 86 |
| stackoverflow | 66 | 66 | 67 | 66 |
| **合计** | **468** | **468** | **484** | **478** |

**模块数逐个 app 完全一致——不存在"版本差 +8 模块"。**

> ⚠️ 上一版这里写"版本差 +8 模块"是**量纲错误**：把平台的 468（**全部叶子**）和本表的 460（**有场景的叶子**）当成了同一个量。带场景叶子才是生成与测试的单位，全部叶子只是树里的节点数。同理 `R2` 里"ctrip 模块 125 vs 平台 133"也是同一个错误。

**真正存在的差异只有测试数 +6（约 1.2%）**，其中 12306 占 +3（平台 138 / 本地 135）。**本地套件是平台套件的约 97.8% 代理**——这反过来印证"快照 ≈ 线上"（Lite 赛道平台写 `2 tasks · 66 tests`，快照 bookstack 34 + keep 32 = 66，完全吻合）。**这是好消息：本地 oracle 基本就是判分用的那一套。**

---

## 4. 五个关键技术决策

### 4.1 以"外部测试的禁用模式清单"为生成约束（最高杠杆）

`arc-bench/scripts/audit-benchmark-tests.js` 的 `forbiddenSpecPatterns` 明令禁止外部测试使用：

```
page.goto(          → 只能通过 UI 点击导航
page.request. / /api/   → 禁止直连 API
localStorage / sessionStorage  → 禁止读浏览器存储
.evaluate( / .route(    → 禁止页面内部操作与请求拦截
toHaveURL( / page.url(  → 禁止断言内部 URL
.locator('.', '#', '[data-')  → 禁止 class/id/data- 选择器
```

**这意味着外部测试只能靠"点击导航 + 可访问名/role 定位 + 可见文本断言"来验收。** 换句话说：

> **考的不是功能是否存在，考的是生成的应用是否暴露出稳定的 accessible name / role 结构。**

这可以同时变成三样东西：
1. **生成约束** — 写进实现生成的 prompt
2. **本地静态检查** — 扫描生成的 JSX，检查每个交互元素是否有 label / role / 可访问名
3. **自动化探针** — 用 Playwright 爬一遍应用，验证每个必需的可访问名存在且场景入口可达（远比生成全套测试便宜）

#### ⚠️ 需求文档的表达方式**不统一**——不能为某一种句式写死抽取器

本节早先引的是 **quickstart 示例**的中文原文（"必须提供以下稳定且唯一的中文可访问名称…"）。**实测：六个赛题里没有一个长这样。**

`docs/06-需求契约实测.md` §三 的实测（关键字出现次数）：

| app | `accessible name` | `labeled` | 汉字数 | 主要句式 |
|---|---:|---:|---:|---|
| 12306 | **0** | 13 | 0 | `input fields labeled "Name", "Passport number", …` |
| bookstack | 0 | **0** | 0 | `"Email address" field`、反引号表示可点击 |
| ctrip | 0 | 2 | 989 | 中英混排 |
| keep | 0 | 2 | 0 | 英文 |
| prestashop | 0 | **0** | 0 | `Click "Sign in" link` |
| stackoverflow | 0 | **0** | 0 | `"Log in" submit button` |

- **`accessible name` 这个词一次都没出现**
- 只有 ctrip 带中文，其余五个纯英文
- bookstack / prestashop / stackoverflow **连 `labeled` 都不用**

#### 抽取器的主路径

**不猜一种句式，用模式表驱动**：把观察到的每种句式各写成一条规则，新增 app 只加规则、不动代码。`pipeline/reqcompile/a11y.py` 已实现四条规则（中文契约 / `labeled` 锚点 / 角色词在前 / 兜底）。

**实测覆盖率：12306 上 98%**（抽取 441 条 / 124 个去重名；测试实际用到 57 个名字，覆盖 56 个）。
⚠️ **这个数只对 12306 成立，不可外推到其余五个 app** —— 原因与实测见下一小节。

> 口径是**子串匹配**——因为 Playwright 的 `getByRole({name})` / `getByLabel` 默认就是
> 大小写不敏感的子串匹配。精确匹配口径只有 74%，会低估。

#### ✅ 已完成（2026-09-20）：对每个 app 抽检召回率

**实测结果（探针 `docs/06-a11y-recall-probe.py`，输出存档 `runs/20260920T-interaction-unit-a11y.txt`，完整表与诊断见 `docs/06` §三补）：**

| app | 生成单元 | 交互单元 | 0 名单元（全部） | 全部空转率 | **0 名交互单元** | **交互空转率（验收口径）** | 召回下界 |
|---|---:|---:|---:|---:|---:|---:|---:|
| 12306 | 117 | 99 | 6 | 5% | 2 | **2%** | 81% |
| bookstack | 34 | 32 | 8 | 24% | 6 | **19%** | 97% |
| ctrip | 125 | 112 | 32 | 26% | 20 | **18%** | 44% |
| stackoverflow | 66 | 49 | 29 | 44% | 14 | **29%** | 76% |
| keep | 32 | 25 | 16 | 50% | 10 | **40%** | 74% |
| prestashop | 86 | 66 | 53 | 62% | 33 | **50%** | 45% |

**两列都要用**：只用交互口径会低估问题广度，只用全单元口径会高估严重度；排序在两列下一致。
（"交互单元"见下方裁定：分母只算场景里含 `click/select/enter/check/submit` 的单元。）

**诊断（抽查验过）**：差异是**种类**不是程度——差异不在"句式规不规整"，而在**名字有没有引号**。
12306 是引号式（`labeled "Name", "Passport number"`）；prestashop / stackoverflow / ctrip / keep 是**散文式**
（`THEN: Display Logo, category menu, search box, …`，**零引号**），对应测试写的是 `/logo/i`、`/search/i` 这类正则。
抽取器入口是 `_iter_quoted()`（`a11y.py:219`），**按构造抽不到无引号名词**。

> ⚠️ 上面那行"12306 上 98%"**只对 12306 成立**，不可外推到其余五个 app。两个数字口径不同，不矛盾：98% 的分母是 57 个名字（6 个 helper + `page` 作 scope），下表的召回下界分母含 13 个 helper 与正则字面量。

**这条推翻了 §4.2 里 L1 闸门的一个前提**：L1 按"需求声明的可访问名都存在"设计，
**它只对 12306 有效**；对上述四个 app 的多数单元它无事可查，
结果是"闸门报绿而外部测试大面积失败"——正是原先担心的形态。

#### ✅ 裁定（2026-09-20，方案会话）：**批准补第 5 条规则，但附三个约束**

原提议：补**第 5 条规则：散文名词枚举**（`Display X, Y, Z` / `Click the X link` / `shows the X`），
并让 L1 按 app 分两类判据——引号式查精确名，散文式查"该名词是否出现在某元素的可见文本/可访问名里"。

**批准。** 方向对、前置性无可争议，而且**价值不止于闸门**（见下）。
但必须附三个约束，因为这条规则有一个**直接顶到 `pass/CNY` 的风险**：

> 🔴 **精度风险（这是加约束的全部理由）**：散文枚举会把 `0 results`、`beijing south`、
> `g/c/d`、`count` 这类词一并捞进来——**它们是文本断言和测试数据，不是应用该暴露的可访问名**。
> 一旦进 L1 的必需名单，生成阶段就会**烧 token 去追幻影名字**。
> 在按 `pass/CNY` 排序的赛制里，这等于**直接扣分**。

**约束 1 — 位置约束，不做无差别名词枚举。**
只抽**祈使宾语位**的词：`Display X, Y, Z` / `Click the X link` / `fill in the X field` /
`the X button` 这类句式里的宾语。不在这些句式里的词一律不进名单。

**约束 2 — 必须同时报精度，不能只报召回。**
每 app 抽 20 条**人工过一遍**，数"像不像可交互对象"。**只测召回一定会得到虚高的分数**——
地面真值里本身就混着非可访问名，所以 80% 是**下界**，而"召回上升"也可能只是拟合上了噪声。

**约束 3 — 散文式 app 的 L1 先 fail-soft，且降级必须可见。**
精度被证明之前，散文类只**告警不判死**。且降级不能静默——
否则会以另一种形式重演"闸门报绿而外部测试大面积失败"。告警要进 `ValidationReport`。

**验收指标改用「交互单元空转率」。**
分母收窄为**场景步骤里含 `click / select / enter / check / submit` 的单元**——
纯散文、无点击/输入的单元本来就不该有可访问名。
否则"空转率下降"可能只是分母里混进了本来就无需命名的单元。数据现成（场景文本已解析）。
**基线已实测（2026-09-20，见上表右列）**：12306 2% · bookstack 19% · ctrip 18% · stackoverflow 29% · keep 40% · prestashop 50%。
第 5 条规则落地后，**这个数下降多少就是那条规则的效果**；同时必须报精度（约束 2），否则下降可能来自幻影名字。
> ⚠️ 判据写词形变化：需求文本写的是 `clicks the "Save" button`（第三人称单数），
> 锚点写死成 `\bclick\b` 会让 bookstack 的交互单元从 32 个掉到 6 个、空转率虚高到 83%。
> 与 `docs/06` §三 记过的 `\btab\bs?` 是同一个坑。

**价值不止于闸门（批准的主要理由之一）**：这条规则同时是**生成 prompt 的 a11y 靶子来源**。
现在 prestashop 50% / keep 40% / stackoverflow 29% / ctrip 18% 的**交互单元拿不到任何一个 a11y 目标**——
补上它省的是**生成阶段的重试 token**，不只是让闸门有话说。

**它仍是 M4 批量生成阶段的前置**（本节原话："应该在批量生成之前做"）。

### 4.2 三层验证闸门（替代 ARC 完全缺失的测试质量闸门）

ARC 的 `_prepare_tests`（`core/phases.py:753-789`）只做形状校验，不查文件是否存在、不查测试能否跑、更没有任何"这套测试对不对"的校验。讲师的三个坑（断言太弱、覆盖不足、过拟合例子）在这条链上全部敞口。

#### 先定死一件事：生成什么测试（原方案这里是矛盾的）

原方案的 §3.3 六阶段**没有测试生成阶段**，而 §4.2 的 L2 却写"测试生成后"。同时 §5 又搬了 ARC 的 `leaf-test-layer-selection` 和 `tdd-test-failure-repair` 两个 TDD 用的 skill。**三处互相矛盾，必须选一边。**

**决策：不生成测试金字塔，只生成「场景探针」（scenario probes）。**

| | 方案 A：不生成任何测试 | **方案 B：只生成场景探针（采用）** | 方案 C：完整测试金字塔 |
|---|---|---|---|
| 成本 | 最低 | **低**（约束在场景数） | 高（需求数 × 三层） |
| 行为反馈 | 无——修复循环没方向 | **有** | 有 |
| 与外部测试的同构度 | 无 | **高**（同一套禁用模式） | 中（单元/集成层外部不考） |

**理由**：内部测试不判分，在 `pass/CNY` 下是纯成本，所以不能要金字塔；但 Lab02 的冻结基准显示测试驱动闭环能把隐藏验证通过率抬 3–4 倍（8.3/36 → 26–35/36），所以不能没有行为反馈。**场景探针是这两者的交点。**

**场景探针的定义**（不是"生成的测试"，是**外部测试的结构同构体**）：

- **一条场景一条探针**，不做单元/集成分层——外部测试只有 E2E，分层是纯浪费
- **约束在与外部测试完全相同的禁用模式清单下**（见 `docs/02` §3）：不用 `page.goto`（除 `/`）、不用 `page.request`、不用 `localStorage`、不用 `.evaluate`、不查 URL、不用 class/id/data- 选择器
- 只能靠**点击导航 + 可访问名/role + 可见文本**定位与断言
- 因此探针失败即代表**外部测试大概率也会失败**——这是它比自研单元测试有价值的原因

**成本入账**：探针生成与运行的 token 必须计入 `pass/CNY` 的分母。这决定了它值不值——第一轮实验就要 A/B："有探针" vs "无探针（只有结构反馈）"。

#### 🔴 闭环自身的三个缺陷（①②是"钱漏在这里"；③是"**把对的改坏**"）

> ③ 是 **M3b-1 进行中插入的**（2026-09-21，方案会话）：它属冻结窗口的例外
> ——按现状跑第三次 gate 0，失败会由**我们自己的闭环**造成，而形态与"比赛模型更差"一模一样。

**① L1 的 `check_db_tables` 有一个已定位的 bug —— 闭环一直在修「幻影」**

- **症状**：盘上 5 个产物跑同一个函数，**全部返回完全相同的 4 条**
  （`sessions` / `test_harness` / `tests` / `users`）——包括那些**已经建了表**的。
- **根因是类型不匹配，不是判据宽严**：集合相减时**左边是元组、右边是字符串**
  ```python
  used    |= {(m.group(1).lower(), rel) for ...}   # (表名, 文件)
  created |= {m.group(1).lower() for ...}          # 只有表名
  used - created        # 元组集合 − 名字集合 = 永远不消掉任何元素
  ```
  修法：`missing = sorted((t, r) for t, r in used if t not in created)`
- **第二处（token 卫生）**：`test_harness` / `tests` 来自 `seed_db.js:15-16` 的 **JSDoc 注释**，
  `FROM tests` 被当成 SQL 读了 → **匹配前先剥掉 `//` 与 `/* */`**。
- **为什么这条重要**：**每一条假发现都会换来一轮定向修复（= 一次全量重发）**，
  而闭环自己占 **65%** 的 token。**它同时在烧钱和改坏产物**（为不存在的问题做修改）。
- **回归夹具现成**：5 个产物还在 `%TEMP%` 下，不必重跑生成。预期：
  `m3b0`→0、`m3b1-low`→0、`flashdef`/`gate0b`→**含 `users`/`sessions`**、`glm`→0（它的问题是**执行顺序**，静态判不出来）。

**② "大请求被网关丢"已经是第三次 —— 升级成客户端规则，别再逐点打补丁**

| 次数 | 位置 | 形态 |
|---|---|---|
| 1 | M3a 后端块 | `RemoteDisconnected` |
| 2 | M3a design | 输出全被 reasoning 吃掉 → 空内容 |
| 3 | 本轮 `selfcheck-review` | `RemoteDisconnected` ×2 |

→ **客户端规则：单次请求的输入预算上限，超限自动分块**（而不是重复同一个大请求）。

**③ 🔴 自检会**把对的**代码改坏，而闭环自己发现不了**（2026-09-21 实测；比 ①② 更危险的一类）

- **证据链**（`runs/20260921T033041Z-quickstart-score-dryrun.*`，`deepseek-chat` 官方档 +
  已含 schema 落位的新管线）：得分 **5/6**，唯一失败是 happy path 的
  `expect(getByRole('combobox', {name:/证件类型/i})).toHaveValue('')` →
  **`Expected: ""` / `Received: "请选择证件类型"`**。
- **需求说"初始值"，测试量的是 `value`**：需求原文是「`证件类型` 为必填原生下拉选择框，
  选项包括"请选择证件类型"（**初始值**）…」，而测试把它编码成 `toHaveValue('')`（空串），
  另一条 `toContainText('护照')` 才是查选项文本。
  → **"初始值"指显示文本，不是 `value`**；正确形态是 `<option value="">请选择证件类型</option>`
  且**表单状态初始为 `''`**。
- **🔴 这个 bug 是闭环自己写进去的**：第 1 轮 `selfcheck-review` 报「初始 option 的 value 为空字符串…
  可能不匹配」，**修复指令是「将初始 option 的 value 设为 '请选择证件类型'」**——产物里正是这一行
  `<option value={ID_TYPE_PLACEHOLDER}>{ID_TYPE_PLACEHOLDER}</option>`。
  第 2 轮 review 再看同一处，报**"已满足，无需修改"**。
  → **假阳性（把对的判成错的）→ 一次 repair 改坏 → 假阴性（把已坏的判成对的）把它锁死。**
- **为什么它比 ①② 严重**：①② 花的是钱，③ 花的是**正确性**；而**闭环里没有任何东西能发现它**——
  L1 是静态的、自检是模型判断的，**没有 oracle**，冒烟与官方测试都在闭环**之后**（`score_app.sh`）。
  **同一份产物若第 1 轮不修，本该 6/6。**
- **两处同型，别只修一处**：`优惠（待）类型`（`PASSENGER_TYPE_PLACEHOLDER`）写法完全相同，
  测试第 53 行断言的也是它 → **只修 `证件类型` 会把失败原地挪到第 53 行**；
  而 `国家/地区代码` 是**对的**（`countryCode: '+86'`，第 54 行要的就是非空 `+86`）
  → **契约是"按需求声明的初始值"，不是"一律空串"。**
- **第三次 gate 0 之前要做两件事**：
  ① 把这条写进生成 prompt 的硬性清单（与可访问名、原生 select、`role=meter` 同列）；
  ② L1 加一条：`<option>` 的 `value` 等于它自己的文字、且需求点名它是"初始值/初始提示" → 报缺陷。
  > 不做的话，第三次 gate 0 很可能以**"我们自己的闭环造成的失败"**告终——
  > 那正是第十四轮审核花一整轮才拆掉的那种混淆（"建没建表"混淆了 `reasoning_effort` 的结论）。
- **顺带记两条记录缺口**（同一次 dry-run）：RunRecord 里 `model: unknown`
  （**无法归属到模型/网关**，正是 `endpoint`/`run_id` 那条待办）、
  `per_test_failed: []` 而 `tests_failed: 1`（**`docs/04` 的"看成员变化不看条数"没法执行**）、
  `arc_test_date: None`（日志里明明设了 `ARC_TEST_DATE=2026-09-20`）。

#### 🔴 决策（2026-09-21）：闭环的存在形式要降级（R8）

**现状**：闭环 = L1 静态检查 + **模型自由审查全部代码** + 定向修复。
它占单轮 token 的 **50.3%**（keep 子集，实测 18,209 / 36,218）/ **62.8%**（quickstart dry-run，64,226 / 102,206），
而它的 **ROI 从未量过**。

**三条支持降级的实测：**
1. **成本**：一半以上的钱花在"把已经生成的东西再审一遍"上。
2. **无 oracle**：L1 是静态的、自检是模型判断的、冒烟与官方判据都在闭环**之后**——
   **闭环里没有任何东西能判定"这次修改到底改对了没有"**。
3. **已实测会改坏**：见上面 `③`（自检把 `<option value="">` 改成失败形态，下一轮又"确认已满足"，
   而唯一那条失败正是它造成的）。

**所以默认形态改成（2026-09-22 按 `Q13` 的答复改写）：**

> **`review` 让位给本地官方判据；只对判据报出的具体缺陷做定向修复。**
> - 🔴 **修复队列要按优先级排序，不能按插入顺序截断**（2026-09-22 拍板，见 `§7 M3b-1` 的「拍板」3a）：
>   现状是 `[:max_files]` 按插入顺序取前 4 个文件，而 L1 的机械发现排在自检前面 →
>   **自检发现的"对不上需求"类被截掉**，那正是"三轮都发现了却没修成"的机制。
>   优先级：**回归项 > 自检的"对不上需求" > L1 机械项**；**回归项允许突破 `max_files`**。
> - **能在本地验的，不要留给平台上的自愈**（与纪律 14 是同一条原则的两个面）。
> - **`review` 只在两种情况显式打开**：① 本地判据覆盖不到的语义问题；
>   ② `L1` 零发现而判据仍大面积失败（那是"L1 覆盖不到的那一类"的诊断手段）。
>   → **"代码形状"这类已经由 L1 十一条管住**，所以 **`review` 的剩余价值本来就很小**
>   ——这正是它占 **50.3%** token 却**量不出收益**的原因。

**验收方式（零生成调用）**：R3 的开/关对照——同一份产物用 `--reuse-app` 判两次，
**若"关掉闭环"的通过条数不降，它就不该默认开着。**

> 🔴 **两条落法上的硬约束（否则这条原则会自己变得很贵）**：
> 1. **本地 oracle 免费的是 token，不是墙钟。** keep 全量判分 = **30 分钟**（失败各烧 60 秒）。
>    → 本地修复环**必须按 spec 选跑**（`npx playwright test REQ-2.2.spec.ts`），
>    **不能每改一个文件就全量判一轮**——否则一轮修复 = 半小时，**墙钟会比 token 更早成为瓶颈**。
> 2. **它不能替掉"平台上的静态闸门"**：平台容器里没有网络、服务起不来（见 §7 M3b-1 的冒烟条），
>    所以**平台侧的闭环仍然只能是静态的**——这恰恰说明为什么要**把工作前移到本地**：
>    **本地多修一条，平台那次跑就少修一条。**

> ⚠️ **这不是否定闭环**：M3b-0 就是靠它把 4 处手工修复变成机器做的。
> 降级的是"**每轮无条件把全部代码再审查一遍**"这个形态，
> **不是**"发现 → 定向修"这条链路。

#### 三层闸门

| 层 | 检查 | 成本 | 何时用 |
|---|---|---|---|
| **L1 可执行性** | 探针能被 runner 收集；应用能 build + start；需求文档里声明的**可访问名**都存在、场景入口都可达 | 低（秒级） | 每次生成后必跑 |
| **L2 空实现反证** | 一条探针若在**未实现状态下就通过**，它是空洞的，必须重做 | 低 | 探针生成后 |
| **L3 参考实现对拍** | 探针必须先在官方参考实现上全绿才冻结 | 中 | **仅开发期，且仅 12306** |

**L2 是无 oracle 环境下的闸门核心**——它不需要参考实现，只需要一次"对空骨架跑探针"的对照。这是 Lab02 那道闸门（`orchestrator.ts:611-676`）在本赛制下唯一可移植的形式。

> L3 的限制：公开快照六个 app 里**只有 12306 带 `project/` 参考实现**，比赛期则完全没有 oracle。所以 L3 只用于开发期校准探针的写法，不能当常规闸门。

**断言强度契约**（写进 prompt + 本地 lint）：禁止 `toBeTruthy` / `toBeDefined` / `toBeVisible` 作为唯一断言；UI 断言必须含至少一个值断言；持久化断言必须从正常运行时路径读回（重载/重新查询），不能断言刚刚写进去的返回值。

#### 从 ARC 搬的两个 skill 要改用途

`leaf-test-layer-selection` 和 `tdd-test-failure-repair` 是为**三层金字塔 + 多次 TDD 往返**写的，与上面的决策不符。搬的时候要：
- `leaf-test-layer-selection` → 改成**场景 → 探针**的映射规则（一条场景一条，不分层）
- `tdd-test-failure-repair` → 保留"改测试还是改实现"的判别逻辑（那是它的真正价值），但预算是**场景级**的，不是层级的

### 4.3 墙钟预算与降级（原方案缺这条，而它决定"有没有分"）

平台要求同一份提交跑完 6 个任务才计聚合分。**一个中途死掉的 run 产出为零。**

原方案的 R1（跑不完）对策写的是"必要时砍需求广度换完成度"——**这个思路是错的**：外部测试是锁定的、跳不掉，砍需求广度不会让 run 更快结束。真正会杀死 run 的是**生成阶段的墙钟 + 判分墙钟**。

> 🔴 **墙钟锚点已换（2026-09-22，第十七轮审核指出同一量在两处差一个数量级）**：
> **旧锚点（作废）**："测试时间基本固定：实测单 app 135 条约 4–8 分钟，六个 app 估 15–30 分钟"——
> **那是参考实现全通过时的形态**。
> **新锚点（用这个）**：**判分是"失败主导"的**——keep 实测 **32 条 = 30.7 分钟**
> （31 条失败各烧 **~60 秒**，通过的那条只花 431ms）；
> 按 `playwright.config.ts` 的定义值（`timeout=60_000` + `workers:1` + `fullyParallel:false`）
> **判分最坏 = 判据条数 × 60 秒**，六 app **478 条 ≈ 8.0 小时**。
> → **凡引用"六 app 要跑多久"，只用 8.0 小时这一套口径；旧的 15–30 分钟不得再出现。**

> 🔴 **判分墙钟的实测真数（2026-09-21，keep）**：**32 条判据 = 30.7 分钟**。
> **而它是"失败主导"的**：31 条失败各耗约 **60 秒**（等待/超时），通过的那条只花 431ms。
> → **含义有两层**：
> ① 做"× 6 外推"要用这个真数（32 条 30.7 分钟），**不要再用"135 条约 4–8 分钟"那条估计**——
> 它是参考实现全通过时的数字，与"大面积失败"的形态差一个数量级；
> ② **通过率上升会显著压缩墙钟**（失败在烧超时），所以"跑得完"和"非零"不是两个独立风险，
> **非零本身就在降低跑不完的风险**。
>
> 🔴 **榜单里其实已经有墙钟包络的实测——A3 的一半可以立刻收窄**（2026-09-21 补）：
> `docs/09 §6` 的榜单快照里，**第 3 行**（JustSoSO，93.0%）`Runtime = 62m` / `Token = 273.28M`；
> **第 4 行**（chrislearn，99.5%）`Runtime = 379m` / `Token = 813.55M`。
> **→ 平台至少容忍过 379 分钟（6.3 小时）的单次 run。**
> 而六 app 按"失败为主"外推是 **478 条 × ≈59s ≈ 7.4 小时（≈444 分钟）**——
> **比已观测到的最大单跑还高**。
> → **"跑得完"不是理论担心，是贴着已知上界的**；这也再次说明为什么"提高通过率"同时是在买时间。
> ⚠️ 前 6 名里第 1/2/5/6 行是 `0s–7s / 0.00M` 的异常档（见 §8 R10），**不要用它们当包络**；
> 只能用第 3/4 行这两条"像真跑"的。
> **副产品**：这两行同时收窄了 **token 配额**——**813.55M 的 run 跑完了**，
> 而我们全项目累计才百万量级 → **A3 里"token 配额"基本不是约束，"墙钟"才是。**
>
> 🔴 **判分墙钟可以**精确算出来**，不必外推**（2026-09-21 补，config 级证据）：
> `repos/arc-bench/playwright.config.ts:3,4,7` 写死了 `timeout = 60_000`（**每条测试**）、
> `expect.timeout = 10_000`，且 **`workers: 1` + `fullyParallel: false`（严格串行）**。
> → **判分最坏墙钟 = 判据条数 × 60 秒**，而且**与实测吻合**：
> **keep 预测 32 × 60s = 32 分钟，实测 30.7 分钟**（96% 吻合）；
> 0/86 那次 86 × 60s ≈ 86 分钟，平台也确实跑完了。
> → **六 app 最坏 = 478 × 60s = 478 分钟 ≈ 8.0 小时**，
> **而已观测的最大单跑是 379 分钟（6.3 小时）→ 最坏情况超出约 1.27 倍。**
> **所以"提高通过率"不只是得分杠杆，它是让 run 能跑完的杠杆**：
> 通过的那条只花 **0.3–1.5 秒**，失败的花满 **60 秒** → **通过数翻倍 ≈ 判分时间减半**。
> ⚠️ **一个必须问清的读法**：榜单的 `Runtime` 是**每任务**的，还是**整个提交（6 任务之和）**的？
> 若是后者，每个 app 只分到 ≈63 分钟（**含 agent + 判分**）——而 **keep 单独就已经约 56 分钟**
> （agent 25 分 + 判分 31 分）。**这条要加进 bbs 的提问清单**：
> 它比"单次提交墙钟上限是多少"更可判（可观测、可证伪）。

**所以管线从第一天就必须有：**

**1. 每个阶段显式墙钟预算 + 看门狗**

```
预算耗尽 → 立即停止生成 → 转入降级模式
```

不是"尽力跑完"，是"到点就收"。因为超时的 run 等于零分。

**2. 降级模式：保证产出「可构建可运行的最小应用」**

> 🔴 **实测修正（2026-09-20，提交二 `probe-v2-scaffold`）：原写的"测试全红没关系"是错的。**
>
> 提交二把骨架成功落地，平台把每一步都跑通了（装依赖 → 构建前端 → 起后端 3000 端口可达 → 86 条测试全执行），
> 结果 **0/86 通过 → 六任务各 0.0% → 不在榜上**。
>
> → **地板不是"能跑完"，是"至少有一部分功能可用"。0 通过 = 不上榜 = 等于没提交。**
> 详见 `docs/09-平台提交与报错记录.md`。

预算耗尽时，管线的义务变成——**前端能 build、后端能 start、且至少有一个场景端到端可走通**。
优先保"深度"不是"广度"：**先让少数场景真的能用，再谈铺开**。

这一条要写进阶段设计，不能事后补：生成阶段要在每个聚类之后检查剩余预算，据此决定继续生成还是收尾。

**2b. 🔴 付费长流程的硬失败 → 必须「就地降级」，不许用异常作废已经花掉的钱**（2026-09-22 升格为原则）

> **原则**：**任何"付费长流程"的硬失败，都必须有一条"就地降级"路径。**
> **抛异常只留给真正不可恢复的情况**——否则等于把已经花掉的钱作废。
> 它与上面第 1、2 条是**同一条原则**，只是发生在**生成阶段**而不是墙钟耗尽时。

**这条是被两次实测损失逼出来的，而且两次是同型**：

| 时间 | 损失形态 | 代价 |
|---|---|---|
| 9/21 run d / run e | **判据侧**失败 → 丢掉已经生成好的产物 | 两次各一整轮生成 |
| 9/22 **R2** | **生成侧**中止 → **10/13 块的花费全部作废**，且产物残缺、**不可判分** | ~10 块生成 |

**根因是一条只写了一半的客户端规则**：第十四轮审核提的是"**预算 + 超限抛 `RequestTooLarge`，
由调用方按语义分块**"——**它只写了"信号"，没写"恢复"**。结果调用方没接，异常一路冒到 `EXIT=1`
（`RequestTooLarge: 网关断开且请求接近预算（19,267 字符 ≈ 80%）`）。

**✅ 已落的正确形态（`f386500`）**：**压缩发生在"拼请求时"**，不再抛异常——
`pipeline/generate/implement.py:102` 的 `REQUEST_SAFE_CHARS = MAX_REQUEST_CHARS × 0.7`，
注释写明"**80% 实测会触发网关断连**"。判据是"**超线就压下来**"，不是"超线就报错"。

> ⚠️ **仍要盯的另一条形态（第十二/十三轮记过）**：这次压的是**输入**体积；
> 另一条是"**单块输出很大 + reasoning 占 90% → 生成时间长到网关读超时**"。
> **R2 重跑时要盯它是否复现**——那一条不能用压缩输入解决。

**3. 优先级排序（写死，不要临场判断）**

> **宁可 6 个 app 都 40% 通过，不要 4 个 app 90% + 2 个 app 根本不存在。**

因为前者有聚合分，后者没有。

**4. 排期上的含义**

这件事属于 **M3**，不是等 M5 才发现。打通最小闭环时就要把预算和降级一起做进去——**它们是骨架的一部分，不是优化**。

**已知的弱证据**：榜单 RUNTIME 列出现过 108 分钟的条目，说明平台似乎容忍较长的 run。但那可能是 Lite 两题的耗时，**只能当弱证据，不能当上限依据**。真值要靠 V6 问主办方。

### 4.4 种子数据是最便宜的得分点

需求文档用**自然语言**规定夹具。实测逐字段对齐：

- 测试侧 `arc-bench/webapp/12306/tests/helpers.ts`：`FIXTURES.registeredUser = { username:'registered_user', password:'Password123!', ... }`
- 需求侧 `12306/requirements/requirements.yaml:194`：*The system contains the verified account registered_user with username "registered_user", ... and password "Password123!".*

**逐字段一致。** 而测试不使用任何私有 reset/API，所以实现必须自己通过 `npm run db:prepare:e2e` 播种。

**漏播 = 大面积失败，且与功能实现质量无关。** 这类失败会严重污染 A/B 信号。

**动作**：写一个夹具抽取器，从需求文档里捞账号/初始数据规格，直接生成 `seed_db.js`。这是低成本高收益项，优先做。

### 4.5 成本结构决定一切

ARC 的钱 70–85% 烧在 TDD 的 ReAct 重试循环（每轮重发完整上下文）。压成本必须三管齐下：

1. **批量** — 按页面聚类，减少调用次数（第 2、4、6 阶段）
2. **精准失败上下文** — 不重发全量日志。E2E 单次输出能到 12–16KB，压到 1–2KB 结构化摘要 + 落盘路径
3. **分层模型** — 结构性生成用便宜档；只在"同一失败指纹重复出现"时升级到强档

**计量先行**：从第一天起就把 usage 落盘（`.arc/metrics.jsonl`，字段 `{ts, stage, node_id, model, input_tokens, output_tokens}`）。没有计量就没有优化，这是 ARC 最大的盲区（它设了 `stream_usage: False`，全仓无任何 usage 统计）。

**成本口径**：分母必须包含失败 run 的消耗。同时记三个数：`tokens_success_only`（找规律）、`tokens_all_attempts`（对齐评分）、`cny_all_attempts`（对齐 pass/CNY）。

#### 🔴 R9 · reasoning 占输出的 86–93% 先按**不可优化项**处理（2026-09-21 定）

**实测**：`deepseek-v4-flash` 默认档的 reasoning 占输出 **86–93%**
（run c：输出 138,694 中 reasoning 128,984 = **93%**；三次跑 86–93%）。

**两个抑制旋钮都已经试过，都没有"可以切换"的证据**：
- `reasoning_effort=low`：**成本侧确实降**（探针 77%→11%→0，总 token 约 1/3）；
  但**通过率侧无法支撑切换**——同网关唯一干净的对照是 **low 5/6 > 默认 3/6**（**方向相反**），
  而那个对比曾被"建没建表"混淆（见 §7 M3b-1 第 0 条）。**结论是"证据不足"，不是"更差"。**
- `glm-5.3-flash`：**便宜约 42%，但 0/6**（`SQLITE_ERROR` 打死 Node）。

→ **所以所有预算按"reasoning 占输出的 86–93%"来算**，不要再把它当"待优化的浪费"来排期。
**真正可优化的是它之外的部分**，而且已经有明确清单：
**闭环的形态（R8，占 50–63%）**＞ 请求分块 ＞ **零 token 的静态检查**（纪律 14）。

> **这条给 M4 一个明确的减法**：**M4 不要再去调 reasoning 旋钮。**
> 若将来要重测，条件是"建表确定化之后、用 **N ≥ 2–3** 的配对跑"——
> **单次对比测的是建表的运气，不是旋钮的效果。**

---

## 5. 复用清单（精确到路径）

### 从 ARC 直接搬（MIT 协议）

| 内容 | 路径 | 为什么 |
|---|---|---|
| 事件与 traceability SDK | `agentic-requirement-compiler/src/arcbench_agent_runtime/` | 协议是平台约定格式，官方明说不要自己实现；整包复制 |
| 应用骨架模板 | `arc-template/templates/web-react-express/` | 验证闭环能不能跑起来全靠它——它同时给了前端 vitest+testing-library 和后端 supertest+playwright+database 脚手架 |
| 技术栈契约字符串 | `agentic-requirement-compiler/src/app_type_handler/web.py:1060-1095` | 原样放进生成 prompt，生成的代码才和测试基建对得上 |
| 五个 SKILL.md | `agentic-requirement-compiler/src/skills/` | 几轮真实跑测攒出来的提示词资产，尤其 `leaf-test-layer-selection` 与 `tdd-test-failure-repair` |
| 决赛增量语义 | `core/workflow.py:154` 的 `_has_existing_application()` + `_plan_requirement_sync()` | 受影响节点 = 新增/变更 + 祖先 + 反向依赖。决赛照这个语义实现，不要重新发明 |

### 从 Lab02 搬

| 内容 | 路径 | 为什么 |
|---|---|---|
| 测试冻结闸门 | `Lab/Lab02/src/run/orchestrator.ts:611-676` | L3 层的实现参考：先在参考实现上跑，不绿就修测试，最多 3 轮，全绿才冻结 |
| token 计量 | `Lab/Lab02` 的 `result.json` + `MODEL_RUN_SUMMARY` | 分阶段 input/output/total 记账的现成实现 |
| 模型选型依据 | `Lab/Lab02/benchmark-results/gui-*/report.md` | 11 模型冻结基准：`glm-5.3` 拿 35/36 且 token 只有 `qwen3.6-plus` 的 1/5 |
| JSON report 解析 | `Lab/Lab02/src/validation/run-validation.ts:194-211` | 递归解析 Playwright JSON 的现成代码 |

### 从 arc-bench 基准仓库用

- `arc-bench/webapp/<app>/{requirements,tests,project}/` — 6 个赛题的需求、460 个外部测试、**参考实现**。开发期的 oracle 和上界基线。
- `scripts/audit-benchmark-tests.js` — 禁用模式清单，是 §4.1 的依据
- `scripts/run-playwright.js` + `package.json` — 本地跑测试的命令接口

---

## 6. 验证与测量

**原则：先证明尺子是准的，再量东西。**

### 基线四件套（缺一不可）

1. ARC 版本：`git rev-parse HEAD` + 工作区是否干净
2. 需求包：`requirements.yaml` 的 sha256 + 资源目录哈希
3. 外部测试包：`arc-bench` 的 commit SHA + spec 数 + `playwright.config.ts` 哈希
4. 模型配置：`MODEL` / `VISUAL_MODEL` / `API_MODE` / base_url host

**另需一条上界基线**：用官方参考实现跑一遍同一套测试。这验证"测试环境是好的"，并给出天花板。

**实测上界（五轮干净测量，2026-09-20）**：

| 轮次 | 通过 | 失败 |
|---|---:|---:|
| R1 | 129 | 6 |
| R2 | 130 | 5 |
| R3 | 130 | 5 |
| R4 | 133 | 2 |
| **R5** | **135** | **0** |

**→ 满分可达：R5 跑出 135/135（3.0 分钟，`runs/20260920T043334Z-12306-round1.log`）。**

🔴 **这推翻了先前两条结论**（2026-09-20 当天晚些时候）：
1. ~~"`REQ-4.3.7` 四轮全失败 = 稳定失败"~~ → **R5 里它通过了**。现在**零稳定失败**：
   135 = 124 条稳定通过 + 11 条翻面。
2. ~~"理论天花板 134/135"~~ → **作废**。实测区间是 **129–135 / 135**，
   满分**可达但不稳定**（五轮一次）。

**对"不追 100%"的影响**：理由从"满分**不存在**"改成"满分**不稳定**"。
目标设定仍然是**不追 100%**（五轮一次的满分不值得作为目标），
但**不要再写"天花板 134"**——那会让一个真实可达的 135 被当成不可能。

> ⚠️ 先前记的"132/132 干净库"，过去被当作"运气好的 run"排除；R5 之后它与 R5 同属"满分"这一类观测，
> **不能再一句话排除**。

### 统计方法（关键，别在这里犯错）

**不要用整跑成功率做主指标。** n=3 时 2/3 与 1/3 统计上完全无法区分（rule of three）。

**正确做法：把粒度降到单条测试用例，但要知道它现在的分辨力有多低。**

12306 的条数口径（三个数并存，各有各的用途）：

| 口径 | 值 | 说明 |
|---|---|---|
| 本地实际收集 | **135** | `npx playwright test --list` → `135 tests in 117 files` |
| 平台标注 | **138** | 差 3 条，**去向未定**（见 §3 逐 app 对照表） |
| spec 文件数 | 117 | 部分文件含多个 `test()` |

**用配对检验时的真实分辨力**：配对 McNemar 的灵敏度由**不一致对**决定，不由总对数决定。
三次 run 合并后有 350–400 个**总对数**，但实测每对 arm 的**不一致对只有 4–8 条**。
所以它能可靠检出的效应量比"5 个百分点"大得多——**大效应才有信号**。

> **实际含义**：per-test 配对是"确认工具"，不是"发现工具"。先用通过率条数筛（判据 1），
> 再用配对检验确认方向。**不要拿它当第一道闸**（上一版把它列为主判据是错的）。

**N 的建议**：冒烟/找大 bug `N=2`；决策"改动是否保留" `N=3` 但主指标用 per-test 配对；要比较整跑成功率则每 arm ≥ 8–10 次。

**噪声底线（五轮实测，2026-09-20）——必须分成两个口径记，混在一起会得出错误结论：**

| 条件组 | 轮次 | 失败数 | 跨度 |
|---|---|---|---|
| **污染**（并发事故窗口 10:32–11:10） | R1–R4 | 6 / 5 / 5 / 2 | **4** |
| **静置**（`load_snapshot` 记为干净） | R5 + Q1–Q3 | **0 / 1 / 0 / 2** | **2** |

| 口径 | 值 | 用途 |
|---|---|---|
| 每轮**失败条数**波动（**静置**） | **0 – 2（跨度 2）** | 可辨差异下限 = **2 条** |
| 会**翻面**的测试集合大小 | **11 条** | 做 per-test 配对时的噪声上界 |

🔴 **只在静置条件下比较**：`load_snapshot` 必须 `status=free`/`free-leftover` **且** `runners=0`；
**含污染轮次的比较一律无效**（「跨度 6」就是把 R5 与 R1–R4 混算出来的假象，已作废）。
权威记录：`runs/20260920T-quiet-seq-README.md`。
⚠️ 静置样本仅 4 轮，偏薄 → **跨度涨到 ≥3 门槛退回 6，≥4 退回 8**。

**已知会翻面的 11 条**（`docs/04` 有完整清单）：`REQ-4.2.3 / 4.2.7 / 4.2.14 / 4.3.6 / 4.3.7 / 5.2.3 / 5.2.5 / 5.3.1 / 5.3.3 / 5.3.5 / 6.2.1`
（**R5 里这 11 条全部通过**——所以"`REQ-4.3.7` 是稳定失败"已作废，见 §6 上界表的修正）

**做 A/B 时的用法**：
1. 门槛是 **静置下的 4 条**（见下面 A 轨道判据）
2. 把 11 条单列记录成员变化，但**不作为保留依据**——它在噪声里本来就会动
3. 若改动声称的收益落在翻面集合内 → **该结论不成立**

### 🔴 由上述推出的一条硬结论：A 轨道现在只能测"回归"，测不了"改进"

11 条会翻面，**其余 124 条在五轮里全部稳定通过**，**零稳定失败**（R5 证伪了 `REQ-4.3.7` 稳定失败）。

所以在 12306 上，当前**唯一可测的通过率信号只有"回归"**：
- **回归**（原本稳定的 124 条被打挂）——可测
- ~~修好 `REQ-4.3.7`~~ —— **R5 之后没有这条了**（已无稳定失败可修）

**中间地带测不出来。** 也就是说：提示词、生成策略、闸门这类"改行为"的改动，
**在噪声压下去之前无法判断有没有用**。

**→ 结论：`CI=1` 重试实验从"便宜的压噪待办"升级为 A 轨道的前置条件。**

若它能把翻面集合从 11 条压到 1–2 条，等于**凭空多出约 9 条可测测试**，
A 轨道才真正可用。**它必须排在所有"改行为"的 arm 之前。**
~~R5 又给了它一个动机：失败数能摆到 0–6……~~ → **静置序列之后结论改了：不开 CI=1**（静置抖动本来就近 0，开重试只是改口径）。见下方裁定。

> 因此排期上：**M4（只降 token）排在 M3 之后**是对的，不是保守——
> 在噪声压下去之前，B 轨道（成本）本来就是唯一能可靠测量的轨道。

### 改动保留的判据（预先登记，跑之前写下来）

**A. 会改变行为的改动**（提示词、生成策略、闸门、调度）：

1. **通过率提升 ≥ 4 条**（= 2 × 静置跨度 2；**且两轮都必须静置**——含污染轮次的比较无效），**且能被两次独立 run 复现**
2. **`pass/CNY` 不下降**（这条是排序口径，必须当一等公民）
3. 跨 app 至少 2 个不劣化；同一 app 两次独立 N 上不翻号
4. 失败原因分类发生预期迁移

**11 条翻面集合的成员变化单独记录，但不作保留依据**——因为它在噪声里本来就会动。

> **门槛从 8 改成 4 的经过**（留着免得再算错）：① 最初据污染的 R1–R4（跨度 4）定 8；② R5 满分后把 R5 与 R1–R4 混算得「跨度 6」；③ **静置序列证明混算是错的**（静置跨 2、污染跨 4）→ 静置条件下门槛 = 2 × 2 = **4 条**。
>
#### ✅ 裁定（2026-09-20）：**静置序列已交付 → 门槛重推完毕，A 轨道解封**

上一版裁定要求"静置序列（连续 3 轮 + 记录结论行与 runner 数）"作为重推输入。
**该输入已交付**（`arm=refimpl-quiet-3r`，见 `runs/20260920T-quiet-seq-README.md`），
而且它**推翻了"拿混合条件推门槛"这个思路本身**：

| 条件组 | 轮次 | 失败数 | 跨度 |
|---|---|---|---|
| **污染**（并发事故窗口 10:32–11:10） | R1–R4 | 6 / 5 / 5 / 2 | **4** |
| **静置**（`load_snapshot` 记为干净） | R5 + Q1–Q3 | **0 / 1 / 0 / 2** | **2** |

→ **"跨度 6"是混口径算出来的假象**（把静置的 R5 与污染的 R1–R4 混成一组）。分组后各自更窄。

**新口径（四条，全部生效）：**

1. **只在静置条件下比较**：`load_snapshot` 必须 `status=free`（或 `free-leftover`）**且** `runners=0`；
   **含污染轮次的比较一律无效**。这是门槛能降低的**代价**——低门槛靠"条件干净且被记录"，不靠余量。
2. 可辨差异下限 = **2 条**；**(保留一个改动的门槛) = 4 条**（= 2 × 静置跨度 2）。
3. **自我修正**：静置样本仅 4 轮，偏薄。**跨度涨到 ≥3 → 门槛退回 6；≥4 → 退回 8。**
4. **A 轨道解封**（有判据了）。但 §6 那条"中间地带测不出来"只被弱化、没被消除——
   静置口径下可判的是**大效应**。

**`CI=1` 重试实验 —— 结论：不开。**
静置抖动本来就接近 0（0–2），开重试**不是在压噪，而是在改变口径**，
还会让本地口径与平台不一致。改为：**本地固定 `retries=0`，并把 `ci_env` 记进 RunRecord**，
让"口径"成为记录的一部分（第六轮审核 §三）。
**记录字段已补**：`retries_effective` / `retries_observed` / `ci_env` / `load_snapshot`（见 `docs/04` §五）。
>
> ⚠️ **配对 McNemar 不是主判据，已从第 1 位降级。** 它的灵敏度由**不一致对**决定，不由总对数决定：
> `350–400` 那是 135×3 的**总对数**，而实测每对 arm 的不一致对只有 4–8 条。
> 算例：某改动真修好 7 条，但观测到 b=9 / c=3 时，McNemar 精确检验 `p≈0.15`，**仍判不显著**；
> 要显著需要 `b−c≈8`。**也就是说它只在极大效应时成立，不该当第一道闸。**

**B. 不改行为的改动**（只降成本：模型路由、上下文裁剪、批量合并、缓存）：

**不要用 A 的判据去审它**——它的预期效果本来就不在通过率上。用独立判据：

1. **通过率落在噪声带内**（失败条数变化 ≤ 4，且 11 条翻面集合无新增成员）
2. **token / CNY 有可测下降**（这是唯一的主指标）
3. `pass/CNY` 上升

> 这条很要紧：**M4 是"只降 token，不加功能"，那一阶段的所有改动都属 B 类。**
> 如果拿 McNemar 去审，会因为分辨不出差异而把有效改动全部判死。

任一判据不满足 → 不保留。

**纪律**：一次只改一处；每个 arm 之间必须完全重建工作区（用 `--clean`，**不用 `--resume`**——它复用队列和已有代码，不是独立重复）；固定 `ARC_TEST_DATE`（测试是日期相关的）。

---

## 7. 里程碑（按阶段推进，不按日期）

### 推进规则

> **阶段是闸门驱动的：闸门通过才进下一阶段。**
> 🔴 **2026-09-23 起，本表不看日期**（用户口径：**"从现在开始计划不要看日期了，越快完成越好"**）——
> 每个阶段**完成即止**：没有"最晚完成"，也**不因为日期到了就启用降级**；
> 降级动作只在**对应闸门判明不通过**时才启用（写在最右列）。
> **跳过闸门 = 拿着不可信的结论往下走**，这一条不变。

| 阶段 | 目标 | 闸门（全部为真才推进） | 闸门判明不通过时怎么办（按状态触发，不按日期） |
|---|---|---|---|
| **M1** 测量就绪 | 有一条能复现的测量管线，噪声底线已知到可支撑判断 | 静置序列 3 轮完成并落盘条件；跨度与门槛**重推并裁定**；`bench.sh` 四子命令 + 崩溃恢复可用；测量口径（是否开重试）已定 | 退回**只用回归判据**：A 轨道继续阻塞，只做 B 轨道 |
| **M2** 首分 | **榜上有分**——不论多少 | **≥1 个任务非零通过并上榜**（0 通过 ≠ 有分，实测）；提交链路全通 | —（M2 已实质达成大半，见下） |
| **M3a** ✅ **能力探针** | **回答 V-1**：生成能力是否成立 | ✅ **已完成** → 命中第一行，**§2 不翻转**。口径：**原始 0/6（构建失败）→ 修构建 3/6 → 再修接线 6/6** | — |
| **M3b-0** ✅ **自动化闭环** | 把 M3a 的 4 处**手工修复换成机器做的** | ✅ **已完成**：零人工 **6/6**（超出地板 4/6）+ held-out 构建成功。<br>⚠️ 口径要带模型：**在 `deepseek-chat` 上**达成，**平台模型尚未复现** | — |
| **M3b-1** 铺开 | 6 个任务**都非零通过** | **零人工**构建成功；产物 `init_db.js` 已修补；模型口径已标定；6 任务**全部非零** | 某个 app **连续两轮**仍未非零 → 按 §7 底部的**保底退路缩一级**（**是换更便宜的一级，不是停铺**） |
| **M4** 降本 | 只降 token，**不加功能** | token 实测基线已建；按 **B 轨道判据**：token/CNY 可测下降且通过率落噪声带内 | 只做 ROI 最高的两项（视觉分析 + 批量生成） |
| **M5** 封包 | 选一个包交上去，然后**不动** | 6 任务完整跑 ≥2 次；打包在不同解压位置各验一次；提交前检查清单走完；**已提交** | —（外面有硬墙，见下） |
| **M6** 决赛 | 增量编译 | *条件性*：仅当初赛进 Top 20 | —（**决赛窗口 10/1–10/7 是赛制的外部事实**，不是我们的排期） |

**硬墙：全文只有两条必须按日期的事，而且都是外部约束、不是我们的排期**——
**9/30 是提交硬墙**（排行榜是快照式的，**晚提交 = 没有提交**）；**10/1–10/7 是决赛窗口**。
**除这两条，`PLAN.md` 不再有任何内部排期日期：阶段完成即止，越快越好。**

---

> ### 🔴 推进现实（2026-09-23 重订：不看日期）
>
> **没有日历绑定，只有闸门绑定。** 阶段按闸门推进；降级动作按**状态**触发（= 上表最右列）。
>
> | 阶段 | 状态（2026-09-23） | 下一步 / 卡在哪 |
> |---|---|---|
> | M1 测量就绪 | ✅ 已达成 | 只剩 `git init` 与 `ARC_TEST_DATE` 两个勾（见 §7 M1） |
> | **M2 首分**（榜上有分） | ❌ **仍未达成**——仍是 0/86 形态、**不在榜上** | **关键路径已重订**：先让一个主赛道 app **本地非零** → 打包 → **平台一次**（同时答 A1 与 A2/V-6） |
> | M3a / M3b-0 | ✅ 已达成 | ⚠️ 口径：**零人工最好读数 5/6**；两个"6/6"都含手工修复 |
> | **M3b-1 铺开** | **4/6 非零**（`keep` / `bookstack` / `stackoverflow` / `prestashop`） | 剩 **`ctrip`**（0/125，机制已钉住 → 见「硬清单的回灌」）与 **`12306`**（最贵的那一个） |
> | M4 降本 | 未开始 | — |
> | M5 封包 | 待做 | **唯一带日期的约束：9/30 提交硬墙**（决赛窗口 10/1–10/7 只在进 Top 20 后才相关） |
>
> **首分（M2）的处置不变**：0 通过 = 不上榜 = **等于没提交**，
> 所以"**先确保有一个 app 非零**"**永远**优先于"再谈深度"。
> **V-6（"非零"的判定口径）不是前置，是搭车项**：它需要一个"主赛道任务部分通过"的产物，
> 而那正是 M2 本身 → **M2 那次平台跑顺带把它答掉**（连同 A1 凭据检查）。
> → 结论：**先把一个主赛道 app 做到非零（两种口径下的共同前提），再用同一次跑决定铺广度还是做深度。**
>
> 🔴 **铺开规则：完成即止（2026-09-23 重订；此前的「9/24 决策规则」整块作废）**
>
> **用户口径（2026-09-23）**："从现在开始计划不要看日期了，越快完成越好。"
> → 原规则（`B_worst` / `B_pred` / `U` / 余量 / 三分支 / 9/24 预注册）**整块删除**：
> 它们存在的唯一理由是"**到某一天算一次还能不能继续**"，而这个判断在"完成即止"下**不存在**。
>
> **新规则（三行，无日期）：**
> 1. **只要还有 app 没非零 → 继续铺**，顺序不变（判分成本升序）。
> 2. **6/6 都非零之后才谈深度**（深度 = 已非零那几个的通过率）。在那之前
>    **不做任何单 app 的深度投入**——广度优先是"0 分风险"的对冲，不是偏好。
>    → **"6/6 之后做什么"已经有第一刀了**：见下面的「**深度阶段**」（KPI / 选哪个 app / 一轮怎么算成了）。
> 3. **升序排序的全部收益 = "任何时候被打断，已拿到的非零最多"**。中断随时可能
>    （墙钟、配额、误操作）→ **顺序本身就是止损机制**，所以别为了省几分钟重排。
>
> **唯一的止损触发（也不按日期）**：某个 app **连续两轮判分仍未非零** →
> 按 §7 底部的**保底退路逐级下移**（缩 app 范围 → 缩需求范围 → 接受"榜上有分即止损"）。
> **是"把这一级换成更便宜的一级"，不是"停铺"。**
>
> ⚠️ **`predicted` 字段仍然要落**（`runs/*.json`）——用途从"喂 9/24 的 `B_pred`"改成两件：
> ① **报出这次送判分的预期成本**（预测失败条数 × 60 秒）；
> ② **同 app 跨轮对比**（"预测失败条数有没有降"是修法有效性的**零 token 证据**）。
> **③ 自己的闸门地位不变**（信息档，见 §7 ③）。

> **旧日期阶段的解码表**（`docs/` 与 `runs/` 里的旧文若还写着日期阶段，按此换算；
> **`PLAN.md` 从此不再用日期阶段**）：
> `9/20 验证` → **M1** ｜ `9/21–9/23 底牌` → **M2** ｜ `9/24–9/27 降本` → **M4**
> ｜ `9/28–9/29 彩排` → **M5** ｜ `9/30 提交` → **M5**
> 注意 **M3（跑得完）在旧版里没有独立阶段**，它是原"9/21–9/23"与"9/24–9/27"之间被忽略的一环——
> 而"跑完 6 个任务才有分"是硬门槛，所以它被提升为一个独立阶段。

**检查点的三条规则：**

1. **审核只看"前提是否被推翻"，不做全面复审。** 全面复审会挤掉执行时间。
2. **方案微调只在闸门处落地。** 阶段进行中不改 `PLAN.md`——见 `AGENTS.md` 的冻结窗口。
   例外：发现"照做会白丢分"级别的错误时立即修，不必等闸门。
3. **闸门不通过时，把"为什么"写进 `STATUS.md`**，而不是悄悄跳过。

**审核清单固定六类**（`AGENTS.md` 有完整版）：事实性错误 / 内部矛盾 / 前提被证伪 /
工具与文档不一致 / 纪律照做会坏事 / 环境与进程状态。

---

### M1 — 测量就绪

**目标**：先有尺子。没有已知噪声底线的测量，任何 A/B 结论都不成立。

- [x] **地基**：`npm run test:audit` 通过；12306 参考实现本地可跑（绕过 Docker）；`bench.sh` 可用
- [x] **静置序列** —— 已交付（`arm=refimpl-quiet-3r` + `runs/20260920T-quiet-seq-README.md`）
- [x] **重推跨度与门槛** —— 已裁定：**静置口径 跨度 2 → 可辨差异 2 条、保留门槛 4 条**；**A 轨道解封**（见 §6）
- [x] **`CI=1` 对照实验** —— **结论：不开**（静置抖动本来就近 0，开重试是改口径不是压噪）
- [x] **`bench.sh recover`** —— 已补
- [x] **RunRecord 测量条件字段** —— `retries_effective` / `retries_observed` / `ci_env` / `load_snapshot` 已落地
- [ ] **`git init`** ← **五轮审核里最老的未执行项**。`pipeline/` 已有 10+ 个文件，
      "一次只改一处"要落到能审计的 commit 上，**越晚越难补**
- [ ] **`ARC_TEST_DATE` 强制写入**（工具层）
- [x] **V4** 网关连通 —— 16 个模型可列，计费含 reasoning token

> **M1 的闸门已基本满足**（只剩 `git init` 与 `ARC_TEST_DATE`）。注意 §6 的口径自带**自我修正规则**：
> 静置样本仅 4 轮，**跨度涨到 ≥3 → 门槛退回 6；≥4 → 退回 8**。

> ⚠️ **不要用 bookstack / keep 起步**：这两个是六个里唯一 YAML 解析不了的（`docs/03` §5.5），
> 且都没有参考实现。要起步用 `stackoverflow`(66) 或 `prestashop`(86)。

### M2 — 首分

**目标**：先有分。这一阶段的全部意义是**把"零分"这个风险消掉**。

#### ✅ 已达成的部分（2026-09-20，两次提交实测）

**提交链路全通**——这比原先预期快得多：

| 环节 | 状态 | 证据 |
|---|---|---|
| agent 被平台调用 | ✅ | `python3 /workspace/submission/main.py /tmp/arcbench/requirements-source --output-dir /workspace/template`，`exit code 0` |
| 参数与环境变量契约 | ✅ 已确认 | 需求是**位置参数（目录）**；输出是 `--output-dir`；注入 `MODEL`/`VISUAL_MODEL`/`ARCBENCH_*` |
| traceability 被平台读取 | ✅ | 平台打印 `109 requirements and 86 scenarios`（PrestaShop 精确值） |
| 应用骨架产出 | ✅ | 用**平台自带模板** `/opt/arcbench/templates/web-react-express` |
| 平台构建前端 / 起服务 | ✅ | 平台自己做 `npm install`（290 包）+ `build` + `start`，3000 端口可达 |
| 测试执行 | ✅ | 86 条全部跑起来 |
| **非零通过** | ❌ | **0 / 86 → 0.0% → 不在榜上** |

#### 🔄 保底路线的形态变了（重要）

**跑通平台的是自研管线（第 1 阶段 + `scaffold.py`），不是 ARC。**
ARC 至今**一次都没跑过**，"保底包 = ARC + 输入修复壳"这个框架**已不再是关键路径**——
自研管线自己就到达了"平台能调、能构建、能跑测试"的位置。

**ARC 仍留着做两条防线**（但不是主线）：
- `pip freeze` 存档为「已知可用版本集」（arc 依赖 `deepagents._models` 等**私有路径**，见 §8 R5）
- 若自研管线在 M3 卡死，ARC + 修复壳仍是一个可提交的备选

#### 剩余工作（**2026-09-21 重订：以 §7 M3b-1「关键路径」为准**）

> ⚠️ **`STATUS.md` 的「下一步」item 0 仍写着"先做模型/成本口径标定"——那条已完成**（见 `docs/12`）。
> **不要照它排期。** 本节的条目才是当前口径。

- [ ] **🔴 通用分块器**（关键路径第一条）—— 从 design JSON 派生块，替掉写死的 `CHUNKS`
- [ ] **🔴 `keep` 最小需求子集本地非零** —— **这才是"给应用加功能"的正解**：
      **不做整个 app**，只做能端到端走通的最少几条 → 从"0/86 不上榜"变成"Junior 榜上有名"
- [ ] **🔴 打包 + 平台跑一次**（需人在线）—— 同时答 A1（凭据）与 A2/V-6（口径）
- [ ] **A3 / V6** 去 `bbs.qiwoo.edu.cn/t/topic/16` 问死三个问题：
      **单次提交墙钟上限？token 配额上限？6 任务能否分次累积完成？**
- [x] **V5 已达成**——平台侧事实全部拿到，见 `docs/09-平台提交与报错记录.md`

#### 两条操作启发式（来自这两次失败，很值钱）

1. **六任务同时报同一个错 → 先查公共环节**，别去翻某个 app 的需求。
   两次失败都是六任务同错，因为错在与任务无关的公共环节。
2. **平台给明确文字，读它，别猜。** 第一次的报错
   `web template is incomplete: expected frontend/ and backend/ directories` 直接指明了缺口；
   我们此前从"跑得完"推了很久，不如读这一行。

### M3a — 能力探针 ✅ **已完成（2026-09-20）→ 命中第一行，§2 不翻转**

#### 🔴 能力口径（**引用时不许省限定词**）

> **原始产物 0/6（构建失败）→ 1 处人工修构建后 3/6 → 3 处人工接线修复后 6/6**

**这三个数必须一起引用。** 因为**平台上没有人在回路里**：
零人工的产物**连构建都过不去** → 在平台上就是 **0/86 = 不上榜**。
"6/6"是**手工修复后**的读数，它证明"判据能过、机制成立"，但**不是**我们当前能交付的水平。
（已发现 `STATUS.md` 有一处丢了"手工修复后"这个限定词——**这类限定词在传播中丢失，是本轮审核唯一要求改的表述**。）

#### 这次拿到的四条事实

| # | 事实 | 意义 |
|---|---|---|
| 1 | ✅ **机制成立**：生成能产出被外部测试认可的东西 | **§2 不翻转**，继续自研。ARC 仍只是防线 |
| 2 | **失败分布本身就是结论**：校验层 3/3 全对，失败全在"注册成功 → 会话 → 首页展示"这条链 | 要补的**不是"更强的生成"，是"生成后的自检与定向修复"** |
| 3 | 🔴 **上游模板自带 DB bug**（见下） | **它是提交二 0/86 的第二重解释，也是"加功能"不充分的原因** |
| 4 | 成本：5 次调用成功路径 **89,948** token；**含失败尝试 103,847**；**reasoning 占输出 87%** | 头号成本项是 reasoning，不是代码量 |

#### 🔴 上游模板 bug —— 它是**得分的前置**，不只是优化

`repos/arc-template/templates/web-react-express/backend/src/database/init_db.js` 里
`initPromise` 的 IIFE **没有 `return`** → **第二次起 `initializeDatabase()` 返回 `undefined`**，
而 `db_runtime.js` 的 `run/get/all/exec/withTransaction` **全都**把它当句柄用 → **第二次查询必抛 TypeError**。

- **它是平台自带模板的 bug**（`/opt/arcbench/templates/web-react-express`，V5 实测平台用那份）
  → **用它的队伍都会踩到**。
- **我们的对策**：`pipeline/generate/scaffold.py` 的 `apply_known_fixes()` —— 幂等 + 模式保护，
  **打在产物上**（不是只改自己副本，因为平台用自己的模板）。
- **推论：这是相对其他队伍的实打实优势**；且**任何依赖 DB 的场景在不修它的情况下必挂** ——
  所以它**不是优化项，是 M3b 的前置**。

#### 🔴 分叉表（测之前就登记，避免事后解释）—— 结果列已回填

| 探针结果 | 判读 | 动作 | **本次实测** |
|---|---|---|---|
| **非零通过**（哪怕 1/6） | ✅ **机制成立** | 进 M3b | ✅ **命中**（人工修复后 3/6 → 6/6） |
| **0 通过，但能构建** | ⚠️ 瞄准问题，非能力问题 | 先补 §4.1 第 5 条 a11y 规则，再重测 | — |
| **0 通过且构建不起来** | 🔴 结构性做不出 | **§2 翻 ARC 主线** | — |

> 分叉表的价值在于**它是测之前写的**。三条实测缺陷（相对路径写错 / 模板 DB bug / 前端三处接线）
> 全部落在前两行的范围内，没有一条是"结构性做不出"。

### M3b — 规模（拆成两步：**先闭环，再铺开**）

> **为什么拆**：M3a 的三条缺陷（相对路径写错 / 模板 DB bug / 前端三处接线）
> **全部可以自动发现**，而手工修**一个**需求就花了 4 处改动。
> 主赛道是 **460 个 ATOMIC 需求** —— **手工修不可能**。
> 所以"铺开"的前置不是更强的生成，是**把这次的手工修复换成自动闭环**。
> 带着一个构建不过的产物去铺 6 个 app，只会得到 6 个构建不过的产物。

#### M3b-0 — 自动化闭环 ✅ **已完成（2026-09-20）→ 两条判据都过**

**目标**：把 M3a 里那 4 处手工修复**换成机器做的**。

**出口判据（两条都过 ✅，证据已独立复核）**：

| 判据 | 结果 | 证据 |
|---|---|---|
| 1. 零人工构建成功 + 通过 ≥4/6 | ✅ **6/6**（超出地板 4/6） | `runs/20260920T-m3b0-quickstart-6of6-zero-human.log` → `6 passed (7.7s)`；build log → `✓ built in 1.45s` 且产出 `dist/index.html` |
| 2. Held-out（换样本、代码不改） | ✅ **构建成功** | `runs/20260920T-m3b0-heldout-build.log` → `✓ built in 1.20s`（样本换 `ticketbooking-demo` REQ-1.2） |

**"零人工"这条被特意验过**：主跑 14 次调用全部落在 **92 秒的连续窗口**内（`11:35:29Z → 11:37:01Z`），构建在 11:39Z——**中间没有可以手工介入的缝隙**。所以它不是"跑完再手工救"。

**过拟合也被定向查过**：`pipeline/verify/l1.py` 的五个检查器都是**需求驱动**的（路径与名字从需求文本抽），逻辑里**没有样本特异分支**（grep `if .*(register|welcome).*in ` 零命中）。→ **held-out 通过不是靠抄答案。**

> 🔴 **但能力口径要再加一个限定词：模型。**
> 这个 6/6 是在 **`deepseek-chat`（官方档）** 上拿到的，而**平台跑的是注入的 `MODEL`**
> （M3a 实测为 `deepseek-v4-flash`）。
> **机制是模型无关的，通过率不是。** → 完整口径是
> **「`deepseek-chat` 上、零人工、6/6」**，而**这条结论尚未在比赛模型上复现**。
> 复现动作已列为 M3b-1 的前置闸门（见下）。

**三件事，按此顺序：**

1. **L1 闸门（可执行性）** —— 让机器自动发现 5.1 与 5.3 这类问题：
   - **构建是否成功**（5.1 的相对路径写错会被这一步抓到）
   - **场景入口是否可达**（`a[href="/register"]` 这类）
   - **需求声明的可访问名是否都在**（5.3 的 `getByText(username, {exact:true})` 属此类）
2. **`apply_known_fixes` 扩成"模板体检"** —— 覆盖 5.2 那类上游 bug。
   现有的**幂等 + 模式保护**设计已被审核确认正确，继续沿用
   （`fix["buggy"]` 不命中就告警跳过，**不做盲替换**）
3. **定向修复** —— 把 L1 的发现回灌成修复指令。M3a 证明这些缺陷**小且具体**（合计 <30 行），适合定向修

#### M3b-1 — 铺开（**M3b-0 通过后**才开始）

**目标**：6 个任务**都非零通过**。0 通过 = 不上榜 = 等于没提交（实测）。

> **宁可 6 个 app 都 40%，不要 4 个 90% + 2 个不存在。** 但地板是**每个 app 非零**，不是"跑完"。

#### 🔴 关键路径（**2026-09-21 重订 —— 与上一版写法不同，这是本次修订最要紧的一处**）

**上一版写的是"闸门第 0 条 → 铺开"。那条路径走不到分。**
依据：`reviews/2026-09-21-合并审核-用于重订计划.md`。

- **闸门第 0 条用的是 `quickstart`，而它不在主赛道**
  （主赛道是 12306 / bookstack / ctrip / keep / prestashop / stackoverflow）。
  → **即使 quickstart 拿到零人工 6/6，M2 依然是 0**——榜单只认主赛道任务。
- **9/21 一天烧掉约 97 万 token，零人工满分 0 次**；而三次跑（c / d / e）的失败
  **全部落在"静态可判"的规则上**（`<meter>` 缺 ARIA 名 / ESM-CJS 混用 / 少一个右括号）。
  → **"用生成去验静态规则"是这一天最贵的教训**（见下方纪律）。
- **真正卡住一切的是分块器写死**：`pipeline/generate/implement.py:208` 的 `CHUNKS` 是**模块级常量**，
  7 块里 6 块指着注册应用的文件（`auth_repository.js` / `RegisterPage.tsx` / …）。
  → **拿它跑 keep 只会产出注册应用 → 必然 0/32。** 这是 M2 与 M3b-1 的**共同前置**。

**关键路径（重订）：**

> **通用分块器 → `keep` 的最小需求子集「本地零人工非零」 → 打包 → 平台跑一次（同时答 A1 与 A2/V-6）**
> → **这条链到"平台跑一次"就结束了；之后按下面的「6 个 app 的阶梯」继续**
> （那一段才是 M3b-1 的**主体**——这条链只到"第一个非零 + 平台口径"为止）。

**为什么是"最小需求子集"而不是整个 keep**：M2 要的是**非零**，不是满分。keep 有 86 模块 / 32 条判据，
全量跑一次是百万级 token，而 A4（比赛模型在真实规模上是否成立）**从未验过**。
只做**能端到端走通的最少几条** → 同时拿到"非零"、验了分块器、以最低成本答掉 V-6。
**这也就是 §4.3 降级地板落到实处的形态**（见下方「产物必须仍能起来」）。

**为什么是 keep**：**32 条判据，六个主赛道 app 里最小**
（bookstack 34 / stackoverflow 66 / prestashop 86 / 12306 117 / ctrip 125）；
且它的需求文件是**坏 YAML 之一，而 `yamlrepair` 已能解析**——顺带验了容错链。

**"通用分块器"的工作量是有界的一件事**（这一点决定了这个重订可行）：
`pipeline/generate/implement.py:175-194` 的**设计阶段已经是通用的**——它输出
`routes[{path,component,purpose}]` / `db_tables[{name,columns,unique}]` / `error_shape` 的 JSON。
**写死的只有"怎么把设计切成块"这一步**。所以要做的是：
**从 design JSON 派生块**（路由→组件、表→schema、加应用外壳与接线），并复用已有的字符预算机制
（`MAX_REQUEST_CHARS`）。**不是重写生成阶段。**

**验收分块器不要用生成**（这是上面那条教训的直接应用）：分块器产出的**分块计划本身是纯产物**——
**先只跑设计阶段（一次调用）拿到计划**，检查三件事：①**覆盖度**（design 里每条 route / 每个表都落进某一块）；
②**每块请求体积在预算内**；③**一文件一调用**（M3a 实测：合并输出越大越容易被网关断连）。
在 **quickstart 与 keep 两个样本**上都过，再上真样本生成。成本是**几千 token，不是几十万**。

**降级为"能力自检"的东西（不再当关键路径）**：
- **闸门第 0 条（quickstart 零人工 6/6）** —— **能力问题已经答完**：比赛模型可用
  （零人工 **5/6**，唯一失败是一条**静态可判**的规则，已进生成期硬清单，见 `a9d559c`）。
  **为刷 quickstart 满分再跑一次生成，不在关键路径上。**
  保留的唯一用途：通用分块器写好后，**用 quickstart 做一次最便宜的分块计划验收**（见上）。
- **`--reuse-app`**（零 LLM 调用复用已有产物判分）保留为**回归工具**，不是进度手段。

#### 🔴 共享前置：最小子集必须对「跨 REQ 的前置」闭合（2026-09-21 实测更正）

**先更正一条已被写进文档的错判。** `docs/12 §十三.五` 与第十六轮审核都写：
keep 的头号失败 `button[Toggle sidebar]`（断言级 ×14）**在需求文本里根本不存在**（"toggle" 零命中），
**只能从参考截图得知** → 据此指向"该投多模态 / 视觉通路"。

**三条实测都不支持它**（2026-09-21 复核，命令见下）：

1. **它在需求文本里，逐字**：`requirements.yaml:565` 与 `requirements.md:413` 都是
   `content: Click the "Toggle sidebar" control and then click it again.`
   （"零命中"来自**大小写敏感**的 grep——原文是大写 `Toggle`；`grep -i` 在 yaml / md 各有 **4 处**命中。）
2. **抽取器已经把它抽对了**：把这句话直接喂给 `_extract_from_text()` →
   **`('Toggle sidebar', 'button')`**。所以**不存在"抽不到、只能看图"的问题**。
   （顺带：`control` 确实不在 `ROLE_WORDS` / `ROLE_NOUNS` 里，但引号式规则不依赖它——
   所以"把 `control` 加进角色词表"也不必要。）
3. **它真正的身份是「共享前置」**：`tests/helpers.ts` 的 `openSidebar()`（`:188-192`）与
   `toggleSidebar()`（`:372-374`）都定位 `getByRole('button', {name: /^Toggle sidebar$/i})`，
   而 **32 个 spec 文件里有 8 个调用它们**（REQ-2.5.4 / 2.7.3 / 2.7.5 / 2.7.6.1–3 / 6.1 / 6.2）——**占 25%**。
   → **这 8 条不是"REQ-6.2 的功能"，是"被一个共享按钮卡住"**；而唯一通过的那条（REQ-2.1）
   恰好只用了 `openHome`，不碰侧栏。

**所以真正的结论是方法论上的一条，它对"最小需求子集"策略是硬约束：**

> 🔴 **最小子集必须对「共享前置」闭合。** 一个被 `helpers.ts` 这类**共享辅助**要求的名字/角色，
> 即使**点名它的那个 REQ 不在子集里**，也必须进生成 prompt 的硬清单——
> 否则扩子集时会一遍遍撞在同一件**从没告诉过模型**的事上。

**两条落地动作（都零 token、都由文本可推）：**
- 生成 prompt 的硬清单里，除"逐字兑现的引号名"以外，**再加一类：共享前置**——
  `complementary` 域内要有 accessible name 为 `Notes` 的 button；以及 `button` 名为 `Toggle sidebar`。
- 注意 `openSidebar()` 是**条件式**的：只要 `complementary` 区域里已经有名为 `Notes` 的 button，
  它就**不会去点那个开关**。→ **"侧栏默认展开 + 里面放 `Notes` 按钮" 与 "提供开关按钮" 是两条并列的路**，
  **两条都写进硬清单**（前者更省事，但只有后者能从需求文本单独推出）。

#### 🔴 硬清单第三类契约：**逐字，不许翻译**（2026-09-23 拍板，第三十轮审核钉出的根因）

> **根因**：prestashop 子集的 **4 个靶子里有 3 个被判"不可达"**，而它们**全都在全量需求文本里**
> ——`requirements.md:144`：`THEN: Display carousel, Popular Products, promotion area,
> Newsletter subscription, footer`——**而产物把它们渲染成了中文**。
> → 所以这是**契约被违反（翻译了）**，**不是信息缺失**（抽取器拿得到）。
> → **修法通用，而且平台侧同样有效**：判据按可访问名/可见文本找，**中英不对应就是找不到**。

**契约本身（写进生成硬清单）**：
> **需求文本里出现的可访问名 / 可见文本 / 靶子字面量，必须是逐字的——英文就英文、中文就中文，
> 不许翻译、不许改写、不许"意译成更自然的表达"。**

- **它和第一类（"逐字兑现的引号名"）是同一条原则**：那一类管**引号里的名字**，这一类管
  **需求行文里的名词短语**（`Display carousel, …` 这种祈使宾语位）。**两类都要进硬清单。**
- **为什么以前没写**：`PLAN.md` 里 `不许翻译` **0 处**（第三十轮审核实测）——
  **它从来没被写下来过**，而它**一直在生效**（判据按名字找）。
- **验收（执行侧做、报数）**：**prestashop 子集重生成 → ③ 的 `unreachable` 从 `3/4` → `0/4`。**
  > ⚠️ **边界：不要为它重生成已非零的 app**——闸门只要"非零"，重生成的收益（深度）
  > **远小于风险**（可能把已拿到的分掀掉，本项目有过一次）。**只做 prestashop 这一个验证样本。**

> ⚠️ **不要因为这一条就去掉图片通路**：`reference/*.png` 确实在需求目录里、平台也确实注入
> `VISUAL_MODEL`，那是**独立的能力问题**（见 §8 R9 与 A1 的视觉半边）。
> **但它的动机例被这条证伪了**，所以它**不该靠"某个名字只在图里"来争优先级**。

**复核命令（三条都跑过）：**
```bash
cd repos/arc-bench/arc-bench/webapp/keep/requirements
grep -n "Toggle sidebar" requirements.yaml requirements.md      # 逐字命中
grep -ril "toggle" .                                           # 大小写不敏感 8 处
cd ../tests && grep -rl "openSidebar\|toggleSidebar" *.spec.ts | wc -l   # 8
cd "$WORK/pipeline" && python -c "
import sys; sys.path.insert(0,'.')
from reqcompile.a11y import AccessibleNameIndex, _extract_from_text
i=AccessibleNameIndex(); _extract_from_text('Click the \"Toggle sidebar\" control and then click it again.','R','content',i)
print([(e.name,e.role) for e in i.by_req().get('R',[])])"   # [('Toggle sidebar','button'), ('','complementary')]
```

#### 🔴 硬清单的**回灌** + 「整串落在同一个元素」（2026-09-23 拍板：**卡着 `ctrip` 的那一条**）

**先摆机制（零 token，可复现）**：`ctrip` 第一轮 **0/125**。判据走
`tests/REQ-2.2.spec.ts:8` → `helpers.ensurePasswordLogin()` → `expectPasswordLoginForm(page)`，
而后者要求**三个单元**（`tests/helpers.ts:591-595`），**每个单元都是一条析取链（任一命中即可）**：

| 单元 | 析取链（`helpers.ts`） | 产物里 |
|---|---|---|
| ① | `/邮箱.*用户名.*手机号/` 或 `/email.*username.*mobile/i` 或 `/account/i` | 执行侧实测只有片段：`用户名`×2、`账号登录`×2 → **链里一环都没命中** |
| ② | `/密码/` 或 `/password/i` | 未命中（同一个表单，没渲染出来） |
| ③ | `/登录/` 或 `/^login$/i` 或 `/sign in/i` | 未命中（挂在进入登录页这一步之前） |

判分日志原文（`runs/20260923T094815Z-ctrip-score-ctrip1b.judges.log:358`）：
`Locator: getByRole('button', { name: /邮箱.*用户名.*手机号/ }).first()` → `element(s) not found`。
⚠️ **报错只显示首环**（`resolveNamed()` 在所有环都不可见时返回 `patterns[0]`）→
**不能从报错推断"只差首环"**。

🔴 **两条机器输出（"某名字不在文本里"这句话从此只能引用它们 —— AGENTS 硬规则 17）**：
1. **`grep -in`（大小写不敏感、`.md` 与 `.yaml` 都查、带行号）**：
   `邮箱` → `requirements.md:1646` / `requirements.yaml:2225`（**都在"下一步，验证新邮箱"**，另一个流程）；
   `用户名` → `:131` `:160`（**都在报错文案里**）；`手机号` → `:210`（**也在报错文案里**）。
   **三词同现且按序：0 处。**
2. **把整串原句直接喂给抽取器**：`_extract_from_text('THEN: The field shows 邮箱/用户名/手机号 as placeholder.', …)`
   → **输出空**；抽取器对 ctrip 抽出的 **302 条名字里 `用户名` / `手机号` 各 0 条**。
   → 结论**比"文本里没有"更强**：**这个"按序同串"的靶子，任何文本路径都产不出来**
   （连"假设需求里写了"都产不出来）⇒ **扩子集无用**（把 `REQ-2.3.x` / `REQ-2.4.x` 加进子集也拿不到）。
   它的唯一来源是 `requirements/reference/login1.png`。

**裁决一：回灌 = 认，并升为标准步骤——入口用 spec 侧（生成之前），不用产物侧（判分之前）。**
> 同一个信息，**spec 侧不要产物** ⇒ 在**这一次生成之前**就能注入；
> 而 ③ 要产物才跑 ⇒ 等它报出来，**这一轮已经结束，只能重跑**。
> `ctrip` 这一轮正是"**7 条只注入 1 条** → L1 只能要求那 1 条 → 判分 0/125 → 现在要再生成一次"，
> **那一步重生成是白花的**（7 条都注入的话，同一次 run 的修复轮就会去追剩下的）。

- **工具是现成的，不用新写**：`eval/subset_closure.py ctrip REQ-2.2` 的 **`[4] 汇总`** 节直接输出
  **「🔴 必须在产物里显示、而需求文本没点到」**，本轮实测**逐字列出 7 条**：
  `'邮箱.*用户名.*手机号'` / `'email.*username.*mobile'` / `'account'` / `'密码'` / `'password'` /
  `'账号登录'` / `'password login'`，**并且已经标注 `⚠️任一即可（析取组）`**。
  → 它**只要 spec + helpers，不要产物**（③ 要产物）⇒ **在这一次生成之前**就能跑。
- **规则（把"对表"从建议变成硬项）**：**`[4]` 的每一行都必须逐字进该 app 的硬清单——不挑不拣。**
  ⚠️ **本轮漏在哪**：ctrip 只注入了 `账号登录`（**7 条里的 1 条**），而 §7 ② 第 3 步写的是
  **"未闭合 = 不许开工"**——**规则在，但"要注入几条"没有判据** → **这就是它漏过去的地方。**
  → **判据改成可数的**：该 app 的 **`PIPELINE_EXTRA_HARD_NAMES`** 必须覆盖 `[4]` 的**每一行**（`grep` 可验）。
- **`subset_closure.py`（spec 侧）与 ③（产物侧）是同一张表的两个入口**，**③ 的本职不变**（判分前拦"名字对不上"）。

🔴 **到达面（2026-09-23 执行侧零 token 核过，`runs/README.md`）——这条决定"回灌"怎么写才有效**：
全仓 `grep EXTRA_HARD`（= 环境变量 `PIPELINE_EXTRA_HARD_NAMES`）**只有一处读取**
（`pipeline/main.py:504`，在**验证闭环**那一段里）→ 注入的硬名**只到这两处**：

| 到达 | 证据 |
|---|---|
| ✅ **验证闭环的 brief**（模型自检 + 定向修复的 prompt） | `main.py:508` 用 `extra_hard` 构 brief → `verify_loop(requirement_brief=…)` |
| ✅ **L1 的 `required` 契约**（`check_accessible_names`，**驱动修复**） | `main.py:532` = 静默硬名 + `extra_hard` |
| ❌ **初始生成**（`design` 那一次 + 各块实现） | `implement.py:459` 的 brief **没带** `extra_hard`；`main.py:478` 调 `generate_app` 时**也没传** |

→ **"回灌"的真实落地路径是：L1 抓到缺失 → 修复轮补上**（`ctrip` 的 `账号登录` 走的就是这条），
**不是"初始生成时就知道"**。⚠️ **判读时必须分清是哪一段 prompt**：复跑看见"串仍不在产物里"时，
**初始 prompt 本来就不会有它**，要判的是"**修复轮有没有把它追进来**"。
→ **这也解释了"7 条只注入 1 条"为什么致命**：**L1 只能要求它知道的那些**——
没注入的 6 条，**没有任何一轮会去追**。
→ **所以"入口在生成之前"这条仍然成立，但理由要改**：不是"初始 prompt 会带上它"，
而是"**这一次 run 的修复轮**会去追它"。③ 在判分前才报 ⇒ **这一轮已经结束，只能重跑**。

**裁决二：「整串落在同一个元素」的归属——不新开第四类、也不并进第三类，而是并进第二类（共享前置）。**

- **表示形态（写进硬清单的写法）**：一条链写成
  **「同一个元素的可访问名 / 可见文本必须含住这一整串（按序）」**，
  **不许拆成几个词各写一行**——拆了就等于**没回灌**（这一轮的产物就是"词都在、但分散在不同元素"）。
  ⚠️ **但也别读成"必须精确等于首环"**：链是**析取**（`⚠️任一即可`），**任一环可命中就兑现**；
  只是**首环最稳**（它是参考图里的原样，也是报错会印出来的那一条）。
- **为什么不新开第四类**（三条理由，都可复核）：
  1. **它已经在第二类里，而且工具早就能算出来**（上面 `[4]` 的输出）。新开一类会让"怎么写进硬清单"
     看起来像新问题，而它其实只是**一句写法细则**。
  2. **"整串落在同一个元素"不是 ctrip 独有**——它是**所有** `getByRole(…, {name: /…/})` /
     `getByText(/…/)` 靶子的通性（`helpers.namedLocators()` 的 9 个 locator 全是"一个元素的名字/文本整体匹配"）。
     单列成类会让人以为**别的 app 可以拆开写** → **误导性分类**。
  3. **第三类（逐字、不许翻译）明确不适用**：第三类的前提是"**词在需求文本里**、被译走了"。
     ctrip 这条**在文本里根本不存在**（上面两条机器输出）→ 塞进第三类**会污染它的前提**，
     下次有人会拿"逐字"去修一个文本里没有的词。
- **所以第二类的定义放宽一句**：从"点名它的那个 REQ **不在子集里**"放宽到
  **"点名它的 REQ 可能根本不存在"**——来源是 `helpers.ts` 的定位器链（乃至只在参考图里）。

**裁决三（2026-09-23 补，回答执行侧留的那个问题）：这一轮只加「落点约束」那一句，`extra_hard` 不接 `generate_app`。**

- **要改的只有一处文本**：`pipeline/generate/implement.py:256` 现成那句
  「同样按可访问名或可见文本兑现，不得改名」→ 补上 **「必须落在同一个元素的可访问名 / 可见文本里（按序）」**。
  它只影响**带 `extra_hard` 的 brief**（= 验证闭环那份）→ **对初始生成零影响**（那条路不带 `extra_hard`），
  **所以不碰生成主干**（符合 §7"铺 6 个 app 之前不动主干"）。
- **暂不把 `extra_hard` 传进 `generate_app`**（`main.py:478` → `implement.py:459`）。
  两个理由：① **修复通路已经在起作用**（`账号登录` 就是它补上的）→
  **"先验证它够不够"是最便宜的可证伪检验**（~26k token 生成 + 一次判分）；
  ② 接 plumbing = 改**所有 app** 的初始 prompt → **可能掀掉已非零的那 4 个**（本项目有过一次）。
- 🔴 **触发条件（写死，免得来回扯）**：复跑后若 **L1 抓到了、但修复轮两轮都没把整串落进同一个元素** →
  **那时才动 plumbing**（`main.py` 一行 + 初始/修复两条 prompt 都写落点约束）。
- 📌 **执行侧零 token 独立得到同一结论**（`runs/README.md`）："需要补的可能只是「**同一个元素、按序**」
  这一句约束，**不是**一整套新机制" + "L1 是源码子串检查，而把一段文本写成一个 `<label>` 同时满足 L1 与 locator"
  —— 两条独立路径同向，按这个执行。

**验收（执行侧做、报数；第 1 条零 token）**：
1. **生成前**：`[4]` 的每一行都出现在该次的 **`PIPELINE_EXTRA_HARD_NAMES`** 里（`grep` 可验）。
2. **生成后**：③ 的 `missing_units` 里 `expectPasswordLoginForm` 那条**消失**，
   或 `REQ-2.2` 由"预测失败"转"预测通过"。
3. **判分复跑**（生成 ~26k token）：这是第一次对"回灌"的检验。
   ⚠️ **读法（按上面的「到达面」）**：判"修复轮有没有把整串追进来"，**不是**"初始 prompt 里有没有它"。

> ⚠️ **边界（与第三类同一条，别搞混）**：**不要为了它重生成已非零的 app**。
> `ctrip` 尚未非零 → 可以做；`keep` / `bookstack` / `stackoverflow` / `prestashop` **不动**。

**裁决四（2026-09-23 第二轮·判分窗口里的新读数之后）：注入形态 =「L1 侧识别形态」（选 ②），不做 ①。**

**先摆价签（第三十五轮审核复算一致）**：把 `'邮箱.*用户名.*手机号'` 这类**正则体源码当逐字字面量**注入
→ L1 按**字面**去找（`l1.py:182` 的 `name not in blob`）→ **契约永远不可能满足** →
闭环 3 轮全 ❌，**17,281 token = 该轮全程的 52%**（自检 3×12,326 + 定向修复 4×4,955），
且 `final_l1=False` 写进记录。
⚠️ **更准的读法（第三十五轮）**：这钱**不全是白花**——产物确实把串收进了**各自单个元素**
（`<p>邮箱/用户名/手机号</p>`，**判据需要的形状**），**只是 L1 不认它**。

**两个选项，选 ②：**
- **① 注入时按形态分流**（正则体 → 注入它的**字面词元** + 按序/同元素约束）
- **② L1 侧识别形态**（正则体契约用**可命中性**判据，**与 ③ 共用一套**）

**为什么选 ②（三条，都可复核）**：
1. **① 会把判据改松，而且绕一圈还是回到 ②**：拆成字面词元注入后，L1 的**字面子串检查**
   会被"**词都在、但分散在不同元素**"满足 —— 那**正好是 ctrip 第一轮 0/125 的形态** →
   **假绿**（纪律 15 同型）。要补"同元素/按序"，就必须在 L1 侧做**可命中性**判据 ⇒ 还是 ②。
2. **② 让"判据的判据"与真判据同源**：判据要的是"**能被某个 locator 命中**"，
   而 ③ 的 `hittable_strings()` 已经实现了这件事（位置集合见裁决五）→
   **一处实现、两处消费**（纪律 14/18：静态优先、零 token）。反过来 ① 会造出
   **第二份"契约的表示"**（注入的词元 vs 原始正则）——**两份一旦漂移，闭环就在优化错的那一份**。
3. **钱的去向**：② **把这 52% 变成"可追到的"**（L1 与判据一致后，闭环要么真修好、要么不再报缺）；
   ① 只是**不再去追**（省钱，但 L1 与判据**仍然不一致**，"缺口发现"这一能力被削掉）。

🔴 **为什么不能两头都改（技术理由，不是风格问题）**：**做 ① 会让 ② 的输入消失**——
注入里不再有正则体，L1 的正则分支**永远不会触发**，"同元素/按序"这条约束就**没有执行者**
（三个词元各自都能被满足）。**两个选项在效果上互斥**，所以必须**同一个选项同时覆盖"注入什么"与"怎么判"**。

**② 的必要配套（不是第二处改动）**：既然 ② 保留正则体作为契约的表示，
**brief 就必须写明"这一条是正则体：要渲染成能被它匹配的串，且落在同一个元素"**——
否则"逐字兑现"这句话本身就在**指令模型把正则源码渲染进 UI**（第三十四轮审核记的活风险）。
这是**同一裁决内的呈现约束**（不改契约集合、不改判据），不是第二条逻辑路径。

**验收（前两条零 token）**：
1. **`ctrip2` 复跑 L1**：不再报 `'邮箱.*用户名.*手机号'`，`final_l1` 不再因它变 False。
2. **历史语料上跑，结论不得变**（纪律 16）：`bookstack` `451/451` + `TP=30/FP=0`；
   `stackoverflow` `1044/1044` + `TP=65/FP=0`。**新增项必须落在 TP 方向**；若出现 FP，
   先查是不是位置集合的问题（见裁决五）。
3. **金丝雀**：纯字面名（无正则体）的契约，行为与今天**完全一致**。

**裁决五（2026-09-23）：`②`/`③` 的输出、落点约束、位置集合都要**按族**——并伴随一个必修的漏报 bug。**

**族由"真正消费它的那个 helper"决定**，不是由同一个文件里长得像的那条链决定
（第三十四轮审核自己踩过这个：`fillPasswordLogin` 用字段族、`expectPasswordLoginForm` 用命名族，
**两族的消费方不同，不能混读**）。判法 = **在 `helpers.ts` 内走调用图**：
`消费方 helper → resolveNamed / resolveField → namedLocators / fieldLocators`，**按 app 逐个解析**，
**不硬编码 helper 名表**（各 app 的 helper 名不同）。

| 族 | 消费方（示例） | 位置集合 |
|---|---|---|
| **命名族** | `expectVisible` / `expectAnyVisible` / `clickIfVisible` / `countVisibleNamed` | **8 个 role 的可访问名**（button/link/tab/menuitem/option/radio/checkbox/heading）∪ **`getByText` 的文本节点** |
| **字段族** | `fillField` / `clickField` | `getByLabel`（label 关联 / `aria-label`）∪ `getByPlaceholder` ∪ `role∈{textbox,searchbox,combobox,spinbutton}` 的**可访问名** |

🔴 **现在的实现两族共用一个 9 类位置集合 → 两个方向都会误判**：
- **字段族假通过**：一个 `<p>密码</p>` 文本节点会被算成命中 `fillField(/密码/)` —— 而 `getByLabel` **不认 `<p>`**。
- **命名族假通过**：只有 `placeholder="Search"` 的元素会被算成命中 `getByRole('button', {name:/search/i})` ——
  而 `textbox` **不在** `namedLocators` 的 8 个 role 里，`getByText` 也**不读** `placeholder`。
  > ⚠️ **位置集合的成员资格要用"最小页面 + 每族 locator 各打一发"实测钉死**（零 token、本地），
  > **不要靠推理**——上面对 `placeholder` / `name` 属性的推断同样要过这一关。

**要做的三件**：① `②`/`③` 的**每一条**打上 **`消费方 helper` + `族`**（helper 名已有 ✔，只差族）；
② **落点约束按族写**（命名族 = "同一元素的可访问名/可见文本"；字段族 = "该字段的 **label / placeholder**"）；
   ⚠️ **`name` 属性不算落点**（第三十六轮审核 B：实测"只有 `name=` 的元素会被算成命中、而真判据打不到"，
   与上面那条"两族共用一套集合"同源）——**文本里也不许再写 `name`**，否则又是一次"被告知的与判据不一致"；
③ **可命中性判据按族取位置集合**（不再 9 类一起用）。

🔴 **伴随必修：`classify_helper` 把 `click*` / `open*` / `fill*` 类 helper 的实参整类判成"测试自己输入的值"。**
- **实测**（本轮零 token 复跑）：`classify_helper('clickNamed')='input'`、`('openBooks')='input'`、
  `('openShelves')='input'`、`('fillField')='input'`、`('login')='input'`；
  只有名字里恰好含 NEED 动词的才算 `need`（`clickIfVisible` 是因为含 **"visible"** 才侥幸进了 `need`）。
- **后果（`bookstack` 实测，最贵的一次漏报）**：**32/34 条失败的首阻，一条都不在 `②③` 的输出里**——
  `Books`（15 条）、`Shelves`（11 条）走 `clickNamed()`、`Email address`（6 条）走 `fillField()`，
  全被判成 `input` → 落进"🟡 测试会自己输入的值（**产物不必预先包含**）"那一栏。
  **`[4]` 对 `REQ-5.1` 的"必须在产物里显示"栏是空的，而 `Books` 就躺在下面那一栏里。**
  ⚠️ **而清单里那两条至少有一条不可信**：`remember me` 在 `login()` 里是 **`count()` 守卫的可选项**
  （`helpers.ts:174-177`，不满足也不挂）。
- **判据**：**`fillField` 的第 2 参（形参名就叫 `labelOrPlaceholder`）与 `click*` 的全部实参，
  都是"必须预先存在"的名字**；只有**填入值**才是"测试自己输入"。
  → 分类要跟着**形参语义**走，**不跟着 helper 名字里的动词走**。
- **验收**：`bookstack` 的 `TP` **= 32**（**真值上限**：34 条里失败 32 条 ⇒ `TP+TN=34`，**≥33 不可达**——
  第三十六轮审核 A 更正）、`FP` **仍为 0**、覆盖率 **仍 451/451**；
  ✅ **已达成**（审核自己复跑：`TP 30→32 / FP 0 / FN 0 / TN 2`，漏掉的两条被抓住）；
  `stackoverflow` 的 `1044/1044` 与 `TP=65` 不变。
  ⚠️ **先过历史语料再进闸门**（纪律 16）——这条改动会**同时改 `②` 与 `③` 的输出**，是最该先跑语料的那类。

#### 🔴 靶子清单交付到 prompt 的**两处缺口**（代码级，2026-09-21 复核）

第 5 条规则的**效果取决于靶子真的到了写代码的那次调用里**。复核发现两处断点：

**① 「必须逐字兑现 / 尽力做完」的分档没有进分块 prompt。**
- 有分档的渲染在 `pipeline/generate/implement.py:164` `build_requirement_brief()`（`:188-204`
  按 `e.pattern` 前缀分 `hard` / `soft`），而它**只喂设计阶段**（`:375` → `_design_prompt`）。
- **分块 prompt 用的是 `pipeline/generate/chunkplan.py:65` `render_nodes()`**（`:342` → `c["brief_slice"]`），
  那里**没有 hard/soft 之分**，所有名字都印成"必须提供的可访问名"。
- → 后果：`docs/12 §十三.二` 说的"生成 prompt 分两块"**在写代码的那次调用里不成立**；
  散文抽出的软靶子会被当成逐字契约 → **要么追幻影名字烧 token，要么与"尽力做完"的意图相反**。

**② 切片超预算时的降级会**把可访问名整块丢掉**——而且**实测已经触发了**（2026-09-22 复核）**
- `chunkplan.py:342-346`：`brief_slice > SLICE_MAX_CHARS`（`=6000`，`:45`）时，
  换成 `render_nodes_summary()`（`:90`），而它**只输出 `id: name —— 一句描述`，没有名字清单**。
- 🔴 **实测（零 token，复用已存设计：`eval/accept_chunkplan.py keep --design-file …`）**：
  **13 块里有 2 块触发 `slice_degraded`，而且恰好是最大的两块**——

  | 块 | 需求数 | 未降级应占 | 阈值 | 降级后 | 该块靶子 硬/软 | 名字还在吗 |
  |---|---:|---:|---:|---:|---:|---|
  | `frontend-home` | 22 | **13,306** | 6,000 | **2,400** | **0 / 60** | `Note content`❌ `Change color`❌ `Light green`❌ `Title`❌ `Close`❌ |
  | `backend-notes` | 17 | **10,144** | 6,000 | **1,916** | **0 / 41** | `Note content`❌ `Title`❌ `Close`❌ `available notes list`❌ |

- ⚠️ **⚠️ 这一条纠正本方案上一版写的修法——它是空操作。** 原文写"降级时**保留硬靶子**
  （引号式名字很短），判据是"降级后硬靶子数不变"。**但 keep 的硬靶子 = 0**
  （它的靶子**全部来自第 5 条规则的散文抽取**，见上表 `0 / 60` 与 `0 / 41`）。
  → 在 keep / prestashop / stackoverflow / ctrip 这些**散文式 app 上什么也保留不了**，
  而"降级后硬靶子数不变"这个判据在它们上面**恒真（0 = 0）——是一个永远不会失败的判据**。
  **这正好撞上纪律 15（"判据没报错" ≠ "判据跑过了"）。**
- ✅ **正确修法（已量，完全放得下）**：降级时**先渲染去重的 `(role, name)` 清单**
  （两块只要 **776 / 1,165 字符**，预算 6,000），**再用剩余预算放场景描述**。
  **判据改成"降级后 `(role, name)` 清单条数不变"**（这条才是能失败的判据）。
- **六 app 的靶子体量（去重后，零 token 可复算，供定尺寸）**：
  quickstart 10 / keep 43 / bookstack 60 / stackoverflow 98 / prestashop 148 / ctrip 207 / 12306 208。
- 🔴 **它可能就是 REQ-2.2 失败的原因，而且给出一个可证伪的预测**：
  `Note editor` 在需求文本里（§C1 已验）→ 抽取器抽到了（已验）→ **但它所在的两块把清单整块丢了（本条）**。
  → **修好降级分支后重跑 `REQ-2.2`，它应当通过。** 这是当前**最便宜的一个假设检验**
  （修法是纯静态改动，**不需要生成、不需要判分**）。
- 🔴 **排期含义：`R2`（全量 keep）必须排在修法之后。**
  现在跑全量 = 读一个**靶子被整块丢掉**的产物的分，钱白花。

> ⚠️ 这两条与上面的「共享前置」是**同一个问题的三个面**：
> 靶子**抽出来了**（规则 5 管），但**要么分档丢失、要么体积降级丢弃、要么共享前置没进清单** ——
> 三种都会让"规则 5 已落地"的结论在**大 app 上复现不出来**。
> 所以 M3b-1 的读数要在**至少一个非 quickstart 的 app** 上确认靶子确实进了那次写代码的调用
> （`plan_report` 里有每块的 `nodes` / `slice_chars` / `slice_degraded`，**用它当证据**）。

#### 🔴 新增工程项：把三类"断言"变成机器的"测量"（2026-09-22，第十七轮审核提出）

**起因**：`AGENTS.md` 第 17 条已经存在，**举例就是 `Toggle sidebar` 本身**，
而"某名字只在图里"这个错判**仍然第三次发生**（第十六轮审核又认可了一次）。
→ **纪律防不住，得靠机器。** 三件都是**零 token** 的：

1. **`eval/a11y_prose_probe.py` 扩一项测量**：对**需求文本**打印
   **"靶子里哪些名字在需求文本里一次都查不到"**（输出名字 + 未命中 / 行号）。
   → **从此"X 不在文本里"这句话只能由机器输出，不能靠肉眼下结论。**
   这是"把纪律变成判据"的一般形态——**凡是要靠人记得做但已经失败过两次以上的标准动作，都该做成脚本。**
2. **核一件事（已核，答案是"两个都跑"）**：抽取器对每个 app **同时**跑"引号规则 + 散文规则"，
   不是二选一。证据：`pipeline/reqcompile/a11y.py:396-414` 的**规则 E 就在 `_extract_from_text()` 后半段**，
   对同一段文本顺次执行 → 引号式精确名**不会**因为走散文路径而被漏掉。
3. **核"那 14 条 `Toggle sidebar` 断言落在哪个 REQ"（已答，见上「共享前置」）**：
   它们经 `tests/helpers.ts` 的 `openSidebar()`/`toggleSidebar()` 被 **8/32 个 spec** 调用，
   **而 `REQ-6.2` 不在生成子集里** → 性质是"**没实现**"，不是"瞄准偏了"。
   **这一条不要再重复排查。**

#### 🔴 拍板（2026-09-22）：**驳回「冻结外壳 + 只扩页面」**；**「外壳与页面分离生成」缩小后降级为后续项**；**先做两件零 token 的**

**依据**：第二十轮审核 + 本轮三方证据（已做实、可复核）：扩子集到 4 条需求 →
共享外壳切片 **1,416 → 2,512 字符** → **生成时丢掉了 REQ-2.1 的种子数据契约**；
闭环**三轮都发现了**（日志里"种子数据"出现 6 次）**却没修成**。弹性压缩无罪
（`e2-keep2`：同一份代码 + 2 条需求 → REQ-2.1 通过 408ms）。

**① 驳回「冻结外壳 + 只扩页面」——它与 M3b-1 的目标不相容**
- keep 的首页**就是入口枢纽**：`frontend-home` 的 ask 明确要求"每个入口都必须是**真链接**"，
  而 `App.tsx` 挂全部路由（`chunkplan.py:298-332`）。
- M3b-1 要扩的那些需求（`REQ-6.2` 侧栏开关、`REQ-1.1` 入口）**必须改首页 / 外壳** →
  **冻结它们 = 新需求不可达 = 把"扩子集"这件事本身堵死。**
- 而且它**不治本轮实测的那条**：坏的不是外壳 `App.tsx`，是 `HomePage.tsx` 的**内容**
  （diff 证据：187 → 191 行，**行数几乎没变**，是"换了内容"而不是"被改小"）。

**② 「外壳与页面分离生成」方向对，但（a）文件层面**本来就是分开的**，（b）要缩小后才成立，且排在后面**
- `frontend-app` 只写 `App.tsx`、`frontend-home` 只写 `HomePage.tsx` —— **已经是两个块、两个文件**。
  → 所以"分离生成"作为**新事**，只能是"**把外壳改成管线确定性生成**"
  （路由 + Provider + import 表全部由 `route_tokens` 推导，模型不碰）。
- 那条**方向对**——本项目在 `schema.sql` 上已经验证过一次"**把结构件从模型手里拿走**"，
  而且成功。但它治的是**另一类**失败：**路由 / 外壳被改坏 → 全站不可达**。
  **本轮那类（页面内容丢契约）它治不了。**
- → **降级为后续项**，等 M3b-1 的广度铺开之后再评估。

**③ 先做这两件（都零 token，且按你给的三条判据都更优）**

**3a. 修 `repair()` 的选择策略 —— 这是"发现了却没修成"的代码级解释**
- 现状（两处叠加）：
  ① `selfcheck.py:274` 是 `for rel, vs in list(by_file.items())[:max_files]` ——
     **按违规出现的插入顺序取前 4 个文件，零优先级**；
  ② 调用方喂的是 `mech + violations`（`loop.py:82`）—— **L1 的机械发现排在前面**。
- → **4 个额度先被 L1 占满**，而自检的"对不上需求"类（种子数据就是这一类）**被截掉**；
  **L1 是静态的，本来就看不出种子数据丢失** → 缺陷被发现了、却排不进修复队列。
- **改法**：**按优先级排序再截断** ——
  ① **回归项**（上一次判分里通过的契约，现在被违反）→ ② 自检的"对不上需求" → ③ L1 机械项；
  且**排在 ① 的文件允许突破 `max_files`**（**回归项不许被截**）。
- ⚠️ **附带一条要写进文档的口径**：`rounds=3` 时**最后一轮不修**（`loop.py:93`），
  所以**实际只有 2 次修复机会**——别按 3 算。

**3b. 加一条零 token 静态检查：需求声明的**种子数据字面量**必须出现在产物里**
- 需求文本里就是字面引号串（keep 实测 5 条）：
  `Seed data: pinned note "Sprint goals" and regular note "Groceries".`
- 而 **L1 现在完全没有这条**（`grep seed` 只命中一处 JSDoc 噪音）→
  **本轮那个失败（种子数据 ×0）零成本就能拦。**
- 这是纪律 14（**静态优先**）的直接应用。它已以 **fail-soft 落地**（`kind` 结尾 `-soft-missing` → 只告警、不驱动修复）。
- 🔴 **决定：升为判死**（2026-09-22，依据第二十一轮审核 §2.1 的 **4/4 逐格复现**：
  两份通过的产物缺 0、两份挂的缺 2，且两份挂的正好复现两次已知失败），**但附两条前置**：
  1. **先滤掉"搜索关键词"那类垃圾字面量**。需求里有
     `Seed data: note "Study schedule" whose title or content contains the keyword "st".`
     ——抽取器把**搜索词**也当成了数据字面量 → `'st'`（2 字符）在**任何**产物里都命中 →
     **它自己那条判据永远不可能失败**（纪律 15 同型）。→ 加最小长度 / 词形约束，
     或对 `contains the keyword` 句式不取字面量。
  2. **加进 `eval/regress_l1.sh` 的语料表**（子集作为输入参数），已知读数
     `m2-keep2 0 / e2-keep2 0 / e1b-keep4 2 / r3b-off 2`。
     否则下次有人动 `l1.py` / `prose.py` 时，它会**静默退化而回归仍报全绿**（纪律 16 只做了一半）。
- **判死的作用域**：**只对"本子集声明的种子字面量"**（即契约集）判死。
  **不要对全量种子判死**——实测不分子集时**又宽又松**（`m2-keep2` 会被报缺 20 条，
  而那 20 条**全部**属于子集外的需求）。
- **升为判死后的收益**：3a 已把"本子集种子字面量"列为**契约（priority 0）**，
  所以它一旦报缺就**必然驱动修复、且不许被 `max_files` 截掉**——
  **"静默丢契约"从"靠自检碰运气"变成"闭环必须修的那一条"。**
- ⚠️ **判别力的边界**（别把覆盖面读宽）：`{REQ-2.1, REQ-2.2}` 的种子**全部来自 REQ-2.1**
  （REQ-2.2 没有种子句）→ 这条检查目前**由一条需求支撑**。

**三条判据的对照**

| 选项 | ① 改动量 | ② 与「6 个 app 非零」相容 | ③ 会不会推倒已验证的部分 |
|---|---|---|---|
| 「外壳与页面分离生成」（全量） | 中–大：新写外壳生成器 + 重排生成阶段 | **有风险**：动的是生成阶段主干，铺 6 个 app 之前动主干要重跑回归 | 部分：外壳重新生成 → 已验的路由要重验 |
| 「冻结外壳 + 只扩页面」 | 小 | **不相容**：`6.2` / `1.1` 必须改首页外壳 → 新需求不可达 | 保住旧的，但**新的做不出来** |
| **③ 3a + 3b（建议先做）** | **小**：两处小改；3b **零 token** | **正相容**：让"扩子集"不再掀掉已拿到的分——**这正是 M3b-1 的前提** | **不推倒**：只改选择策略 + 新增一条 fail-soft 检查 |

> **一句话**：**这一轮不是"架构选型"的题，是"闭环的选择策略 + 缺一条静态检查"的题。**
> 架构级改动要建在**它治得了的失败**上——而本轮实测的失败，两件小改就够。

#### 🔴 6 个 app 的**阶梯**（2026-09-22 补：此前只设计了 `keep` 那一段）

**为什么必须补**：`§7` 的闸门是"6 个任务**都**非零"，而上面「关键路径」只写到
`keep` + 打包 + 平台跑一次 —— **其余五个 app 的推进路径从未设计**。
而这条路的约束**不是 token 是墙钟**：

| app | 判据条数 | 判分最坏（× 60 秒） |
|---|---:|---:|
| `keep`（已非零） | 32 | 32 分钟 |
| `bookstack` | 34 | 34 分钟 |
| `stackoverflow` | 66 | 66 分钟 |
| `prestashop` | 86 | 86 分钟 |
| `ctrip` | 125 | 125 分钟 |
| `12306` | **135**（117 个 spec 文件） | 135 分钟 |
| **合计** | **478** | **≈ 8.0 小时** |

**生成侧每个 app 约 3 万 token（可忽略）** → 所以瓶颈是"**判分是串行的**"：
`score_app.sh` 现在持锁，**同一时刻只能有一个 app 在判**。
**这 8 小时是这条路的真实代价（判分串行、且每次都吃全局锁），必须排进判分队列，不能等它自己冒出来。**

**① 顺序：按判分成本升序 —— `bookstack(34) → stackoverflow(66) → prestashop(86) → ctrip(125) → 12306(135)`**
- **为什么升序**：闸门是"6 个都非零"，所以**"每 1 分钟判分换到几个非零"是唯一的 KPI**；
  升序让非零**攒得最快**，也保证**任何时候被打断，已拿到的非零最多**
  （**触发规则已改成「完成即止」**——见上面「完成即止」那一块；
  **按日期、按"剩余墙钟 vs 剩余成本"判的旧写法都已作废**）。
- **`bookstack` 排第二的两个附带理由**：① 它是**另一个坏 YAML**——`yamlrepair` 那条路 `keep` 刚走过；
  ② 34 条，判分最便宜。→ **判据：先做"生成链路已被验证过 + 判分便宜"的那个。**
- **`12306` 排最后**：它最贵（135 分钟）。它是唯一带参考实现的 app，但**参考实现的用处是"失败后调试"**，
  不改变判分墙钟 → **不构成提前的理由**。
- ⚠️ **顺序只影响"各自什么时候非零"，不影响总墙钟**（串行总量恒为 ≈8 小时）。
  **所以别为了省几分钟重排**——排序的收益全在"非零攒得多快"。

**② 入门最小子集：把 `keep` 的教训变成一条可执行规则**
- **教训（已实测）**：子集必须对**跨 REQ 的前置**闭合——`tests/helpers.ts` 的 `openSidebar()`
  被 **8/32 个 spec** 调用，而它要的 `button[Toggle sidebar]` 由 `REQ-6.2` 命名、**`REQ-6.2` 不在子集里**
  → **那 14 条断言必挂**（见上面「共享前置」）。
- **共享前置在哪（这就是"怎么找"）**：**每个 app 的 `tests/helpers.ts` 就是前置的集散地**——
  它导出 `FIXTURES`（种子值）+ 一族**带定位器的辅助函数**
  （`openHome` / `openLoginPage` / `login` / `openShelves` / `openBooks` / `getShelfRow` /
  `expectVisible` / `fillField` / …；`bookstack` 实测 **17+ 个 `export`**）。
  **所以"前置"= 从子集的 spec 出发、经 helper 闭包能到达的那些定位器。**
- **规则（三步，全部零 token）**：
  1. **数候选 REQ 的 spec 调用了几个 helper**（`grep -oE "h\.[a-zA-Z_]+\(" <spec>`）。
     **调用越少越"入门"**——`keep` 的 `REQ-2.1` 之所以一次就过，正因为它只用
     `openHome` + `expectTextsVisible` 两个。
  2. **取闭包**：这些 helper 的**函数体**里（含它们互相调用）出现的**每一个定位器**
     （`getByRole` / `getByLabel` / `getByText` / `getByPlaceholder` / `toPattern(...)` 里的字面量）
     **都是该子集的前置**。
  3. **对表**：闭包里每个定位器，**要么需求文本点名了它**（→ 抽取器自动进硬清单），
     **要么必须显式写进该 app 的生成硬清单**。**未闭合 = 不许开工。**
- **工具**：建议 `eval/subset_closure.py <app> <REQ ids...>`，打印三栏——
  **闭包里的定位器 / 已被需求覆盖 / 未被覆盖（必须进硬清单）**。
  它零 token，而且**"照做/不照做"可直接判**。

**③ 判分前的零 token 筛（这一步省的是 30–135 分钟）**
- **现状的缺口**：`score_app.sh` 的冒烟挡"**起不来**"，**挡不了"起来了但名字对不上"**——
  而后者正是 `keep` 那轮 31 条失败的形态。
- **两半都现成**：① **测试侧的靶子**：`eval/a11y_prose_probe._test_targets()` 已经在抽
  `getByRole('x', {name:/…/})` 与裸 `getByRole('x')`，而且**它 glob 的是 `tests/*.ts`（含 `helpers.ts`）**；
  ② **产物侧**："这个名字有没有出现在前端源码里"——`l1.check_accessible_names` 正是干这个的。
- **要做的**：把两者对成一张表 —— **`eval/predict_judge.py <app> <artifact> <subset>`：
  逐 spec 打印"预测通过 / 预测失败 + 缺哪些靶子"。**
  ⚠️ **必须同时打印覆盖率**：现有抽取器只认 `getByRole` 那两种写法，
  **`getByLabel` / `getByText` / `{name: '字面量'}` 都漏** → 不打印
  "本 spec 共 N 处定位调用、解析器认了 M 处"，**低 M 会伪装成"没有缺失"**（**纪律 15 同型**）。
- 🔴 **判据（2026-09-22 更正：原写"预测失败条数 = 0 才送判分"，比闸门真正要求的严）**：
  **闸门要的是"非零"（0 通过 = 不上榜），不是满分** → 预测"3 条失败 / 31 条通过"时
  **送判分就能拿到非零**，而按"= 0"去修等于**用 token 与墙钟换深度**。
  → 改成 **"筛掉无望的"那一类判据（拦截用下界，排序用成本）**：
  1. **预测通过条数 = 0 → 拦，不许送**（一分不得，还要吃掉最坏墙钟）。**必须先修再送。**
     > 🔴 **修什么？先看上面「硬清单的回灌」那一节**——`ctrip` 这个形态（0/125）的修法是把
     > `subset_closure.py` 的 `[4]` 每一行**整串**写进硬清单，**不是把串拆成几个词**。
     > ⚠️ **理想情况它根本不该走到 ③ 才被发现**：`[4]` **在生成前**就能跑（零 token，不要产物）。
  2. **预测通过 ≥ 1 → 可以送**（闸门要的就是这个）；但**必须把预测失败条数当成本报出来**
     （用途见上「完成即止」：**报成本 + 同 app 跨轮对比**，不再是喂某个决策日）。
  3. ⚠️ **不再设"预测失败 ≤ K"这种上限**：多出来的信息用于**排序**
     （要铺多个 app 时按预测失败条数**升序**排），**不用来拦截**。
  4. 🔴 **覆盖率护栏：覆盖率低了，不许用于拦截**（2026-09-22 补）。
     **`覆盖率 < X%` 时，③ 的结论只能打印、不得用于拦截。**
     > ⚠️ **`X` 现在故意不定**：拦截分支**还没接进闸门**（见下面的标定结论），
     > 所以 `X` 只在它**真被启用**的那一刻才有意义——**那时再定，别现在拍一个没数据支撑的数。**
     > **为什么**：**误放行只花一次判分墙钟，误拦截花的是 token 迭代**——后者更隐蔽也更贵。
     > 抽取器已知会漏（`getByLabel` / `getByText` / `{name: '字面量'}`），
     > 而**低覆盖率伪装成"没有缺失"**这件事，原来只写在"必须打印覆盖率"上，
     > **没写在"拦截"上**——打印了但照样拿它拦，等于没加护栏。

- ✅ **标定已回来 → 阈值现在定了**（2026-09-22 晚，依据第二十七轮审核，我逐格核对过）：
  - **实测**：`bookstack` 全量 34 spec → **预测通过 4 / 失败 30**，**TP=30 FP=0 FN=2 TN=2**，
    **精度 100% / 召回 94%**，**覆盖率 451/451**；`keep` 三份小样本 TP=4 FP=0 FN=0 TN=2（**只有 6 个 spec 的规模**）。
  - **决定一：`预测通过 = 0 → 拦` 保持"信息"档，不接进闸门。**
    因为**至今没有任何标定样本命中过这个分支**——它**从没被触发过**（**等于没被测过**，纪律 15 同型）。
    **若将来真触发，先按覆盖率判它算不算数**（见上面第 4 条）。
  - **决定二：`predicted` 写进 RunRecord，字段名就叫 `predicted`**
    （它是"这次送判分要花多少墙钟"与"跨轮修法有没有效"的**唯一机器可读落盘**）：
    ```json
    "predicted": {"passed": 4, "failed": 30, "coverage": "451/451"}
    ```
    ⚠️ **覆盖率必须一起落**——否则"这次预测算不算数"没有依据。
    ⚠️ **9/24 已作废**：它原先的用途是喂"决策日"的 `B_pred`，**那个决策已整块取消**（见上「完成即止」）。
  - **决定三：③ 的召回盲区不影响它的用途，记在案但不改用途**——
    但**盲区的表述要精确**（2026-09-23 据第二十八轮审核 A 更正）：
    > **缺口 = 对 loose / 正则靶子走的是"源码子串检查"，而判据要的是"能被某个 locator 命中"。**
    > **例（`header`）**：它只在 `<header>` 标签名与 axios 的 `headers:` 键里出现 →
    > **源码里有，但任何 locator 都命中不到**。
    > ⚠️ 早先引的 `sidebar` 例子**不准确**（产物里除 `aria-label="Sidebar"` 外还有
    > `<h2>Sidebar</h2>` 文本节点）——**例子错、方向对**，所以按上面这条更正。
    **执行侧会按同一口径收紧工具**（要求 loose 靶子落在**文本节点 / `aria-label` / `title` /
    `placeholder` / `name`** 这些"可达位置"），**验收判据三条**：
    **① `bookstack` 精度不变**（`451/451` 覆盖率与 `TP=30/FP=0` 都不能变）
    **② `stackoverflow` 那条 FN 消失 ③ 金丝雀仍会失败**
    （这类"收紧"最容易连带把判据修坏——本项目有前例）。
    而**盲区本身仍在**：FN 的另一半是**结构性**失败（页面/流程不存在）——
    ③ 只查"名字在不在源码里"，**抓不住"流程没实现"**。但它挡的是"**名字对不上**"那一类，
    而那正是 `keep` / `keep2` / `bookstack` 上**最贵的形态**（`0/32` → 30 条超时）。

**④ 什么时候开始生成下一个 app：写死触发点**
- **触发点：当前 app 的产物通过 ③ 的对表 → 立刻开始生成下一个 app**（**不等判分结束**）。
- **为什么**：判分是**串行的墙钟**（30–135 分钟），而生成只要约 3 分钟 / 3 万 token。
  等判分结束再生成 = 把两段墙钟**串起来**，白等几个小时。**生成 ∥ 判分**
  （生成不是测量，**不需要锁**）。
- **配套**：判分要排成**队列**（锁是全局独占的，一个跑完接着下一个）；
  队列的**入队条件就是 ③ 通过**。

**⑤ 声明：R1 可能放松闸门，但本轮按保守口径设计**
- **闸门仍是"6 个都非零"**。**若** R1 证明平台按**聚合**计分（V-6 = (b)），
  则"每个 app 非零"是**保守上界**，可以降门槛（例如"≥3 个非零 + 一个做深"）。
- **但本阶梯不依赖 R1**：升序 / 闭合 / 对表 / 并行这四条在**任何口径**下都成立。
  → **R1 回来之后只调"要做几个"，不调"怎么做"。**

#### 🔴 深度阶段（6/6 之后）：KPI、第一刀、一轮的判据（2026-09-23 用**已有读数**定）

> **为什么现在就能定**：输入端已齐 —— 六份判分日志里有**每条失败的首阻 locator**
> （`waiting for …`），`③` 的 `missing` 列有**每条 spec 缺什么**，`runs/*.json` 有逐条结果。
> **不需要新读数**。**规模化的深度排期留到 12306 / ctrip 的新读数回来再写。**

**① KPI：`新增通过条数 / (Δtoken, Δ判分墙钟)`（边际效率），不是"某个 app 做到 X%"。**
- 榜按**通过率**排名，而分母是**固定的**（主赛道 478 条）→
  "`bookstack` 从 2/34 抬到 12/34（+10）"与"6 个 app 各抬 2 条（+12）"**在榜上是同一件事**。
  → 所以目标函数就是**绝对新增通过条数**，代价 = token **+ 判分墙钟**。
- 🔴 **判分墙钟 ∝ app 的总条数，而且每轮全量重判** → **在大 app 上反复迭代，每次都要付它全部的钟**。
  → **深度阶段要往"便宜轮"上走**（`keep` 32 / `bookstack` 34），**不是**往"条数多的 app"上走。
- 每一刀都要过 `§6` 的**噪声门槛（≥4 条）**才记账；**过不了就记"未达门槛"**，
  不许写进"已完成"（11 条翻面测试每轮随机换人，条数净增可能来自噪声）。

**② 第一刀 = `bookstack`：它的失败是"两层结构"，别的 app 是"每 spec 一个独立功能"。**

| app | 判据条数 | 判分最坏 | 失败块 | 失败块的**首阻** Top | 唯一靶子 |
|---|---:|---:|---:|---|---:|
| **`bookstack`** | **34** | **34 min** | 32 | **`Books` 15 + `Shelves` 11 + `Email address` 6 = 32（100%）** | **37** |
| `stackoverflow` | 66 | 66 min | 65 | `log in` 42 + `sign up` 3 | 158 |
| `prestashop` | 86 | 86 min | 85 | `CLOTHES` 44 + `sign in` 32 | 213 |
| `ctrip` | 125 | 125 min | 125 | `邮箱.*用户名.*手机号` 49 + `出发城市` 42 | — |
| `keep` | 32 | 32 min | 30 | `Title` / `Edit labels` / `Work` / `Search` … **各 2 条**（24 个不同名字） | — |

> 首阻 = 该失败块里**第一条** `waiting for` 的 locator（`judges.log` 的 `Call log` 段）。
> **它才是"这一条是怎么挂的"，比 `③` 的清单更硬**——见裁决五那条漏报。

- **读法**：`bookstack` 的 32 条失败**首阻只聚在 3 个名字上（100%）**，
  而 `prestashop` / `stackoverflow` 的失败摊在 **213 / 158 个不同靶子**上 →
  **前者一刀是"补共享前置"，后者的每一刀只能点亮一条**。
- **第二层也聚在一起**：`bookstack` 有 **19/30 条 spec 的其余缺口只剩"种子数据"**
  （`③` 的 `fixture:need`，如 `'Shelf 4.1'` / `'Book 5.1'`），而 `so` / `ps` 的"只差种子"是 **0 条**
  （含 fixture 缺口的：`bookstack` 25/30、`prestashop` 23/85、`stackoverflow` 9/65）。
  → **`bookstack` 是唯一"首阻 + 其余缺口都落在同一处"的 app**，
  也是 `R3`（"种子夹具漏播导致大面积假失败"）的**第一个量化样本**。
- **这 4 个名字都在需求文本里**（纪律 17 的机器输出：`grep -in` 带行号 + **喂给抽取器逐字产出**）：

  | 名字 | 需求行 | 所在 REQ | 消费方 | spec 数 |
  |---|---|---|---|---:|
  | `Books` | `requirements.md:200` | **`REQ-5.1`**（View Books List） | `openBooks() → clickNamed()` | 15 |
  | `Shelves` | `:87` | **`REQ-4.1`**（View Shelf List） | `openShelves() → clickNamed()` | 11 |
  | `Email address` | `:55` | **`REQ-2.2`**（Log In Successfully） | `login() → fillField()` | 6 |
  | `BookStack logo` | `:21` / `:28` | **`REQ-1.2`**（Back to Homepage） | `returnHomeByLogo()` | 6 |

  （抽取器实测输出：`Books` / `Shelves` → `button`；`Email address` / `Password` → `textbox`；
  `Login` → `button`。**四个名字全都在文本里、也全抽得出来**——见下面这条推论。）
  ⚠️ **前三个是"首阻"（合计 32 = 全部失败），第四个 `BookStack logo` 不是首阻**
  （它是 2 条 spec 的**第二层缺口**）——**放在同一张表里，是因为同一个动作就把它一起修了**。
- 🔴 **所以这一刀的动作不是"注入"，是"把它们点名的 REQ 纳入子集"**：
  **`{REQ-1.2, REQ-2.2, REQ-4.1, REQ-5.1}`**（当前子集 = `{REQ-1.1, REQ-2.1}`）。
  **零注入、零新机制**——名字由抽取器自动进硬清单。
  ⚠️ **这正是「共享前置」那条的第二次实测**：点名名字的 REQ **不在子集里**。
  ⚠️ **`REQ-3.1` 是自然的第五个**（它自己的 spec 也在 6 个 `login()` 消费者里），
  但**增加子集 = 增加外壳竞争** → **先不加**，看首轮结果再定。
- **预期点亮几条（三层，口径写清，别混读）**：
  - **上界 = 32/34**：只是"**首阻层清空**"，**不是**"会通过"——每条 spec 后面还有它自己的功能断言。
  - **中界 ≈ 19+3 = 22**：若同一次生成把**种子数据**也渲染出来，那 19 条"只差种子"的有机会；
    ⚠️ **第三层（各自 REQ 的功能）可能仍在** → **这是乐观上界，报告时要打折**。
  - **保守 = 3–5 条**：只把首阻 + 这 4 个 REQ 自己的功能做出来。
  → **报告时只报实测**；三层只用来**说明为什么选它**，**不当预期**（`B_pred` 系统性乐观的教训）。
- **代价**：一次生成（子集变大 → 预计 **40–60k token**，不是 30k）+ **一次判分 34 分钟**。
- ⚠️ **风险（已知结构，不是新发现）**：首阻里 **26/32 落在外壳（`Books` / `Shelves`）**——
  所以这一刀**顺带是"外壳够不够用"的第一次量化检验**。**若首阻没清掉，先查外壳那一块**，别去查页面。

**③ 一轮的判据（按 `docs/04`：比成员，不比条数）**
1. **首阻层（零 token，读日志）**：`waiting for` 集合里 **`Books` / `Shelves` / `Email address` 消失**。
   这条比条数硬：它直接证明这一刀**打到了点上**，且**完全不受判分噪声影响**。
2. **成员（主判据）**：新通过的集合里**必须包含** `REQ-4.1` 或 `REQ-5.1` 的 spec（这一刀直接负责的）；
   **且上一轮那 2 条（`REQ-1.1` / `REQ-2.1`）仍通过**（回归）。
   ⚠️ **不许只看"新增几条"**：11 条翻面测试每轮随机换人，净增可能来自噪声。
3. **条数（参考，且要过门槛）**：净增 **≥4** 才算"保留这一刀"（`§6`：静置跨度 2 → 门槛 4）；
   **<4 → 记"未达门槛"**，不进"已完成"。
4. **若第 1 条清空而第 2 条没中** → 结论是"**首阻层通了，但这一刀不够深**"→
   下一步是**把种子数据补齐**（第二层），**不是**再改外壳。

#### 🔴 顺序与纪律（2026-09-21 晚，收入全局建议的 R2/R3/R5/R6/R7）

**R6 · 所有进度闸门必须落在主赛道产物上**（**这条最要紧，它就是防漂移的规则**）
> quickstart **可以**当**工具验收**（例如上面那条"分块计划的廉价检查"），
> 但**不能当进度判据**。9/21 约 **97 万 token、零人工满分 0 次**，基本烧在一个不产生分的样本上。
> **判据**：任何写进 `STATUS.md` 作为"进度"的读数，必须注明是哪个**主赛道** app 的哪几条判据。
>
> 🔴 **显式禁令（2026-09-22 补，第十七轮审核要求"补一句硬话，否则下次很容易再漂回去"）**：
> **`quickstart` 上的通过率读数，禁止写进 `STATUS.md` 的「已完成」或作为任何闸门判据。**
> 它只能出现在两处：① 「工具验收」（分块计划这类零 token 的廉价检查）；
> ② 附在**能力自检**一节，且**必须同时写明"它不产生任何分"**。
> **理由**：这条禁令是有前科的——9/21 的漂移就是把 quickstart 满分当成了进度。

**R5 · 广度优先：先把 6 个都做到「已跑完 ∧ 非零」，再谈任何一个的深度**
> `§4.3` 早就写死"**宁可 6 个都 40%，不要 4 个 90% + 2 个不存在**"——
> **原则是对的，是执行漂移了。**
> ⚠️ **但它的强度取决于 V-6**：若口径是 **(b) 按聚合**，深度对平均分反而更有利
> （一个 60% + 五个 0 → 均值 10% ＞ 六个都 5%）。所以**「广度优先」在 (a) 下是硬要求，
> 在 (b) 下是可选项**——而 (a)/(b) 由 keep 那次平台跑答掉，**答案到手之前按 (a) 执行**（保守）。
> 🔴 **2026-09-23 补**：排期现在由上面「**完成即止**」定——**6/6 非零之前不做任何单 app 的深度投入**。
> 所以 R5 这段**仍是"为什么"**（0 分风险的对冲），**但不再是"排期选项"**：`(b) 下可选项` 那句话
> **不改变动作**——因为"0 通过 = 不上榜"这条对**整份提交**成立，赌 (b) 的收益不值得押上这个风险。

**R7 · 止损闸门（原写"决策日 9/25"，2026-09-23 去掉日期）**，仍然要**可判**
> 判据（**按状态触发，不按日期**）：**某个 app 的第一次判分拿到 0 通过、而且"缩范围"这一级还没试过** →
> **先按 §7 底部的保底退路缩一级**（缩 app 范围 → 缩需求范围），**不要原地打磨同一个 app**。
> ⚠️ **本闸门不改闸门，只改"卡住时先动哪一步"**——"6 个都非零"仍是闸门（R5）。
> 它原来那半句（"切最小可交付提交"）**保留但改成 M5 的状态项**：
> **提交硬墙之前，手里必须始终有一个"保证 `completed`"的包**（M5 的封包闸门）。

**R2 的前置 · 跑全量 keep 之前必须先调预算**
> 全量 keep 实测是 **13 块**（设计：6 路由 / 12 端点 / 4 表，`runs/20260921T-accept-keep-design.json`）。
> 而现在的预算是 **keep 25 万**（`pipeline/generate/implement.py:105` 的 `APP_TOKEN_BUDGET`；
> 六 app 合计上界 **176 万**，默认档 30 万）。
> **不调预算 → 触发降级 → 读的是降级产物的分 = 白跑一轮。**
>
> **为什么必须调（这是口径问题，不是数值问题）**：那批预算是**有意设的**（代码注释原文：
> "保守档；**quickstart 单需求 20.7 万是锚点**。要改就改这里，别临场判断"）——
> 也就是说它**按"5 块 / 1 个需求"标定**，而 keep 是 **13 块**。**锚点选错了，不是值偏小。**
>
> **按什么重新定标？——按墙钟，不按 token。**
> - **token 不是约束**：`README.md:83` 的 F01 写"Token 配额…对所有队一视同仁"，
>   而榜单第 4 行是 **813.55M token 且跑完了** → **我们一次提交的上界 1.76M 只是它的 0.2%**。
> - **墙钟才是约束**，而且现在是**精确公式**（见 `§4.3`）：
>   **判分 = 判据条数 × 60 秒**（`playwright.config.ts` 写死 + 严格串行），
>   六 app 最坏 **478 分钟**，已**超过**观测到的最大单跑 **379 分钟**。
>   而**预算调高 = 调用更多 = agent 阶段更长**，是在同一个墙钟里抢时间。
> - → **所以定标方法是"能塞进墙钟的调用次数"**：实测单次调用 ≈ **103 秒**
>   （run c：13 次调用 / 1,341 秒，reasoning 重）→ 13 块 + 设计 + 闭环 ≈ **40–60 分钟**，
>   在包络内是安全的。**要调的是"块数 × 单块预算"与这个 40–60 分钟的关系，不是"配额还剩多少"。**

**R3 · 闭环 ROI —— 必须分成 R3a / R3b 两件事**（2026-09-22 按第十九轮审核更正）

> ⚠️ **上一版把两者混成了一条。它们是不同的测量**：
>
> | | 做法 | 量的是什么 | 状态 |
> |---|---|---|---|
> | **R3a** | 同一份产物 `--reuse-app` **判两次** | **判分的重复性**（翻面率） | ✅ **已做**：`r3a-1` 与 `r3a-2` 都是 **1/32、同一条** → 两次之间**翻面 ≈ 0**。⚠️ 但 **n ≈ 1.3，够不着结论**，只能说方向对 |
> | **R3b** | **闭环开 / 关各生成一次** | **闭环的 ROI** | ✅ **已做（2026-09-22 闭合）**：闭环 **ON** = 29,213 token / **1 通过**（`REQ-2.1`，443ms）；**OFF** = 14,316 token / **0 通过**（`REQ-2.1` 落在 32 条失败里，11.1s 超时） |
>
> 🔴 **结论（2026-09-23 据实测更正，原推论作废）**：**闭环 +104% token 正是那 1 分的来源**，
> 且有独立静态证据（种子字面量 on 家族在、off 家族全无，`bash eval/regress_l1.sh` 的 `seed_literals` 列可复现）。
> → **原写"若关掉闭环通过条数不降，它就不该默认开着"的提法已撤回**——
> **闭环默认保持开着**；它确实占单轮 token 的一半，但**那是"非零"的价钱**（`§4.2 ③` 记的"把对的改坏"仍是它的风险，按 R8 的修复优先级约束）。

**闸门（逐条必须为真）：**

- [x] 🔴🔴 **第 0 条：用比赛模型复现 M3b-0 的 6/6** —— **结果 3/6，两次都是。但原因已定位，且不指向"模型差异"。**
      > **真实卡点是「建表不确定」**。判决性证据（同一批产物，看有没有真的 `CREATE TABLE`）：
      >
      > | 产物 | 配置 | 真建表 | 通过 |
      > |---|---|---|---|
      > | `m3b0` | deepseek-chat | ✅ `auth_repository.js:22/37` | 6/6 |
      > | `m3b1-low` | flash + low | ✅ `auth_repository.js:43/68` | **5/6** |
      > | `m3b1-flashdef` | flash 默认 | ❌ **只有 `init_db.js:74` 的注释** | 3/6 |
      > | `m3b1-gate0b` | flash 默认 | ❌ **只有注释** | 3/6 |
      >
      > **没建表 → 应用起来就崩**（`glm` 那组已给出机制：`SQLITE_ERROR: no such table: users`
      > → 未捕获 rejection → Node 退出）。
      > → **所以 3/6 不是"平台模型更差"，是"数据库 schema 没落地"。**
      > 上一版据此写"机制是模型无关的、通过率不是"——**那个判断被这条混淆推翻了**，
      > 现在只能说：**模型差异的影响尚未被干净地测过。**

      **后续三次跑（9/21）已经把这条答完，读数是（`docs/12 §十一`）：**

      | 跑次 | 配置 | 零人工读数 | 唯一失败 | 该失败是否静态可判 |
      |---|---|---|---|---|
      | run c | 比赛模型，建表已落位 | **5/6** | `<meter>` 没有 ARIA 名 | ✅ 已进硬清单 `a9d559c` |
      | run d | 同上 | **冒烟失败** | ESM/CJS 混用 | ✅ 已补 `check_module_system` |
      | run e | 同上 | **冒烟失败** | 少一个右括号 | ✅ 已补 `syntax_balance` |

      → **结论（能力侧）：比赛模型可用**；最好零人工读数 **5/6**，且**每次失败都收敛成一条可静态判的规则**。
      **⚠️ 但这次跑的样本是 quickstart，不是主赛道任务** → 它**不产生任何分**（见上「关键路径」）。
      **因此本条从"闸门"降级为"能力自检"：结论已足够，不再为它跑生成。**
- [ ] 🔴🔴 **建表必须确定化：schema 由管线落位，不交给模型改对地方。**（**这是 gate 0 的真实卡点**）
      模型单独产出一段 schema，**由管线注入到 `return database;` 之前**，并确保启动时执行。
      > **为什么不能靠"让模型改对位置"**：`init_db.js` 的引导注释在 `initializeDatabase()` 的 IIFE 里，
      > 而 **`return database;` 正是我们为修上游 bug 加的那一行，位置就在注释之后、`})();` 之前**。
      > 而模型改代码的习惯是**追加到块的末尾** → 很容易落到 `return database;` **之后**，
      > 成为**永不执行的死代码**，表现与"完全没建表"一模一样（就是上面那两组）。
      > **管线落位把这件事从"模型改对位置"变成"结构上不可能漏"。**
      **同一个 1 需求样本、同一套判据、只换模型** —— 换成平台注入的 `MODEL`（实测 `deepseek-v4-flash`），
      用**比赛网关**（不是官方档）。约 10 万 token。
      > **为什么它排在最前**：M3b-0 的 6/6 是在 **`deepseek-chat`（官方档）** 上拿到的，
      > 而**平台跑的是注入的 `MODEL`**。**机制是模型无关的，通过率不是。**
      > 不复现就铺开，最坏情况是"**本地 6/6、平台 0/6**"而不知为何——那时已经烧掉几百万 token。
- [x] 🔴🔴 **闭环必须能发现"应用一起来就崩"**（M3b-1 之前的**能力缺口**）
      **已按实际可达的形态落地**（2026-09-21）：
      ① **`eval/score_app.sh` 冒烟先行**——`GET /` 不是 200 就**明确拒绝往下跑判据**
      （否则 `0/N` 会被误读成"功能没做对"），并打一行 `ERR_CONNECTION_REFUSED` 计数；
      ② **崩溃类缺陷已进 L1**：`syntax_balance`（少一个括号，精确命中 run e）、
      `module_system`（ESM/CJS 混用，命中 run d）。
      > ⚠️ **定位更正**：上一版写"**闭环里**加一步冒烟（起服务 → 打入口 → 200）"——
      > **那个形态在平台容器里做不到**：agent 跑起来之后没有网络，`npm install` 装不了，
      > 服务起不来。**所以冒烟的落点是本地打分脚本**（`eval/score_app.sh`，即关键路径第二条的
      > 自然组成部分），**不是容器内的闭环**。执行会话已在 `docs/12:151` 记明这个理由。
      > 🟡 **而这依赖一条未验证的前提**：「agent 无网络」的**出处是课堂里讲师的一句预期**
      > （`class/03:438`「我们预期是智能体不能上网」），却被三处文档（`docs/02 §1` / `docs/09` /
      > `docs/11`）写成了"实测"，`pipeline/verify/l1.py:4` 甚至标着"实测"。
      > 而 `docs/09:133` 同时记着容器里「pip 走清华镜像；npm 走容器内配置」——**未必没有网络**。
      > → **验证很便宜：并进 A1 那一行启动日志**（同一次提交里加一次网络可达性探测）。
      > 若其实有网络，**L1 可以不必只是静态代理**，闭环也能真的把应用起起来打一下。
      > **实测反例（当初为什么提这条）**：`glm-5.3-flash` 那组 0/6，**6 条里 5 条是 `ERR_CONNECTION_REFUSED`**
      > （第 1 条能跑、走到断言才失败 → 应用当时是活的；第 2–6 条全连不上 → **应用在第一条测试之后就没了**）；
      > 正确形态是"**注册流程触发后端崩溃**"，而文档原先只写了"用户名拼接"（那解释 1/6）。
- [x] 🔴🔴 **第一条（关键路径）：通用分块器** —— **已落地**（`pipeline/generate/chunkplan.py`，`f1a1917`）：
      从 design JSON 派生块，替掉了原来的 `CHUNKS` 常量；`plan_report` 带每块的
      `nodes` / `slice_chars` / `slice_degraded`。
      ⚠️ **但它派生的块有没有把靶子送到写代码那次调用，还没在大 app 上验过**
      （见上「靶子清单交付到 prompt 的两处缺口」）——**这是它下一步的验收，不是它的完成**。
- [ ] 🔴🔴 **第二条（关键路径）：`keep` 的最小需求子集 —— 本地零人工非零** ← **这就是 M2 的内容**
      按场景索引挑出**能端到端走通的最少几条**（先满足"应用能起来 ∧ 该子集判据过"），
      生成 → `eval/score_app.sh` 打分 → 出 RunRecord（**零人工口径**）。
      ⚠️ 判据必须用 **keep 的官方测试**（`--app keep`），**不是 Lab04 那 6 条**——只有主赛道判据才产生分。
      **现状：已达到（1/32），但口径要带全**——生成子集是 `PIPELINE_REQ_IDS=REQ-2.1,REQ-2.2`
      （32 条需求只做 2 条），**而且用的是官方档**。
      > 🔴 **`1/32` 既不是能力读数，也不是平台预期**（第十九轮审核重申，前例：
      > **同一产物官方档 6/6、比赛模型 3/6**）。
      > **正确的读法**：它证明的是"**机制在主赛道 app 上可达非零**"。
      > **能力读数要靠 `R2`（全量 keep）；平台预期要靠 `R1`。**
- [ ] 🔴🔴 **第三条（关键路径）：平台跑一次 —— 拆成 R1a / R1b 两步**（**这一步只能由人点**：
      浏览器自动化不支持上传，`capability_unsupported`）
      > 🛑 **操作硬约束（2026-09-22，比任何技术项都急，第十七→十九轮审核连续两轮点名）**：
      > **R1 必须用 `output/bundle.zip` 的 12:11 及之后版本**（含 `f386500` 的"拼请求时就压缩"）。
      > **修复前的包在平台上跑全量 app 也会中途 `Run failed` → 0 分。**
      > - **若已经上传过旧包 → 要重传**（同一套逻辑，但那份包会死在生成阶段）。
      > - **读那次日志时，先问"生成跑完了吗"**——**别把"我们自己的中止规则"读成平台问题**
      >   （R2 就是这么死的：请求 19,267 字符 ≈ 80% → 抛异常 → `EXIT=1`）。
      **⚠️ 包必须是"带真实生成"的，不能又是零调用骨架跑**——零调用只证明链路，**不触发 A1**
      （前三次提交都是零调用，所以 A1 至今未验证）。
      - **R1a：同一个快照先只跑 `keep`**（agent + 判分 ≈ 1 小时）→ **答 A1**
        （掩码凭据行在 agent 启动日志里，**与跑几个 app 无关**；而 `docs/08` 已记
        "只跑 1 个任务不产生聚合分、不污染榜单"）。
        **若 A1 是 `absent`，省下另外 5 个 app 的墙钟。**
      - **R1b：A1 过了再跑 6 个** → **答 A2/V-6**（run 是否 `completed`、榜单是否出现条目）。
    > ⚠️ **R1a/R1b 的唯一前提是"同一快照能跑多次任务"**——那正是 `Q12` 要问的。
    > **问不到就按"只能跑一次"排**（即一次跑 6 个），**别赌**。
    >
    > 🔴 **R1 一次答三件事，这是把它排在最前的又一个理由**（2026-09-22 补，`Q20`）：
    > **A1 + A2/V-6 + R10/V-4**。对账方法是一行话：
    > **拿同一次 run 的两个数比——我们自己的合计（`.arc/metrics.jsonl`）vs 平台榜单那一行的 `Token`。**
    > | 观测 | 结论 |
    > |---|---|
    > | 平台 ≈ 我们的 | 计量口径正确 → 前几名 `0 token / 80–100%` 是**异常** → 按 `Q17.b` 问主办方（规则/公平性） |
    > | **平台 ≈ 0，而我们是几百万** | **计量只统计走平台网关的那部分**（最可能；我们本地迭代就在用 `PIPELINE_LLM_*` 指向官方档）→ **R10 结案，`pass/CNY` 的优化方向要重估（M4 的整个前提）** |
    > | 中间值 | 又是一条新口径（例如只算 agent 阶段）→ 记下来当新基线 |
    > **op 前提（必须在 R1 之前做）**：把这次 run 的 `metrics.jsonl` **单独存一份并记进 RunRecord**
    > ——顺手补掉 `model: unknown` 那个记录缺口，**否则事后无法归属，对账做不成**。
- [ ] 🔴🔴 **第 3.5 条（**排在所有全量生成之前，0 token**）：修掉切片降级丢靶子**
      改 `chunkplan.py:342-346` 的降级分支：**先渲染去重 `(role, name)` 清单、再用剩余预算放场景描述**；
      判据换成"**降级后 `(role, name)` 清单条数不变**"（旧的"硬靶子数不变"在散文式 app 上恒真，
      是个永远不会失败的判据）。
      > **为什么它必须排在前面**：实测 13 块里有 2 块已触发降级（最大的两块），
      > **现在跑全量 = 读一个"靶子被整块丢掉"的产物的分，钱白花。**
      > **修法是纯静态改动——不需要生成、不需要判分，成本是零。**
      > **而且它给出一个可证伪的预测**：修好后重跑 `REQ-2.2`，**它应当通过**（见上「靶子交付」条）。
- [ ] 🔴 **第四条（V-6 到手之后才定顺序，两条路线二选一）**：
      **⚠️ 无论走哪条，前面都必须先过第 3.5 条（修降级）。**
      - **(b) 按聚合** → **先做深**：跑 **keep 全量**（13 块；**先调预算**，否则降级=白跑），
        拿第一个真正的"离上榜多远"读数。
      - **(a) 按任务** → **必须铺广度**：**每个 app 各做一个最小子集**（`PIPELINE_REQ_IDS` 已经支持），
        拿到"6 个都非零"，**再谈任何深度**。
        > 这条路顺带把 **A4（规模）** 也验了一半：六个 app 的设计各派生出不同的分块计划，
        > **通用分块器的"通用"才第一次被真正考到**。
        > ⚠️ 而且六个 app 的靶子体量差异很大（去重后 quickstart 10 / keep 43 / bookstack 60 /
        > stackoverflow 98 / prestashop 148 / ctrip 207 / 12306 208）——
        > **降级阈值 6000 字符在大 app 上会频繁触发**，所以第 3.5 条对这条路更重要。
      - **答案到手之前按 (a) 准备**（保守）——但要**先把两边的成本都算出来**再决定。
- [x] 🔴 **底层能力（已具备）**：每个 app 都能**在本地用官方测试打分**（去平台化迭代的接线）
      生成 → 起服务 → `npm run test -- --app X --target-url http://127.0.0.1:<port>` → 出 RunRecord。
      **为什么它值钱**：M3b-1 的目标是 **6 组**「生成 → 构建 → 起服务 → 跑测试 → 定向修复」的**多轮**循环。
      **若唯一测试场地是平台**，每一轮都要用户上传 + 逐任务点选 + 吃配额 + 等待
      → **用户成为吞吐瓶颈**，而这个阶段恰恰最需要多轮迭代。
      **本地条件已经具备**：6 个 app 的官方测试都在
      `repos/arc-bench/arc-bench/webapp/<app>/tests/`，runner 支持 `--app X --target-url`；
      `docs/10 §八` 已写过同类做法。
      **纪律沿用不变**：静置条件 + `load_snapshot` + 每轮重置库（`docs/04` §三）。
      → **平台提交从"用来试"变成"本地过线后的确认"。**
- [ ] 🔴 **零人工介入下构建成功** —— **这条目前不成立**（M3a 原始产物构建就失败），
      而它此前是隐含前提。**"零人工"是硬要求**：平台上没有人能进去修一行 import 路径。
      **由 M3b-0 交付。**
- [ ] 🔴🔴 **平台容器里能不能调到模型、拿什么凭据 —— 至今未验证**（**这条会决定 M3b-1 在平台上能否启动**）
      在 agent 启动日志里加一行**掩码的存在性检查**：
      `key: OPENAI_API_KEY=set(len=56) / ARC_API_KEY=absent / base_url=… / model=…`。
      > **为什么它列在闸门里**：`docs/09 §1` 记的平台注入变量是
      > `MODEL` / `VISUAL_MODEL` / `ARCBENCH_*`——**没有 API key**。
      > 而此前三次提交（probe-v1 / probe-v2 / doctor）**全是零 LLM 调用**，所以这条**从未被触发过**。
      > `llm.py` 的回退链本身是对的（`PIPELINE_LLM_*` → `OPENAI_API_KEY`/`ARC_API_KEY` → 工作区 `.env`），
      > **但若平台一个 key 都不注入而网关又需要 key，M3b-1 会直接失败在第一次生成调用上。**
      > 下一次提交（哪怕仍是零调用）就能定论——**别等铺开时才发现**。
- [ ] 🔴 **产物里的 `init_db.js` 已被修补**（模板 DB bug），且 **DB 相关测试不再抛 `TypeError`**
      —— 本地能验修补是否落地；**平台那次跑确认它在平台路径上生效**，所以仍要进提交核对清单
- [ ] 🔴 **每个 app 的 token 上限（配置项）+ 到额降级 —— 而且降级产物必须「仍能起来」**
      §4.3 只有**墙钟**预算，没有 **token** 预算。量级已实测：
      **一次"生成 + 闭环"在比赛模型上 ≈ 20–31 万 token / 22–34 分钟，reasoning 占输出的 86–93%**
      （run c 单次 **207,533 token / 1,341 s**）；一个 86 模块的 app 是**百万级**，
      6 个 app 是**数百万到千万级**，而**配额上限至今未知**（A3/V-6）。
      **在未知配额下铺开 = 铺到一半可能没额度了。**
      > 🔴 **降级的地板要写准**：不是"跑完一半就停"，而是
      > **停止填充分块之后，产物仍然是一个「能启动 + 已填部分可用」的应用**。
      > 这才叫降级——**半截产物（缺路由/缺表）在平台上是 0 分，与没提交等价**。
      > **结构上它是成立的**：§3 的第 [3] 步本来就要求骨架**确定性写文件（路由槽 + 空组件 + API stub + schema + seed）**，
      > 分块只是**往槽里填**。所以"填空的完成度"天然就是降级旋钮：
      > **按需求的依赖顺序增量填，每填完一组就验一次"应用还能起来"，到额就停在上一组。**
      > → **于是"最小需求子集"（关键路径第二条）本身就是这个降级路径的最低档**，不是额外的机制。
- [ ] **模型/成本口径已标定**（见下）—— **不能跳过**，因为 M3b-1 是 token 量爆炸的地方
      ⚠️ 标定要拿**两栏**成本当基线（成功路径 / 含失败尝试），否则是**在错数上标定**
- [x] 🟡 **三处便宜的收尾**（第十四/十五轮审核点名）—— **2026-09-21 三处都已落地**：
      ① `check_js_balance.py` / `jsscan.py` 补了默认排除 + 认正则字面量（`bc62006`）；
      ② `<meter>` 那类"必须真有 ARIA 名来源"做成了 L1 检查 `check_aria_name_sources`
      （`l1.py:407`，与 `syntax_balance` / `module_system` 一起，L1 现在共 **11 项**）；
      ③ `eval/regress_l1.sh` 回归语料脚本（`bc62006`）。
- [ ] 🟡 主赛道单任务**无人工非零通过** → 6 任务**全部非零**
- [ ] **墙钟预算与降级模式（§4.3）要从第一版就做进去**，并在真实预算耗尽时验证过降级路径。
      ⚠️ 降级地板是"**至少一个场景端到端可走通**"，不是"能构建能启动"
- [ ] 三层验证闸门（§4.2）上线；第 5 条 a11y 规则按 §4.1 的裁定的三个约束落地
- [ ] **a11y 靶子的价值已被实测翻倍**：86 条测试正是卡在"找不到可访问名 / 可见文本"，
      我们抽出来的清单**就是生成阶段可以直接照着填的靶子**

> **平台那次跑排在本阶段中段，不要压到最晚日。** 它受用户可用性影响。
> 而且按上面第一条（本地打分为主证据），**用户不在线不阻塞进度**——
> 平台只做"本地过线后的确认"。

#### 🔴 四条纪律（2026-09-21 重订时加入；每条都对应一次已发生的损失）

1. **「零人工」是唯一的读数口径。** 手工补过一个属性、改过一行接线之后的读数，**必须标注**，
   而且在**任何闸门判据里都不算数**。
   > **为什么**：本方案有过**两次"6/6"**，两次都不是零人工读数——
   > M3a 那次是**手工修了 3 处接线**，`gate0c` 那次是**手工补了一个 `aria-label`**。
   > **零人工最好读数 = 5/6。** 口径不写全会一路传下去（第十四轮审核已经因为一次口径混淆，推翻过我两个结论）。

2. **静态优先：能用零 token 静态判的规则，不许用生成去验。**
   > **为什么**：9/21 一天约 **97 万 token、零人工满分 0 次**，而三次跑（c/d/e）的失败
   > **全部是静态可判的**（`<meter>` 缺 ARIA 名 / ESM-CJS 混用 / 少一个右括号）。
   > 每一条都已经补成了 L1 检查或 prompt 硬清单。**用生成去验静态规则是本项目最贵的浪费形态。**

3. **"判据没报错" ≠ "判据跑过了"。每个闸门都要留一条"判据确实执行了"的证据。**
   > **为什么**：`gate0_run.sh` 的路径 bug（缺 `cygpath -w`）曾让**判据①在 c / d / e 三次里静默失败**，
   > 而三次都"看起来正常"。→ 闸门记录里必须能回答"这一次判据真的跑了吗、跑了几条"。

4. **历史产物当回归语料**（`%TEMP%` 下已有 6+ 份）：每加一条新检查，**先在语料上跑一遍**，
   看它是"又宽又松"（假阳性）还是"又紧又假"。
   > **为什么**：`check_db_tables` 的假阳性让闭环**一直在修不存在的问题**（每条假发现 = 一次全量重发，而闭环占 65% token）；
   > 而 `check_js_balance.py` 指向装过依赖的目录时**扫 7616 个文件、报 322 条误报**（缺 `node_modules` 默认排除）。
   > **没有语料，就无法在接进闸门之前发现这两种毛病。**

#### 🔴 纪律：每次提交前，必须在**平台模型**上跑一次确认

**平台模型 = 网关的 `deepseek-v4-flash`、且不加 `reasoning_effort`。**

> **为什么它必须是一条纪律而不是一次实验**：标定刚证明**换个模型能把 6/6 变成 0/6**（A 组 vs C 组）。
> 而本项目已决定"本地迭代用 `deepseek-chat`（官方档，便宜）"。
> **这两件事放在一起，正好构成"本地 6/6 → 平台 0/6"的经典组合。**
>
> **本地便宜迭代 + 平台模型确认，两件事都要，不能只留前者。**

适用范围：**M3b-1 的每一次平台跑、以及 M5 的每一次提交**。

#### ⚠️ 铺开前必须先标定模型与成本口径

**M3b-1 是 token 量爆炸的地方**（1 个需求约 9 万 token → 86 个需求是另一个量级）。
而 M3a 已实测 **reasoning 占输出 87%**，且这**取决于选哪个模型**。
带着错的模型铺开 = 按错的口径烧掉大部分预算。

**所以 `PLAN §7 M4` 里那三个"抑制 reasoning"的旋钮实验，要在 M3b-1 之前做掉**——
它不是 M4 的优化，是 M3b-1 的**标定**。区别在于目的：
这里只用同一个 1 需求样本**选型**（约 10 万 token 一次），不做系统性降本；系统性降本仍留在 M4。

#### ⚠️ 三个结构级问题应在关键路径上答掉

| 项 | 为什么它卡在这 | 怎么答（都很便宜） |
|---|---|---|
| **A1 平台容器里有没有凭据能调模型** | **从未验证**：三次提交全是零 LLM 调用。若平台一个 key 都不注入，**所有本地成果在平台上归零**——这是全项目最大的单点风险 | 启动日志加一行**掩码的存在性检查**，**随下一次提交（哪怕零调用）** |
| **A2 / V-6 "非零"的判定口径** | **(a) 按任务 vs (b) 按聚合**，工作量差约 6 倍。**M3b-1 的目标怎么写取决于它** | **就用 keep 那次平台跑**：看 run 是否被标 `completed`、榜单是否出现条目 |
| **A3 / V-5 平台墙钟与配额上限** | 它决定**一次 run 内能塞进多少次生成调用** → 决定"6 个都非零"够不够得着 | bbs 提问（**优先问墙钟**）；同时看 keep 那次的 runtime 与 token 计数 |

#### 🔴 保底退路（**每一级"还没非零"时都可用的默认动作**）

**0 通过 = 不上榜 = 等于没提交。** 所以退路的目标只有一个：**让某个主赛道任务非零**，
哪怕它是"最简应用"。

按**代价从低到高**排，**卡住就逐级下移**（触发 = 上表 M3b-1 那条"连续两轮未非零"），
不要原地打磨。**全部落在"我们已经会做的事"上：**

1. **缩小 app 范围**：`keep` 的最小需求子集（关键路径第二条）——本来就是按这个目的设计的。
2. **缩小需求范围**：从 keep 的场景索引里挑**判据最少、依赖最少**的那一条
   （例如"创建一条并看到它"），实现它 → 该场景的判据过 → **非零成立**。
   > **这条的力量在于：它把"非零"与"功能完整度"解耦。** 榜单要的是非零，不是完成度。
3. **接受"榜上有分即止损"**：拿到非零就停手，把剩余时间投到"再多一个 app 非零"上，
   **不再追任何单个 app 的完成度**。

**替代方案（若第 1 级连一条都走不通）**：换更小的主赛道 app 再试
（bookstack 34 条，是第二小的；**注意它也是坏 YAML**）——但它仍是"缩范围"，不是换路线。

> 🔴 **ARC 已退出初赛退路（2026-09-22 定，依据 `Q14` 的答复）。**
> 三条理由：① **一个从没执行过的退路不是退路**，是"计划形状的期望"，
> **挂到最后一刻去够是最坏的时机**；② **跑一次也答不了该答的问题**——`arc compile`
> 只能在 quickstart / 最小样本上验"能不能跑"，**验不了"能不能得分"**（那要主赛道非零），
> 而它比现管线更贵；③ 它的结构性劣势没变（严格串行 DFS / 每叶子 3 场会话 / TDD 最多 15 往返）。
> **`§2` 的"ARC 作为防线"仍然成立，但"防线"不再等于"退路"**——两者的区别是：
> 防线是"如果时间充裕才去验"，退路是"卡住时会去够的东西"。
> → **这条要在 `§2` 与 M2 的"剩余工作"里同步**，防止后面还有人去够它。

> **反过来说**：只要第 1–2 级里任意一级拿到非零，**M2 就达成了**，
> 剩下的时间才是"要不要铺到第 6 个任务"的问题。**别把顺序倒过来。**

### M4 — 降本（只降 token，不加功能）

> 🔴 **前提警告（2026-09-20 榜单一手数据）**：M4 的前提是"**先能跑，再省**"。
> 但榜上**真正跑通**的两个队伍花的 token 是 **273.28M（93.0%）与 813.55M（99.5%）**。
> 如果"能跑到有分"的门槛本身就在这个量级，那么**省 token 的杠杆比本方案最初假设的小得多**——
> **"到得了能跑"才是全部，"省"是次要的。**
> 这同时让 R10（0-token 却拿 100%）更值得查：它可能说明我们对计量的理解有缺口。
> → **M4 不得在 M3 达成之前启动**；且启动后要**先量一遍"能跑"的实际成本**，再决定优化空间有多大。

**目标**：`pass/CNY` 上升。这一阶段**不做任何"改行为"的改动**——在噪声压下去之前那类改动测不出来。

#### ✅ 第一杠杆已找到并验证：`reasoning_effort`（在**比赛网关**上实测，约 3×）

`runs/20260920T-m3b1-model-probe.json`（**在 `api.arc-bench.com` 上跑**）：

| 配置 | 总 token | reasoning 占比 | 产出代码字符 |
|---|---:|---:|---:|
| `deepseek-v4-flash`（默认） | **1,377** | **0.77** | 856 |
| `deepseek-v4-flash` + `reasoning_effort=low` | **545** | **0.11** | **1,269** |
| `deepseek-v4-flash` + `reasoning_effort=minimal` | **442** | **0** | **1,125** |
| `glm-5.3-flash`（同 prompt） | **481** | 不报该字段 | **1,450** |
| `kimi-k2.7-code-highspeed` | — | — | ❌ `400 invalid temperature: only 1 is allowed` |

**总 token 降到约 1/3，而产出的代码反而更多**（1,125–1,269 vs 856 字符）。
→ **成本侧：这是目前找到的最大杠杆。** `glm-5.3-flash` 是另一个候选（同样约 1/3）。

> ⚠️ **但通过率侧结论不成立，必须先重测再采用。**
>
> 原写"`reasoning_effort` 不进生成阶段（掉通过率）"——**那个对比被"建没建表"混淆了**：
> 同网关下唯一的干净对照是 **low = 5/6 > 默认 = 3/6**（见 §7 M3b-1 gate 0 的判决性证据表），
> **与"掉通过率"方向相反**。而那两个默认档的产物**根本没建表**。
>
> → **重测条件**：先让"建表"确定化，再用 **N ≥ 2–3** 跑 ±`reasoning_effort` 的**配对**。
> **单次对比测的是建表的运气，不是旋钮的效果**（这也符合 §6 的噪声纪律：看成员变化，不看单轮条数）。
>
> **在重测之前**：`minimal` / `low` **不得**作为默认配置写死。

⚠️ **`kimi-k2.7-code-highspeed` 只接受 `temperature=1`** → 客户端要能**按模型**处理这个约束，
否则一次 400 就会打断一条块的生成（`llm.py` 现在固定 `temperature=0.2`）。

#### 🔴 成本结构：**闭环自己占 65%，生成只占 35%**

M3b-0 主跑按阶段拆（`runs/20260920T-m3b0-quickstart-metrics.jsonl`）：

| 阶段 | 次数 | token | 占比 |
|---|---:|---:|---:|
| **selfcheck** | 3 | 37,339 | **40.5%** |
| implement | 4 | 29,630 | 32.2% |
| **repair** | 6 | 22,626 | **24.6%** |
| design | 1 | 2,563 | 2.8% |

→ **降本顺序必须按这个结构排**，不是按直觉：

1. **`reasoning_effort`**（已测，约 3×）
2. **自检不要重发全文** —— input 占 75%，而 selfcheck 那 3 次是**最大的单笔**
3. **压低 L1 假阳性的数量** —— 每个假阳性都直接换来**一轮 repair = 一次全量重发**。
   （`docs/11 §六.1` 记"自检仍报 17 条、有真有假"，但当时没和 token 挂钩）

#### ⚠️ 标定要端到端做，指标是 `token / 通过条数`

探针只有 2–3 次**极小**调用，`code_chars` 是很粗的代理；而且 **reasoning 在更难的任务上可能是有价值的**
（86 模块的铺开、repair 的诊断）。

所以**不要用探针就把 `minimal` 钉死**：用 **M3b-0 那个样本**分别跑 default / low / minimal，
看 **每通过一条测试花多少 token**，以及**通过率有没有掉**。

> ⚠️ **度量口径：用 `token / 通过条数`，不是 token 绝对值。**
> 否则"省 token 换掉通过率"会被记成收益。

#### 其余按 ROI 排序

1. 关掉/收窄视觉分析（判分是行为测试不是像素比对；可访问名契约从**需求文本**抽取即可，不需要靠看图）
2. 批量生成替代逐节点三阶段（起步用 `stackoverflow` / `prestashop`）
3. 上下文裁剪与缓存
4. token 计量落盘 + 成本对账

**判据用 B 轨道**（见 §6）：通过率落噪声带内 + token/CNY 可测下降。

#### ⚠️ 成本分母必须两栏（M3a 起就有了第一个真实差异）

`PLAN §4.4` 早就规定"分母必须包含失败尝试"，而 M3a 的记录正好是第一例：
**成功路径 89,948 / 含失败尝试 103,847**（差 13,899，来自两次 `design` 调用，
其中一次 `out=556` 而 `reasoning=556` → **正文 0 字**，是真实的重试损耗）。

- 记三个数：`tokens_success_only` / `tokens_all_attempts` / `cny_all_attempts`
- **`metrics.jsonl` 要加 `endpoint` 字段**：同一个"只回两个字"的调用，
  **比赛网关计 97 token，DeepSeek 官方 API 计 10**（**十倍口径差**）。
  本地迭代改用官方 API 是合理决定，**但记录里必须能分辨哪条是哪个表**，
  否则本地 token 数与榜单的 `pass/CNY` 混在一起就无法对账。

### M5 — 封包

- 主赛道完整 6 任务跑**至少两次**，取最好一次作为候选包
- **打包路径问题必须在这时暴露完**（不同解压位置、不同工作目录各试一次）——讲师点名的第一大坑
- 记录每次提交的 token / 通过率 / 耗时，形成"哪个包值得留"的判断表
- **上午提交已验证的最好包。之后不动。**
  任何改进都必须本地验证过再决定是否覆盖；**不要在同一天用未验证的新包替换已验证的旧包**

### M6 — 决赛（条件性）

仅当初赛进 Top 20 才启动。核心是增量编译（§9）。**准备成本在 M3 期几乎为零**——
只要那时不把"模块 → 产出文件"映射丢掉。

---

## 8. 风险清单

| # | 风险 | 等级 | 对策 |
|---|---|---|---|
| R1 | **6 个任务跑不完**（9/19 前无人完成） | 🔴 高 | 见 §4.3：每阶段墙钟预算 + 到点降级为"可构建可运行的最小应用"。**"砍需求广度"不是对策**——外部测试锁定跳不掉，砍广度不缩短 run |
| R1b | **2 个赛题的需求文件不是合法 YAML**（bookstack / keep），ARC 加载器硬失败 → 2 个任务无完成记录 → 聚合分不成立 | 🔴 高 | 自研管线的解析器必须容错（`pipeline/reqcompile/yamlrepair.py`，见 `docs/03` §5.5）。<br>⚠️ **2026-09-23 更正**：原对策写"保底包必须是 ARC + 输入修复壳"——**该说法已陈旧**：① 实测跑通平台的是**自研管线**（ARC 一次都没跑过）；② **ARC 已退出保底退路**（§7）。风险本身仍在：**任何走 ARC 加载器的路线都会在这两个坏文件上硬失败** |
| R2 | **平台线上任务包 ≠ 公开快照** —— 逐 app 核对后**模块数完全一致**，差异**只有测试数 +6（约 1.2%）**，12306 占其中 +3 | 🟢 低 | 本地套件是平台的约 97.8% 代理，可作 oracle。**仍以相对提升为判据**不依赖绝对值 |
| R3 | **种子夹具漏播导致大面积假失败** | 🔴 高 | §4.3 的夹具抽取器，优先做 |
| R4 | **平台墙钟/token 配额未知** | 🔴 高 | V6 当天问死；按最坏情况排期 |
| R5（依赖兼容）<br>⚠️ **别与"第 5 轮 run"混** | 两个不同的风险：**Python 3.13 兼容性** + **ARC 对 deepagents 私有路径的依赖** | 🟡 中 | **① 安装/导入/配置体检 —— ✅ 已验证（2026-09-20 12:23，不再是「大概率」）。** 13 个依赖在 3.13.11 下装成功；`deepagents`+`langchain-openai` import 成功；ARC 入口 `import main` 通过；`arc doctor` → exit 0。`pip download deepagents --no-deps` 得到**纯 Python wheel**（`deepagents-0.7.15-py3-none-any.whl`，无编译产物）。<br>⚠️ "`import main` 失败"**不判别**：它只证明**当前没有解释器装了这批依赖**（系统 Python 从未装、探针 venv 在 `$TEMP` 未保留），**不是 3.13 不兼容**——上一版据此把风险上调是错的。<br>**② 🔴 真正该记的风险：ARC 依赖 deepagents 的私有内部。** `agents/runtime/factory.py:10` `from deepagents._models import get_model_provider`、`:11` `from deepagents.backends.filesystem import _raise_if_symlink_loop`。**私有路径 + `requirements.txt` 未锁版本** → 平台装到漂移后的版本时，**任何走 ARC 的包都会在 import 阶段直接炸**。<br>⚠️ **影响面已变窄（2026-09-23）**：原写"**9/23 的保底包就是 ARC**"——**不成立**：保底包是自研管线的产物，且 **ARC 已于 2026-09-22 退出保底退路**（§7）。所以这条**不再决定成败**，只在"将来真要用 ARC 当零件库"时才咬人 |
| R5-收口 | 上述两条的判据 | — | ① ✅ **已做**（2026-09-20 12:23，临时 venv）：`import main` 通过 + `arc doctor` exit 0。<br>② ✅ **已做**：`pip freeze` 快照落盘为 **`docs/07-依赖已知可用版本集.txt`**（漂移时用来定位，必要时回钉）。<br>③ ⚪ **作废**：原第 ③ 项是"真的把 ARC 跑一次"（`example/ticketbooking-quickstart`），**目的是验证保底路线**——而 ARC 已退出保底退路（§7）→ **没有要验的保底路线了**。<br>→ **R5 不再卡任何阶段**；若将来真要用 ARC 当零件库，再单独排一次 |
| R6 | 非确定性无控制（无 temperature / seed） | 🟡 中 | 用噪声底线过滤；per-test 配对检验 |
| R7 | 平台环境差异（Linux 容器 / `lsof` 是否存在 / `ARC_TEST_DATE` 时区） | 🟡 中 | 不在本地假设；V5 的真实提交暴露 |
| R8 | 二次审查判定"预埋答案" | 🟡 中 | 模板只放基础设施、零业务语义；全仓 grep 无题目特定字符串 |
| R9 | 视觉分析 base64 图片按文本计费（单张 2.6MB 图 → 潜在百万 token 级） | 🟡 中 | 第一次真实运行核对"输入 token / 图片数"比值；必要时本地缩放。<br>⚠️ **2026-09-21 更正**：原本推动这条的动机例（"keep 的头号失败名字只在截图里"）**已被证伪**——那个名字逐字在需求文本里、抽取器也抽得到（见 §7 M3b-1「共享前置」）。**所以图片通路是一条独立的能力问题，不再有"非做不可"的紧急性**；它的成本仍要按这条量，且它依赖 A1 的视觉半边（`VISUAL_MODEL` 凭据） |
| R10 | 🔴 **榜单前 6 名里多个队伍 `token ≈ 0` / `runtime ≈ 0` 却拿到 80–100% 通过率**——计量口径存在我们还不理解的机制 | 🟡 中<br>**但可能很值钱** | **在搞清之前不要按榜单调整成本策略**（否则会朝一个错的目标优化）。两种可能：① 他们的应用不是现场用 LLM 生成的 → 撞"不得预埋答案"红线、要过二次审查；② 平台计量口径与我们的理解不同。<br>**它可能直接决定 `pass/CNY` 该怎么算才划算**，值得在 M3/M4 花时间搞清。<br>证据：`docs/09-平台提交与报错记录.md` §6 |

### 8.1 会让计划失败的**未验证假设**（合并审核 A1–A6，按风险排序）

| # | 假设 | 现状 | 怎么验（都便宜） | 若为假的后果 |
|---|---|---|---|---|
| **A1** | 平台容器里**能调到模型**（有凭据） | 🔴 **从未验证**：三次提交全是零 LLM 调用；`docs/09` 的注入清单有 `MODEL`/`VISUAL_MODEL`/`ARCBENCH_*`，**没有 API key** | 启动日志加一行**掩码的凭据存在性检查**，随下一次提交（哪怕零调用）。**同一行顺带探测网络可达性**（见 §7 M3b-1 的冒烟条目——"agent 无网络"至今只是讲师的一句预期） | **平台上生成必然失败 → 所有本地成果归零**（全项目最大的单点风险） |
| **A2** | "非零"的判定口径（按任务 vs 按聚合） | 🟡 **未答**；唯一观测是 6×0.0% 不在榜，**无法区分两种读法** | **`R1a` 只跑 keep**：看 run 是否 `completed`（`R1b` 跑 6 个再看榜单是否出现条目） | 工作量差约 **6 倍** |
| **A3** | 单次墙钟 / token 配额上限 | 🟡 **未知**（9/21 一天已烧约 97 万 token，**可能已在试探配额**） | 论坛提问 + 看 keep 那次的 runtime 与 token 计数 | 排期不成立 |
| **A4** | 比赛模型在**真实规模**（86 模块）上仍成立 | 🟡 只有 1 个需求 / 6 条判据的读数 | keep 子集那次的第一次真实规模读数（**这也是它只做子集的理由之一**） | 铺开后大面积 0 |
| **A5** | 榜单前排 `0-token / 0s` 拿 80–100% 的机制 | 🟡 **未解** | 自己在 run 页对照 `Runtime` / token（预测：我们是 ≈0s / 0.00M） | `pass/CNY` 的优化方向可能整套错（= R10） |
| **A6** | 平台线上的需求文件**是否也是坏 YAML**（公开快照 2/6 坏） | 🟡 未知 | keep 那次提交的日志（**keep 正好是坏 YAML 之一 → 顺带验掉**） | 两条路线都要额外容错 |

> **A1 与 A2 由同一次平台跑答掉**，而那次跑本来就排在关键路径上（§7 M3b-1 第三条）。
> **这是"最小需求子集"这个设计最值钱的地方：一次提交同时买三件事**（非零、A1、A2）。

---

## 9. 决赛预留（初赛期只花零成本，别到决赛还债）

决赛 = **你初赛的 agent + 平台替你选的那个初赛最佳应用 + 一份需求增量**。提交物仍然只是 agent 包，应用由平台注入。

**现在就要做的一件事**：为每个需求模块维护**"模块 → 产出文件"映射**，存进 `.arc/traceability/`。

有了这份映射，决赛的增量编译退化成"需求树 diff → 受影响模块集合 → 只重生成这些文件 → 只跑这些模块的测试"。**不需要现在多写代码，只需要现在不要把映射丢掉。**

另外确认一条：决赛路径判定为 evolution 模式后，**不会拷贝模板**（`base.py:64-68` 只打一行日志）——所以初赛被模板卡住的风险，决赛反而没有。

---

## 10. 需要用户提供 / 确认

| 项 | 状态 |
|---|---|
| API key | ✅ 已有（工作区 `.env`） |
| 队伍名 / 邮箱 / GitHub | ✅ 已有（`ACCOUNT.md`） |
| ARC-Bench 平台账号是否已注册 | ⏳ **待确认**（Register → Hackathon account → 用 2436448088@qq.com） |
| 官网报名是否确实完成（9/7 截止） | ⏳ **待确认** |
| 单次提交的墙钟 / token 配额上限 | ⏳ **待查**（V6） |

---

## 11. 9/20 首日验证结果（已实测，非推断）

V1–V3 当天完成，结果如下。**这些都是实测数字，方案据此做了修正。**

### 环境底数

| 项 | 实测 | 备注 |
|---|---|---|
| Node / npm | v24.15.0 / 11.19.0 | 满足要求 |
| Python | 3.13.11 | ✅ **安装 + 导入 + 配置体检都过了**（2026-09-20 12:23–12:25 实测）：13 个依赖装成功；**`deepagents` / `langchain-openai` 两个重依赖 import 成功**；**ARC 入口 `src/main.py` 完整 import 图通过**；`arc --help` 正常；**`arc doctor` → `✓ Configuration is valid`（exit 0）**。<br>⚠️ **仍未做的两件**：① `arc compile` 从未真正跑过（要真实 LLM 调用）；② `pip freeze` 未存档。见 §8 R5 与 R5-收口 |
| Docker | ❌ **不可用** | `npm run reference` 走 Docker，**必须改用直接启动**（见下） |
| 磁盘 | 686G 可用 | 充裕 |

### V1 — 测试契约审计 ✅

`npm run test:audit` → **Benchmark test contract audit passed.** §4.1 的禁用模式清单得到验证。

### V2 — 上界基线 ✅（绕过 Docker）

`npm run reference` 依赖 Docker，本机没有。**替代路径已验证可行**：

```bash
# 1. 构建前端
cd arc-bench/webapp/12306/project/frontend && npm install && npm run build
# 2. 准备数据库并起后端
cd ../backend && npm install && npm run db:prepare:e2e
PORT=3301 npm run start          # 探活 HTTP 200
# 3. 跑官方测试
cd repos/arc-bench && npm run test -- --app 12306 --target-url http://127.0.0.1:3301
```

**结果：干净数据库上通过 129–135 / 135（五轮测量分布，最好一轮 135/135）。** 测试环境确认是好的。

> ⚠️ 先前这里写的"132 通过 / 0 失败"曾被当作"运气好的 run"排除。**R5（135/135）之后它不能再这样排除了**——它与 R5 同属"满分"这一类观测。**"天花板 134/135"已作废**（见 §6 上界表）。权威数据在 `docs/04-测量协议.md` §三。

> 注意：六个 app 里**只有 12306 带参考实现**（44 个文件）。其余五个只有 requirements + tests。所以 oracle 只在开发期对 12306 可用。

### V3 — 噪声底线 ⚠️ **发现了会毁掉所有实验的陷阱**

**分母先钉死**：`npx playwright test --list` → `Total: 135 tests in 117 files`。**12306 是 135 条测试**（117 个 spec 文件，部分含多条）。

同一份冻结的参考实现、同一套测试，逐次收紧条件后的演进：

| 阶段 | 条件 | 结果 | 结论 |
|---|---|---|---|
| 早期（非工具化） | 不重置库 | 131 / 4 → 120 / **15** | **DB 状态累积是主要噪声源** |
| 早期（非工具化） | 重置库 | 132 passed（日志已丢，数字不可信） | 一次运气好的 run |
| **五轮工具化**（`docs/04`） | 每轮 reset + 重启后端 | 129 / 130 / 130 / 133 / **135** | 实测区间 129–135；失败条数波动**分组算**：静置 0–2 / 污染 2–6 |

**结论：数据库状态在运行之间累积，是主要噪声源。** 不重置数据库就做 A/B，差异会被完全淹没。

**噪声底线（权威值在 `docs/04-测量协议.md` §三）：**
- 失败**条数**波动 **0–6（跨度 6，五轮）** → 比较整跑通过率时可辨差异下限曾记 4 条，**但跨度的算术前提已变，待裁定**（裁定前 4–6 条的差异不下结论）
- 会**翻面**的测试集合 **11 条** → per-test 配对的噪声上界
- ~~其中 `REQ-4.3.7` 四轮全失败~~ → **R5 证伪：它通过了。零稳定失败**；实测区间 **129–135**，满分可达但不稳定

**由此产生的强制操作纪律：**

1. **每次测量前必须重置数据库**：`npm run db:prepare:e2e`，并重启后端（后端持有数据库连接，且有内存态）
2. 每轮之间必须重建工作区（`--clean`，不用 `--resume`）
3. 跨轮对比必须用 per-test 配对，并把那 11 条翻面测试**单列**出来看成员变化，而不是只看条数

⚠️ **两处已修正的错误记录**（保留作为教训）：

- 分母曾记成 132。当时的显示过滤器 `grep -E "(passed|failed) \("` 会把 `N skipped` 整行吃掉——"被跳过"伪装成了"不存在"。**不过"那 3 条是 skipped"目前只是假设**：后续所有工具化运行里 skipped 都是 0，从未复现。`bench.sh` 已修过滤器。
- 噪声底线先后写过 4 和 ≥6，都偏低。真实值见上面。**教训：分母或底线对不上时，先怀疑记录工具，再怀疑被测对象。**

**失败形态**：97% 的失败是 `expect(locator).toBeVisible()` 与 `toBeChecked()` 超时，即**交互时序问题**，不是功能缺失。这说明**通过率的大头来自"UI 在压力下是否稳定可见/可交互"，而不是"功能是否实现"**。这一点直接影响生成策略（第 4、5 阶段要把可访问性稳定性和渲染时序当成一等目标）。

### V4 — 网关连通与模型清单 ✅

`https://api.arc-bench.com/v1` 可达，key 有效，`deepseek-v4-flash` 正常返回。

**可用模型共 16 个：**

```
deepseek-v4-flash              ← 当前默认，最便宜档
deepseek-v4-flash-vision-exp   ← 视觉模型（实验版）
deepseek-v4-pro
glm-5.2 / glm-5.3 / glm-5.3-flash
kimi-k2.6 / kimi-k2.7-code / kimi-k2.7-code-highspeed / kimi-k3
minimax-m3
qwen3.6-flash / qwen3.6-plus / qwen3.7-max / qwen3.7-plus / qwen3.8-max
```

**对模型路由的三点影响：**

1. **Lab02 基准里的性价比冠军 `glm-5.3` 可用**（35/36 且 token 只有 qwen3.8-max 的 1/5）。这是批量生成阶段的头号候选，`glm-5.3-flash` 更便宜，值得一起测。
2. **视觉模型有专用档 `deepseek-v4-flash-vision-exp`**，且它本身就是 flash 档——所以视觉分析不必回退到主模型，成本可控。注意是 `exp`，稳定性需实测。
3. **计费包含 reasoning token。** 实测一次"只回 ok"的极简调用：prompt 88 + completion 18（其中 **16 是 reasoning**）= 106 token。**推理开销是纯额外成本**，在批量生成时会累积。需在第一轮实验中确认能否通过参数抑制，或确认它是模型固有的。

> 注意 Lab02 基准用的是 `qwen` 系列，但 `qwen3.8-max` 是基准里最贵的一档（token 是 glm-5.3 的 5 倍），**不要用它做批量生成**。

### 墙钟实测（影响 R1 排期）

> 🔴 **2026-09-22 更正**：本节原写"一个 app 的约 135 条测试本地耗时 3.6–8.0 分钟，
> 六个 app 合计约 484 条，纯测试时间估算 15–30 分钟"——**那是参考实现全通过时的形态**，
> 而真读数是**失败主导**的，两者差一个数量级。**以下为现行口径，旧数不得再引用：**

- **判分慢在失败上**：keep 实测 **32 条 = 30.7 分钟**——**31 条失败各烧 ~60 秒**，
  而通过的那条只花 **431ms**。
- **定义值（更硬）**：`repos/arc-bench/playwright.config.ts` 写死 `timeout=60_000`、
  `workers:1`、`fullyParallel:false` → **判分最坏 = 判据条数 × 60 秒**。
- **六 app（478 条）最坏 ≈ 8.0 小时**，**再加生成时间**。
  对照：榜单已观测到的最大单跑是 **379 分钟**（6.3 小时）。
- 这是"6 个任务能否跑完"这个最大风险的量化依据，**也是"提高通过率 = 买时间"的依据**。

### 对方案的修正

| 原方案 | 修正 |
|---|---|
| §6 说"连跑 3 次测噪声" | 增补：**每轮前重置数据库 + 重启后端**，否则噪声被放大 4 倍 |
| §1 "不追 100%" | 得到验证：参考实现自己都跑不到稳定的 100% |
| §4.2 L3 参考实现对拍 | 限定为**仅对 12306 可用**（唯一带 `project/` 的 app） |
| §7 V2 步骤 | 写入绕过 Docker 的三条命令 |
| §8 R1 风险 | 用 3.6–8.0 min/app 的实测值支撑排期判断 |

---

## 附：本方案的事实依据

- 平台契约：`raw/ARC-Bench_平台_中文.txt`（api-doc 抓取）
- 应用契约：`repos/agentic-requirement-compiler/src/app_type_handler/web.py`
- 模板脚本：`repos/arc-template/templates/web-react-express/{backend,frontend}/package.json`
- 禁用模式：`repos/arc-bench/scripts/audit-benchmark-tests.js`
- 夹具对齐：`repos/arc-bench/webapp/12306/{tests/helpers.ts, requirements/requirements.yaml:194}`
- ARC 三个硬伤：`core/phases.py:753-789`（无闸门）、`agents/model/openai_api_adapter.py:104-114`（`stream_usage: False`）、`core/phases.py:17`（预算 5 vs 文案 10）
- 榜单数据：`raw/主页_中文.txt`（9/11 Smoke 快照）、`raw/ARC-Bench_平台_中文.txt`（9/19 Lite 榜）
- 模型基准：`repos/agentic-software-engineering-hackathon/Lab/Lab02/benchmark-results/`
