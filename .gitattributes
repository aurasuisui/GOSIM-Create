# 换行符归一化。
#
# 为什么必须有这个文件：本机是 Windows，默认 `core.autocrlf` 会把文本文件转成 CRLF。
# 而 `eval/bench.sh` 是 Git Bash 脚本 —— **shebang `#!/usr/bin/env bash` 一旦带上 `\r`
# 就会失效**（`bad interpreter`），而且这个故障看起来像"脚本坏了"，很难一眼归因。
# 所以脚本一律强制 LF。
#
# 规则：默认 LF；只有 PowerShell / 批处理保持 CRLF（它们需要）。
* text=auto eol=lf

*.sh    text eol=lf
*.py    text eol=lf
*.md    text eol=lf
*.txt   text eol=lf
*.jsonl text eol=lf
*.json  text eol=lf

*.ps1   text eol=crlf
*.bat   text eol=crlf
*.cmd   text eol=crlf

# 二进制/产物，不做换行转换
*.zip   binary
*.png   binary
*.docx  binary
