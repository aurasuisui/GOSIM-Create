# 12 · M3b-1 前置：模型 / 成本口径标定

> **它回答的问题（`PLAN.md` §7 M3b-1 第一条，"不能跳过"）**：M3b-1 是 token 量爆炸的地方
> （1 个需求约 9 万 token → 86 个需求是另一个量级），而 **reasoning 占比取决于选哪个模型**。
> 带着错的模型铺开 = 按错的口径烧掉大部分预算。
>
> **执行时间**：2026-09-20 20:1x–20:4x。**样本**：`ticketbooking-quickstart` REQ-1（与 M3b-0 同一样本）。
> **判据**：Lab04 的 6 条验证测试。**度量口径（`PLAN.md` §7 明文）**：`token / 通过条数`，不是 token 绝对值。

---

## 一、结论（一句话）

**三个旋钮都测了；`reasoning_effort` 网关接受，但它与"换便宜模型"都会掉通过率——
按 `token/通过条数` 口径，目前最优仍是基线 `deepseek-chat`。**
**而且：便宜的**小样本探针**会把两个错配置都判成"更优"——端到端标定把它们挡下来了。**

---

## 二、第一段：小样本筛选（便宜，但**不足以定论**）

同一个代表性小任务（写一个含校验的 Express 路由文件，约 40 行），逐候选记 token 与 reasoning：

| 模型 | extra | in | out | reasoning | 占比 | **可见正文长度** | 成本指数(in+out) |
|---|---|---:|---:|---:|---:|---:|---:|
| `deepseek-v4-flash` | — | 141 | 1236 | 949 | **77%** | 856 | 1377 |
| `deepseek-v4-flash` | `reasoning_effort=low` | 62 | 483 | 52 | **11%** | **1269** | **545** |
| `deepseek-v4-flash` | `reasoning_effort=minimal` | 62 | 380 | **0** | 0% | 1125 | 442 |
| `glm-5.3-flash` | — | 70 | 411 | 0 | — | **1450** | **481** |
| `glm-5.3-flash` | `reasoning_effort=low` | 70 | 491 | 0 | — | 1562 | 561 |
| `kimi-k2.7-code-highspeed` | — | ❌ | | | | | 网关报错（名字/权限未定） |

**两个旋钮的答案（小样本阶段）**：
- **旋钮 2「网关是否认 `reasoning_effort`」→ 认。** 而且效果很大：`low` 把 reasoning 从 949 压到 52，
  成本指数 −60%，**正文反而更长**（856 → 1269 字符）——没有截断，校验要素（必填/长度/重复/201）一个不缺。
- **旋钮 1「换模型族」→ `glm-5.3-flash` 更便宜**（成本指数 481），且它**不报 reasoning 字段**、正文最长。
- 旋钮 3（`max_tokens` 截断）**本阶段不必测**：上面两个候选都没触发截断，没有可省的截断空间；
  等真出现"输出被上限截断"再测（`PLAN.md` 要求成对测）。

> ⚠️ **如果只看这一段，结论会是"`reasoning_effort=low` 是严格更优"**——而它是**错的**（见下）。

---

## 三、第二段：端到端确认（决定性的那一段）

同一样本、同一条管线（生成 + 验证闭环 + 构建 + 起服务 + 6 条判据）：

| # | 配置 | 网关 | 调用数 | **token（含全部尝试）** | **通过** | **token / 通过条数** | 判决 |
|---|---|---|---:|---:|---:|---:|---|
| A | `deepseek-chat`（默认） | DeepSeek 官方 | 14 | 92,158 | **6/6** | **15,359** | ✅ **当前最优** |
| B | `deepseek-v4-flash` + `reasoning_effort=low` | 比赛网关 | 8 | **81,862** | 5/6 | 16,372 | ❌ 更贵（按口径） |
| C | `glm-5.3-flash`（默认） | 比赛网关 | 9 | **53,723** | **0/6** | **∞** | ❌ 最差 |

- **B**：token 少 11%，但**少过 1 条** → 按口径反而贵 7%。
- **C**：token 少 42%（最便宜），但**一条都没过** → 口径下等于无穷。

🔴 **C 的根因我第一版写错了，2026-09-21 更正**（`PLAN.md` 更新时指出，我复核后确认它是对的）：

我原先只写了"用户名渲染成拼接文本"（`当前用户：{user.username}`）——**那只解释 1/6**。
实际证据：**6 条里有 5 条是 `page.goto: net::ERR_CONNECTION_REFUSED`**（第 1 条跑到了断言才失败 →
当时应用还活着；第 2–6 条全连不上 → **应用在第一条测试之后就死了**）。
后端日志给出了死因：

```
Error: SQLITE_ERROR: no such table: users
  --> in Database#get('SELECT * FROM users WHERE lower(username) = lower(?)', …)
    at backend/src/database/db_runtime.js:30
（未捕获的 promise rejection → Node 进程退出）
```

**真正的主因是"注册流程触发后端崩溃"**：glm 把 `CREATE TABLE IF NOT EXISTS users/sessions`
写在自己的 `auth_repository.js` 里（`auth_repository.js:34/49`），而那几条 CREATE **在使用前没跑到** →
查询先抛 `no such table` → **未捕获的 rejection 把 Node 打死**。

对照：`deepseek-chat` 那组（配置 A）也没把建表放进 `init_db.js`，但它跑通了、**6/6**——
**差别只在"运行期什么时候执行到那几条 CREATE"，静态判不出来。**

→ **这条直接决定了 M3b-1 的闸门形状**（`PLAN.md` §7 已登记）：
闭环必须能发现"**应用一起来就崩**"，而正确的仪器是**真起服务、真打一次入口（冒烟）**，
不是静态检查。见下 §四补。

---

## 四、这条标定真正教了什么（比数字重要）

**小样本探针会把错配置判成更优，而且理由看起来很充分。**
`low` 与小模型在"40 行代码"这个尺度上：更少的 token、更长的正文、要素齐全——
**唯一看不出来的就是"它不理解判据要什么"**。

→ `PLAN.md` §7 坚持"用 `token / 通过条数`、且要端到端"是对的；
**任何"省 token"的旋钮，都必须在 6 条判据上复测才能进 M3b-1。**

**同时暴露验证闭环的一个真实短板**：自检（review）对"显示形态不匹配"的把握**随模型而异**——
`deepseek-chat` 抓到了用户名拼接并修掉，`glm-5.3-flash` 没抓到。
→ M3b-1 铺开前，**"用户名/实体值必须单独成元素"这类约束应该进生成 prompt 的硬性清单**，
而不是只指望事后的自检（自检是发现机制，不是保证）。

---

## 五、对 M3b-1 的直接决定

1. **模型**：暂定 **`deepseek-chat`（DeepSeek 官方档）做本地迭代**；平台上提交时用**网关的 `deepseek-v4-flash` 且不加 `reasoning_effort`**
   —— 注意 A/B 两行**跨了网关**，不能直接当成"模型优劣"；要拿平台上真实口径，
   需要在**同一网关**上补一次 `deepseek-v4-flash`（默认 reasoning）的端到端跑（见下"未做"）。
2. **旋钮**：`reasoning_effort` **不进生成阶段**（掉通过率）；`max_tokens` 暂不动。
3. **成本基线口径**：用**本次三行的 token（含全部尝试）**当基线；`PLAN.md` 要求的"两栏"
   在本地就是"这次 run 的全部调用"（重试/修复都记在同一份 `metrics.jsonl` 里）。
4. **必须补的两件**（M3b-1 开工前）：
   - 同一网关上的 `deepseek-v4-flash`（默认）端到端一次 → 才有干净的"同模型 ± reasoning_effort"配对
   - 把"实体值单独成元素 / 退出链接形态"写进生成 prompt 的硬性清单（上面 §四）

---

## 六、复现

```bash
# 第一段（小样本筛选，几分钟，token 很便宜）
python eval/m3b1_model_probe.py

# 第二段（端到端；每种配置一份新目录）
PIPELINE_LLM_EXTRA='{"reasoning_effort":"low"}' PIPELINE_REQ_IDS=REQ-1 \
  python -u pipeline/main.py compile <quickstart> -o "$TMP/cfg-b/app"
# 换模型族：用 PIPELINE_LLM_{API_KEY,BASE_URL,MODEL} 覆盖（平台路径不受影响）
```

**证据**（`runs/`）：
- `20260920T-m3b1-model-probe.json` —— 第一段逐候选明细
- `20260920T-m3b1-flash-low-metrics.jsonl` + `-5of6.log` —— 配置 B
- `20260920T-m3b1-glm-flash-metrics.jsonl` + `-0of6.log` —— 配置 C
- 配置 A 的证据在 `docs/11` 的 M3b-0 归档里（同一样本、同一条管线）

> ⚠️ **一次测量事故（记下来免得重犯）**：配置 B 第一次跑出"0/6"，我先当成配置结论——
> 其实是**后端的 npm 依赖没装**（我只装了前端），后端起不来、`GET /` 返回 000。
> **教训：跑判据前必须先确认探活 200**；等待循环超时了就该停下来，不能继续。修好后重跑得 5/6。

---

## 七、2026-09-21 追加：由 C 的根因带来的两处改动

1. **`pipeline/generate/implement.py`：放开 `backend/src/database/init_db.js`。**
   原先 `FORBIDDEN_PREFIXES = ("backend/src/database/",)` **把模板指定的扩展点也一起禁了**——
   而 `init_db.js` 的注释里就写着"把建表语句集中在这个文件里"。于是"建表往哪放"变成模型自由发挥：
   A 组放仓储模块里跑通了，C 组放仓储模块里没跑到就崩了。
   **现在**：只保护 `db_runtime.js` / `test_harness.js` / `seed_db.js` / `prepare_e2e.js` / `index.js`，
   **放行 `init_db.js`**，并在生成 prompt 里明确"建表必须写进 `initializeDatabase()` 主体"。
   （这不是给某个样本打补丁：`init_db.js` 本来就是模板契约里的建表位置。）
2. **试过一个静态检查"表有没有建"，结论是它不该存在**（已撤）。
   两版判据都错：`FROM X` 无 `CREATE TABLE X`（A 组会被误报，它其实通过）、
   "必须建在 init_db.js 里"（A 组同样被误报）。**这是运行期属性**——
   正确的仪器是 `PLAN.md` §7 那条**冒烟**：起服务 → 打入口 → 确认 200。
   agent 侧没有网络、装不了依赖、起不来，所以**冒烟属于本地打分脚本**（那正是 M3b-1 的"第一条"）。

---

## 八、2026-09-21：闸门第 0 条（**用比赛模型复现 6/6**）—— ❌ **不成立：只复现到 3/6**

`PLAN.md` §7 M3b-1 把这条排在最前（"最先做，最便宜，最决定性"），理由是
"**机制是模型无关的，通过率不是**"。**实测证明它是对的**：

| 配置 | 网关 | token（含全部尝试） | 通过 | 无人工构建 |
|---|---|---:|---:|---|
| `deepseek-chat`（M3b-0 基线） | DeepSeek 官方 | 92,158 | **6/6** | ✅ |
| **`deepseek-v4-flash`（平台注入的 `MODEL`）** | **比赛网关** | **156,343** | **3/6** | ✅ |

**同一份需求、同一套判据、同一条管线，只换模型 → 6/6 掉到 3/6。**
- 失败的三条仍是**同一类**：`getByText(username, {exact:true})` 找不到 → **用户名没单独成元素**
- **应用全程存活**（`ERR_CONNECTION_REFUSED` 0 次；6 条判据跑完后再探活仍 200）
  → 这次不是"应用崩了"，是**纯瞄准问题**
- 成本也更高：**156,343 token（10 条记录），其中 reasoning 89,658 = 输出的 86%**
- ⚠️ **本行数字我先前手抄错过**：曾写 **131,360**（8 次调用）——那是**中间态**。
  这一次 run 里有两轮 selfcheck→repair（`selfcheck-review` ×2、repair ×3），
  只抄前半段就低估了。**正确值：156,343 / 10 条**；
  于是 `token / 通过条数 = 156,343 ÷ 3 = **52,114**`（不是 43,787 = 131,360 ÷ 3）。
  → 这是"**成本表必须由脚本聚合、禁止手抄**"这条纪律的由来（见 §十一）。

**结论：M3b-1 不能按现在这个配置铺开。** 若铺开，最坏情况正是计划警告的那句
"**本地 6/6、平台 0/6 而不知为何**"，代价是几百万 token。

### 下一步（按依赖顺序，都在铺开之前）

1. **把判据的定位方式写进生成 prompt 的硬性清单**（不是指望事后自检）。
   现象是稳定的、可枚举的：判据用 `getByText(<值>, {exact:true})` 找**实体值**（用户名…）、
   用 `getByRole('link', {name})` 找退出入口——**值必须单独成元素、链接必须是链接**。
   `deepseek-chat` 的自检抓到了并修掉；`deepseek-v4-flash` 与 `glm-5.3-flash` 都没抓到
   → **说明它不该是"自检的运气"，而应是生成阶段的显式约束。**
2. **闭环加冒烟**（`PLAN.md` §7 那条）：起服务 → 打入口 → 确认 200；有场景标题时再跑第一个断言。
   它是本地打分脚本的一部分（agent 侧没网络、起不来）。
3. **然后重跑本条闸门**（同样本、同判据、只换模型）——过了才谈铺开。

---

## 九、2026-09-21 续：闸门第 0 条**重跑仍不成立（3/6）**，根因抓到并已定位到一处设计缺陷

### 重跑做了什么

按 §八 的下一步，先落了两处修复，再重跑同一条闸门（同样本 / 同判据 / 同模型 `deepseek-v4-flash` / 比赛网关）：

1. **把"定位契约"写进生成 prompt**（`STACK_CONTRACT`，每个生成块都会带上）：
   实体值必须单独成元素、导航/退出用真链接、错误进 `role="alert"`、下拉用原生 `<select>`、强度用 `<meter>`。
2. **`eval/score_app.sh`**：本地打分脚本，**第一步就是冒烟**（起服务 → `GET /` 必须 200 →
   否则**明确拒绝往下跑判据**）。它也是 `PLAN.md` §7 M3b-1"第一条"要的那根线。

### 结果：**3/6**（与修复前一样）

- ✅ **定位契约生效了**：产物里 `HomePage` 已经是 `<span>{user.username}</span>`，
  `RegisterPage` 走 `useAuth().register(payload)` + `navigate('/')`
- ❌ 但三条 happy-path 判据仍挂，失败断言仍是 `getByText(<用户名>, {exact:true})` 找不到

### 🔴 根因（两组 artifact 对照，同一个）

| artifact | 模型 | 仓储里有 `CREATE TABLE` | 结果 |
|---|---|---|---|
| M3b-0 | `deepseek-chat` | ✅ 有 | **6/6** |
| 标定组 | `deepseek-v4-flash` | ❌ **没有** | 3/6 |
| 闸门 0 组 | `deepseek-v4-flash` | ❌ **没有** | 3/6 |

两个 flash 产物的后端里，**`CREATE TABLE` 只出现在模板自己的注释里**——
`users` 表**从来没被创建过** → `SELECT … FROM users` 抛 `SQLITE_ERROR: no such table` →
route 把它吞成 **500** → 注册永远不成功 → 用户名永远不出现 → 那三条判据必挂。
（`glm-5.3-flash` 那次的同款缺陷更凶：未捕获的 rejection 直接把 Node 打死。）

**所以闸门第 0 条不过的原因不是"瞄准"，而是"应用的功能根本没跑起来"。**

### 我在这上面犯的一个错，纠正一下

我早先做过一版静态检查"SQL 用到的表有没有被 CREATE"——**它是对的**（能正确区分上表三组）。
但我随后收紧成"必须建在 `init_db.js` 里"，那版**误报了 M3b-0**（它建在仓储模块里、照样 6/6），
**于是我把整个检查撤掉了——撤错了**。

正确做法本该是**放宽判据而不是删掉检查**。现在函数与判据说明已恢复到 `verify/l1.py`，
但**暂时没接进闸门**：恢复后它仍误报（**同样的正则单独跑是对的，in-function 路径上我没定位到原因**），
而**留一个会误报的检查比没有更糟**——它会把假发现喂给定向修复。→ **待修项，已标注在代码里。**

### 顺带发现：验证闭环在比赛网关上**不稳**

重跑时 `selfcheck-review` 那一步被网关**连续丢弃**（`RemoteDisconnected`，无响应），
重试 2 次后仍在等。原因合理推测：**review 的 prompt 把全部代码塞进一次请求**，
而这个模型 reasoning 很重 → 又是 M3a 那个"大请求被断连"的形态。
→ **review 也该分块**（按文件或按组），与生成阶段的分块同理。这是重跑没能完整跑完闭环的直接原因。

### 下一步（顺序已明确）

1. **让"建表"这件事可靠**（本题的根因）：要么把它变成**闸门的硬判据**（修好上面那个 bug 再启用），
   要么在 prompt 里做成**不可跳过的显式产物**（例如要求单独输出一段 schema 文件、由管线落位）。
2. **把 review 分块**（消除网关断连，闭环才能跑完）。
3. 然后**第三次跑闸门第 0 条**——过了才谈 M3b-1 铺开。

---

## 十、2026-09-21 续：两个闭环缺陷的处理（计划把它们列在最前）

### ① `check_db_tables` 的 bug —— **已修并回归通过**

计划把根因定位出来了（我上一轮没找到，是因为我按"函数被定义两次"去猜）：

```python
used    |= {(m.group(1).lower(), rel) for ...}   # (表名, 文件) —— 元组
created |= {m.group(1).lower() for ...}          # 只有表名 —— 字符串
used - created        # 元组集合 − 名字集合 = 永远不消掉任何元素
```

**所以五个产物返回的是同一批 4 条**。修法两处：
1. `for table, rel in sorted((t, r) for t, r in used if t not in created)`
2. **匹配前先剥 JS 注释**（`seed_db.js:15-16` 的 JSDoc 里写着 `FROM tests`，被当成 SQL 读了）

**回归（`PLAN.md` 给了预期值，5/5 全对）**：

| 产物 | 预期 | 实际 |
|---|---|---|
| `m3b0` | 0 | ✅ 0 |
| `m3b1-low` | 0 | ✅ 0 |
| `m3b1-flashdef` | 含 `users`/`sessions` | ✅ `['sessions','users']` |
| `m3b1-gate0b` | 含 `users`/`sessions` | ✅ `['sessions','users']` |
| `m3b1-glm` | 0 | ✅ 0（它的毛病是**执行顺序**，静态判不出——与计划预测一致） |

**意义**：这条闸门现在能**在生成阶段**就拦住"从没建表"，不必等到注册接口 500 或 Node 被杀。
（它此前一直在修「幻影」——每条假发现 = 一次全量重发，而闭环占 65% 的 token。）

### ② 「大请求被网关丢」→ **客户端规则已立**

`pipeline/generate/llm.py` 新增：
- `RequestTooLarge` 异常 + `MAX_REQUEST_CHARS`（默认 24000 字符，`PIPELINE_LLM_MAX_REQUEST_CHARS` 可调）
- 超预算 → 直接抛，附"请按语义分块"的提示
- **"请求偏大 + 被断连"不再重发同一个大请求**，直接抛"该分块"

⬜ **尚未做**：`selfcheck.review` 本身还没按文件分组。预算与规则已就位，分组是下一步——
**做完它才谈第三次跑闸门第 0 条**（否则闭环还会卡在同一个 review 调用上）。

---

## 十一、2026-09-21（下午）：三条优先中的 ① 落地、③ 连撞四种"基础设施形态"

### 一句话

**② 建表确定化已实现，并在运行期验证成立**（干跑 5/6 —— 唯一那条失败还是**我们自己的审查噪声**
造成的，不是能力问题）。**第三次跑闸门第 0 条连撞四种"基础设施形态"**，四处已修三处，
**闸门本身的读数仍未拿到**。四种形态都已记在下面——它们的价值不低于闸门读数：
**平台上会遇到同一批**。

### 十一.一 ① 建表确定化：schema 从"模型找位置"改成"管线落位"

**做法**（改动提交 `e569ce9`）：

1. 新增生成块 `backend-schema`：模型只产出 `backend/src/database/schema.sql`（**纯 SQL**，一个独立产物）
2. `generate/schema.py` 把它**注入** `init_db.js` 的启动流程：`PRAGMA` 之后、**IIFE 内那个
   `return database;` 之前**，`await` 执行
3. 三个锚点按序尝试（标记块 / IIFE 内 `return` / PRAGMA 之后）；靠
   `// ===PIPELINE SCHEMA BEGIN===` 做**替换式**幂等（重跑不叠层）
4. `init_db.js` 进 `FORBIDDEN_PREFIXES`（建表位置归管线所有，模型碰不到）
5. L1 新增 `check_schema_injected`：注入块丢了/落后了要说出来；闭环里它是**零 token 的机械修复**
   （`table-not-created` 也被路由到 `schema.sql`，不再让模型跑到仓储模块里写 `CREATE TABLE`）

**自测（零 token，在模板副本上做，12 项全绿）**：三个锚点各命中一次；重跑只替换不叠层；
schema 加表后注入块跟着更新；`check_db_tables` / `check_schema_injected` 注入后都是 0；
**删掉 `schema.sql` 时两条检查静默放行**（老产物兼容）。
自测还抓到锚点的一个真 bug：不带 `(?=\s*})();)` 约束时会匹配到**函数级**那个 `return database;`
（2 空格缩进），把建表放到 `await initPromise` 之后 → 并发调用者会在建表前拿到连接。已收紧。

**运行期判据**：干跑产物 → `check_db_tables` = 0，`check_schema_injected` = 0，
冒烟 `GET /` 200，**Lab04 判据 5/6**（注册、会话、三个校验类全过）。

### 十一.二 ③ 第三次闸门第 0 条：四次尝试，四种"基础设施形态"

| # | 现象 | 归因 | 处置 |
|---|---|---|---|
| 1 | `implement-backend-schema 返回了空内容` | flash 把输出**全烧在 reasoning 上**（同 run 的 design 用了 83% 输出） | ① 空内容**改走重试**（同一请求换个采样就能出正文）② **失败调用也要记账**（原来 `_record` 在空检查之后 → 那次调用在 metrics 里直接不存在） |
| 2 | `RequestTooLarge: 网关断开且请求偏大（12,974 字符）` | **阈值错了**：老规则"偏大 = > 预算一半"（12,000），13k 的请求只是中等大小，被误判 → **整个 run 作废** | 阈值收紧到 **0.8× 预算**（19,200）；真太大的情况预发送检查已经拦了 |
| 3 | `implement-backend-auth` **连续 3 次** `RemoteDisconnected` | 那一块要 **2 个文件**（仓储+服务），输出 ~2,800 token、reasoning 占 91% → **生成时间长到网关读超时** | 生成块**拆到"一文件一调用"**：`backend-auth` → `repo` + `service`；`frontend-shell`（4 文件）→ `frontend-auth-client` + `frontend-shell`；`_side_extra` 只带这一块真要调用的上游文件（并加 7,000 字符总量上限） |
| 4 | 进程在 chunk 5/8 处**消失**，没有异常 | **我自己**：用长 `sleep` 轮询的长任务被取消时，同一进程树里的生成进程被一起收了 | 改 `nohup` 分离启动；跑之前先清 3301 端口（上一轮遗留的后端占着 `database.db`，`rm` 也会失败） |

**形态 3 的数字值得单独记住**：修好之后，单块 reasoning 花掉 **10,074** 与 **19,105** token
（分别占该次输出的 91% / 90%）。也就是说 **flash 在比赛网关上每块要烧 1–2 万推理 token**，
而网关**计费包含 reasoning**。这直接影响"token / 通过条数"这个成本口径 —— 见 §十一.四。

### 十一.三 干跑（便宜档 `deepseek-chat`）暴露的三处——都不是能力问题

干跑用 DeepSeek 官方档在 quickstart 上跑完整链路（102,206 token / 68.7s）：

1. **工具外壳残留**：产物 `RegisterPage.tsx` 末尾多了一行 `</parameter>` → esbuild
   `Expected identifier but found "/"` → **整轮构建失败**（平台上同样会失败）。
   → `parse_files` 落盘前删掉「整行、标签名来自已知外壳集合」的行；真 JSX 的 `</div>` 有回归验证。
2. **审查把"合格"报成缺陷**：第 2 轮 review 回了 33 条，多数正文写着"满足需求/无需修改"，
   → 换来**一整轮全量修复**（4 个文件重写 ≈ 12k token）。
   → `_is_non_defect` 过滤，判据**刻意不对称**：出现任何否定/缺陷词一律保留。
3. **下拉初始 value 的契约被"修"坏了**：需求是「选项**文本** = 请选择证件类型、**value 必须为空串**」，
   判据直接断言 `toHaveValue('')`。生成是对的，审查凭"可能不匹配"把它报成缺陷、修复照做
   → value 变成提示文本 → **第 1 条判据当场失败（5/6 而不是 6/6）**。
   → 这条契约写进 `REVIEW_SYSTEM`（不要报）、`REPAIR_SYSTEM`（不许动）与 `STACK_CONTRACT`（生成期就对）。

**教训**：审查的**猜测会被当成真缺陷去改代码**，而"改坏"的代价常常大于"不修"。

### 十一.四 记账纪律（这次是被一个具体错数逼出来的）

- 错数：闸门第 0 条第一次的数字写成 **131,360**（8 次调用），真值 **156,343**（10 条记录）、
  reasoning 89,658 = 输出的 86%。成因：**一次 run 里有两轮 selfcheck→repair**，抄到中间态。
- 于是三件事：① metrics 每条记录加 **`run_id` + `call_index`**；② 新增
  **`eval/aggregate_metrics.py`**（成本表**由脚本聚合，禁止手抄**，带 `--expect-total` 护栏）；
  ③ 两处错数已改（`token/通过条数` = **52,114**，不是 43,787）。

### 十一.五 V-6「非零」口径：卡点不在工具，在"生成计划是注册专用的"

要"某一个 app 有分"，最小的是 **keep（32 条测试）**。实测它的需求 brief **15,923 字符 = 预算的 0.7×**
——**不需要分块**，架构上够用。（副产品测量：`12306` brief **104,328 字符 = 4.3×**，
bookstack 18,209 = 0.8×。**只有 12306 需要分块**。）

但真正的卡点是：**`CHUNKS` 与它的 `ask` 是照着注册需求写死的**——
产物文件名（`RegisterPage.tsx` / `auth_*`）、校验清单（证件类型/优惠类型/协议）全是注册专用。
拿它跑 keep，模型收到的还是"实现注册页"，**产出的还是注册应用 → keep 必然 0/32**。

**所以 V-6 缺的不是"更深的迭代"，而是"通用分块器"**：由需求树派生功能组
（按父节点分组的叶子 → 每组一个后端块 + 一个前端块），外壳由管线确定性拼装
（各页面各自导出 `<Route>` 片段，`App.tsx` 由管线汇总，避免每组互相覆盖）。
这正是 M3b-1「铺开」的第一件事，也因此**被闸门第 0 条挡在后面**。

### 十一.六 第三次跑闸门第 0 条（run c）：**零人工 5/6 → 补一个属性 6/6**

**配置**：同一样本（quickstart REQ-1）+ 同一套 6 条 Lab04 判据 + 同一条管线，
模型 = 平台注入的 `deepseek-v4-flash`，网关 = 比赛网关，`reasoning` 默认（不加任何旋钮）。

| 读数 | 值 |
|---|---|
| 生成 + 闭环 | 13 次调用 / **207,533 token**（reasoning 128,984 = 输出的 **93%**）/ 用时 1,341 s |
| L1 闸门 | **7 项全绿，每一轮都是**（含新增的 `db_tables` 与 `schema_injected`） |
| 建表落位 | `injected=True`，2 条语句，锚点 `before-return-database` |
| 判据（**零人工**） | **5/6** |
| 判据（手工补 1 个属性后） | **6/6**（`6 passed (5.6s)`） |

**唯一那条失败，形态是"瞄准类"，而且根因很具体**（探针 `output/lab04run/probe_meter.js`）：

- 判据定位：`getByRole('meter', { name: /密码强度|password strength/i })`
- 产物里 `<meter id="passwordStrength" aria-valuenow={strength}>` **确实在**，`aria-valuenow`
  也会随输入变（填 `short` → 1）；`getByRole('meter')` 命中 1、`getByLabel(/密码强度/)` 命中 1
- **但 `getByRole('meter', {name})` 命中 0 —— 它的 ARIA 可访问名是空的**
- 结论：**只写 `<label htmlFor>` 不给 `<meter>` 提供 ARIA 名**（label 关联只喂 `getByLabel`）。
  加上 `aria-label="密码强度"` → 命中 1 → **6/6**。

**这一处已写进三处 prompt（生成期硬清单 / 审查不要报 / 修复不许删）**，见提交 `a9d559c`。
证据快照：`runs/20260921T-gate0c-RegisterPage.after-one-attribute.tsx`、
`runs/20260921T-gate0c-6of6-after-one-attribute.txt`（⚠️ 按提交纪律，`runs/` 的原始件不入库，
要复核得重跑：`eval/gate0_run.sh c` + `output/lab04run/probe_meter.js`）。

**与之前两次 flash 的读数对照（同一模型、同一样本）**：

| 跑次 | 建表 | 零人工判据 |
|---|---|---|
| 标定组（`flashdef`） | ❌ 从没建表 | 3/6 |
| 闸门 0 组（`gate0b`） | ❌ 从没建表 | 3/6 |
| **run c（本次，建表落位 + 定位契约）** | ✅ 注入到 `return database;` 之前 | **5/6**（+1 属性 = 6/6） |

→ **② 的判据两条都成立**：`check_db_tables`=0 / `check_schema_injected`=0（生成期），
冒烟 200，happy-path 判据从"必挂"变成"通过"。

### 十一.七 第四次跑（run d）：**建表没问题，栽在"模块系统不一致"**（第二种崩溃形态）

**配置与前一次完全相同**（同样本 / 同判据 / 同模型 / 同网关 / 默认 reasoning）。
读数：16 次调用 / **307,163 token**（其中 reasoning **208,587 = 输出的 92%**）/ 用时 2,048 s。
（这两个数是 `eval/aggregate_metrics.py` 从 `runs/20260921T-m3b1-gate0d-metrics.jsonl` 聚合出来的——
**不手抄**；我先前的 174,276 是中途一轮的中间态。）

| 项 | 值 |
|---|---|
| 生成 | 8 块全出、10 个文件、`schema_injection` 注入成功（2 条语句） |
| L1 | 每轮 7 项全绿（**含 `db_tables` / `schema_injected`——建表这条已经稳了**） |
| 闭环 | 第 1 轮自检报 4 条（3 条真缺陷 + 1 条正是 meter 那条，见下）；第 2 轮 0 条 |
| 判据 | ❌ **冒烟就挂**：`GET /` 连不上 → 按约定**不往下跑判据**（否则 0/N 会被误读成"功能没做对"） |

**根因（后端日志原文）**：

```
file:///…/backend/src/auth/auth_repository.js:2
import { run, get } from '../database/index.js';
SyntaxError: The requested module '../database/index.js' does not provide an export named 'get'
```

`auth_repository.js` / `auth_service.js` 用了 **ESM**，而脚手架 `database/index.js` 是
**CommonJS**（`module.exports`）→ Node 立刻退出 → 后端起不来。

**两点值得记下来**：

1. **闭环的审查发现了它**（"后端模块系统不一致…会导致 SyntaxError 无法启动"），
   **但修复改的是 `app.js`** —— 症状文件 ≠ 根因文件，修完还是起不来。
   （第 1 轮最多修 4 个文件，实际修了 2 个：`RegisterPage.tsx` + `app.js`。）
2. **它是静态可判的** → 已补 `check_module_system`（提交 `81d195a`）：
   后端 `.js` 出现顶层 import/export 且 `backend/package.json` 没有 `"type": "module"` 就一定炸。
   回归：`gate0b`→0、`gate0c`→0、**`gate0d`→2（精确点名 `auth_repository.js` 与
   `auth_service.js`）**——正是那两个真正的肇事文件。

**另一个漂亮的交叉验证**：run d 第 1 轮自检的第 3 条是——
"密码强度 `<meter>` 只有 `<label htmlFor>` 而没有 `aria-label`，可访问名在
`getByRole('meter', {name})` 下为空"。那是**审查独立发现的、与我手工诊断完全一致的那条缺陷**。

### 十一.八 两次跑合起来看

| 跑次 | 建表 | 崩溃形态 | 零人工判据 |
|---|---|---|---|
| 标定组 / 闸门 0 组（改前） | ❌ 从没建表 | — | 3/6 · 3/6 |
| **run c** | ✅ 注入成功 | 无（冒烟 200） | **5/6**（补一个 `aria-label` → **6/6**） |
| **run d** | ✅ 注入成功 | **ESM/CJS 混用 → 后端起不来** | ❌ 冒烟失败（按纪律不跑判据） |

→ **建表这条已经稳定**（两次都注入成功、`check_db_tables` 两次都是 0）；
**剩下的两处都进了闸门**：`<meter>` 的 `aria-label` 写进生成硬清单，
ESM/CJS 变成 `check_module_system`。
**闸门第 0 条的判定要等再一次跑**（run e，带着这两条规则）。

### 十一.九 run e 的读数（补跑，**未重新生成**）：只差一个右括号

run e 是上一次会话的**第五次**闸门尝试：**生成成功、判据侧没跑成**（`npm install` 挂在那一步，
而当时 `score_app.sh` 把 npm 输出吞了 → 无从判断）。本轮用 `eval/score_app.sh` **补跑**（零生成调用）：

| 项 | 值 |
|---|---|
| 生成（上一轮已付） | 15 次调用 / **233,548 token**（reasoning 145,581 = 输出的 **91%**） |
| `db_tables` | ✅ 0 项（**建表成功**） |
| `schema_injected` | ✅ 0 项（**注入成功**） |
| `module_system` | ✅ 0 项（这次没犯 ESM/CJS 的错） |
| **`syntax_balance`** | ❌ **1 项**：`backend/src/app.js` 括号不配平 |
| 判据 | ❌ **冒烟就挂**（后端进程起不来）→ 按纪律**未跑判据** |
| 补跑时的 `npm install` | ✅ 成功（96 模块）→ **上一次的失败是环境性的**，不是产物问题 |

**根因（与模板逐字对照，一行之差）**：模板的 `app.js` 里那个 else 分支以

    });

结尾，模型写成了

    }

—— **少一个右括号** → `SyntaxError: missing ) after argument list` → 后端立刻退出。
也就是说：**一个字符**决定了"5/6 的应用"还是"起不来的应用"。

**回收这一次的成本**（这正是 `--reuse-app` 存在的理由）：判据侧复跑 **0 token**
（`metrics.jsonl` 的 mtime 未变 + `%TEMP%/m3b1-gate0e/reuse-note.txt` 留痕）。

### 十一.十 本轮补的四件事（都为了让"下一次失败"可诊断、可复用）

1. **`score_app.sh`：install/build 失败要打 npm 原始输出**（前端与后端两处）。
   演示：拿一个损坏的 `package.json` 跑 → 看到 `npm error code EJSONPARSE … Unexpected end of JSON input`
   + 判读提示（ENOTFOUND=网络 / EBUSY=占用 / ERESOLVE=依赖冲突）。
   **没有这一条，run e 的 23.4 万 token 只能靠"再跑一次生成"去碰运气。**
2. **`score_app.sh`：冒烟失败也写 RunRecord**（`smoke_failed: true`，`tests_total: 0`）。
   以前"应用起不来"只表现为 `runs/` 里少一个文件 → 下一个人会以为这轮没跑过。
3. **新增 `check_syntax_balance`（L1 第 9 项）** + `pipeline/verify/jsscan.py` + CLI `eval/check_js_balance.py`：
   词法级括号/引号配平扫描。**run e 的产物精确命中 `app.js`**；
   假阳性实测：官方模板 + 6 份历史产物 ≈**108 个文件 → 0 个问题**。
   → 这一类"一个括号毁掉整轮"的错误从此在**生成阶段**就被点名。
4. **`eval/gate0_run.sh --reuse-app`：把"生成"与"判分"解耦**（本轮四次失败三次在判据侧）。
   实测：`bash eval/gate0_run.sh c --reuse-app` 走完判分且**零 LLM 调用**（gate0c 产物 6/6，
   见 §十一.六；那一份是手工补过 `aria-label` 的，不是零人工读数）。

**顺带修掉一个一直静默失败的地方**：`gate0_run.sh` 的内联 Python 拿到的是 Git Bash 路径
（`/c/Users/…`），Windows 版 Python 解析不了 → `ModuleNotFoundError: No module named 'verify'`
→ **判据 ① 在 c / d / e 三次调用里全都失败了**（我先前报的建表数字是手工另跑得到的）。
现在统一走 `cygpath -w`，并且判据 ① 从 2 项扩成 **4 项崩溃类检查**。

### 十一.十一 三次跑横向对照（同一模型 `deepseek-v4-flash` @ 比赛网关，默认 reasoning）

| 跑次 | token | 建表 | 崩溃类静态检查 | 零人工判据 | 失败归属 |
|---|---:|---|---|---|---|
| **run c** | 207,533 | ✅ 注入成功 | 未跑（当时没这几项） | **5/6**（补 1 属性 → 6/6） | `<meter>` 缺 `aria-label`（瞄准类） |
| **run d** | 307,163 | ✅ 注入成功 | `module_system` **会命中**（补跑回归验证） | ❌ 冒烟挂 | ESM/CJS 混用（崩溃类） |
| **run e** | 233,548 | ✅ 注入成功 | **`syntax_balance` 1 项** | ❌ 冒烟挂 | **少一个 `)`**（崩溃类） |

**读法（避免事后解释）**：
- **建表这条已经稳**：三次都注入成功，`check_db_tables` 三次都是 0。
- **三次里两次死在"崩溃类"、一次死在"瞄准类"，而两类现在都进了闸门**
  （`syntax_balance` / `module_system` 在 L1；`<meter aria-label>` 在生成硬清单）。
- 三次合计 **748,244 token**，**零人工满分一次都没有**；`5/6` 是当前最好读数（run c）。
  **不算统计结论**：每个配置各一次，只能说明"与诊断一致"。
- ⚠️ **仍然未知**：配额上限与单次墙钟上限（V-6 要问的两个问题）。所以**不要再盲跑第六次**，
  先把上面两条规则在一次生成里验证（那才是第六次的**唯一理由**）。

---

## 十二、2026-09-21（下半段）：走重订后的关键路径 —— 分块器通用化，**keep 拿到非零**

### 一句话

按 `PLAN.md` §7 M3b-1 重订的关键路径走：**零 token 收尾 → 通用分块器（不用生成验）→ keep 最小子集**。
**结果为真：keep 上出现第一条主赛道判据通过**（`REQ-2.1: Note Listing`），且**零人工、24 秒、3.5 万 token**。

### 十二.一 零 token 收尾（三件）

1. **启动日志加掩码凭据体检**（A1）：`pipeline/main.py` 在第一阶段**之前**打一行
   `model / base_url / key(set(len=N)) / VISUAL_MODEL / ARCBENCH_*` 的存在性——
   **只报长度、一个字符都不打**（前缀也算值的一部分，日志会被平台留存）。
   它是整条链上**唯一"为假就全盘归零"**的假设。
2. **`<meter>` 的可访问名从 prompt 硬清单下沉成 L1 检查**（`check_aria_name_sources`，L1 第 9 项）。
   准确性验证：夹具 3/3 正确；**真产物 `m3b1-dryrun` 现场构建起服务 + 探针验证**
   （`getByRole('meter',{name:/密码强度/})` = 0、`getByLabel` = 1）→ **报得对，不是假阳性**：
   这条静态检查抓到了一个**生成 + 判据都漏掉的潜伏缺陷**（那次判据更早的断言就挂了，没走到 meter）。
3. **`check_js_balance.py` 默认排除** `node_modules/ dist/ build/ .git/ …` + 新增
   **`eval/regress_l1.sh`**（把 `%TEMP%` 下 10 份真实产物当回归语料、每份带已知读数当期望）。
   过程中它抓出一个**真误报**：gate0d 的 `auth_service.js` 里有
   `if (!/^[A-Za-z0-9.!#$%&'*+/=?^_`{|}~-]+$/.test(x)) …` —— 正则的字符类里同时有 `'` 和反引号，
   不认正则的扫描器读成"单引号字符串跨行未闭合"并级联报三条不存在的错误，
   **而 `node --check` 说那个文件是好的**。补上 `_regex_at()` 后：
   **拿 `node --check` 当客观裁判，10 份产物的全部 110 个后端 `.js` → TP=1 FP=0 TN=109 FN=0。**

### 十二.二 通用分块器（`generate/chunkplan.py`）：从 design JSON 派生块

写死的 `CHUNKS`（7 块里 6 块指着注册应用的文件）已删除。派生规则：
表→建表脚本、端点按 `/api/` 首段分组→一资源一文件、路由→一页一文件、
外壳拆成 `frontend-app`/`frontend-home`、**`frontend-auth` 是条件块**（设计与需求里出现登录才生）。
每块声明自己的 `wants`（要哪些上游原文），不再按块名写死。

**每块只带自己那一片需求**（关键词命中），没命中的叶子统一进外壳块——**不减这一步会静默丢需求**。

**验收不用生成**（`eval/accept_chunkplan.py`，§7 的纪律）：各一次设计调用 + 四项静态检查。
过程中它抓出**三个真 bug**：
1. 首页块漏接"命中 `/` 那条路由"的需求 → **静默丢 7 条需求**（keep）；
2. `backend-app` 带全部资源的需求切片 → **95% 预算**（已改成不带切片，46%）；
3. 路由 token 里的裸 `/` 命中几乎所有需求文本 → 首页块 27 条需求、**103% 预算**；
   加上"每块切片硬上限 6000 字符、超限降级成清单摘要"后降到 56%。

| 样本 | 块数 | 最大请求 | 覆盖度 | 结果 |
|---|---:|---:|---|---|
| quickstart | 7 | 52% 预算 | 1/1 需求 | ✅ 四项全过 |
| keep | 13 | 56% 预算 | **32/32 需求** | ✅ 四项全过 |

### 十二.三 token 预算 + 到额降级（`PLAN.md` §4.3）

- 按 app 的预算：keep/bookstack **25 万**、stackoverflow 30 万、其余 32 万，默认 30 万
  （锚点是实测：quickstart 单需求用 flash 约 **20.7 万**）。
- 块分两档：`core`（schema / 各资源路由 / app.js / **外壳**）决定"应用能不能起来"；
  `page`（页面与登录容器）是可选。**到额先切 page，并给 core 预留额度**；
  core 即使超预算也照写。
- **切了页面也必须能构建**：`_sync_shell_ask` 把外壳块的 ask 重建成"只列真的写出来的页面"
  （否则 `App.tsx` 会 import 不存在的文件 → 构建直接失败）。生成顺序因此是 页面 → 外壳。
- 到额即跳过验证闭环（闭环是打磨，不决定有没有分）。

干跑实测（预算压到 1）：core 四块照写、两个 page 块被切、日志逐条说明。

### 十二.四 keep：第一条主赛道判据通过（**M2 的形状**）

`PIPELINE_REQ_IDS=REQ-2.1,REQ-2.2`（两个需求）+ 官方档（便宜）先跑结构：

| 项 | 值 |
|---|---|
| 生成 | **24.3 秒 / 34,915 token**（6 次调用 + 闭环 5 次）|
| 块 | schema → notes 路由 → app.js → App.tsx → HomePage.tsx（5 个文件，一文件一调用）|
| L1 | **10 项全绿**（含新增的 syntax_balance / module_system / aria_name_sources）|
| 建表 | `schema.sql` 注入成功（1 条语句）|
| 构建 + 冒烟 | ✅ 前端 93 模块构建通过、`GET /` 200（2 秒）、`db:prepare:e2e` 成功 |
| **判据** | **`REQ-2.1: Note Listing` 通过（339ms）** ← **非零** |

**为什么 REQ-2.1 能过而 REQ-2.2 不能**（都是静态可解释的）：
- REQ-2.1 的契约**全在文本里**（种子数据 `"Sprint goals"` / `"Groceries"` + 标题 `Pinned` / `Others`），
  种子数据是**需求原文要求**的（`Seed data: pinned note "Sprint goals" …`），所以不算预埋答案。
- REQ-2.2 需要的五个名字里，**`"Note editor"`（那个 dialog 的名字）在需求文本里根本不存在**——
  它只出现在判据 helper 里（`getByRole('dialog', {name: /^Note editor$/i})`），
  多半只在参考截图里可见。**文本链路到不了它** → 这条判据在当前手段下拿不到。
  （其余四个名 `Title`/`Note content`/`Take a note`/`Close` 都在需求文本里，我们也抽到了。）

**顺带修掉一个"错角色"的抽取 bug**（这一条直接影响了 keep）：需求原文
`enter a title in the "Title" field and content in the "Note content" field, and click "Close"`
里，`Close` 被判成 **`[textbox]`**（正解 `button`）。后果不是"少一条"而是"错一条"：
生成照它做、审查照它报、**修复来回改了三轮**（m2keep 的闭环 3 轮全围着它转）。
根因是两处：列表消费把 `, and click ` 当普通连接词；规则 C 的第二个 `field` 又把 `"Close"` 附上去。
修法：**动作动词（`click/点击/press/tap/select`）之后的引号名不继承上一段的角色**。
修完 keep REQ-2.2 = `[Title]textbox / [Note content]textbox / [Take a note]button / [Close]button`，
全量回归仍绿（12306 标题契约 1:1、可访问名覆盖率 98%）。

---

## 十三、2026-09-21（第三段）：§4.1 第 5 条规则落地 + keep 重跑

### 十三.一 为什么是这条规则

keep 那 31 条失败**全是"名字/角色对不上"**（`Toggle sidebar`×14 / `complementary`×7 / `Notes`×7 /
`Note editor`×6 / `List view`×6 / `Title`×5 / `Search`×4 / `Settings`×4）——
不是崩溃、不是连接问题（`ERR_CONNECTION_REFUSED` = 0）。而四个散文式 app 的**交互单元空转率**
是 18–50%（keep 40%）。所以第 5 条规则既是**闸门**，更是**生成 prompt 的靶子来源**。

### 十三.二 实现（`reqcompile/prose.py` + 三处接线）

- **位置约束**（裁定约束 1）：只抽祈使宾语位 / 角色名词位——
  `Click the X link`、`fill in the X field`、`the X button`、`Structure` 名词位。
  动词表**带屈折变化**（`clicks/clicking/presses/…`）——踩过 `\bclick\b` 的坑。
- **输出 (role, name) 对，并支持纯 role 目标**：`sidebar` → `complementary`、
  `note editor` → `dialog`。keep 的 `getByRole('complementary')`×7 **只要该角色存在、不要名字**，
  这一类是可直接命中的。
- **L1 分两类**（裁定约束 3）：引号/规则式 → 精确名、驱动修复；散文式 → `check_prose_names`
  **只告警**（`soft_findings`：不进 findings、不影响 passed、不驱动修复）。
  实测：3 条不存在的散文靶子 → `passed` 仍 True、3 条都进 soft。
  **为什么必须这样**：散文抽取会把 `0 results`、`count` 这类词捞进来，一旦驱动修复，
  生成阶段会**烧 token 追幻影名字**，而赛制按 `pass/CNY` 排序。
- **生成 prompt 分两块**：**必须逐字兑现**（引号式）与**从散文抽出的目标（尽力做完）**，
  后者标注"名字按你判断取最贴近的"——避免当成逐字契约。

### 十三.三 空转率（裁定指定的验收指标，同一口径、分母不变）

| app | 基线（9/20） | 现在 | |
|---|---:|---:|---|
| 12306 | 2% | **1%** | ✅ |
| bookstack | 19% | **0%** | ✅ |
| ctrip | 18% | **8%** | ✅ |
| **keep** | **40%** | **8%** | ✅ |
| **prestashop** | **50%** | **8%** | ✅ |
| stackoverflow | 29% | **8%** | ✅ |

**六个全部下降、无一变差**（口径 = `docs/06-a11y-recall-probe.py` 自己的定义，直接跑它得到）。

### 十三.四 精度（裁定约束 2：必须同时报精度）

人工过 20 条后 precision ≈ **55%**，junk 三类可修，已修：
1. **冠词交替顺序错了**：`(?:the|a|an)` 里 `a` 先匹配 → 把 `an "X"` 吃成 `n "X"`
   （`'n "Action undone" notification'`）。改成 `(?:an|a|the)`。
2. **引号碎片**（`'exactly "Note trashed'`）——那是引号式规则的活，散文规则一律不收。
3. 功能词/数字/停用词过滤（`BAD_WORDS`）。

修完精度 ≈ **70%**，junk 三类消失；剩下的"不确定"多是 `Display/View` 类的**可见文本**目标
（`'home page'` / `'available notes list'`），不是控件——**下一步可按动词分成"控件靶子"与
"可见文本靶子"两块**（后者正好服务 `getByText` 断言）。

**召回（仅测量）**：从判据里取 (role,name) 当地面真值，keep 的靶子清单覆盖 **82–85%**。
⚠️ 测试**只用于测量**，不进抽取路径（平台上 agent 看不到测试）。

### 十三.五 一个必须记下来的发现：最高频靶子**不在文本里**

`button[Toggle sidebar]` 是 keep 的头号失败（14 次断言），但**"toggle" 在 keep 的需求文本里
一次都没出现**（只有 `sidebar navigation` / `User can see the sidebar`）。
→ 这个名字**只能从参考截图得知**（`requirements/reference/home_page.png`）。

而：**`reference/` 目录就在需求目录里**（`requirements/reference/*.png`，含 `home_page.png`
`list_view.png` `create_note_form.png` …），**平台会注入 `VISUAL_MODEL`**。
→ **图片链路是下一步最有价值的杠杆**：它能把"只能从截图知道的名字"变成靶子。
（同时要提醒：`VISUAL_MODEL` 的凭据是否真的可用，正是平台那一次跑要顺带回答的 A1。）

### 十三.六 keep 重跑（reQ-2.1 + REQ-2.2，官方档）

- 靶子 33 → **96 条**（散文 63 + 引号/规则式 33）；设计/分块计划不变（5 块，一文件一调用）；
  L1 **10 项全绿**（含新的 `prose_names`）；建表注入成功；**23 秒 / 36,218 token**。
- 闭环里能看到模型**开始按散文靶子做事**（给 `main` 加 `aria-label="home page"`、
  给列表容器加 `aria-label="available notes list"`）——这正是"靶子清单进 prompt"的预期效果。
- **判分（32 条）在跑**：`runs/*-keep-score-m2keep-rule5.*`（失败用例各耗 ~60s，约 30 分钟）。
  上一次同配置是 **1/32**；本次是否上升是本段的收官读数。

### 十三.七 两处口径（审核 §3.4 要求带着走）

- **判分墙钟：32 条 / 30.7 分钟**（失败用例各 ~60s，**慢的是失败而不是通过**）。
  PLAN 里"墙钟 × 6 外推"要用这个真数，而不是 12306 的 3.6–8 分钟。
- **1/32 = 3.1% 是否"够上榜"仍未验证**（A2/V-6）：若按平均通过率展示会落在 Junior 档，
  但**只要非零就是"有分"**——这条口径只能由平台那一次跑回答。
