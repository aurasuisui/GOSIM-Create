# 会话进度日志（从 `STATUS.md` 搬出来的逐轮叙述）

> **为什么有这个文件**（第二十四轮审核 §三 C，2026-09-22）：
> `STATUS.md` 头部写着「**只记"状态"，不记推理过程**。推理和证据放 `PLAN.md` / `docs/` / `runs/`」，
> 而它当时已经长到 **1041 行 / 90KB** —— 逐轮的完整叙述堆在里面，把"状态"淹没了；
> 而它是**每个会话的必读第一文件**，越长每次开工的代价越大。
> → **内容不删，换地方**：逐轮叙述（下面这些 `9/xx` 段）搬到这里，
> `STATUS.md` 只留 **一行指针 + 当前状态**。要复盘某轮时按需读本文件。
>
> **规则**：本文件**只追加**、不重写历史段（要更正就在该段下面加一行"更正"）；
> 每轮结束时在 `STATUS.md` 的索引里加一行，并把叙述追加到这里。

---

### 9/20 12:0x 新增（方案层）

- [x] **R5 依赖兼容性 —— 安装阶段已过；ARC 本体尚未真实跑过**（原风险项，`PLAN.md` §8）
      **已验证**（Python 3.13.11，临时 venv，未污染工作区）：
      1. `requirements.txt` 全部 **13 个依赖安装成功**
      2. 两个重依赖 **`deepagents` + `langchain_openai` import 成功**（首日 `ModuleNotFoundError` 的主角）
      3. **ARC 入口 `repos/agentic-requirement-compiler/src/main.py` import 成功**（完整 import 图）
      4. `arc --help` 正常；**`arc doctor` → `✓ Configuration is valid`（exit 0）**
         （需显式加载工作区 `.env`：ARC 只找**当前目录**的 `.env`）
      → **结论：环境齐备，不需要回退 3.11/3.12。** ⚠️ **但 `arc compile` 从未跑过一次**——
      那需要真实 LLM 调用；9/23 的保底包就是它，**这一步仍待做**（见「下一步」第 6 条）。
      复现（临时目录，不污染工作区，`-B` 避免写 `repos/` 的 `__pycache__`）：
      ```bash
      python -m venv "$TEMP/r5probe" && "$TEMP/r5probe/Scripts/python.exe" -m pip install -r pipeline/requirements.txt
      "$TEMP/r5probe/Scripts/python.exe" -B -c "import deepagents, langchain_openai; print('heavy imports OK')"
      cd repos/agentic-requirement-compiler/src && "$TEMP/r5probe/Scripts/python.exe" -B main.py doctor
      ```
- [x] **六 app 可访问名召回抽检**（`PLAN.md §4.1` 指定"必须在批量生成之前做"的那件事）
      探针 `docs/06-a11y-recall-probe.py`（审计快照），输出存档
      `runs/20260920T-interaction-unit-a11y.txt`（交互口径）+
      `runs/20260920T-a11y-recall-all-apps.txt`（收窄前版本，保留供对照）。
      **关键数字——L1 闸门空转率（交互单元 / 全部单元两个口径）：**
      `12306 2%/5% · ctrip 18%/26% · bookstack 19%/24% · stackoverflow 29%/44% · keep 40%/50% · prestashop 50%/62%`
      **诊断：差异是种类不是程度**——12306 是引号式需求（`labeled "Name"`），
      其余四个是**散文式**（`Display Logo, category menu, search box`，零引号）+ 测试用 `/logo/i` 松散正则；
      抽取器入口 `_iter_quoted()`（`a11y.py:219`）**按构造抽不到无引号名词**。
      → 详见 `docs/06` §三补 / §三补-2 与 `PLAN.md` §4.1（含裁定与三个约束）

### 9/20 12:2x–12:4x 新增（执行层：`eval/bench.sh` 并发安全 + 第 5 轮基线）

- [x] **`eval/bench.sh` 防事故 5 条全部落地并验证**（见下方「第二轮审核遗留 · ✅ 并发安全」）
      其中第 2、3 条判据是**审核连续三轮点名"唯一会毁数据的项"**——现在修完了。
      **验证的关键一条**：在**真实测量进行时**（R5）抢锁被拒 + `status` 给出
      `🟢 正在跑，不要动`（证据含端口占用、3 个 runner 活着、锁持有者活着）——
      那正是两次 run 作废的事故场景。
- [x] **第 5 轮干净基线：135/135（3.0 分钟）** —— `runs/20260920T043334Z-12306-round1.log`
      / `.json`（`arm_id=refimpl-baseline-r5`，`pass_rate=1.0`）。
      🔴 **它推翻了两条已写进 `AGENTS.md` 的结论**（`REQ-4.3.7` 稳定失败、理论天花板 134/135），
      全部文档已按事实改正（`AGENTS.md` / `PLAN.md` §6+§11 / `docs/04` §三+§四 / `runs/README.md` / 本文件）。
      ⚠️ 它同时把噪声跨度从 4 抬到 6 —— **门槛的算术前提变了，那属"测量方法"，已挂起待裁定**。

### 9/20 13:2x–13:5x 新增（第六轮审核的 4 条整改）

- [x] **2.1 修掉一个我引入的死锁缺陷 → 新增 `./eval/bench.sh recover [--force]`**
      （详见「下一步」第 1 项的说明）。**已用造出来的死锁现场端到端验证**：
      死 PID 锁 + 端口被占 → `test` 被拒（复现审核描述的"常规子命令全部不可用"）→
      `recover` 预检通过 → `--force` 恢复 → `status` 回 🟢；
      **安全属性也测了**：活锁 → 拒；死 PID 但心跳新鲜 → 拒（防"把正在跑的测量强杀掉"）。
- [x] **2.2 记录缺字段 → RunRecord 新增 3 个字段**
      `ci_env` / `retries_effective`（`playwright.config.ts` 的 `retries: CI ? 1 : 0` → 口径可判别）/
      `retries_observed`（从日志里数）/ **`load_snapshot`**（本轮**开跑前**的环境快照：
      `status=…；runners=…；port=…；ci=…`，取锁**之前**采集，记的是外部世界的状态）。
      已实测：无 CI → `retries_effective=0`；`CI=1` → `1`。
- [x] **2.3 `R5-收口` ①② 完成**：工作区根建了**正式 `.venv`**（Python 3.13.11）→
      `import main` 通过 + **`arc doctor` → `✓ Configuration is valid`（exit 0）**；
      `pip freeze` 存档为 **`docs/07-依赖已知可用版本集.txt`**（104 行，含采集时验过的三条与关键版本）。
      其中 `deepagents==0.7.15` 是**私有路径风险的落点**（ARC 在 `factory.py:10-11` 模块级导入
      `deepagents._models` / `deepagents.backends.filesystem._raise_if_symlink_loop`）。
      ⚠️ **③ 仍未做：`arc compile` 从未真正跑过**（要真实 LLM 调用）——
      在它跑通之前，"保底包能上榜"仍只是假设。
- [x] **2.4 `STATUS.md` 的内部矛盾已消除**（同一件事既写"已落地"又写"仍未落地"）。
- [x] **补充项**：`bench.sh` 现在同时往 **`runs/.inflight.log`** 追加认领/释放记录
      （只追加、从不修改 → 无覆盖风险）；`AGENTS.md` 的例外说明已更新为"已补"。
- [x] `.gitignore` 补 `.venv/`。

### 9/20 14:4x 新增（V5 前置：提交包 + 平台侧事实）

- [x] **提交包构建 + 合规校验工具化**：新增 `eval/package.sh`
      （在 `pipeline/` 内部打 zip，保证第一层就是 `main.py`；然后逐条校验 `docs/02` §一 的硬规则）。
      `output/bundle.zip`（136K）**全绿**：第一层有 `main.py`、根层无 `package.json`/`index.js`/`index.ts`、
      无 `.env`、无 `__pycache__`/`.git`/`node_modules`。
      → 9/28–29 彩排要反复打包，这个脚本就是那次彩排的尺子。
- [x] **V5 的入口已摸清，并核对到三条会影响方案的平台事实**（打开页面实测，非推断）：
      ① **平台要我们自己提供 API Key**（表单字段必填）→ 计量挂在我们交的 key 上；
      ② **Model / Visual Model 在提交时指定**，名字与 `.env` 的 `MODEL`/`VISUAL_MODEL` 同名；
      ③ 提交 = 一个快照 + **之后逐任务选跑**（只跑 1 个任务不产生聚合分、不污染榜单）。
      另：**平台侧逐任务模块/测试数已逐项对上**（468 / 484，本地 478，差 +6）。
      → 详见 `docs/08-V5-提交操作指导.md`
- [x] **顺带确认**：`ACCOUNT.md` 里的"待确认"三项早已确认，平台地址与用途表仍然准确。

### 9/20 15:3x 新增（V5 第一轮结果 → 修掉"平台第一道闸"）

- [x] **V5 第一次提交已发生（用户手动上传 + 跑六个任务）** —— 平台报错：
      `Your run failed: web template is incomplete: expected frontend/ and backend/ directories`
- [x] **诊断：这条闸检查的是"run 产出了什么"**，不是"zip 里有什么"。证据：
      ① 报错是 per-run 的 "Your run failed"；② ARC 自己第一步就 `copy_template()`
      把 `web-react-express/`（含 `frontend/`+`backend/`）拷进 workspace；
      ③ 我们的 `cmd_compile` 只写事件 + traceability，**从不拷模板**。
- [x] **修复（第 3 阶段最小版）：新增 `pipeline/generate/scaffold.py`**，
      把模板内容拷进输出目录，产出 `frontend/` + `backend/`；模板查找优先级
      `ARC_AGENT_TEMPLATES_ROOT` → `ARCBENCH_TEMPLATE_DIR` → 包内 `templates/`；
      **已有应用不覆盖**（兼顾决赛 evolution）；找不到模板抛错并写 run-failed 事件。
      **本地已验证**：quickstart 编译后输出根 = `backend/ frontend/ reqcompile/ README.md template.yaml`。
- [x] 重新打包 `output/bundle.zip`（141K，合规校验全绿），提交名建议 `probe-v2-scaffold`。
- 🔴 **仍未拿到的 V5 信息**：那一跑里 `doctor` 打出的 `ARCBENCH_*` / argv / cwd
      （平台怎么调我们用）——见 `docs/08` 第二轮的"请回传"清单。

### 9/20 16:0x 新增（**V5 完成：平台怎么调我们，全部已成事实**）

- [x] **V5 结论（实测日志 `runs/2d5a2a44160f`，Stdout 面板 2881 行）** —— **权威记录 `docs/09-平台提交与报错记录.md`**。要点：
      · **调用**：`python3 /workspace/submission/main.py <需求目录> --output-dir /workspace/template`
        （需求目录是**位置参数**，输出用 **`--output-dir`** 长选项；两者我们都支持 ✓）
      · **输出目录 = `/workspace/template`**，`ARCBENCH_*` 都指向它下面的 `.arc/`
      · **平台注入 `MODEL` / `VISUAL_MODEL`** → 之前"平台把它们当环境变量注入"的推断成立
      · **平台自带模板** `/opt/arcbench/templates/web-react-express`，我们的查找优先级正确命中它 ✓
      · **平台自己装依赖 + 构建 + 起服务**（frontend 290 包 / 16s → build → backend → 3000 可达）
        → **`frontend/dist/index.html` 由平台生成，我们不必在 agent 里构建**（此前的未决问题解除）
      · **平台读到了我们的 traceability**：`Traceability initialized with 109 requirements and 86 scenarios`
        （= PrestaShop 精确值）→ 第 1 阶段产物被消费 ✓
      · **Python 3.12**（本地 3.13，我们的代码无 3.13 专属特性）
- [x] **第二次提交的结局**：`generation-agent exit code: 0` → 平台构建/起服务全部成功 →
      测试 `passed=0, failed=86, score=0.0` → `Runner exited with test failures or runtime errors`。
      **原因纯粹是"应用只有骨架、没有功能"**，不是基础设施故障。
- 🔴 **我们不在榜上**（`/competition?competition=arc-bench-web` 实测 15 行，无 `suisui`）。
      榜单分 **Senior（≥80%）/ Junior（<80%）** 两级；Junior 只展示排名+用户+通过率+覆盖度。
- ⚠️ **待解现象（不要据此下结论）**：前 6 名里多名队伍 **token ≈ 0 / runtime ≈ 0 却拿到 80–100% 通过率**。
      要么他们的应用不是现场用 LLM 生成的（会撞"不得预埋答案"红线），要么计量口径有我们还没理解的机制。

### 9/20 18:0x–18:5x —— ✅ **M3a 能力探针完成：机制成立**（`docs/10-M3a能力探针.md`）

- [x] **`pipeline/` 第一次真的调用 LLM 生成功能**。新增 `generate/llm.py`（自写 HTTP + 逐次 usage 落盘 + 重试）、
      `generate/implement.py`（1 次设计 + **4 个分块**实现）、`main.py` 接线
      （`PIPELINE_GENERATE` / `PIPELINE_REQ_IDS` 开关）。
- [x] **判据：Lab04 的 6 条验证测试**（quickstart 的 REQ-1，1 个需求 / 3 场景 / 10 个可访问名）。
      **结果：3/6（原始产物）→ 3/6（修模板 bug 后）→ ✅ 6/6（手工最小修复前端 3 处后）**
      → **按预登记的分叉表命中第一行"机制成立"，且修复后满分**。
- [x] **修掉一个上游模板 bug（我们只是受害者）**：`init_db.js` 的 `initializeDatabase()`
      第二次调用起返回 `initPromise`（resolve 成 undefined）→ `db_runtime` 的
      `run/get/all/exec/withTransaction` 全崩。新增 `scaffold.py::apply_known_fixes()`，
      **修在产物上**（因为脚手架优先用平台自带模板，只改自己副本修不到平台那份）。
- [x] **三个失败根因查清**：① 一处 import 路径写错（构建失败）；② 上游模板 bug（见上）；
      ③ 前端三处接线/瞄准缺陷（`onRegistered` 没传、注册没走 auth context、用户名没单独成元素）。
- [x] **成本首次有数字**：**89,948 token**（in 21,417 / out 68,531），
      **其中 reasoning 59,972 = 输出 token 的 87%** ⚠️ —— 推理是主要成本，且同样计费。
- [x] **接上 DeepSeek 官方测试档**：`.env` 里 `PIPELINE_LLM_*` 三行（**.env 不提交**），
      本地迭代不再烧比赛额度。对照：同一句"只回两个字"，网关 97 token / 官方 10 token。
- [x] **仪器校验已做**：第 3 次 **6/6** 说明判据能过、不是坏尺子
      → 省掉了原计划解 `demo_full.zip` 做参考实现对拍的步骤。

### 9/20 19:2x–19:5x —— ✅ **M3b-0 自动化闭环完成**（`docs/11-M3b0自动化闭环.md`）

- [x] **出口判据 #1**：**零人工介入下构建成功 且 6/6 通过**（地板 4/6）。
- [x] **出口判据 #2（held-out）**：换 `ticketbooking-demo` 的 REQ-1.2、**代码一行不改** →
      **零人工构建成功**（97 模块）。→ 闭环**不是**对 quickstart 的特异化。
- [x] 新增三个模块：`verify/l1.py`（**静态**闸门：import 解析 / 路由与入口 / 可访问名存在 /
      脚手架未改 / scripts 完整）、`verify/selfcheck.py`（模型自检 + 定向修复）、
      `verify/loop.py`（有界轮次编排，默认 3 轮）。`main.py` 接线（`PIPELINE_VERIFY` / `PIPELINE_REPAIR_ROUNDS`）。
- [x] **M3a 的四类手工修复全部机器化**（实测抓到并修掉）：import 路径错（L1）／上游模板 bug
      （`apply_known_fixes`）／`onRegistered` 没传 · 绕过 auth context · 用户名拼接（自检）。
- [x] **闭环第一版没收敛**，四处缺陷逐个修掉（详见 `docs/11` §三），其中最关键的一条是
      **允许修复一并新建缺失文件 + 给它已存在文件清单**——它补的是模型"爱 import 自己没产出的辅助文件"
      这个**系统性**弱点，不是样本补丁。
- [x] 成本：主跑 **92,158 token / 14 次调用**；held-out **78,821 / 16 次**。
      ⚠️ **跨网关比绝对值无意义**（M3a 用比赛网关 + `deepseek-v4-flash`，87% 是 reasoning；
      M3b-0 用 DeepSeek 官方 + `deepseek-chat`，该 API 不报 reasoning 字段）。

### 9/20 20:1x–20:4x —— ✅ **M3b-1 前置：模型/成本口径标定完成**（`docs/12-M3b1模型成本标定.md`）

- [x] **三个旋钮都测了**（同一样本 + 6 条判据，口径 = `token / 通过条数`）：

| 配置 | 网关 | token（含全部尝试） | 通过 | token/通过 |
|---|---|---:|---:|---:|
| `deepseek-chat`（默认） | DeepSeek 官方 | 92,158 | **6/6** | **15.4k** ✅ 最优 |
| `deepseek-v4-flash` + `reasoning_effort=low` | 比赛网关 | 81,862 | 5/6 | 16.4k ❌ |
| `glm-5.3-flash`（默认） | 比赛网关 | **53,723** | **0/6** | ∞ ❌ |

- [x] **旋钮 2 答"网关认 `reasoning_effort`"**：小样本上 reasoning 949→52（占比 77%→11%）、成本指数 −60%，
      且**正文没截断反而更长**。
- [x] 🔴 **但端到端把它否掉了**：`low` 少 11% token 却少过 1 条；`glm-5.3-flash` 最便宜（−42%）
      却 0/6（原因查清：入口对、能构建、L1 全绿，但**用户名渲染成拼接文本**
      → `getByText(exact)` 匹配不到，与 M3a 的 5.3c 同类）。
- [x] **这条标定的真正教训**：**小样本探针会把这两个错配置都判成"更优"且理由充分**——
      `PLAN.md` 坚持"端到端 + token/通过条数"是对的。**任何省 token 的旋钮都必须复测 6 条判据。**
- [x] **顺带暴露闭环短板**：自检对"显示形态不匹配"的把握**随模型而异**
      （deepseek-chat 抓到并修掉用户名拼接；glm 没抓到）→ 这类约束应进**生成 prompt 的硬性清单**，
      不能只指望事后自检。
- [x] 新增 `eval/m3b1_model_probe.py`（小样本筛选脚本）与 `llm.chat(extra=…)` /
      `PIPELINE_LLM_EXTRA` 全局旋钮口（平台上由代码默认值决定，不靠环境变量）。
- ⚠️ **一次测量事故**（记下来免得重犯）：配置 B 首次跑出 0/6，我先当成配置结论——
      其实是**后端 npm 依赖没装**（只装了前端），后端起不来（`GET /` → 000）。
      **教训：跑判据前必须先确认探活 200；等待循环超时了就该停下。**

### 9/21 09:xx —— 🔴 **M3b-1 闸门第 0 条：用比赛模型复现 6/6 → 不成立（3/6）**

- [x] **按 `PLAN.md` §7 M3b-1 的"最先做"那条做了**：同样本 + 同判据 + 同管线，**只换模型**为
      平台注入的 `MODEL`（`deepseek-v4-flash`）、走**比赛网关**。
- **结果：**

| 配置 | 网关 | token | 通过 |
|---|---|---:|---:|
| `deepseek-chat`（M3b-0 基线） | DeepSeek 官方 | 92,158 | **6/6** |
| `deepseek-v4-flash`（平台注入的 MODEL） | 比赛网关 | **156,343**（10 条；reasoning 占输出 86%） | **3/6** |
| ↳ `token/通过条数` | | **52,114** | |

- [x] **失败形态查清**：三条仍是 `getByText(username, {exact:true})` → **用户名没单独成元素**；
      **应用全程存活**（连接被拒 0 次、跑完探活仍 200）→ 是**纯瞄准问题**，不是崩溃。
- [x] **计划的判断被证实**："机制是模型无关的，通过率不是"——同一份需求、同一套判据，6/6 → 3/6。
- [x] **顺带更正了 `docs/12` 的一处错诊断**（`PLAN.md` 指出）：glm 那组 0/6 的主因
      **不是**"用户名拼接"（那只解释 1/6），而是 **5/6 是 `ERR_CONNECTION_REFUSED`**——
      后端因 `SQLITE_ERROR: no such table: users` 触发**未捕获 rejection 直接退出**。
- [x] **两处相应改动**：① `implement.py` **放开 `backend/src/database/init_db.js`**
      （模板指定的建表位置，原先被我一起禁掉，导致"建表往哪放"变成模型自由发挥）；
      ② **撤掉一个我试做的静态检查"表建了没有"**——两版判据都会误报（`deepseek-chat` 那组
      写在仓储模块里照样 6/6），它是**运行期属性**，正解是**冒烟**。
- 🔴 **下一步（铺开之前）**：① 把判据的定位方式（`getByText(<值>,{exact:true})` / `getByRole('link')`）
      写进生成 prompt 的**硬性清单**；② 闭环加**冒烟**（起服务 → 打入口 → 200）；
      ③ 然后重跑本条闸门，过了才谈铺开。

### 9/21 10:xx —— 🔴 **闸门第 0 条重跑：仍 3/6；根因定位到"后端从没建表"**

- [x] 先落了两处修复再重跑：① **定位契约写进生成 prompt**（实体值单独成元素 / 真链接 / role=alert /
      原生 select / meter）；② **`eval/score_app.sh`**——本地打分脚本，**第一步是冒烟**
      （`GET /` 必须 200，否则明确拒绝往下跑判据）。
- [x] **定位契约确实生效了**：产物 `HomePage` 已是 `<span>{user.username}</span>`、
      `RegisterPage` 走 `useAuth().register()` + `navigate('/')`。但**仍 3/6**。
- [x] 🔴 **根因（两组 artifact 对照，同一个）**：两个 `deepseek-v4-flash` 产物的后端里
      **`CREATE TABLE` 只出现在模板注释里——`users` 表从没被创建** →
      `SELECT … FROM users` 抛 `SQLITE_ERROR: no such table` → route 吞成 **500** →
      注册永不成功 → 三条 happy-path 判据必挂（glm 那次则是未捕获 rejection 打死 Node）。
      → **闸门第 0 条不过的原因不是"瞄准"，是"功能根本没跑起来"。**
- [x] **纠正我自己的一个错误**：早先那版静态检查"表有没有 CREATE"**是对的**（能区分三组），
      但我收紧成"必须建在 init_db.js"后误报了 M3b-0，**于是把检查整个撤了——撤错了**。
      放宽判据才是对的。函数与说明已回到 `verify/l1.py`，但**暂未接进闸门**：
      恢复后仍误报（正则单独跑对、in-function 路径没定位到原因），
      **留会误报的检查比没有更糟** → 已标注为待修。
- [x] **顺带发现**：`selfcheck-review` 在比赛网关被**连续丢弃**（`RemoteDisconnected`）——
      review 把全部代码塞进一次请求 + 模型 reasoning 重 = M3a 那个"大请求被断连"的形态。
      → **review 也要分块**，否则闭环跑不完（这次重跑就是卡在这里）。
- ⬜ **下一步**：① 让"建表"可靠（修好判据并接进闸门，或做成不可跳过的显式产物）；
      ② review 分块；③ **第三次跑闸门第 0 条**——过了才谈铺开。

### 9/21 10:5x —— ✅ **计划点出的两个闭环缺陷：① 已修并回归通过；② 已立客户端规则**

- [x] **① `check_db_tables` 的 bug 修好并接回闸门**（`PLAN.md §4.2` 已定位根因）：
      **类型不匹配** —— `used` 是 `(表名, 文件)` 元组集合、`created` 只有表名，
      `used - created` **永远不消掉任何元素** → 每个产物都报同样的 4 条。
      加修：**匹配前先剥 JS 注释**（`seed_db.js` 的 JSDoc 里有 `FROM tests`，被当 SQL 读了）。
      **回归（计划给了预期值，5/5 全对）**：
      `m3b0`→0 ✅ · `m3b1-low`→0 ✅ · **`flashdef`/`gate0b`→`users`/`sessions` ✅**
      （正是闸门 0 失败的根因）· `glm`→0 ✅（它的毛病是**执行顺序**，静态判不出，与计划预测一致）。
      → 这条闸门现在能**在生成阶段就拦住"从没建表"**，不必等到注册 500。
- [x] **② 「大请求被网关丢」升级成客户端规则**（`pipeline/generate/llm.py`）：
      新增 `RequestTooLarge` + `MAX_REQUEST_CHARS`（默认 24000 字符，`PIPELINE_LLM_MAX_REQUEST_CHARS` 可调）。
      超预算直接抛；**"请求偏大 + 被断连"不再重发同一个大请求**，直接抛"该分块"。
      依据：这个形态已出现三次（M3a 后端块 / M3a design / 本轮 selfcheck-review ×2）。
      ⬜ **尚未做的**：`selfcheck.review` 本身还没改成按文件分组（预算已就位，分组是下一步）。
- ⬜ **③ 第三次跑闸门第 0 条**——等 ② 的 review 分组落地后跑（否则闭环还会卡在同一处）。

### 9/21 11:xx–13:xx —— ✅ **① 建表确定化落地（运行期已验证）**；③ 连撞四种"基础设施形态"

**详细记录见 `docs/12-M3b1模型成本标定.md` §十一**（含每次失败的原话与归因）。

- [x] **① 建表确定化完成**（提交 `e569ce9`）：模型只产出纯 SQL 的
      `backend/src/database/schema.sql`，**管线注入**到 `init_db.js` 的启动流程
      （`PRAGMA` 之后、IIFE 内那个 `return database;` 之前、`await` 执行）。
      三个锚点 + 标记块替换式幂等；`init_db.js` 进禁写清单；
      L1 新增 `check_schema_injected`（闭环里是**零 token 机械修复**），
      `table-not-created` 路由到 `schema.sql`。
- [x] **判据成立（运行期）**：干跑产物 `check_db_tables`=0 / `check_schema_injected`=0 /
      冒烟 200 / **Lab04 5/6**；自测 12 项全绿（自测还抓到锚点一个真 bug：会误匹配**函数级**
      那个 `return database;`，把建表放到 `await` 之后）。
- [x] **③ 第三次闸门第 0 条：四次尝试，四种形态，三处已修在管线里**：
      ① flash **空内容**（输出全被 reasoning 吃掉）→ 改走重试 + **失败调用也记账**；
      ② `RequestTooLarge` **阈值错**（12,974 字符被判"太大"→ 整个 run 作废）→ 收紧到 0.8× 预算；
      ③ 2 文件的块**连续 3/3 被网关断连**（输出太大、生成太久）→ 生成块拆到"一文件一调用"（现 7 块）；
      ④ 进程中途消失 = **我自己**用长 sleep 轮询把同进程树的生成进程一起收了 → 改 `nohup` 分离。
      ⬜ **闸门读数仍未拿到**（第 5 次尝试在跑）。
- [x] **③ 拿到两次闸门读数**（`docs/12` §十一.六–八，原始件在 `runs/`）：
      **run c** = 建表注入成功 + L1 每轮全绿 + **零人工 5/6**（唯一失败 = `<meter>` 缺 `aria-label`，
      **补这一个属性 → 6/6**）；**run d** = 建表也成功，但 **ESM/CJS 混用 → 后端起不来**
      （冒烟就挂，按纪律不跑判据）。两次合计 207k + 307k token，reasoning 占输出 90%+。
      → **建表这条已经稳**；剩下的两处都进了闸门：
      `<meter aria-label>` 写进生成硬清单（`a9d559c`）、ESM/CJS 变成
      **`check_module_system`**（`81d195a`，回归：gate0b→0 / gate0c→0 / **gate0d→2 且精确点名肇事文件**）。
      ⬜ **run e 在跑**（带这两条规则），它的读数才是"闸门第 0 条"的判定。
- [x] **附带一个交叉验证**：run d 的**自检独立报出了 `<meter>` 缺 `aria-label` 那条**——
      与手工诊断一致（探针 `output/lab04run/probe_meter.js`）。
- [x] **干跑（便宜档）暴露三处**：工具外壳残留 `</parameter>`（一行就让构建全挂）/ 审查把"合格"
      报成缺陷（换来整轮无效修复）/ 下拉初始 value 契约被"修"坏（直接掉一条判据）。
- [x] **第三优先 1+2 完成**：`score_app.sh` 三处（跑前清 sqlite、记 `load_snapshot` + 出 RunRecord、
      `ERR_CONNECTION_REFUSED` 计数）；metrics 加 `run_id`+`call_index`；新增
      `eval/aggregate_metrics.py`（**成本表由脚本聚合，禁止手抄**）；两处错数已改
      （真值 **156,343 / 10 条**，`token/通过` = **52,114**）。
- [x] **V-6 卡点定位**：keep 的 brief **15,923 字符 = 预算 0.7×**（不需要分块；12306 是 4.3×）。
      但 `CHUNKS` 的**文件名与 ask 是注册专用的** → 跑 keep 还是产出注册应用 → 必然 0/32。
      **缺的是"通用分块器"**（由需求树派生功能组 + 外壳由管线拼装）——那是铺开的第一件事。
- ⚠️ **flash 的成本要重新认识**：它在比赛网关**单块烧 1–2 万 reasoning token**（占输出 90%+
     实测 19,105），而网关**计费包含 reasoning**。`token/通过条数` 这个口径会明显恶化。

### 9/21 15:xx —— ✅ **run e 的读数收回（只差一个右括号）**；判据侧补三处 + L1 第 9 项

- [x] **run e 生成成功、判据侧曾因 `npm install` 失败中断，本次为补跑读数**（**零生成调用**）：
      15 次调用 / **233,548 token**（reasoning 91%）；`db_tables` ✅0 / `schema_injected` ✅0 /
      `module_system` ✅0 / **`syntax_balance` ❌1**；判据 = **冒烟就挂，按纪律未跑**。
      根因：模型把模板 `app.js` 的 `  });` 写成 `  }` —— **少一个右括号** →
      `SyntaxError: missing ) after argument list` → 后端起不来。**一个字符定生死**。
      补跑时 `npm install` **成功**（96 模块）→ 上次失败是**环境性**的，不是产物问题。
- [x] **`score_app.sh` 两处**：① install/build 失败打 **npm 原始输出** + 判读提示
      （演示：坏 `package.json` → `EJSONPARSE` 原文可见）；② **冒烟失败也写 RunRecord**
      （`smoke_failed: true`），不再表现为"runs/ 里少一个文件"。
- [x] **`gate0_run.sh --reuse-app`**：生成与判分解耦。实测 `bash eval/gate0_run.sh c --reuse-app`
      走完判分且**零 LLM 调用**（`metrics.jsonl` mtime 未变 + `reuse-note.txt` 留痕；
      gate0c 产物 **6/6**，但那份是手工补过 `aria-label` 的，**不是零人工读数**）。
- [x] **L1 第 9 项 `check_syntax_balance`**（+ `pipeline/verify/jsscan.py` + CLI `eval/check_js_balance.py`）：
      词法级括号/引号配平。**run e 的产物精确命中 `app.js`**；
      假阳性实测：官方模板 + 6 份历史产物 **≈108 文件 → 0 问题**。
      → "一个括号毁掉整轮"这类错误从此在**生成阶段**被点名。
- [x] **顺带修掉一个一直在静默失败的地方**：`gate0_run.sh` 的内联 Python 拿到 Git Bash 路径
      （`/c/Users/…`）→ `ModuleNotFoundError` → **判据 ① 在 c/d/e 三次调用里全失败**
      （我先前报的建表数字是手工另跑得到的）。改用 `cygpath -w`，并把判据 ① 从 2 项扩成
      **4 项崩溃类检查**。
- 📊 **三次跑合计 748,244 token，零人工满分一次都没有**（c=5/6、d/e=冒烟挂）。
      **建表这条已经稳**（三次都注入成功）；两次死在崩溃类、一次死在瞄准类，**两类现在都进了闸门**。
      ⚠️ 单次对照，**不是统计结论**。详见 `docs/12` §十一.九–十一。

### 9/21 15:5x–17:xx —— 🎯 **走重订关键路径：通用分块器落地，keep 拿到第一条主赛道非零**

三段全部按 `PLAN.md` §7 M3b-1 重订的关键路径走完。详见 `docs/12` §十二。

- [x] **段一 零 token 收尾三件**（`e1e4c30` / `fc6854b` / `bc62006`）：
      ① 启动日志加**掩码凭据体检**（`model / base_url / key(set(len=N)) / VISUAL_MODEL / ARCBENCH_*`，
      **只报长度不打值**）——A1 是唯一"为假就全盘归零"的假设，且它在 zip 第一层的 `main.py` 里；
      ② `<meter>` 的可访问名从 prompt 硬清单**下沉成 L1 第 9 项**（`check_aria_name_sources`），
      **现场验证过**：`m3b1-dryrun` 构建起服务 + 探针 → `getByRole(meter,{name})`=0 / `getByLabel`=1
      → 报得对、且抓到了一个**生成与判据都漏掉的潜伏缺陷**；
      ③ `check_js_balance` 默认排除 `node_modules/dist/build/.git` + 新增 **`eval/regress_l1.sh`**
      （10 份真实产物当语料、每份带已知读数当期望）。过程中修掉一个**真误报**（正则字符类里的
      `'` 与反引号被读成跨行字符串），修完 **`node --check` 对照 110 个文件 TP=1 FP=0 TN=109 FN=0**。
- [x] **段二 通用分块器**（`f1a1917`）：删掉写死的 `CHUNKS`，改由 `generate/chunkplan.py`
      从 design JSON 派生（表→schema、端点→一资源一文件、路由→一页一文件、外壳两块、
      auth 为条件块）。**验收不用生成**（`eval/accept_chunkplan.py`）：
      quickstart 7 块 / keep 13 块，最大 56% 预算，**keep 32/32 需求落位**，四项全过。
      验收过程抓出三个真 bug（首页块丢 7 条需求 / `backend-app` 95% 预算 / 裸 `/` 命中一切 → 103%）。
- [x] **段三.1 token 预算 + 到额降级**（`f83f9fc`）：按 app 预算（keep 25 万…默认 30 万，锚点 = 实测
      quickstart 20.7 万）；块分 `core`/`page` 两档，**到额先切 page 并给 core 预留**，
      core 超预算也照写；**切了页面外壳会自动只接线写出来的页面**（否则构建必挂）；
      到额即跳过闭环。干跑（预算=1）验证过。
- [x] **段三.2 keep 最小子集 = 非零**：`PIPELINE_REQ_IDS=REQ-2.1,REQ-2.2` + 官方档：
      **24.3 秒 / 34,915 token**，5 个文件、L1 十项全绿、建表注入成功、构建 + 冒烟 200、
      **`REQ-2.1: Note Listing` 通过（339ms）← 主赛道第一条非零**。
      原因已定位：REQ-2.1 的契约全在文本里（种子数据+标题）。
      🔴 **下面这句已更正（2026-09-21 22:xx，方案会话）**：原文写「REQ-2.2 需要的
      `dialog[Note editor]` 在需求文本里根本不存在（只在判据 helper/参考截图里）→ 文本链路到不了」——
      **前半句不成立**：`grep -ic "note editor" requirements/requirements.yaml` **命中 2 处**；
      **后半句"文本链路到不了"也缺证据**——它是**待查的链路缺口**，不是**已证明的通路缺失**。
      → 排查方向改成先查"靶子有没有送到写代码那次调用"：
      `PLAN.md §7 M3b-1`「靶子清单交付到 prompt 的两处缺口」（分档丢失 / 体积降级丢弃），
      **用 `plan_report` 的 `slice_chars` / `slice_degraded` 当证据**，不要先假定"看不见图"。
- [x] **顺带修一个"错角色"抽取 bug**（`ecfcfad`）：`…the "Note content" field, and click "Close"`
      把 `Close` 判成 `[textbox]`（正解 `button`）→ 生成照做、审查照报、**修复来回改三轮**。
      修法：动作动词（click/点击/press/tap/select）之后的引号名不继承上一段角色。全量回归绿。
- [x] **段三.3 打包**：`output/bundle.zip`（70 条目，第一层 `main.py`，含 chunkplan/schema/jsscan/l1…，
      合规校验全绿）——**已含 ①② 的修复与掩码凭据行**，可交给用户跑 keep。
- ⬜ **keep 的 32 条全量计数仍在跑**（`runs/20260921T093005Z-keep-score-m2keep-official.judges.log`）：
      失败用例各耗 60 秒，跑完约 35 分钟。**非零已成立**（1 条通过），全量分母待它跑完补记。
      ⚠️ 它**占用 3301 端口**，跑完后 `score_app.sh` 会把后端留在那里（要停就 taskkill 占用 3301 的进程树）。

### 9/21 18:xx–19:xx —— 🎯 **§4.1 第 5 条规则落地（靶子清单）**；keep 重跑中

- [x] **段一 收尾**：停掉占 3301 的 PID 4776；清「在飞工作」；「当前阶段」改成
      "M2 本地半边已成立、等平台一次跑"（审核 §3.2/§3.3 两处）。
- [x] **段二 靶子清单（`reqcompile/prose.py` + 三处接线，提交 `d9b2d86`）**：
      位置约束（只抽祈使宾语位 / 角色名词位，动词**带屈折变化**）、**输出 (role, name) 对**、
      **支持纯 role 目标**（`sidebar`→`complementary`、`note editor`→`dialog`——keep 的
      `getByRole('complementary')`×7 只要角色存在、不要名字）；
      L1 **分两类**（引号式精确名 + 驱动修复 / 散文式 `soft_findings` **只告警不判死**；
      实测 3 条缺失散文靶子 → `passed` 仍 True）；
      生成 prompt 分「**必须逐字兑现**」与「**散文抽取、尽力做完**」两块。
- [x] **段三 廉价验收**：**交互单元空转率六个 app 全降、无一变差**（同口径直跑
      `docs/06-a11y-recall-probe.py`）：12306 2%→**1%** · bookstack 19%→**0%** ·
      ctrip 18%→**8%** · **keep 40%→8%** · **prestashop 50%→8%** · stackoverflow 29%→**8%**。
      **精度**（约束 2，人工过 20 条）：55% → **70%**（修了冠词交替顺序、引号碎片、功能词过滤）；
      **召回 82–85%**（从判据取 (role,name) 当**测量**地面真值，绝不进抽取路径）。
      新增 `eval/a11y_prose_probe.py`（零 token：打样本 + 量召回）。
- [x] **段四 keep 重跑（官方档）**：靶子 33 → **96 条**；设计/分块计划不变（5 块一文件一调用）；
      L1 十项全绿（含新 `prose_names`）；建表注入成功；**23 秒 / 36,218 token**；
      闭环里模型已按散文靶子做事（给 `main` 加 `aria-label="home page"` 等）。
      ⬜ **判分（32 条）在跑**（`runs/*-keep-score-m2keep-rule5.*`，约 30 分钟）——
      **上一次同配置是 1/32，本次是否上升是收官读数**。
- [x] **打包已刷新**：`output/bundle.zip` 含第 5 条规则与掩码凭据行。
- 🔴 **发现（下面这段已被证伪并更正，2026-09-21 22:xx，方案会话）**：
  原文写 keep 的头号失败 `button[Toggle sidebar]`（14 次断言）**在需求文本里根本不存在**
  （"toggle" 一次都没出现）、只在参考截图里，据此指向"该投图片链路"。**三条实测都不支持它**：
  ① 它**逐字在文本里**——`requirements.yaml:565` 与 `requirements.md:413` 都是
  `Click the "Toggle sidebar" control and then click it again.`（"零命中"来自**大小写敏感**的 grep）；
  ② **抽取器已经把它抽对了**——把原句直接喂给 `_extract_from_text()` → `('Toggle sidebar','button')`；
  ③ 它真正的身份是**共享前置**——`tests/helpers.ts` 的 `openSidebar()`/`toggleSidebar()` 要它，
  而 **32 个 spec 文件里有 8 个调用**（REQ-2.5.4 / 2.7.3 / 2.7.5 / 2.7.6.1-3 / 6.1 / 6.2）；
  **唯一通过的 REQ-2.1 恰好吃 `openHome`、不碰侧栏**。
  → **那 14 次断言失败是因为侧栏不在生成子集里，不是因为名字不可知。**
  **图片链路因此降为独立的能力问题**（仍受 A1 视觉半边与 `§8 R9` 成本约束），
  而它的动机例同样已被证伪（`Note editor` 也在文本里：`grep -ic` 命中 2 处）。
  完整更正、复核命令与两条落地动作见 `PLAN.md §7 M3b-1` 的「共享前置」一节。
- 📌 **两处口径**：判分 **32 条 / 30.7 分钟**（慢的是失败不是通过）→ PLAN 的"墙钟 × 6 外推"用它；
  **1/32 = 3.1% 是否够上榜仍未验证**（A2/V6，等平台那一次跑）。

### 9/22 13:xx– —— 🎯 **R2 前置四个零 token 项落地**；R2 / R3a 在跑

- [x] **段一 清场**：停掉占 3301 的 PID 13804（9/21 18:15 判分留下的后端，陈旧约 17 小时）；
      `bench.sh status` 留了"🟢 空闲"的开工记录。
- [x] **段二 四个前置（提交 `76afa12`）**：
      ① **预算按实测每块成本外推**（不是拍的）：官方档 7,243/块 → 13 块 ≈ 9.4 万；
      **比赛模型 25,942/块 → 13 块 ≈ 33.7 万**（flash 的 reasoning 占输出 90%+，同块成本 3.6 倍）
      → keep/bookstack/stackoverflow **70 万**、12306/ctrip/prestashop **90 万**。
      **不调就会触发降级 → 读的是降级产物的分 = 白跑。**
      ② **把"名字不在需求文本里"变成机器输出**（`eval/a11y_prose_probe.py` 的 `names_not_in_text()`）：
      实测 keep 的 41 个去重靶子名 **0 个未命中**；抽查 `Toggle sidebar` → **line 565**。
      ⚠️ **我上一轮的错判（"Toggle sidebar 只在截图里"）正是这条要防的**——
      当时的 `grep … | head -12` 把命中行截掉了就下了结论；这是硬规则 17 的**第三次**同型错误。
      ③ **抽取路由**：引号式（规则 A–D）与散文式（规则 E）**顺次执行、不是二选一**——
      证据：`Toggle sidebar` / `List view` / `Notes` 都是引号式提取（role=button），
      同时另有 65 条散文靶子。④ **14 条 `Toggle sidebar` 断言的性质**：
      被 8 个 spec 调用（REQ-2.5.4/2.7.3/2.7.5/2.7.6.1-3/6.1/6.2），**全在生成子集之外**
      → 是"**没实现**"不是"瞄准偏了"；**下一步杠杆是扩大 REQ 子集**。
      顺带补一个抽取缺口：多词结构名（`note editor`）原先只有纯 role 条目，
      而判据按名字找它（`dialog[Note editor]`×4）→ 现在同时产出带名字的目标。**召回 82% → 85%**。
- 🟡 **段三 R2 在跑**：**全量 keep + 比赛模型**（`deepseek-v4-flash` @ 比赛网关，预算 70 万）——
  这是"**离上榜多远**"的读数。产物 `%TEMP%/r2-keep/app`、日志 `gen.log`（13 块计划）。
- 🟡 **段三 R3a 在跑**：同一产物（规则 5 的 keep 子集）**第二次**判分
  （`runs/*-keep-score-r3a-1.*`）→ 与第一次比，得到**我们生成产物的判分噪声底线**（此前未知）。
  ⚠️ R3a 是**重复性**，不是闭环 ROI——ROI 要**同一配置闭环开/关各生成一次**（R3b）。
- ⏭ **未做（下一步）**：R3b（闭环开/关各一次生成 + 各判一次）；R2 的判分（等生成完）；扩大 REQ 子集。
- 📌 平台那一次（R1，带真实生成）由用户做，不串行等待——它同时答 A1（凭据，含视觉）与 A2/V-6。
  禁令仍在：**quickstart 的通过率读数不写进本栏、不当任何闸门判据**（`PLAN.md:1230`）。

### 9/22 14:xx– —— 🔴 **R2 死在半路（我立的规则中止了整轮）**；零生成读数已收回

- [x] **段一 清场 + 变体认定**：停掉占 3301 的 **PID 10888**（r3a-1 留下的后端）。
  **`m2-keep2` 依据它自己的 `metrics.jsonl` 认定为**：子集（REQ-2.1+2.2）+ **闭环开** + **官方档**
  （`run_id=20260921T101419Z-p19840`，stage 序列含 `selfcheck-review×3 / repair×2`）——**不是** R3b 的"闭环关"。
- [x] **读数（零生成）· `m2-keep2` = 1 / 32**（仅 `REQ-2.1: Note Listing`，`runs/20260922T031033Z-keep-score-r3a-1.json`）：
  与上一轮 `m2keep-rule5` 同一条 → **"规则 5 没破坏已通过项"的对照成立**。
  🟡 **r3a-2（同一产物重复判分）在跑**（5/32 时仍 1 通过）→ 给"判分翻面"补 n=2。
- [x] **🔴 R2 无产物可判**：`%TEMP%/r2-keep` 生成**中止在 10/13 块**
  （`RequestTooLarge: 19,267 字符 ≈ 80%，stage=implement-frontend-page-labelmanagerpage`，`EXIT=1`）
  → 缺 `App.tsx` 外壳与首页块、**闭环完全没跑**，产物残缺；
  **判它得到的是"残缺应用的分"，不是"离上榜多远"。**
- [x] **已修（同一个 commit）**：把"超线就抛异常"改成**拼请求时就按弹性优先级压体积**
  （安全线 = 预算 70% = 16,800；四级收紧档；默认档不变）。
  **零 token 验证**：全量 keep 的 13 块从最大 **19,267（80%）→ 10,818（45%）**，全部在安全线以下。
  ⚠️ **这也是平台风险**：bundle 里是同一套逻辑，修之前提交，平台跑全量 app 也会中途 `Run failed` → 0 分。
  **已重打包**（含本修复）。
- [x] **顺手修** `extract_run.py` 的 `arc_test_date=None`（写死 + "由调用方填写"而没人填）
  → 从环境读；验证重抽得 `2026-09-20`（**没有它两轮读数不可比，而 R3a 正是拿两轮比**）。
- ⏭ **下一步**：① **R2 重跑**（现在请求体积已压到 ≤45%，可以跑了；档位要写死是官方档还是比赛模型）；
  ② **R3b**（闭环开/关各生成一次 + 各判一次）—— 未做；③ 扩大 REQ 子集（`Toggle sidebar` 那 14 条属"没实现"）。
- 📌 **口径**：1/32 是**官方档 + 2 条需求子集**的读数，**既不是能力读数也不是平台预期**；
  前例仍在（同一产物官方档 6/6、比赛模型 3/6）→ **本地数字不能当平台预期**，那只能等 R1。
  禁令仍在：**quickstart 的通过率读数不写进本栏、不当任何闸门判据**（`PLAN.md:1230`）。

### 9/22 15:xx– —— 🔴 **扩子集首轮 0 分（Express5 裸 `'*'` 崩在启动）→ 已修 → 重跑中**

- [x] **段一 清场**：3301 已空闲（r3a-2 的后端已不在）；「在飞工作」栏清空。
- [x] **r3a-2 读数：1 passed / 31 failed（30.5 分钟）**，与 r3a-1 **逐条一致**
  → **我们生成产物的判分重复性：翻面 ≈ 0（n=2）**。⚠️ 这是**重复性**，不是闭环 ROI。
- [x] **PLAN §4.3 的 2b（就地降级）补上"调用方那一半"**：块循环整体 try/except，
  **失败就跳过继续**、失败清单进 `chunks_failed`。实测（坏端点）：13 块全部失败、**全部跳过**、
  run 正常收尾。⚠️ 已知边界：**设计调用失败仍中止**（没有块计划可降级）。
- [x] **段二 扩子集首轮 = 0 分，根因静态可判**：子集扩到 **`{REQ-1.1, REQ-2.1, REQ-2.2, REQ-6.2}`**，
  生成正常（5 块 / 35,555 token / L1 全绿），**但冒烟挂**：
  `app.js:25` 用了 **Express 4 的 `app.get('*', …)`**，而模板是 **Express 5**
  → `PathError: Missing parameter name at index 1: *` → 后端起不来 → 0/32。
  **两处修**：① **L1 第 11 项 `check_express5_routes`**（坏产物 1 条/精确到第 25 行；
  两份已知能起的产物 0 条）② `STACK_CONTRACT` 加硬约束（照抄模板正则或用 `/*splat`）。
- 🟡 **重新生成后冒烟通过**（`GET /` 200）→ **判分在跑**（32 条约 30 分钟）：
  `runs/*-keep-score-e1b-keep4.*`、`%TEMP%/e1b-keep4/`。
  **这是段二的读数**（通过数是否从 1 上升、新增通过的是哪几条）——本轮结束时未落袋。
- 🔴 **扩子集不是免费的（本轮最重要的负面发现）**：重跑判分跑到 8/32 时 **0 通过**，
  而 `{REQ-2.1,REQ-2.2}` 子集上 **`REQ-2.1` 连续两轮都通过**。
  逐条看前 8 条全 `x`（含 REQ-1.1 与 **REQ-2.1**）。
  **读法**：4 条需求 → 设计变了 → **`HomePage.tsx` 是多条需求共享的外壳**被重新生成成"更强的版本"
  → 原先能过的 `REQ-2.1`（种子数据 + `Pinned`/`Others`）不再成立。
  → **扩子集可能把已经拿到的分掀掉**；下一步要**先看 REQ-2.1 差在哪**，再决定扩还是守。
- ⏭ **未做**：段三（全量 keep 的结构读数）、段四（R3b 闭环 ROI，9/25 决策日前不能缺）。
- 📌 硬约束：**R1 必须用 12:11 之后的 `output/bundle.zip`**（本轮的 Express5 修复已重打包）；
  读它的日志**先问"生成有没有跑完"**，别把"我们自己的中止规则"读成平台问题。
  quickstart 的读数不进本栏、不当任何判据（`PLAN:1230`）。

> ⏱️ **本节时段是按产物时间戳换算的本地时间**（该轮 UTC `2026092xT…Z` = 本地 +8）。
> 此前几节写过 `16:xx/17:xx/18:xx`，那是**没有换算**的笔误（第二十一轮审核 G 点名的那处同源）。

### 9/22 13:xx–14:xx —— ✅ **归因做实：共享外壳被多需求竞争挤掉**（非请求压缩）；隔离判分在跑

- [x] **段一 收尾**：`e1b-keep4` 最终 **1 passed / 31 failed（28.1 分钟）**；停掉占 3301 的 PID 21956。
- [x] **通过数没变，变的是哪一条**（记读数必须连这个一起记）：4 条需求那轮的 1 分是
  **`REQ-2.7.3: Default label`（311ms）** —— **一条不在子集里的判据**；2 条需求那轮是 `REQ-2.1`。
  → **通过数会掩盖"分数搬家"**。
- [x] **段二·动作 1（免费 diff，详见 `docs/12` §十五.六）**：`m2-keep2`（2 条需求）的 `HomePage.tsx`
  有种子数据 `Sprint goals`/`Groceries` **×2**；`e1b-keep4`（4 条）**×0**，且**整份产物里都没有**
  → **不是挪位置，是整块丢了**；行数几乎一样（187→191）→ 不是"外壳被改小"，是"换了内容"。
- [x] **闭环为什么没救回来**：4 条那轮**三轮 review 全都点名了种子数据**（日志里出现 6 次），
  但**修复没落地**。两轮闭环形状一样（3 review + 2 repair，**都只修 `HomePage.tsx`**），
  差别是**要修的东西变多了**：review 输出 token 336/273/138 → **631/1011/584**。
- 🔴 **结论（可复核）**：**扩子集 → 共享外壳切片 1,416→2,512 字符 → 同一个文件要同时满足 4 条需求
  → 生成时丢掉 REQ-2.1 的种子数据契约；闭环三次都发现，但单文件修复预算被更多违规分摊，没落地。**
  **请求压缩不背锅**（隔离判据见下）；**"外壳单文件 + 修复预算单文件"是真凶。**
  本轮**未做**架构级改动（分离生成留给方案会话，等这条结论被采纳）。
- ✅ **段二·动作 2（隔离变量）已出结论**：`e2-keep2`（**同一份代码 + 2 条需求**）的判分**第 2 条**
  就是 **`REQ-2.1: Note Listing` 通过（408ms）** → **请求压缩无辜**。
  → **归因关闭**：真凶是**子集扩到 4 条需求后共享外壳被竞争挤掉**（切片 1,416→2,512 字符；
  闭环三轮都发现却没修成）。**三方证据 + 路径见 `docs/12` §十五.六**（判据日志 / HomePage diff /
  `metrics.jsonl` 的 review-repair 形状 / 两次 gen.log 的分块计划）。
  ⚠️ `e2-keep2` 判分仍在跑（32 条约 30 分钟），最终计数待落袋——**但归因不依赖它**。
- ⏭ **未做**：R3b（闭环 ROI，9/25 前不能缺 —— 又被挤掉）、段四（全量 keep 结构读数）。
- 📌 **R1 必须用最新包**（含弹性压缩 / Express5 检查 / 就地降级）；读日志先问"生成跑完没 / 后端起来没"。
  quickstart 的读数不进本栏、不当判据（`PLAN:1230`）。

### 9/22 14:5x–15:0x —— 🎯 **R3b 已开跑（token 侧已定）**；e2-keep2 最终读数收回

- [x] **读数纪律落地**（提交 `ac4b1e5`）：`RunRecord` 新增 **`per_test_passed`**（"通过的是哪几条"），
  并把 `per_test_failed` 改成**同一来源**——列表格式日志里失败行带 `x` 前缀，
  而 `RE_FAILED` 要求行首就是 `[browser]` → **它一直是空的**（`tests_failed` 却有数）。
  实测：e2-keep2 日志 → 通过 `[REQ-2.1.spec.ts:7]`、失败 30 条。
- [x] **e2-keep2（隔离实验）最终读数**：**1 passed / 31 failed（30.5 分钟）**，
  通过的是 **`REQ-2.1: Note Listing`（408ms）** → **归因彻底关闭：请求压缩无辜**。
- [x] **R3b · token 侧（已定，脚本聚合）**：同配置（`REQ-2.1,2.2` + 官方档）
  **闭环开 29,213 token（11 次调用，20.6s）** vs **闭 14,316 token（6 次调用，10.1s）**
  → **闭环多花 14,897 = +104%（接近翻倍）**，与"闭环占单轮 token 一半"的旧口径一致。
- [x] **R3b · `r3b-on`（闭环开）判分跑完：1 / 32（30.7 分钟），通过的是 `REQ-2.1`（443ms）**。
- 🔴 **R3b 的答案（判分第 2 条给出）**：`r3b-off`（闭环**闭**）的 **`REQ-2.1` 失败（11.1s）**，
  而 `r3b-on`（闭环**开**）**通过（443ms）** → **闭环正是那 1 分的来源**。
  **ROI（本配置）= 多花 104% token（+14,897）换 +1 条通过**；
  `token/通过条数`：闭环开 **29,213/1** vs 闭环闭 **∞（0 通过）**。
  旧口径"闭环占单轮 token 一半"被量化成 **51%**（14,897/29,213）——**旧口径是对的**。
  ⚠️ 边界：配置写死（`REQ-2.1,2.2` + 官方档）；**换配置（大子集/比赛模型）这个 ROI 不必然成立**。
  `r3b-off` 的最终计数仍在跑（`runs/*-keep-score-r3b-off.*`）。
- ⏭ **未做**：段二（全量 keep 13 块：看共享外壳是否也被改坏）。
- 📌 **R1（平台）必须用最新包**；读日志先问"生成跑完没 / 后端起来没"。
  quickstart 的读数不进本栏、不当判据（`PLAN:1230`）。

### 9/22 15:3x–15:5x —— ✅ **按拍板做完 3a + 3b**；r3b-off 判分跑完（0/32）

- 🟡 **r3b-off 判分仍在跑**（20/32 时 **0 通过**，与其"闭环闭"的身份一致；预期最终 0/32）。
  它跑完才停后端、清在飞栏。证据：`runs/20260922T073128Z-keep-score-r3b-off.*`。
  **✅ 最终读数（后续会话收回）：`0/32`** —— `per_test_failed` 32 条且**含 `REQ-2.1.spec.ts:7`**
  → 预登记的分叉（"off 挂 → 闭环是那 1 分的来源"）**成立**，R3b 配对闭合。
- [x] **3a · `repair()` 按优先级排序再截断**（`PLAN.md:1251` 拍板项）：原先按**插入顺序**取前 4 个文件、
  且 L1 机械项排在前面 → **4 个额度先被 L1 占满**，自检的"对不上需求"类（**种子数据**）被截掉，
  而 L1 静态上看不出它。现在：**契约项（可访问名/种子字面量）> 自检 > L1**，
  且**契约项允许突破 `max_files`**。顺带把 `rounds=3 → 实际只有 2 次修复机会` 写进日志。
- [x] **3b · 种子数据字面量静态检查**（fail-soft）：**精度实测**后按子集限定 → 干净的判别器：
  `m2-keep2`（REQ-2.1 过）**缺 0 条**、`e1b-keep4` **缺 2 条**、**`r3b-off` 也缺 2 条** ——
  同一批产物里两次已知失败的零成本复现，**并独立确认 R3b 的结论**。
- [x] **守恒自检**（`extract_run.py`）：`passed+failed+skipped+flaky == total` 不等就报错 +
  "有汇总却一条逐条都抽不到"的告警——**"判据没报错 ≠ 判据跑过了"**（同类已第二次）。
- [x] **五份记录回抽复核**：我先前写的"通过的是哪几条"**全部成立**
  （六个 2 条需求轮 → `REQ-2.1`；`e1b-keep4` → `REQ-2.7.3`）。
- [x] **已重打包**（`output/bundle.zip`，含 3a/3b + Express5 + 弹性压缩 + 就地降级）→ **R1 可用**。
- ⏭ 段二（全量 keep 13 块）按拍板**降级为可选**，本轮未做。

### 9/22 16:xx —— ✅ **第二十一轮审核 10 条收口**（零 token，1 个 commit）+ 3b 升为判死落地

> 两件 🔴 都是"**判据本身不可信**"：记录缺字段、守恒自检是恒真式。处理方式统一为
> **每条都留一个能失败的东西**（变异测试 / 护栏 / 机器可复现的回归列）。

- [x] **清场**：`r3b-off` **0/32** 已落袋（`REQ-2.1` 在 32 条失败里）→ 22 份 record 全部 `git add`
  （两份此前未被跟踪）；停掉 3301 上 r3b-off 留下的后端；锁空、无 runner；**「在飞工作」栏已清**。
- [x] **🔴 A·5 份记录的 `per_test_passed` 写回文件**（此前只核过没回写）：新工具
  `eval/backfill_per_test.py`（用同一个解析器、只改三个键、幂等、带"有汇总却抽不到逐条就拒绝写"的护栏）。
  **顺带修了同型的第三次**：`RE_TEST_LINE` 原先要求 `[浏览器]` 标记，而 **quickstart(Lab04) 那 8 份日志没有**
  → 那些记录一条逐条都抽不到。修后 **15 份 judges 日志的逐条数 == 日志自报 `Running N tests`**。
- [x] **🔴 B·守恒自检不再恒真**：改用日志自报的 `Running (\d+) tests` 当对照量。
  **三格变异测试全对**：真实=静默；`31 failed→3 failed`=**报警**（旧版静默）；加合法 `3 skipped`=静默+口径提示（旧版误报）。
- [x] **🟡 C·`bench.sh` 的 `ambiguous` 分支文案**改成按 `[ -d "$LOCK_DIR" ]` 分两支的真话
  （锁为空 → 子命令**会真的执行**，所以别跑）；`recover` 未动。
- [x] **🟡 D·`score_app.sh` 现在在锁里跑**：新增 `bench.sh hold … -- <命令>`；`score_app.sh` 判分窗口
  由持锁父进程包住；`acquire_lock` 加"**调用方已持锁 → 继承**"分支。
  🔴 **继承判据不能用进程树**：WMI 父链在 Git Bash 三层嵌套下**认不出祖父**（实测子进程的 parent
  是个 WMI 里查不到的 PID）→ 改用**环境令牌**。**验证** = `eval/test_lock.sh`（新）**12 条判据全绿**，
  含"有活持有者时被拒""继承不拒也不删祖宗的锁""失败退出也释放"。附带：判分轮现在自动认领在飞栏。
- [x] **🟡 H·`regress_l1.sh` 加 `seed_literals` 列**（子集从需求文本现算）→ 3b **机器可复现**：
  `m2-keep2 0 / e2-keep2 0 / e1b-keep4 2 / r3b-off 2`（另 `r3b-on 0`）。
- [x] **🟢 I·种子抽取去掉"搜索关键词"与 <3 字符**：`"st"` 消失（六个 app 里只有 keep 24→23）。
- [x] **🟡 F/G/J**：`runs/README.md` 补 6 个 arm（含 token、通过的是哪条、**已知瑕疵三条**）；
  `STATUS.md` 时间口径订正（`最后更新` / 在飞栏 / 三个段头）；`docs/12` 的 `18 → 20 条` 并把 `24/49` 统一到去重口径。
- [x] **顺手**：`main.py` 开头**回显 knobs**（子集/闭环开关/轮次/只设计/预算档位，档位**从代码读**）。
- [x] **已重打包**（`main.py` 变了）→ `output/bundle.zip` 合规校验全绿，**R1 用这个**。

> ⚠️ **未做（且不该在本轮做）**：审核 §四.E（放行标记退化成常规路径）属 `AGENTS.md`，**等方案会话拍板**。
> 本轮对它的遵守是**把整批卫生收成 1 个 commit**，而不是 10 条小改落 10 个 commit。

### 9/22 17:0x–17:5x —— 🎯 **预注册复跑：`REQ-2.1` 回来了**（**2/32**；命中预注册第 ① 格）

> 预注册（跑之前落盘）：`%TEMP%/r4-keep4/PREREG.md`。基线 = `e1b-keep4`（1/32，通过的是**子集外**的 `REQ-2.7.3`）。
> **唯一变量 = 3a + 3b 生效**。完整记录：`docs/12` §十九、`runs/README.md` 的复跑小节。

- [x] **生成**（闭环 ON，4 条子集）：**37,290 token**（基线 37,476 → **3b 判死没抬高成本**）。
  **机制链完整**：第 1 轮 **`seed_literals=2` → `L1 passed=False`**（**3b 真在硬判**，
  旧版这里是 soft、`passed` 不会动）→ 修复 → 第 2、3 轮 **0 条**；
  **自检侧独立报出同一条**（`priority 0`）→ 两个来源互证，不是孤证。
- [x] **判分**（官方档、静置、29.0 分钟）：**2 / 32**，通过 = **`REQ-2.1`（812ms）** + `REQ-2.7.3`（736ms）；
  种子字面量在产物里（第三只眼 `grep` 复核：`frontend/src/pages/HomePage.tsx`）。
- [x] **判读（按预注册，不许事后改）**：命中 **第 ① 格**——`REQ-2.1` 通过 **且** 种子补上 ⇒ **3a+3b 生效**。
  ⚠️ **结论不建在"1 → 2"上**（差 1 条 < 可辨差异下限 2）：它靠 ① **成员翻面**（`REQ-2.1` 由挂转过，
  且过在 812ms 的"通过形态"，不是"慢一点"）② **机制链**（3b 硬判 → 修 → 复验 0 条 → 产物里真有）。
- [x] **两处瑕疵都写进记录**：`load_snapshot` 记成"正在跑"（**工具 bug**：快照在拿锁**之后**采，
  本轮新加的锁引入的；**已修**——改成外层拿锁前用 `bench.sh verdict` 的机器可读形式）；
  `git_dirty=true` 但 `git diff b3c497e --stat -- pipeline/` **为空** → 产物对应的代码干净。
- [x] **中断轮（第一次判分，17/32）**：**MSYS fork 失败**（`dofork: child -1 … 0xC000026B`,
  errno 11）——与 `STATUS` 记的形态 ④（长 `sleep` 轮询收掉同进程树）**同源**，触发窗口正是我那次被取消的长轮询。
  处置：两份日志改名 `.INVALID-interrupted.*`（**不计入任何通过数**）→ `bench.sh recover` 预检 +
  `--force` 恢复 → 换**后台任务**方式重判（这才是长判分轮该用的启动方式）。
  ⚠️ 这条链（`status` 不自动清 + `recover` 要求人工确认）**第一次在真故障上用上，表现正确**。
- [x] **顺带新增**：`bench.sh verdict`（**机器可读现场**：`status=free runners=0 port=free listeners=0`）——
  收紧前它是 `status` 的人类文案，按关键词抓会先命中「=== 锁 === 空闲」，按 emoji 锚定在脚本里
  （locale 不同）匹配不到；`docs/04` §一 要求的"静置筛"从此有可机器校验的字段（审核 §四.F 的同源建议）。




### 9/22 18:0x–19:0x —— 🎯 **n=2：`REQ-2.1` 在判分噪声下复现（两轮都过）**；记录自证收口

- **对象**：同一份产物 `%TEMP%/r4-keep4/app`（**不重新生成、零 token**），判分 **28.9 分钟**。
- **预注册（跑之前落盘 `%TEMP%/r4-keep4/PREREG-n2.md`）两格**：
  ① 两轮都过 → `REQ-2.1` 的"通过形态"在判分噪声下复现（**这是我们要的**）；
  ② 任一轮挂 → 这一分不稳，按 `docs/04` 的翻面口径处理，**不许**说"结论稳了"。
- **读数**：**2 / 32**，通过集合与上一轮**逐条相同** ——
  **`REQ-2.1`（726ms）** + `REQ-2.7.3`（448ms）（上一轮 812ms / 736ms）→ **命中第 ① 格**。
- ⚠️ **口径（别读宽）**：n=2 只覆盖**判分噪声**（与 `r3a-1/r3a-2` 同类），**不覆盖生成方差**——
  要覆盖生成方差必须重跑生成（有 token）。**且毫秒带有漂移**：9/21 那批通过形态在 **339–478ms**，
  本轮两轮是 **726–812ms**（仍属"sub-second 通过"，与失败侧 11.1s 超时不同族，但**别再说"落在同一带内"**）。
- **记录**：`runs/20260922T104719Z-keep-score-r4-keep4-n2.json`（`load_snapshot=status=free runners=0
  port=free listeners=0`，**拿锁前采**；`snapshot_note` 自带说明）。
- **第二十四轮审核的四件**：
  - 🔴 **A（修完）**：新指纹的 `tokens_total` 原来是**闭环之前**的数（15,833 = 全程 42%）→
    改成两个数分开命名（`tokens_generation_only` / `tokens_total`，闭环后 `tokens_total`=37,290，闭环占 **57.5%**）。
  - 🟡 **B（修完）**：`runs/README` 里"28 份只有 1 条"用的是**严格法**（只认 `status=free` 字面量），
    与它自己教的**字段法**不是同一口径 → 现在把**两个口径的判据与计数都写明**（严格 2 / 字段 14），
    并写明 `code_clean` **旧记录多数没有这个字段**（未记 ≠ 不干净）。
  - 🟡 **C（做完）**：`STATUS.md` 1041 行 → **463 行**；逐轮叙述搬进本文件（本文件 623 行），
    STATUS 只留索引（理由见本文件头部）。
  - 🟡 **D（已满足）**：STATUS 在工作区里是干净的——审核看到的 18:47 mtime 是 `set_inflight`
    读-改-写留下的（内容与 HEAD 相同），不是未提交改动。

### 9/22 20:0x– —— 🎯 **阶梯 ②③ 两个零 token 工具落地**；bookstack 开跑

- **② `eval/subset_closure.py`**（三步闭合）：`keep {REQ-2.1,REQ-2.2}` 上复现出**已知的那个漏法**——
  唯一 🔴 未覆盖是 `'Others'`（组标题，需求文本没点名）；
  `bookstack {REQ-1.1,REQ-2.1}` 上抓到 🔴 **`'BookStack'`**（spec 要它显示，而**本子集文本没写**，
  全量文本里有 —— 正是 `keep` 的 `Toggle sidebar` 那一类）。
- **③ `eval/predict_judge.py`**（预测 + 覆盖率 + `--truth` 混淆矩阵）：**先当信息不当闸门**
  （§7 ③ 的"预测失败 = 0 才送判分"比闸门"非零"更严，复核前不用它挡判分）。
  标定三份结果已知的产物（keep 子集）：**TP=4 FP=0 FN=0 TN=2**（精度/召回 100%，N 小）。
- **口径演进（都留了金丝雀）**：解析器一开始只认字面量定位器 → bookstack 覆盖率 **5%**；
  补上 `{ name }` / `{ name: expr }` 变量族与**调用实参**（名字在调用点）后 → **100%**；
  keep 93% → 100%（补 `getByTitle`）。**金丝雀仍会失败**（`getByRole(roleVar, …)` → 未识别）。
  另加**动词分类**：`expect*/visible*` 的实参 = 必须显示；`fill*/create*/click*` 的实参 = 测试自己输入
  （混在一起会让硬清单灌水——实测：`createNote('Weekend plan', …)`）。
- **换器件的接口**：`PIPELINE_EXTRA_HARD_NAMES`（环境变量 → brief 的"必须逐字兑现"块 + L1 契约）。
  **刻意不写进仓库**：仓库里出现题目特定字符串会撞"不得预埋答案"（红线 9）。
- **bookstack 阶梯①开跑**（子集 `{REQ-1.1, REQ-2.1}` = helper 最少的候选里覆盖两个入口面的那两个）：
  生成 **18,004 token / 9 次调用 / 14.3s**，`final_l1` 通过；**第 1 轮 L1 就抓到 `'BookStack'` 缺失**
  （`accessible_names: 1`）→ 修复 → 第 2 轮 0 条 —— **补充硬名的链路当场兑现**。
  指纹：`git_dirty=False` / `pipeline_dirty=False`（**出生处**采的，所以记录不必事后解释）。
  判分 34 分钟，**后台任务**启动（不用长 sleep 轮询），跑前快照 `status=free`。

**bookstack 判分结果（34.7 分钟）**：**2 / 34 = 5.9% —— 非零**，命中预注册第 ① 格。
通过的是子集里的两条：`REQ-1.1: Open Homepage`（480ms）、`REQ-2.1: Enter Login Page`（468ms）。
⚠️ 2/34 只说明"这个 app 有分"——其余 32 条属**子集外**（多为页面/流程不存在）；闸门只问非不非零。

- **③ 的标定（同产物全量 34 spec）**：**TP=30 FP=0 FN=2 TN=2**（精度 100% / 召回 94%）。
  两条 FN = `REQ-5.3.2` / `REQ-5.5.1`，失败是**结构性**的（书/章节页面与流程不存在），
  而 ③ 只查"名字在不在源码里" → **已知盲区：抓得住"名字对不上"，抓不住"流程没实现"**。
  → 别把"预测通过"读成"会过"；**也别拿它当闸门**（§7 ③ 的阈值比闸门更严，复核前不用）。
- **这一轮是"工具当场兑现"的一轮**：② 抓出 `'BookStack'` → 注入 → 生成**第 1 轮 L1** 就报缺
  → 修复 → 第 2 轮 0 条。**零 token 发现 + 零 token 验证修好了。**
- **第一份带出生处指纹的记录**：`stamped_at_product=true` / `git_dirty=false` / `pipeline_dirty=false`。

### 9/22 21:0x–22:2x —— ✅ **红线 9 清零（+ 常驻闸门）**；**stackoverflow 非零（1/66）**

- **红线 9**：提交包里**不许有题目特定字符串**。审核数出 19 处（4 个文件，只算五个非 keep 的 app 名）；
  我的口径更宽（含"keep 作为 app 名"，排除英文词/依赖名）→ **30 处 / 7 个文件**，现已 **0**。
  - 最要紧的是 `APP_TOKEN_BUDGET`（**功能代码**：按 app 名查两档预算）→ 删表，改成
    **按 brief 体量推导** `clamp(560_000 + 8×brief字符, 300_000, 1_200_000)`；
    系数是解出来的（两档都不得低于旧值），**验收：六个 app 全部 ≥ 旧表**（719k/742k/975k/938k/1.2M/1.17M）。
  - 其余 29 处（注释/文档串）→ **事实留下、样本名移出**：带样本名的原表搬进 `docs/06` **§十一**。
  - **新增常驻闸门** `eval/check_bundle_strings.py`（接进 `eval/package.sh`）：五个 app 名零容忍；
    `keep` 只禁**题目引用形态**（英文词与 `agentkeepalive` 放行）。**变异测试**：塞一行 → rc=1 ✅。
- **三处小修**：`STATUS` 的 67→**66**；`predict_judge` 的警告分清 **spec N / 产物 N**；
  `schema.py` 的 `reason` 文案分出"文件不存在"与"文件里没有建表语句"。
- **② 在 stackoverflow 上暴露两个解析缺口**（覆盖率第二次当场兑现）：
  ① **非导出函数没收** —— 它把定位器集中在一个内部函数 `resolveNamed` 里 → 那些 spec 的闭包覆盖率
  显示 `0/0`（看着像"干净"，其实是解析器没看见）→ 改成导出+非导出都收，覆盖率 `0/0 → 1044/1044`；
  ② 认证类 helper 的 fixture 落进"无法分类"→ 把 `login/signup/register` 加进动词表。
  **金丝雀仍会失败**，bookstack 的标定**不变**（TP=30 FP=0 FN=2 TN=2）。
- **stackoverflow 阶梯①**：子集 `{REQ-1.1}`（helper 最少 + 闭包最小 + 未覆盖只含一个**名字** `questions`）；
  生成 **13,830 token**（预算 564,152 由新公式推得）；**第 1 轮 L1 抓到 `'questions'`** → 修复 → 第 2 轮 0 条。
  判分 **1/66**（1.0 小时）→ **非零** ✅，但过的是**子集外**的 `REQ-1.3`，
  **子集那条自己挂了**（③ 的第一次 FN，原因 = "字符串在源码里 ≠ 文本/可访问名可达"）。
- **口径**：1/66 只说明"这个 app 有分"，不是能力证据；**③ 仍只当信息**（两条盲区已记进记录）。

### 9/23 09:0x– —— ✅ **③ 收紧：loose 靶子必须落在 locator 可命中的位置**（审核 28 §三 A）

- **问题**（审核回产物核出来的，并更正了我上轮的措辞）：对 loose 靶子（`/header/i` 这类），
  ③ 走的是**裸的源码子串**检查，而判据要的是「能被某个 locator 命中」。
  我上轮说 `sidebar`「只出现在 aria-label」是**不完整**的 —— 产物里还有 `<h2>Sidebar</h2>` **文本节点**；
  真正的缺口是**检查口径**，不是"某个名字恰好在标签里"。
- **改法**：`arg(re)` 类靶子只在**可命中的位置**按正则匹配 ——
  **JSX 文本节点（`>X<`）+ `aria-label` / `title` / `placeholder` / `name` / `alt`**；
  另加一条**不可判**提示（出现 `aria-label={表达式}` 这类动态值时显式打印，
  不再让"看着干净其实是没看见"的形态静默通过）。
- **验收（审核要求三件一起看，缺一不算）全过**：
  ① bookstack 34 spec：**TP=30 / FP=0 不变**、覆盖率 **451/451 不变**；
  ② stackoverflow `REQ-1.1`：**「预测通过」→「预测失败」**，点名的正是 `'header'`
     （**没有**误报 `sidebar`，与审核的更正一致）；顺带全量 66 spec 标定
     **召回 0%（FN=1）→ 精度 100% / 召回 100%（TP=65 FP=0 FN=0 TN=1）**；
  ③ 金丝雀仍会失败（`getByRole(roleVar, {name})` → 0/1）→ 判据没被修松。
- **已知边界**（写进代码注释）：静态检查做不到"真的跑一遍 locator 解析"，
  所以这条只把**最危险的方向（漏报）**收窄；`aria-label={dynamic}` 这类仍是盲区。

### 9/23 09:1x–12:xx —— 🎯 **stackoverflow n=2（非零复现）**；prestashop 开跑（④ 的第一个"无注入" app）

- **stackoverflow n=2**（同一份产物、零 token）：**1 / 66**，通过的还是 **`REQ-1.3`（768ms）**，
  **失败集合也逐条相同**（对称差 0）→ 命中预注册第 ① 格：这一分在**判分噪声**下站得住。
  ⚠️ 限定词：只覆盖判分噪声（不含生成方差）；它是**子集外**偶发通过，**不是能力证据**。
- **prestashop（阶梯 ④ 第一个）**：子集 **`{REQ-2.1}`** —— 与 bookstack/stackoverflow 不同，
  这一轮**没有注入任何硬名**：② 的「未覆盖」栏为**空**（4 个靶子 `carousel` / `popular products` /
  `newsletter` / `footer` 全部被**它自己的需求文本**点名）。
  生成 **22,356 token**（7 块；预算 562,704 由新公式推得），**第 1 轮 L1 就全绿**（首次无需修复）。
  `git_dirty=true` 但 **`pipeline_dirty=false`**（PLAN.md 在方案会话手里）——正是加这个字段的用途。
- **③ 在判分前预测「失败」**（`%TEMP%/ps1/predict.txt`）：4 个靶子里 3 个（`popular products` /
  `newsletter` / `footer`）在产物里**不可达**，且提示有 `aria-label={模板串}` 这类动态写法。
  回产物核过原因：**需求文本是英文术语、模型把它们渲染成了中文**
  （产物里是 热门商品 / 促销活动 / 订阅我们的资讯），所以判据的 `/popular products/i` 找不到。
  → **新的失败形态**：不是"没做"，是"**译了**"。而 `PIPELINE_EXTRA_HARD_NAMES` 那套机制**正好能治它**
  （注入后进 brief 的"必须逐字兑现"块 + L1 契约 → L1 会在闭环里报出来）——
  但本轮按预注册**照原样判分**（③ 只当信息，不用它挡判分）。

**prestashop 判分结果（1.5 小时）**：**1 / 86 —— 非零**（第 4 个 app），通过的是**子集外**的
`REQ-2.2.1: Carousel Auto Switch`（5.1s）；**子集那条 `REQ-2.1` 挂了**，而 **③ 判分前就预测它挂**
（3/4 靶子不可达）→ 收紧后的**第一次正确预测失败**。
根因是**新失败形态**：需求文本用英文术语（`Popular Products` / `Newsletter` / `footer`），
模型**译成了中文**（热门商品 / 促销活动 / 订阅我们的资讯）→ 判据找不到英文原词。
**不是"没做"，是"译了"** → 现有 `PIPELINE_EXTRA_HARD_NAMES` 能治（注入 → brief 硬名块 + L1 契约），
但本轮按预注册照原样判分；「把 ③ 的 unreachable 回灌硬清单」列为**待拍板**的后续。

### 9/23 14:2x– —— 🎯 **判分队列（prestashop n=2 → ctrip）**；②③ 口径三轮定稿；契约验收 3/4 → 1/4

- **判分队列（串行）**：① prestashop n=2（零 token，预注册 `%TEMP%/ps1/PREREG-n2.md`）→ ② ctrip 第一轮。
  判分**串行**（`score_app.sh` 持锁），其间并行做零 token 的活。
- **②③ 的要求单元口径定稿**（ctrip / bookstack 的真实形态逼出来的，**三版**）：
  ① 扁平化 → 把 `expectAnyVisible(page, [[a,b],[c]])` 的候选当成各自必需（**太紧**）；
  ② 按调用点整组 → 又把 3 个单元并成一组（**太松**）；
  ③ **终版**：按**层级**解析 —— 跳过 scope 后的实参里，数组的**每个元素 = 一个要求单元**，
  单元内部若还是数组 → 那些候选「任一即可」；且单元语义**按被调 helper 的行为分**
  （`expect*` → 单元之间都要；`click/fill*` → 整串候选合成一个单元、任一）。
  **回归三条一起看**：金丝雀 0/1 ✅ / bookstack TP=30 FP=0 + 覆盖率 451/451 ✅ /
  stackoverflow 精度 100% 召回 100% ✅（都复跑过）。**已知边界**写在代码里：
  `clickIfVisible([a,b])` 这类"挨个试"的候选目前每个都算「需要显示」（偏紧），只影响硬清单大小。
- **指纹加 `git_subject`**（第三十轮审核 §三 A）：hash 会被历史重排/阶段合并换掉（21 份记录的 hash
  全部不可达），**subject 会被阶段合并抄进正文而存活** → 现在指纹与记录都带它；
  老产物按 commit 反查（**可达才查得到，查不到写 None，不编**）。
- **契约验收（「逐字，不许翻译」，PLAN §7 已拍）**：prestashop 子集**重生成一次**（20,101 token）→
  ③ 的 unreachable **3/4 → 1/4** ✅（`Carousel` / `Popular Products` / `Newsletter subscription`
  现在**逐字是英文**了）。⚠️ **没到 0/4**：剩下 `footer` —— 它**不是翻译问题**（产物里有
  `<footer>© 2024 Shop. All rights reserved.</footer>`，区域在、但没有「footer」这个**文字**），
  属"结构词没有被写成可见文本"。补法现成（`PIPELINE_EXTRA_HARD_NAMES=footer`，与 ctrip 的
  `账号登录` 同一条通道），但**按 PLAN 的"只做这一个验证样本"没有再跑**。
- **ctrip（第五个 app）**：子集 **`{REQ-2.2}`**（helper 最少的候选里，闭包最小、未覆盖栏最小）；
  未覆盖 = `clickIfVisible` 的候选对（`账号登录` / `password login`）→ **注入一个**（`账号登录`，
  与它的中文需求一致）。生成 **26,302 token**，**第 1 轮 L1 就抓到注入的 `账号登录`** → 修复 → 第 2 轮 0 条。
  ③ 已预测（**判分前落盘** `%TEMP%/ctrip1/predict.txt`）：2 个单元缺 → 预测**失败**。
- **12306 的 ② 提前做完**（最后一个 app）：子集 **`{REQ-1.2}`**（未覆盖栏**为空**、闭包只有 1 个靶子），
  **不需要注入**；全量 brief 116,427 字符 → 预算 1,200,000（上限钳位，按设计如此）。

**prestashop n=2 结果（1.4 小时，零 token）**：**1 / 86**，通过的还是 **`REQ-2.2.1`（5.0s，第一轮 5.1s）**，
**失败集合也逐条相同**（对称差 0）→ 命中预注册第 ① 格。
→ 至此**两个 app 的「非零」在判分噪声下重复过**（`stackoverflow` / `prestashop`），
口径都是「成员相同 + 失败集合逐条相同」。

**ctrip 第一轮判分被中断（第三次 MSYS fork 死法）**：跑到 **71/125** 时日志停止增长
（`dofork: child -1 … 0xC000026B, errno 11`），锁的持有者死、心跳停、无 runner →
`status` 给「ambiguous（需人工确认）」→ 按证据确认后 `recover --force` 恢复。
两份日志当场改名 `…ctrip-score-ctrip1.INVALID-interrupted.*`（**不计入任何通过数**）。
**中断产物里唯一可用的读数**（机制级旁证，不进结论）：**子集那条 `REQ-2.2` 在 11.9s 挂**
—— 与 ③ 判分前的预测（预测失败）**同向**。
⚠️ 这已经是**第三次**同一形态（`r4-keep4` 17/32、`ctrip1` 71/125，加上最早那次生成轮），
且本次**没有长 `sleep` 轮询**（我在用后台任务 + 阻塞等待，等待被 harness 取消）→
说明触发条件不是"我们怎么等"，而是**长判分轮本身的进程churn**。
→ 已按纪律**恢复并重跑**；这条值得升级进 `AGENTS.md`（**属方案侧**，我不动那个文件）。

**ctrip 第一轮结果（重跑 1.4 小时）**：**0 / 125 —— 第一个没拿到非零的 app**（全部 timeout），
⇒ 阶梯「6 个都非零」停在 **4/6**（keep / bookstack / stackoverflow / prestashop）。
**预注册第 ② 格命中**：③ 判分前就预测子集那条会挂，实际确实挂 →
这一轮不是「跑了但没分」，是**子集装置没兑现**。
机制：产物渲染了 登录 / 账号登录 / 用户名 / 密码，但判据要的是
**能被 locator 命中的 pattern**（`getByRole('button', {name:/邮箱.*用户名.*手机号/})`），
而单元的候选是**复合串**、产物只有「用户名」这种片段 → **不是没做，是没按判据要求的串写**。
→ 下一轮靶子已明确：把 ③ 的**单元**回灌 `PIPELINE_EXTRA_HARD_NAMES`（属方案会话在拍的那条）。

---

## 2026-09-23 晚 · 第六个 app（`12306`）生成 + 判分 ‖ ctrip 第二轮生成（判分排队）

**本轮结构**：判分是串行的，而**生成不占锁** → 12306 判分在跑的同时把 ctrip 第二轮生成做掉（阶梯 ④）。

### 一、`12306`（最后一个 app）

- **② 提前做完**（零 token）：子集 **`{REQ-1.2}`**（`REQ-1.2.spec.ts` = `openHome` + 只断言 `'Quick Guide'`），
  覆盖率 **11/11**、闭包 12 个靶子（具体 1 个）、**未覆盖栏为空 → 无需注入**。
- **生成**：`PIPELINE_GENERATE=1 PIPELINE_REQ_IDS=REQ-1.2`，官方档 `deepseek-chat`。
  生成段 **4 次调用 / 7,675 token**、闭环 **1 轮 L1 首轮全绿 + 自检 0 条** → 全程 **9,663 token**。
  预算 **562,832**（= 560,000 + 8×354；**子集 brief 354 字符**）。
  ⚠️ `STATUS` 之前写的 1,200,000 是**全量 brief**（平台形态）的值 —— 子集模式本来就给更小的预算（设计如此）。
  产出 3 个文件、`degraded=false`。⚠️ 两处形状值得记：`App.tsx` **内联了一份 HomePage**（`pages/HomePage.tsx`
  从未被 import，渲染的是内联那份），文案是通用填充（"Welcome to our store…"）。
- **③ 判分前预测**（落盘 `%TEMP%/12306-1/predict.txt`）：**`REQ-1.2` ✅ 通过**（覆盖率 11/11）。
- **判分**：后台任务启动，拿锁前快照 `status=free runners=0 port=free listeners=0`；冒烟 HTTP 200。
  **早测读数 = `ok 2 … REQ-1.2 … (522ms)`** → **预注册第 ① 格命中**（子集装置兑现，第六个 app 非零）；
  终值与完整记录待汇总行（本段随后补）。

### 二、`ctrip` 第二轮（`ctrip2`）：PLAN 裁决一/二/三 的第一次检验

- **生成之前先提交**了 `a96163d`：`implement.py:255` 补上落点约束
  「**整串必须落在同一个元素的可访问名 / 可见文本里（按序）**」——
  这样产物出生处指纹才是 `pipeline_dirty=False`（`git_commit=a96163d`，实测 ✔）。
  按 PLAN 裁决三：**没有**把 `extra_hard` 接进 `generate_app`。
- **注入 = `[4]` 的全部 7 行**（PLAN:1268「不挑不拣」）：
  `账号登录` / `邮箱.*用户名.*手机号` / `email.*username.*mobile` / `account` / `密码` / `password` / `password login`
  —— 验收项 1（零 token，`grep` 可验）逐条 ✅。产物目录换成**空目录** `%TEMP%/ctrip2/app`（避开 `design.json` 复用）。
- **生成 33,069 token**（生成段 15,788 + 闭环 17,281）。闭环 **3 轮 L1 全 ❌**（`accessible_names` **5 → 2 → 2**）。
- **③ 判分前预测：`REQ-2.2` ✅ 通过**（第一轮是「预测失败 1」）→ **PLAN 的验收条件 1 已满足**（`predict.txt` 已落盘）。

#### 🔴 本轮最重要的机制发现：**L1 的 ❌ 与判据的目标不是同一件事**

L1 最后两条 finding 说 `邮箱.*用户名.*手机号` / `email.*username.*mobile`「在前端源码里一次都没出现」，
而产物里明明有（各自在**一个**元素里、按序）：

```
LoginPage.tsx:11  <p>邮箱/用户名/手机号</p>
LoginPage.tsx:12  <p>email/username/mobile</p>
```

**表示形态错配**：`[4]` 的行是**正则链的源码**，把它当「逐字硬名」注入后 ——
`check_accessible_names`（`l1.py:182-188`）做的是**源码子串**检查，于是 L1 去追一个
**自然产物永远不会有的字面串**；而判据 `getByText(/邮箱.*用户名.*手机号/)` 要的正是
**一个元素里按序出现**这三段 → **会命中**。

→ 🔴 **不能把"L1 仍 ❌"读成"PLAN 的触发条件到了（该动 plumbing）"**：触发条件写的是
「修复轮两轮都没把整串落进**同一个元素**」，而实测修复轮**把它落进了同一个元素**（自检那轮还专门
把两个串收进各自的 `<p>`），只是用的是自然写法（`/` 连接）而不是正则源码。
→ 建议（属方案侧）：这类链注入**首环的字面形态**（`邮箱/用户名/手机号`），
或让 `check_accessible_names` 对含正则元字符的条目走**正则匹配**。

**代价（可复算）**：这一轮的**闭环 = 17,281 token（全程 33,069 的 52%）**，拆开是
**自检 3 次 12,326** + **定向修复 4 次调用 4,955**；
而三轮 L1 的 `accessible_names` findings 是 **5 → 2 → 2** ——
即**闭环的每一轮都被这条"不是判据要求的靶子"驱动**：`[4]` 的 7 行里缺的 5 条第一轮就补进产物了，
剩下那 2 条 L1 **永远追不到**（它找的是正则源码那一串，产物里是 `/` 连接的自然写法），
**闭环却为它们又跑了两轮自检 + 两次修复**。
→ 这就是"**正则体靶子被当逐字字面量注入**"的账：**约 1.7 万 token（全程一半）**，
换来的是"把串收进各自单个元素"这个**判据确实需要的**形状 —— **不是白花，但也没被 L1 认可**。

**判据侧逐条落点（零 token 核过）**：`<p>邮箱/用户名/手机号</p>` → `getByText` 命中单元 ①；
`<label>密码</label>` → 单元 ②；`<button>登录</button>` / `<h1>账号登录</h1>` → 单元 ③；
首页 `<Link to="/login">登录</Link>` → `openLoginPage` 的 `clickFirstAvailable` 命中。
`App.tsx` 这次**正常 import 了两个页面**（无内联重复）。

**`12306` 判分结果（2.4 小时）：1 / 135** —— 通过的是**子集那条** `REQ-1.2.spec.ts:7`（**522ms**），
134 条全部 timeout（`failure_class_counts={'timeout': 134}`、`ERR_CONNECTION_REFUSED=0` → **没有**基础设施失败）。
→ **预注册第 ① 格命中**（子集装置兑现），**阶梯到 5/6，只差 `ctrip`**；
也是 **③ 第一次「预测通过」被证实**（覆盖率 11/11，判分前落盘 `%TEMP%/12306-1/predict.txt`）。
⚠️ 与 stackoverflow（过的是子集外 `REQ-1.3`）/ prestashop（过的是子集外 `REQ-2.2.1`）**不同**：
**这一轮的非零是子集装置自己换来的**。
**口径**：零人工 / 静置（拿锁前快照 `status=free runners=0 port=free listeners=0`）/
出生处指纹 `stamped_at_product=true`、`pipeline_dirty=false` / **无 n=2**（判分噪声下的稳定性**未测**）。
🎯 **5/6 → 队列里只剩 `ctrip2` 一格；`REQ-2.2` 一过就是 6/6。**

---

## 2026-09-23 深夜 · 判分窗口里的代码活：裁决四/五 落地 + bookstack 深度第一刀生成

### 一、裁决五的**必修漏报**（`classify_helper`）：分类跟着**形参语义**走

**病灶**（PLAN 实测）：`classify_helper('clickNamed')='input'`、`('openBooks')='input'`、`('fillField')='input'`
→ bookstack **32/34 条失败的首阻**（`Books` 15 / `Shelves` 11 / `Email address` 6）**一条都没进硬清单**，
全躺在"🟡 产物不必预先包含"那一栏 —— `[4]` 对 `REQ-5.1` 的"必须显示"栏当时是**空的**。

**做法**：`param_roles()` —— 形参 → **污点传播**（含 `for (const x of xs)` 循环变量与 `const` 派生名）
→ 出现在 `getBy*/.locator(` 里 = `need`；出现在 `.fill(/.type(/.selectOption(` 里 = `input`；
再走**调用图传播**（形参传给另一个 helper 的第 k 个形参 → 继承它的角色，迭代到不动点）。
`_arg_targets` 改成**按第几个实参**判角色；`unknown` 退回名字判（**不再默认"输入"**）。
加 `lru_cache`：stackoverflow 66 spec 从数分钟 → **14 秒**。

**语料验收**（纪律 16）：`bookstack` **TP 30 → 32（= 全部 32 条真失败）、FP 仍 0、覆盖率仍 451/451、召回 94% → 100%**；
`stackoverflow` **TP=65 / FP=0 / 1044/1044 不变**；金丝雀（变量 role + 变量 name）仍 **0/1**。
⚠️ **PLAN 写的"TP ≥33"在这份真值下不可达**：34 条里真通过 2 条 → **TP 上限就是 32**。
→ 单独复核 `REQ-5.1`：修前"需要显示 0 / 测试自己输入 2"（`Books` 在输入栏），**修后 2 / 0** —— 症状消失。

### 二、裁决四：L1 对"正则体"改用**可命中性**判据（与 ③ 共用一处实现）

新增 `pipeline/verify/hittable.py`（一处实现、两处消费：L1 与 ③）；L1 的 `check_accessible_names`
对**正则体**走可命中性、**纯字面名行为与今天完全一致**；brief 对正则体加呈现约束
（不照抄正则源码、渲染成能被它匹配的串、整串落在同一个元素）。
⚠️ **不能两头都改的理由**写进了代码注释：做 ① 会让 ② 的输入消失（注入里不再有正则体 → L1 的正则分支永不触发）。

**位置集合的成员资格：实测钉死**（`eval/locator_probe.js`，最小页面 + 每族 locator 各打一发）：

| 位置 | 命名族（8 role 名 ∪ `getByText`） | 字段族（`getByLabel` ∪ `getByPlaceholder` ∪ 4 个输入 role） |
|---|---|---|
| 文本节点 | ✅ | ❌（`getByLabel` 不认 `<p>`/`<label>` 的文本） |
| `aria-label` | ✅ | ✅ |
| `title` | ✅ | ✅ |
| `placeholder` | ❌ | ✅ |
| **`name` 属性** | ❌ | ❌（**实测两族都不命中 ⇒ 不再是位置**；旧实现把它当位置 = 修松，本轮去掉） |

**验收三条**：① ctrip2 产物 + 那 7 条名字 → L1 findings **0**（`final_l1` 不再因它们变 False）；
② 语料 `bookstack 451/451 + TP=32/FP=0`、`stackoverflow 1044/1044 + TP=65/FP=0` **都不变**；
③ 纯字面金丝雀行为不变（不在→报、在→不报；不存在的正则体仍报）。另：`regress_l1.sh` 全绿。

### 三、裁决五的**族感知**（②③ 每条打「消费方 helper + 族」）

`helper_families()`：族由**真正消费它的那条链**决定（helpers.ts 内走调用图，**不硬编码 helper 名表**）。
实测 ctrip：`expectPasswordLoginForm → named`、`fillPasswordLogin → field`（**正是 PLAN 举的那对
"两族消费方不同、不能混读"**）；bookstack：`clickNamed → named`、`fillField → field`。
③ 的"可命中性"按族取位置集合；② 的 `[4]` 与 ③ 的输出都印族；brief 的落点约束两族的话都给
（注进来的名字不带族信息 —— 族在 `eval/` 侧算、不进提交包）。

### 四、bookstack 深度第一刀（**生成已做，判分排在 ctrip 之后**）

- 子集 **`{REQ-1.2, REQ-2.2, REQ-4.1, REQ-5.1}`**、**零注入**（② 实测：覆盖率 56/56、
  12 个具体靶子全是"需要显示"、「未覆盖」栏为空）→ 与方案点名的 4 条**一致**。
- 生成 **89,568 token**（生成段 45,321 + 闭环 44,247 = 49%）、**11 个文件 / 11 块**、`degraded=False`。
- L1 结尾 **❌ 5 条**（`seed_literals`：`BookStack User` / `bookstack_user@example.com` / `Password123!` /
  `Shelf 4.1` / `Book 5.1`）。**回产物核过：这 5 条在 frontend 与 backend 里都不存在** ——
  **不是假阳性**，是**真的没做**（修复轮改的是页面结构，没加种子）。
  → 对应预注册判据第 4 条（**首阻清了而成员没中 → 下一步补种子数据**），不是机制问题。
- ⚠️ **`App.tsx` 内联 HomePage 的老形态又出现了一次**（自检第 1 轮就抓到、priority 1，修复轮动过它）。
- 预注册：`%TEMP%/bookstack-deep1/PREREG.md`（三层照 PLAN + 一轮判据四条）。

**`ctrip` 第二轮（`ctrip2`）判分结果（1.5 小时）：2 / 125** —— 通过的是
**`REQ-2.2.spec.ts:7`（752ms，子集那条）** + `REQ-2.3.1`（675ms）；123 条 timeout、
`ERR_CONNECTION_REFUSED=0`（无基础设施失败）。
→ 🎯 **六个 app 全部非零（6/6）—— `PLAN.md` §7 的阶梯闸门达成。**
→ **PLAN 的验收条件 1 在真判分上兑现**：`REQ-2.2` 从"预测失败"转"预测通过"（③ 判分前落盘），**并且真的过了** ——
这是"**硬清单回灌 + 落点约束**"这条链的**第一次端到端验证**。
⚠️ **一轮不变量的核对**：只变一处（注入 1 条 → 7 条 + 落点约束那一句），其余逐字不变；
通过数 **0 → 2**（Δ=2 正好是可辨差异下限），且**成员里含子集那条**（不像 stackoverflow/prestashop
只有子集外的偶发通过）→ 这一分**是子集装置换来的**。**N=1，不据此宣称机制普遍有效。**
🔴 **`final_l1=False` 的正确读法**（已写进记录 `notes`）：**是注入形态，不是"产物没过 L1"** ——
`[4]` 的行是正则链源码，当逐字硬名注入后 L1 的字面子串检查永远追不到（产物里是 `/` 连接的自然写法）。
代价 17,281 token = 全程 52%。这条已被**裁决四**治掉（同产物 + 那 7 条名字 → findings **0**）。

**bookstack 深度第一刀判分结果（35 分钟）：2 / 34** —— 通过 `REQ-1.1`（431ms）+ `REQ-2.1`（465ms），
**与 `bookstack1` 逐条相同**（同样 2/34、同样这两条）⇒ **预注册第 ④ 格命中：「首阻清了、而成员没中」**。
- ② 复核确认 `Books`/`Shelves`/`Email address` 已不在「测试自己输入」栏（`REQ-5.1` 0/2 → **2/0**）；
- **新首阻 = 种子数据**，**两个工具独立同向指认**：L1 的 5 条 `seed_literals` 缺失，
  ③ 对 `REQ-4.1`/`REQ-5.1` 报的缺口正是 `'Shelf 4.1'` / `'Book 5.1'`；**回产物核过：真的不存在**（非假阳性）。
→ 下一步按预注册：**补种子数据**，不是改机制。
- ③ 的第二个标定点：覆盖率 **451/451**、**TP=32 / FP=0 / FN=0**（第 2 份产物也是 100%/100%）。
  ⚠️ **③ 是判分之后才跑的**（PREREG 写"判分前落盘"，**没照做**）→ **不是预注册命中**，记录里如实标注。
- **口径**：条数 2 < 4（保留门槛）→ **本轮不记账**；n=1。
