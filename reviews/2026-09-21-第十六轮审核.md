# 第十六轮审核 · 2026-09-21 18:xx（第 5 条规则落地 + keep 重跑）

> 审核对象：18:10–18:18 的改动（`reqcompile/prose.py`（新）、`a11y.py`、`implement.py`、`main.py`、
> `verify/{l1,loop}.py`、`eval/a11y_prose_probe.py`（新）、`output/bundle.zip`、`STATUS.md`、`docs/12`）
> 审核时环境：**3301 被 PID 13804 占（keep 重跑的判分阶段，18:15 起，预计 18:52 结束）**；锁空闲
> 要求：每条结论附可复现方法（命令 / 数据 / 行号）

---

## 一、四段全部交付，且主声明**我独立复现了**

| 段 | 交付 | 核对 |
|---|---|---|
| 段一 收尾 | 停掉占 3301 的 PID 4776；清「在飞工作」；「当前阶段」改对 | ✅ `STATUS.md:418-420` |
| 段二 靶子清单 | `reqcompile/prose.py`（提交 `d9b2d86`）：位置约束（动词**带屈折变化**）、**(role, name) 对**、**支持纯 role**（`sidebar`→`complementary`、`note editor`→`dialog`）；L1 **分两类**（引号式精确名+驱动修复 / 散文式 `soft_findings` **只告警**）；生成 prompt 分「必须逐字兑现」与「尽力做完」 | ✅ `prose.py:13-21`；**fail-soft 被实测验证**：3 条缺失散文靶子 → `passed` 仍 True |
| 段三 廉价验收 | **交互单元空转率六 app 全降** | ✅ **我自己复跑 `docs/06-a11y-recall-probe.py`**，得到同一张表：12306 **1%** / bookstack **0%** / ctrip **8%** / keep **8%** / prestashop **8%** / stackoverflow **8%**（此前 2/19/18/40/50/29） |
| 段四 keep 重跑 | 靶子 33 → 96；分块计划不变（5 块、一文件一调用）；L1 十项全绿；建表注入成功；**23 秒 / 36,218 token**（官方档） | ✅ 与官方档 ~5–7s/调用 的基线一致；判分仍在跑 |

**并且没有回归**：在飞的这轮里 `REQ-2.1: Note Listing` **仍通过（431ms）**，
与上一轮同一位置同一结果 → 干净的对照。

---

## 二、五条要带着走的

### 2.1 🔴 本轮读数**不能用来评价第 5 条规则**

判据跑的是 keep 全量 32 条，但**生成子集仍是 `PIPELINE_REQ_IDS=REQ-2.1,REQ-2.2`**（2 条需求）
→ 能通过的判据上限就是那两条。**所以"还是 1/32"不等于"规则没用"**，
而是"子集是绑定约束"。**规则 5 的效果必须靠扩子集来读。**

### 2.2 🟡 "召回 100% × 6 个 app"值得抽检一条

靶子数大幅上升（keep 33→96、prestashop 39→**201**、12306 441→**622**），
而六个 app **同时** 100% 有点太整齐（此前是 44–97%）。
→ 抽检方法：把抽取路径的输入换成**不含测试目录**的需求目录，看数字是否不变
（确认抽取没有碰到测试文件）。纪律本身写对了（"从判据取 (role,name) 当**测量**地面真值，绝不进抽取路径"），
但 100%×6 这种整齐度值得一次机械核对。

### 2.3 🟡 靶子清单的体积要和请求预算一起看

靶子会进每一块的 prompt：prestashop 201 条、12306 622 条 → **10–12KB 量级**，
而 `MAX_REQUEST_CHARS = 24000`。要在铺到那些 app 之前确认它不会把请求顶到"必须分块"。

### 2.4 🟡 他们找到的图片链路方向对，但两个前提要先量

**发现**：keep 的头号失败 `button[Toggle sidebar]`（14 次断言）**在需求文本里根本不存在**
（"toggle" 零命中），只在 `requirements/reference/*.png` 里；而平台会注入 `VISUAL_MODEL`。
→ 方向对（多模态需求树里，截图就是需求的一部分），但：
1. **先量图片 token 成本**（`PLAN §8 R9`：单张 2.6MB 图若按文本计费是灾难级）——
   在一张图上核"输入 token / 图片数"的比值，再决定是否铺开；
2. **它依赖平台注入 `VISUAL_MODEL` 的凭据**（A1 的视觉半边）→ **必须和 A1 在同一次平台跑里一起看**。

### 2.5 📌 两处口径（他们要带进下一步）

- 判分 **32 条 / 30.7 分钟**（失败用例各耗 ~60s，**慢的是失败不是通过**）→ `PLAN` 的"墙钟 × 6 外推"用这个真数。
- **1/32 = 3.1% 是否够上榜仍未验证**（A2 / V6）→ 等平台那一次跑。

---

## 三、下一步（按价值）

1. **等这轮跑完**（约 18:52）记读数——**预期仍 ≈1/32**；它是"规则 5 未改变已通过项"的有效对照。
2. **扩 keep 的 REQ 子集**（先加 `REQ-1.1` 入口那条——它现在失败但只花 10.8s，最便宜，再加几条最简单的）
   → 重跑官方档 → **这才是规则 5 的效果读数**。
3. **平台那一次（用户做）**：同时收 A1（含视觉凭据）、A2/V6（run 是否 completed、榜单是否出现）。
4. 达到"本地通过数明显 >1"之后，再谈把 keep 的 REQ 子集扩到全量、以及打包替换。

---

## 附：本轮命令

```bash
python docs/06-a11y-recall-probe.py | grep -E "^(12306|bookstack|ctrip|keep|prestashop|stackoverflow)"
grep -E "^\s+(ok|x) " runs/20260921T101519Z-keep-score-m2keep-rule5.judges.log | head -8
grep -nE "位置约束|精度|fail.?soft|PURE_ROLE" pipeline/reqcompile/prose.py | head
netstat -ano | grep ":3301" | grep -i listen     # PID 13804 = 在飞那轮
```
