# 第三十三轮审核：日期清除 + ctrip 的三条裁决（执行侧在飞：12306 判分）

> **审核对象**：`be61e22`（方案：清排期语言 + 拍 ctrip 回灌/整串落点）、`a7ae46d` / `7370750` / `d8349ec`（执行：零 token 备料），
> 以及**正在跑的** `12306-1` 判分。
> **方法**：裁决逐条读 + "到达面"自己 grep 核 + 现场状态查证。

---

## 一、结论

**方案的日期清除与三条裁决都落地，而且是最小改动**；执行侧的零 token 备料（备注入清单 / 子集扫描 /
到达面核查）也到位。**本轮无 🔴**。

现场：**`12306-1` 的判分正在跑**（18:59 由 `bench.sh` 自动认领，135 条 → 最坏 135 分钟，ETA ≈ 21:15），
两个会话结束时它还活着 —— 老形态"**轮次比会话长**"，但这次**锁由 bench.sh 持有、在飞栏自动认领** ✔
（那正是两周前 D 项修好的东西第一次在**真判分**上生效）。状态：`status=running runners=3 port=busy`。

---

## 二、我复核并**成立**的

### 2.1 日期清除：确认没有"向前看"的排期留下

```bash
grep -nE "剩 [0-9]+ 天|逾期|9/24|9/25|9/26|9/27|9/28|9/29" PLAN.md
```
只剩三类，**都合规**：
- `907-910`：新的「**完成即止**」规则，并明写"原规则（`B_worst`/`B_pred`/`U`/余量/三分支/9/24 预注册）**整块删除**" ✔
- `931-933`：**旧日期阶段的解码表**（读旧文档时换算用）✔
- `1575` / `1629`：`predicted` 字段与 R7 的"**9/24 已作废/去掉日期**"注记 ✔
（`69` 那条是**历史**叙述——"当时初赛只剩 8 天"，过去式 ✔）

阶段表删「最晚」列 → 改成「**闸门判明不通过时怎么办（按状态触发）**」；硬墙只留 9/30 提交与 10/1–10/7 决赛
（**外部事实，不是我们的排期**）✔ —— 这个区分做得对。

### 2.2 三条裁决：都成立，而且是最小改动

| 裁决 | 内容 | 我的判定 |
|---|---|---|
| 一 | **回灌 = 认**（入口用 spec 侧 `subset_closure.py` 的 `[4]`，7 条逐字、不要产物） | ✔ 入口选得对（用**需求文本**判"是不是本子集该说的"，而不是拿产物当依据） |
| 二 | **并进第二类**（不新开第四类、不并第三类），理由含"整串落在一个元素是**所有 locator 靶子的通性**，单列成类会误导" | ✔ 理由成立——第三类管"语言"（不许翻译），这条是"**落点**"，归"按可访问名/可见文本兑现"那类才是同义词 |
| 三 | 这一轮**只加 `implement.py:256` 那句落点约束**；`extra_hard` **不接 `generate_app`**，并写死触发条件（L1 抓到但两轮修复都没落进同一元素，才动 plumbing） | ✔ 最小改动 + 触发条件明确（防止"顺手把 plumbing 改了"这种越界） |

并且 `predicted` 字段**保留但用途改成两件**（不再喂那个已取消的决策）✔；验收三条 + 边界
（不为它重生成已非零的 app）✔。

### 2.3 「到达面」——**我自己 grep 核过，成立**

```bash
grep -rn "EXTRA_HARD|extra_hard" pipeline/main.py pipeline/generate/implement.py
```
`PIPELINE_EXTRA_HARD_NAMES` 全仓**只在 `main.py:504` 读一次**（在验证闭环段里）→
`build_requirement_brief(..., extra_hard=…)` 在 **508**（验证 brief）、L1 的 required 在 **532** 用到它；
而 `run_generation`（~478）**没有**把 `extra_hard` 传下去 ✔
→ **注入名到得了"验证闭环 brief + L1 契约"，到不了初始生成** ✔ —— 与执行侧的结论逐字一致。

**含义**（这条要紧，它改了复跑的读法）：注入名是靠 **"L1 抓到 → 修复轮补上"** 落地的
（ctrip 的「账号登录」走的就是这条），所以复跑的第 ③ 格**不能**写成"回灌没进 prompt"——
初始 prompt **本来就不会有它**；要说清是**哪一段** prompt。

### 2.4 执行侧的零 token 备料

- **没有便宜路**：逐个子集跑 ② → `REQ-2.3.1` 与 `REQ-2.2` 的 spec 正文相同（都 `ensurePasswordLogin`）→
  换 REQ 换不掉缺口；其余候选要么未覆盖更多、要么要 8 个名字全可见 ✔
  → **结论：没有"换个子集绕过去"的便宜路，方案那条决定确实在关键路径上** ✔
- **备注入清单**：从 `predicted.missing_units` 抽单元 + **第三处证据**（判据自己拍的
  `error-context.md` 快照 → `/密码/` 与 `/登录/` 的元素都在场，唯一没有对应元素的是
  「邮箱.*用户名.*手机号」单元）+ 三条必须带进 prompt 的语义（`.*` 不跨元素 / 兜底只试第一个候选 /
  只认那 9 类位置）+ 限定词（单元 2/3 没被执行到，"它们没问题"是**推断**不是实测）✔
  → 备料落在 `%TEMP%/ctrip1/INJECT-LIST.md`、预注册落在 `PREREG-rerun.md`，**等拍板才跑** ✔（按指令）

---

## 🔴 必须先改（不满足这条不许合 main）

**本轮无。**

（阶段仍未跑完：`12306` 在判、`ctrip` 待复跑 → 所以"先别合 main"，但**不是因为审核有问题**。）

---

## 三、下一步（关键路径已全在执行侧）

1. **接住在飞的 `12306-1`**（135 条；**不要在同一条命令里轮询**——第 19 条刚补、MSYS fork 已三次同形）：
   跑完抽记录（带 `predicted` + `notes` 三段）+ 清在飞栏 + `README`/`docs13` 补行。
2. **ctrip 复跑**（已解锁）：加 `implement.py:256` 那句落点约束 → 按 `PREREG-rerun.md` 的三格跑
   （第 ③ 格按 §2.3 校正措辞）→ 判分（125 条 → 后台任务）。
3. 若 ctrip 非零 → **6/6** → 报用户走审核 → 无 🔴 → `./eval/stage-merge.sh "阶段名"` + `git push`。
4. **R1（用户侧）**：与本地判分完全可并行。

---

## 四、复现命令

```bash
grep -nE "剩 [0-9]+ 天|逾期|9/2[4-9]" PLAN.md          # 只剩解码表与"已作废"注记
grep -rn "EXTRA_HARD\|extra_hard" pipeline/main.py pipeline/generate/implement.py   # 到达面
sed -n '/在飞工作/,/^## /p' STATUS.md | grep "|" | head -4                          # 在飞=12306-1
./eval/bench.sh verdict                                                             # status=running（只读）
```

**审核者**：第三十三轮 · 2026-09-23 19:1x（本地）
**本文件是审核会话本轮唯一写入的产物**；未跑任何 bench 子命令（只用了只读的 `verdict`），
未动 `repos/` / `pipeline/` / `eval/` / `runs/` / `PLAN.md` / `AGENTS.md`。
