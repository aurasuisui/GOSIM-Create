# 第三十一轮审核：ctrip 0/125（阶梯停在 4/6）+ 本轮四条收口

> **审核对象**：`9652fb5` / `f0b7e6e`（要求单元口径，三版定稿）、`e272dc0`（记录与 STATUS）、
> `6bfc580`（prestashop n=2）、`bed0691` / `92e196e`（ctrip 中断与第一轮 0/125）
> + 方案侧已落地的两条 🔴 与第三类契约。
> **方法**：**两处标定我自己复跑**（口径改动的最大风险）；ctrip 的机制归因回产物核。

---

## 一、结论

**这一轮的工程面质量很高**：两条 🔴 文档项**按我实测的数字**落地、`git_subject` 落地（还带"从 hash 反查 subject"的退化路径）、
"逐字、不许翻译"以**第三类契约**拍板（含验收判据与"别重生成已非零 app"的边界）；
`③` 那个"三版才定"的口径改动，我**复跑两处标定，结果完全不变** → **没有把判据改松**。

**本轮没有「必须先改」**。但**两件决定悬着**（都不挡合并，但**挡 ctrip 第二轮 / 挡 9/24**）：
`回灌` 没拍、以及 ctrip 新暴露的**复合 pattern** 那一类还没写进契约。

---

## 二、我复核并**成立**的

### 2.1 两条 🔴 文档项：按实测数字落地 ✔

- `AGENTS.md:262-271`："历史重排的后果要记住——**比旧文写的更彻底**（2026-09-23 更正，实测）…
  **记录里的 `git_commit`：可达 0 / 不可达 21 / 无指纹 15** … **规则：两次重排（9/22、9/23）之后，
  所有旧记录的 `git_commit` 都不可达。复核以 `subject` + `runs/*.json` 的内容 + `docs/` 为准**" ✔
- `AGENTS.md:212-219`：判据 = "**没有标注为「必须先改」的问题**"，并固定了那一节的标题格式
  `## 🔴 必须先改（不满足这条不许合 main）` + "未被标为「必须先改」的（含 🟡/🟢）不挡合并" ✔

### 2.2 `git_subject` 落地 ✔

`pipeline/provenance.py:100`（盖在产物出生处）+ `eval/extract_run.py:201`（现采）+ `:251`
（**指纹里没有时，从 hash 反查 subject** 的退化路径）✔ —— 这正是"hash 会被重排换掉、subject 能活下来"的落地。

### 2.3 第三类契约（逐字、不许翻译）✔

`PLAN.md:1231+`：根因（prestashop 3/4 靶子不可达而那些词**全在全量需求里**）、契约正文
（"不许翻译、不许改写、不许'意译成更自然的表达'"）、**验收**（prestashop 子集重生成 → unreachable `3/4 → 0/4`）、
**边界**（"不要为它重生成已非零的 app"）、以及"**别因此去掉图片通路**" ✔ —— 边界都想到了。

### 2.4 ⭐ **`③` 的三版口径：我复跑两处标定，结果不变**（本轮最该核的一条）

```bash
REQS=$(ls …/bookstack/tests/*.spec.ts | …) ; python eval/predict_judge.py bookstack %TEMP%/bookstack1/app $REQS --truth …bookstack1.json
REQS2=$(ls …/stackoverflow/tests/*.spec.ts | …) ; python eval/predict_judge.py stackoverflow %TEMP%/so1/app $REQS2 --truth …so1-n2.json
```
| 标定 | 我复跑 | 与他声称 |
|---|---|---|
| bookstack 34 spec | 覆盖率 **451/451**、预测通过 4 / 失败 30、**TP=30 / FP=0** | ✅ 一致 |
| stackoverflow 66 spec | 覆盖率 **1044/1044**、预测通过 1 / 失败 65、**TP=65 / FP=0 / TN=1（精度 100% / 召回 100%）** | ✅ 一致 |

→ 口径连着改了三版（扁平化"太紧" → 整组"太松" → 按层级解析），**两个方向的标定都没被带动** ✔
（这类"收紧/改口径"最容易连带改坏判据，本项目有前例，所以我每次都要复跑。）

### 2.5 ctrip 的机制归因：有产物证据 ✔

- 产物里 `用户名` ×2、`账号登录` ×2，而失败行是
  `getByRole('button', {name: /邮箱.*用户名.*手机号/})` —— **判据要求的是复合串（按序）**，
  我在产物里**找不到任何一行同时含这三个词** ✔
- → "**不是没做，是没按判据要求的串写**"成立；且它是**契约类的新形态**（不是"没做"、也不是"翻译了"）

### 2.6 中断轮与读数

- 首次判分在 **71/125** 被 MSYS fork 中断（**第三次**同形）→ `*.INVALID-interrupted.*` 改名 +
  `recover --force` + 重跑；记录是 `ctrip1b`、`notes` 写明重跑 1.4 h、中断产物不进任何通过数 ✔
- `0/125` 的读法写对了：**预注册第 ② 格命中**（③ 判分前就预测子集那条会挂）→
  "不是'跑了但没分'，是子集装置没兑现"，阶梯停在 **4/6** ✔
- prestashop n=2：同一成员（`REQ-2.2.1` 5.0s）+ **失败集合逐条相同** ✔

---

## 三、🟡 悬着的三件（不挡合并，但挡 ctrip 第二轮 / 挡 9/24）

### A. `回灌` 决定还没拍 —— **ctrip 第二轮明确在等它**

`STATUS` 写着"① ctrip 第二轮（**等方案拍板**『③ 的 unreachable 回灌硬清单』）"，
而 `PLAN.md` 里 `回灌` 目前**没有相关决定**（1140 行那条是"L1 发现回灌成修复指令"，另一件事）。
要定的不只是"回不回灌"，还有**表示形态**：ctrip 这条是**复合串**，
硬清单里得写成"**一个元素的可访问名必须含这一串（按序）**"，而不是拆成三个词各写一遍。
→ 载体已经存在：`predicted.missing_units` 就是这个形状 ✔（工具侧不用大改）。

### B. 复合 pattern 还没进契约（第三类只管"翻译"）

第三类的范围是"需求文本里的词**逐字**（别翻译）"；ctrip 这条是"**多个词要按序落在同一个元素的名字里**"
——**是不同的失败形态**（产物三个词都有，只是分散在不同元素）。
→ 建议作为**第四类**（或并入 A 的回灌口径）写进 §7 硬清单一节。

### C. 9/24 的预注册还没写（明天就要用）

数据已齐（ctrip `0/125` + 第二轮成本未知；12306 待判；`predicted` 在记录里；召回按下界口径取最差）。
→ 建议今晚或明早补上，免得当天边算边解释。

### D.（便宜）MSYS fork 已**第三次**同形，第 19 条可以再具体一点

现在写的是"用**后台任务**启动"。实测三次都发生在**长等待**里 → 建议加一句：
**"长判分轮不要在**同一条命令**里轮询——启动即返回，过一阵另起一次调用查看进度。"**

---

## 🔴 必须先改（不满足这条不许合 main）

**本轮：无。**

（上一轮那两条已落地 ⇒ 若这一阶段的里程碑已达成，**合并现在不被我的审核挡**；
但阶段是否"跑完"由 `PLAN.md` §7 的闸门判——阶梯现在 **4/6**，未达成。）

---

## 四、下一步

- **执行侧**：`12306`（最后一个 app；② 已提前做完：子集 `{REQ-1.2}`、无需注入）→ 生成 → ③ 预测 → 判分（**135 分钟，最长的一轮**）
- **方案侧**：A（回灌 + 复合表示形态）、B（第四类）、C（9/24 预注册）、D（第 19 条补一句）
- **R1 在用户侧**（提交包已按今日 pipeline 改动重打包，红线 9 仍 0 处）

---

## 五、复现命令

```bash
# 2.4 两处标定（我自己跑的）
REQS=$(ls repos/arc-bench/arc-bench/webapp/bookstack/tests/*.spec.ts | sed 's#.*/##;s#\.spec\.ts##' | tr '\n' ' ')
python eval/predict_judge.py bookstack %TEMP%/bookstack1/app $REQS --truth runs/20260922T120254Z-bookstack-score-bookstack1.json
REQS2=$(ls repos/arc-bench/arc-bench/webapp/stackoverflow/tests/*.spec.ts | sed 's#.*/##;s#\.spec\.ts##' | tr '\n' ' ')
python eval/predict_judge.py stackoverflow %TEMP%/so1/app $REQS2 --truth runs/20260923T011156Z-stackoverflow-score-so1-n2.json
# 2.5 ctrip 机制
grep -rhoE "用户名|账号登录|邮箱.*手机号" %TEMP%/ctrip1/app/frontend/src | sort | uniq -c
```

**审核者**：第三十一轮 · 2026-09-23 18:5x–19:0x（本地）
**本文件是审核会话本轮唯一写入的产物**；未跑任何 bench 子命令，未动 `repos/` / `pipeline/` / `eval/` / `runs/` / `STATUS.md`。
