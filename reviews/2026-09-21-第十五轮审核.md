# 第十五轮审核 · 2026-09-21（重订关键路径三段：收尾 / 分块器 / keep 非零）

> 审核对象：16:45–18:01 的改动（`pipeline/generate/chunkplan.py`、`implement.py`、`main.py`、`verify/l1.py`、
> `jsscan.py`、`eval/{regress_l1.sh,accept_chunkplan.py,check_js_balance.py}`、`reqcompile/a11y.py`、
> `output/bundle.zip`、`STATUS.md`、`docs/12`、keep 那轮的判分产物）
> 审核时环境：**3301 被占（PID 4776，判分脚本留下的后端）**、锁空闲；keep 全量判分 **18:01 刚跑完**
> 要求：每条结论附可复现方法（命令 / 数据 / 行号）

---

## 一、结论：核心目标达成 —— **keep 拿到主赛道第一条非零**

| 项 | 值 | 核对 |
|---|---|---|
| keep 判分 | **32 条 / 1 通过 / 31 失败**（`pass_rate 0.0312`） | `runs/20260921T093005Z-keep-score-m2keep-official.json` ✅ |
| 通过的那条 | `REQ-2.1: Note Listing`（**339ms**）| `STATUS.md:403`；判分日志 ✅ |
| 测量条件 | `load_snapshot = status='🟢 空闲'`、`retries_effective = 0` | RunRecord ✅ **静置条件下的单次读数** |
| 生成口径 | `PIPELINE_REQ_IDS=REQ-2.1,REQ-2.2` + **官方档** | `STATUS.md:401` |
| 耗时 | 判分 **30.7 分钟**（31 条失败各耗 ~60s） | `judges.log` 末尾 `1 passed (30.7m)` |

→ **M2 的本地半边成立**（"≥1 个任务非零通过"里的**非零**部分）；
**剩下的半边只等平台那一次跑**（上榜/口径），那是用户侧动作。

---

## 二、我独立复核的（都对）

1. **31 条失败全部是"瞄准类"**——我从 `judges.log` 里数了失败的断言：
   `getByRole('button',{name:/^Toggle sidebar$/i})` **×14**、`getByRole('complementary')` **×7**、
   `.../^Notes$/i` **×7**、`role=dialog /^Note editor$/i` **×6**、`/^List view$/i` **×6**、
   `textbox /^Title$/i` **×5**、`/^Search$/i` **×4**、`/^Settings$/i` **×4** …
   **不是崩溃、不是连接问题**（`ERR_CONNECTION_REFUSED` = 0）→ 干净的单类失败。
2. **我上轮三条建议全部落地**：`eval/regress_l1.sh` ✅；`check_js_balance.py` 已含 `node_modules` 排除 ✅；
   `verify/l1.py` 新增 `RE_METER`（`:55-56`）与 `check_aria_name_sources`（`:376`）——**meter 类可访问名已下沉为静态检查** ✅。
3. **A1 的掩码凭据行进了 `main.py`**（`:135-161`）：只打来源 / `base_url` / key 的**存在性与长度**，不打印值 ✅。
4. **分块器**：新模块 `pipeline/generate/chunkplan.py` + `eval/accept_chunkplan.py` +
   两份验收产物（`runs/20260921T-accept-{quickstart,keep}-design.json`）——
   **正是 `PLAN §7` 定的"不用生成、只跑设计阶段"的验收法** ✅。
5. **`jsscan-vs-node-crosscheck.txt`**：他们拿 Node 自己的解析器交叉验证了静态配平扫描器 ✅
   —— 这是很正确的做法（"判据没报错 ≠ 判据跑过了"的正面应用）。
6. `output/bundle.zip` 已在分块器落地后**重建**（17:43）✅；`STATUS.md:16/379-412` 记了三段与 commit `f1a1917` ✅。

---

## 三、要处理的四条

### 3.1 🔴 keep 的 31 条失败**指向同一个方向**：靶子清单（§4.1 第 5 条规则）现在是头号杠杆

失败的名字与角色**都被点出来了**（见 §二.1）。这验证了
`reviews/2026-09-21-进度对齐审核.md` §2.5 的预测：**keep 是散文式需求，它的失败会集中在"名字/角色对不上"**。

两条要记住的：
- **其中有一类是纯 role**：`complementary`（= `<aside>`，侧栏）、`dialog`（= 编辑框）。
  → 靶子清单应是 **(role, name) 对**，不只是名字。
- **靶子不能从测试反推**（平台上 agent 看不到测试）→ 必须从**需求文本**抽（散文名词枚举），
  **测试只能当地面真值做校准**（你们已有 recall 探针）。
  → 落地方式建议：**先做 rule 5 + role 映射，再用"只跑设计阶段"那套廉价验收在 keep 上量召回**，
  最后才跑生成。这仍然是"不要用生成去验静态可判的东西"。

### 3.2 🟡 `STATUS.md:40` 的「当前阶段」仍是旧文本

仍写"**下一步是 M3b-1：把「跑通」铺到规模（6 任务全部非零）**"——我在对齐审核 §2.2 提过，仍未改。
它是任何会话第一眼读到的一段；而实际状态是"M2 本地半边已成立，等平台一次跑"。

### 3.3 🟡 现场未收尾：**3301 被占**（PID 4776）+ 5 个进程（2 node / 3 python）

判分脚本自己提示了"后端仍在运行……要停就 taskkill 占用 $PORT 的进程树"，
`STATUS.md:16` 也记了"遗留：keep 的 32 条全量判据仍在后台跑（占 3301 端口）"——**但它 18:01 已经跑完**，
所以那一栏与端口都该清。**下个会话开工前先清**（先确认没有生成在跑，再 `bench.sh stop` 或按脚本提示 taskkill）。

### 3.4 🟡 两处口径要带着走

- **判分耗时 30.7 分钟 / 32 条** → `PLAN` 里"墙钟 × 6 外推"要用**这个真数**更新
  （而不是早先 12306 的 3.6–8 分钟：失败用例各耗 ~60s，慢的是失败而不是通过）。
- **1/32 = 3.1% 是否够"上榜"仍未验证**（A2/V-6）——那正是下一步平台跑要答的。
  若平台按平均通过率展示，3% 会落在 Junior 档，但**只要非零就是"有分"**（这是待验的口径）。

---

## 四、下一步（唯一主线动作，且完全在用户手上）

**打包已就绪**（`output/bundle.zip`，17:43）→ **请用户在平台上只跑 `keep` 一个任务**，
一次同时收三件事：

1. **A1**：日志里那一行**掩码凭据体检**（`MODEL` / `base_url` / key 存在性）——它是唯一"为假就全盘归零"的假设；
2. **A2 / V-6**：该 run 是否被标 `completed`（而不是像 0/86 那样 failed）、**榜单是否出现条目**；
3. **同一快照能否反复跑任务**（决定 M3b-1 能否在同一份提交上多轮迭代）。

---

## 附：本轮命令

```bash
python -c "import json;d=json.load(open('runs/20260921T093005Z-keep-score-m2keep-official.json',encoding='utf-8'));print(d['tests_total'],d['tests_passed'],d['pass_rate'],d['load_snapshot'])"
grep -oE "getBy[A-Za-z]+\([^)]*\)" runs/20260921T093005Z-keep-score-m2keep-official.judges.log | sort | uniq -c | sort -rn | head
grep -nE "meter|aria" pipeline/verify/l1.py | head -3
grep -n "凭据体检" pipeline/main.py
test -f eval/regress_l1.sh && echo ok
netstat -ano | grep ":3301" | grep -i listen     # PID 4776 仍需清
```
