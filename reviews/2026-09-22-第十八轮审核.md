# 第十八轮审核 · 2026-09-22 12:0x（段一/二完成，段三读数未落袋）

> 审核对象：9/22 11:08–11:41 的产物（`eval/a11y_prose_probe.py`、`runs/*-keep-score-r3a-1.*`、`STATUS.md`）
> 审核时环境：**3301 被 PID 10888 占（单个 listener）**、**无锁**、`runs/.inflight.log` 无新认领；
> 6 个 python 进程在跑；`%TEMP%` 下有 `r2-keep`、`m2-keep2`、`m2-keep` 三份产物

---

## 一、落袋的读数：**R3a 第一遍 = 1/32（30.3 分钟）**

| 项 | 值 | 对照 |
|---|---|---|
| `runs/20260922T031033Z-keep-score-r3a-1.json` | `tests_total 32 / passed 1 / pass_rate 0.0312` | 与 9/21 18:46 那轮 **完全一致** |
| 耗时 | **30.3 分钟** | 静置基线 30.7 分钟 → **差 0.4 分钟** |
| 跑前 `load_snapshot` | `status='🟢 空闲'`、`port3301_listeners=0` | 条件干净 |
| 通过的那条 | `REQ-2.1: Note Listing` | 两次同一位置、同一结果 |

### 1.1 这个读数本身有价值：**判分的重复性极好（这两次之间翻面 ≈ 0）**

两次判分（不同时间、同一份产物）**条数与成员都不变** → **判分噪声在这份产物上约等于零**，
与 12306 参考实现那边"0–2 条翻面"是**不同量级**。

> **但要记住为什么，别把它外推**：这里 **31/32 是确定性失败**（功能不存在 → 找不到元素 → 超时），
> 确定性失败当然稳定。**等通过率升上来（有更多"真跑起来"的用例）时，判分噪声会回升**
> （那时才会出现 12306 那种时序翻面）。→ **"判分无噪声"只对"大面积失败"的产物成立。**

### 1.2 我原先的一个担心，被数据否掉了（更正我自己）

我担心 R3a 的两遍会与 R2 的生成并发、污染"判分重复性"。实测 **30.3 vs 30.7 分钟**没有干扰特征，
且失败由 60 秒等待主导（不是 CPU 竞争）→ **这条担心站不住，撤回**。
（仍建议：**判分与生成不要并行**，理由不是 CPU，而是"判分是独占资源"的现场纪律。）

---

## 二、没落袋的：R2 与 R3a 第二遍都**还没开始判分**

- `runs/` 里**只有 `*-r3a-1.*`** 一组 score 产物 → **没有 `r3a-2`，也没有 R2 / R3b 的判分日志**
  （`score_app.sh` 在启动时就会 tee 出日志文件，所以"没有文件"= **没在跑**，不是"跑着还没写完"）。
- 所以段三的两个读数**都还没落袋**；3301 的占用者**更可能是 r3a-1 留下的后端**
  （那个脚本按设计会把后端留着，它自己在末尾提示了"要停就 taskkill 占用 $PORT 的进程树"）。
- `%TEMP%` 下 `r2-keep` / `m2-keep2` **产物已存在** → **生成已经做过**，缺的只是"判分"这一步。
  → **别重新生成**：直接对这两份产物跑判分（`score_app.sh <目录> keep <arm>`），
  这是 §四那套"生成与判分解耦"的直接应用，**零生成 token**。

**另一处**：`r3a-1` 的记录里 `arc_test_date: None`，而脚本日志明明设了 `ARC_TEST_DATE=2026-09-20`
→ **提取器仍未捕获该字段**（`PLAN` 已记过这条记录缺口，至今没修）。

---

## 三、下一步（都在执行侧，且都不需要再花生成 token）

1. **清场**：确认无判分在跑 → 停掉占 3301 的 PID 10888。
2. **把已经生成好的两份产物判掉**（`r2-keep` = 全量 keep；`m2-keep2` 需先确认它是什么变体：
   R3b 的"闭环关"？还是上一轮的？**记清楚再判**，否则读数没法归属）。
3. **补 R3a 第二遍**（`--reuse-app` 对同一份产物）= 判分重复性的第二个样本；
   有它以后，"翻面 ≈ 0"才是有 n=2 支撑的结论。
4. 然后才有 R2 的"离上榜多远"读数，再据此定 9/25 决策日的方向。
5. 顺手修 `arc_test_date` 的提取（一行的事，但它是"记录能自证"的一部分）。

---

## 附：本轮命令

```bash
python -c "import json;d=json.load(open('runs/20260922T031033Z-keep-score-r3a-1.json',encoding='utf-8'));print(d['tests_passed'],'/',d['tests_total'],d['load_snapshot'])"
grep -E "passed \(" runs/20260922T031033Z-keep-score-r3a-1.judges.log | tail -1     # 30.3m vs 基线 30.7m
ls -t runs/*score*.log | head -4                                                    # 只有 r3a-1
ls -d "$TEMP"/r2-keep/app "$TEMP"/m2-keep2/app                                      # 产物已在，缺判分
netstat -ano | grep ":3301" | grep -i listen                                        # PID 10888
```
