# 第二十轮审核 · 2026-09-22 13:1x（扩子集被实测打回 + 归因有混淆）

> 审核对象：`*-keep-score-e1b-keep4.*`（在跑）、`l1.py:446` 的 `check_express5_routes`、
> `implement.py:95` 的 STACK_CONTRACT 硬约束、`implement.py:455-510` 的就地降级
> 审核时环境：判分在跑（8/32 时 0 通过）

---

## 一、核对（都对）

| 项 | 核对 |
|---|---|
| Express5 静态检查 | ✅ `l1.py:446-476`，docstring 直接引了 `PathError: Missing parameter name at index 1: *`，hint 指向"照抄模板 / `/*splat`" |
| STACK_CONTRACT 硬约束 | ✅ `implement.py:95`（**预防 + 检查两件都做了**，这是正确做法） |
| 就地降级 | ✅ `implement.py:455-510`：`failed` 清单 + "跳过该块继续" + 到额降级；`:488` 的判断（"跳过一块 = 这个页面没做，但应用**仍可构建可运行**"）是对的 |
| REQ-2.1 的失败形态 | ✅ `toBeVisible` 失败、**11.1 秒**（通过时 431ms）→ 元素不存在，与"共享外壳被改坏"一致 |
| r3a-2 | ✅ 1/32、30.5 分钟、与 r3a-1 逐条一致 → **判分翻面 ≈ 0（n=2）** |

**"扩子集不是免费的"是这一轮最有价值的发现**——它把 `PLAN §7` 早写过的"共享前置"风险变成了实测。

---

## 二、🔴 归因有混淆：这一轮同时变了**两个**变量

| 变化 | 落地时间 | 影响面 |
|---|---|---|
| **弹性请求压缩**（`f386500`，安全线 70%） | 12:1x | **每一个块的 prompt 内容**（设计/上游原文/骨架/切片四级收紧） |
| **子集扩到 4 条需求** | 本轮 | 设计 → 分块 → 共享外壳 `HomePage.tsx` |

而 `e1b-keep4` 是**压缩上线后的第一次生成**。所以"REQ-2.1 掉了"目前的归因是**未证明的**：
可能是"外壳被改坏"，也可能是"**压缩把模型需要的上下文挤掉了**"。

**两个动作能把归因做实（都便宜）：**

1. **免费、且最直接**：**diff 两份产物的 `HomePage.tsx`**（2 条需求那份 vs 4 条需求那份）——
   直接看外壳**哪里变了**，比从测试失败反推可靠。
2. **~35k token + 30 分钟判分**：**用当前代码重跑 2 条需求子集**（`REQ-2.1,REQ-2.2`）。
   若 REQ-2.1 **又过** → 压缩无辜，是子集/外壳的账；若**还是挂** → 压缩（或第三因素）要被追责。
   → **在归因做实之前，不要动"外壳与页面分离生成"这种架构级改动**（那是大工程，别建在未证明的假设上）。

---

## 三、一条值得固化的规则（同类已第三次）

"模型语料偏 vs 模板栈版本"已经烧掉三轮生成：**少一个 `)`** → **ESM/CJS** → **Express 4 的 `'*'` vs Express 5**。
共同点：**模型的语料里旧写法占绝对多数，而模板钉的是新栈**。

> **规则**：每遇一次这类冲突，**同时**做两件——① `L1` 静态检查（生成阶段就点名）
> ② `STACK_CONTRACT` 硬约束（预防）。**两件都做**，不要只做其中一件。
> 并维护一张"已知冲突对"清单（Express4 `'*'` ↔ Express5 `/*splat`；CJS ↔ ESM；…）供后续检查复用。

---

## 四、R1（平台那次）的硬约束：**必须用最新包**

包里现在含三处修复（弹性压缩 `f386500`、Express5 检查、就地降级）。**Express5 那条尤其致命**：
后端起不来 → 平台侧测试全挂；用旧包跑就等于白烧一次 run。
→ **用户上传前先确认包的时间戳晚于最后一次修复**；读日志先问"生成跑完了吗/后端起来了吗"。

---

## 五、下一步

1. **先做上面的归因两件**（diff + 2 条需求对照重跑）——它决定下一步是"改架构"还是"改子集策略"。
2. **R3b（闭环 ROI）**：9/25 决策日前不能缺（它占单轮一半 token）。
3. **段三（全量 keep 结构读数）**：现在更该做——它恰好能回答"全量时外壳是否也会被改坏"。
4. `e1b-keep4` 跑完后记最终计数（0 或 1）并停后端；「在飞工作」清栏。
