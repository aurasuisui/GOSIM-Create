# 第十九轮审核 · 2026-09-22 12:1x（R2 死在半路 + 弹性压缩 + arc_test_date）

> 审核对象：`f386500`/`4793a05`/`ba5f6f9`/`fba5699` 四个 commit、`output/bundle.zip`（12:11）、
> `%TEMP%/r2-keep/gen.log`、`runs/20260922T040833Z-keep-score-r3a-2.*`（在跑）
> 审核时环境：3301 被 PID 25968 占（r3a-2 判分中）；锁空闲

---

## 一、事实核对（都对）

| 项 | 核对结果 |
|---|---|
| R2 死因 | `gen.log` 原文：`RequestTooLarge: 网关断开且请求接近预算（19267 字符 ≈ 80%，stage=implement-frontend-page-labelmanagerpage）：应当分块再试` + `EXIT=1` ✅ 与报告一致 |
| 修复形态 | `implement.py:99-102`：`REQUEST_SAFE_CHARS = MAX_REQUEST_CHARS × 0.7`，注释写明"80% 实测会触发网关断连"；压缩发生在**拼请求时**（`_chunk_messages`），**不再抛异常** ✅ 设计正确 |
| 包 | `output/bundle.zip` 12:11 重打（晚于 `f386500`）✅ |
| `arc_test_date` | `eval/extract_run.py:184` 改为 `os.environ.get("ARC_TEST_DATE")`，注释直接引用了 r3a-1 那次 `None` ✅ |
| 变体认定 | `m2-keep2` 由它自己的 `metrics.jsonl` 认定（`run_id=20260921T101419Z-p19840`、官方档、`selfcheck-review×3 + repair×2`）✅ **依据来自产物记录而不是猜**，这正是要的做法 |

**两轮同结果**：`m2-keep2` 与 `m2keep-rule5` 都是 **1/32、同一条**（`REQ-2.1`）→ "规则 5 没破坏已通过项"的对照成立 ✅

---

## 二、🔴 这条教训要升格为原则（它同时也是对我自己一条建议的补全）

我在第十四轮提的客户端规则是"**预算 + 超限抛 `RequestTooLarge`，由调用方按语义分块**"——
只写了**信号**，没写**恢复**。这一轮的结果是：**调用方没接，异常一路冒到 `EXIT=1`，10/13 块的花费全部作废**，
而且**产物残缺、不可判分**。

> **原则（建议写进 `PLAN`）**：**任何"付费长流程"的硬失败，都必须有一条"就地降级"路径。**
> 抛异常只留给真正不可恢复的情况；否则等于把已经花掉的钱作废——
> 这与 `PLAN §4.3`（墙钟耗尽 → 降级为"至少一个场景可走通"）是**同一条原则，只是发生在生成阶段**。
> 今天已经发生两次同型损失：run d/e 的判据侧失败丢掉了生成，R2 的生成侧中止丢掉了 10/13 块。

**一处仍需观察**：这次压的是**输入**体积；而第十二/十三轮记录过另一条形态——
"**单块输出很大 + reasoning 占 90% → 生成时间长到网关读超时**"。R2 重跑时要盯这一条是否复现。

---

## 三、⚠️ 操作顺序上的硬约束（比技术项更急）

**用户那次平台跑（R1）必须用 12:11 之后打的包。**
包里是同一套逻辑，**修复前提交 → 平台跑全量 app 也会中途 Run failed → 0 分**（他们自己指出的）。
→ 若用户已经上传过旧包，**要重传**；读那次日志时先问"生成有没有跑完"，别把"我们自己的中止规则"读成平台问题。

---

## 四、读数口径（他们已在维持，重申一条）

- `1/32` = **官方档 + 2 条需求子集** → 既不是能力读数，也不是平台预期（前例：同一产物官方档 6/6、比赛模型 3/6）。
- **R3a 是判分重复性，不是闭环 ROI**；R3b（闭环开/关各生成一次）仍未做。
- `r3a-2` 与 `r3a-1` 逐条一致 → "判分翻面 ≈ 0"目前 n≈1.3，够不着结论，但方向对。

---

## 五、下一步（按价值，且都便宜）

1. **R2 重跑**（请求已压到 ≤45%，可以跑了）：**档位要写死**——官方档给"结构是否成立"，平台预期交给 R1。
2. **R3b（闭环 ROI）**：它占单轮 token 的一半，到现在仍未被量过；这是 9/25 决策日前最该补的对照。
3. **扩 REQ 子集**：`Toggle sidebar` 那 14 条属"没实现"（在 `REQ-6.2`、不在子集里），不是瞄准问题。
4. 清场：r3a-2 跑完后停掉它留下的后端（3301 / PID 25968）。

---

## 附：本轮命令

```bash
tail -4 "$TEMP/r2-keep/gen.log"                       # RequestTooLarge 19267 ≈ 80% + EXIT=1
grep -nE "REQUEST_SAFE_CHARS|0\.7" pipeline/generate/implement.py
ls -la --time-style="+%m-%d %H:%M" output/bundle.zip  # 09-22 12:11
grep -n "ARC_TEST_DATE" eval/extract_run.py            # :184 从环境读
```
