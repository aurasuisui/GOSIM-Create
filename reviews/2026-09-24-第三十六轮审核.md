# 第三十六轮审核：整阶段送审 —— **6/6 达成，无 🔴，可以合 main**

> **审核对象**：`b874728`（ctrip2 = 2/125 → 6/6）、`2254b18` / `335dabe`（STATUS）、
> `c1a760d` / `f66b849`（裁决四/五落地）、`9f2e508`（bookstack 深度第一刀 = 2/34）。
> **方法**：**六个 app 逐个非零我从记录里自己数**；裁决四/五的落地各自复核；classify 修复的验收我自己复跑。

---

## 一、结论：**这一阶段达成，且本轮无"必须先改"** → **可以合 main**

| 闸门 | 判据 | 我的复核 |
|---|---|---|
| **6 个 app 都非零** | 每个 app 至少一条通过 | ✅ **六个都非零**（下表，逐条 test_id 我自己读的） |
| 零人工 | 产物一个字节没手改 | ✅ 记录 `notes` 都写着；改的都是管线与 brief，不是产物 |
| 出生处指纹 | `stamped_at_product` / `pipeline_dirty=False` | ✅ 记录里有（`git_subject` / `git_branch=work` 也在） |

| app | 最新记录 | 通过数 | 通过的是哪几条 |
|---|---|---:|---|
| keep | `…r4-keep4-n2.json` | 2 | `REQ-2.1` / `REQ-2.7.3` |
| bookstack | `…bookstack-score-bookstack2` | 2 | `REQ-1.1` / `REQ-2.1` |
| stackoverflow | `…so1-n2.json` | 1 | `REQ-1.3` |
| prestashop | `…ps1-n2.json` | 1 | `REQ-2.2.1` |
| **ctrip** | `…ctrip2.json` | **2** | **`REQ-2.2`** / `REQ-2.3.1` |
| 12306 | `…12306-1.json` | 1 | `REQ-1.2` |

→ **合并建议**：`./eval/stage-merge.sh "六个 app 全部非零（阶梯完成）"` + `git push`。

---

## 二、我复核并**成立**的

### 2.1 ctrip 那一分：**整条链的第一次端到端验证**

`REQ-2.2` 从第一轮的"预测失败"到第二轮的"**真判通过**（752ms）"，中间只动了**一处**：
注入 1→7 条 + **落点约束** → 通过数 0→2（Δ=2 = 可辨差异下限，且**含子集那条**）✔
→ 方案的「验收条件 1」在真判分上兑现 ✔（这是"② 抓未覆盖 → 回灌 → 落点约束"这条链的第一次端到端验证）。

### 2.2 裁决四：**一处实现、两处消费**（我核过 import）

```
eval/predict_judge.py:56  # 🔴 一处实现、两处消费（PLAN 裁决四）：可命中性口径搬到 pipeline/verify/hittable.py
eval/predict_judge.py:60  from verify.hittable import hittable_strings
pipeline/verify/hittable.py  ← L1 与 ③ 共用同一份
```
→ 满足"让判据的判据与真判据同源"✔；且 `hittable.py` 里**明确写了为什么排除 `name` 属性**
（"一个只有 `name=…` 的元素会被算成命中，而真判据打不到它"）✔ —— **这是实测结论，不是推理**。

### 2.3 裁决五 + 必修：**位置集合实测钉死 + classify 修复的验收我自己复跑**

```bash
REQS=$(ls …/bookstack/tests/*.spec.ts | sed 's#.*/##;s#\.spec\.ts##' | tr '\n' ' ')
python eval/predict_judge.py bookstack %TEMP%/bookstack1/app $REQS --truth runs/…bookstack1.json
# 我得到的： 合计 451/451（100%）；预测通过 2 / 预测失败 32；TP=32 FP=0 / FN=0 TN=2
```
→ 与它报的 **TP 30→32 / FP 0 / 451/451 完全一致** ✔（原先漏掉的两条被抓住了，精度与召回都到 100%）。
→ 位置集合：PLAN 裁决五那张表与 `hittable.py` 的 `RE_ATTR_NAMED` / `RE_ATTR_FIELD` 对齐 ✔；
   `eval/locator_probe.js`（"最小页面 + 每族 locator 各打一发"）确实存在并被用过 ✔ ——
   **PLAN 要求"不要靠推理"的那一条，执行侧照做了** ✔。

### 2.4 纪律面（这几条做得好，值得记）

- 🔴 **③ 是判分之后才跑的** → 记录里**如实标注"不是预注册命中"** ✔（口径没被悄悄放宽）
- 深度第一刀的判读落在**预注册第 ④ 格**（首阻清了、成员没中）→ 下一步正是 PLAN 写好的"补种子数据" ✔
- 条数 2 < 4（保留门槛）→ **本轮不记账** ✔
- 方案的验收"TP 30→**≥33**"——真值里失败就是 32 条，所以 **≥33 不可达** → 履行侧如实报告 ✔
- `final_l1=False` 的原因（注入形态）写进 `notes` ✔，不会被下游读成"产物没过 L1"
- `bench.sh verdict` = `status=free runners=0 port=free listeners=0`、工作区干净 ✔

---

## 三、建议处理（**都不挡合并**）

### A. 🟡 PLAN 的验收判据"TP 30→≥33"不可达 → 改成"=32（真值上限）"

真值：bookstack 全量 34 条里失败 **32** 条 → `TP=32` 就是天花板（`TP+TN=34`）。
"≥33" 会让下一个人以为"还差一条没修"。**一行改**。

### B. 🟡 字段族的**落点约束文本**里还留着 `name`（而判据已实测排除它）

- `PLAN.md:1400` 与 `f66b849`（brief 里那句）都写"字段族 = 该字段的 **label / placeholder / name**"；
- 而 `hittable.py` 已**实测**"只有 `name=` 的元素会被算成命中、但真判据打不到" → **`name` 不是位置**。

→ 两处文本都应删掉 `name`。理由：模型被允许用 `name`，而检查器不认 → 又是一次"被告知的与判据不一致"
（与刚花掉 52% 的那类同源，只是轻得多）。**一行改。**

### C. 🟢 深度阶段的下一步已在 PLAN 的分支里（不用新决定）

第一刀没有可辨变化（2/34 与 bookstack1 逐条相同）→ **新首阻 = 种子数据**
（L1 的 5 条 `seed_literals` 缺失 / ③ 报 `'Shelf 4.1'` 与 `'Book 5.1'`，回产物核过是真的没做）→
按 PLAN 走"补种子数据"那一支即可；34 分钟的判分轮也印证了 KPI 的推理（往便宜轮走）。

---

## 🔴 必须先改（不满足这条不许合 main）

**本轮无。** → **可以合并、可以 push。**

---

## 四、最高价值的剩余动作（用户侧，与本地无关）

**R1：把 `output/bundle.zip` 传到平台跑一次。** 本地 6/6 只是**本地**闸门；
"榜上有分"由平台那次跑决定（而且同一份提交要跑完 6 个任务才计分）。
提交包已清过红线、且是今日重打包的一版 → **现在就能传**。

---

## 五、复现命令

```bash
# 六个 app 逐个非零（我用的脚本）
python -c "对 6 个 app 各取最新含 tests_total 的记录，打印 tests_passed 与 per_test_passed"
# 裁决四：一处实现两处消费
grep -rn "hittable" eval/predict_judge.py pipeline/verify/l1.py | head
# 裁决五 + 必修的验收（我自己跑的）
REQS=$(ls repos/arc-bench/arc-bench/webapp/bookstack/tests/*.spec.ts | sed 's#.*/##;s#\.spec\.ts##' | tr '\n' ' ')
python eval/predict_judge.py bookstack %TEMP%/bookstack1/app $REQS --truth runs/20260922T120254Z-bookstack-score-bookstack1.json
```

**审核者**：第三十六轮 · 2026-09-24 09:1x（本地）
**本文件是审核会话本轮唯一写入的产物**；未跑任何 bench 子命令（只用只读 `verdict`），
未动 `repos/` / `pipeline/` / `eval/` / `runs/` / `PLAN.md` / `AGENTS.md`。
