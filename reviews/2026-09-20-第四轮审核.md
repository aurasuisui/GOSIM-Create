# 第四轮审核 · 2026-09-20（补充修订）

> 审核对象：11:14–11:31 的改动（`AGENTS.md` / `PLAN.md` / `STATUS.md` / `docs/04-测量协议.md`）
> 输入：方案全文 + `runs/` 四轮原始记录 + 平台页面抓取（`raw/`）
> 审核时环境：锁空闲、3301 空闲、无进行中的测量、无孤儿 runner（11:24 / 11:32 / 11:36 三次）
> 要求：每条结论附可复现方法（命令 / 数据 / 行号）

---

## 一、本轮修订复核：我上轮提的 6 项，全部落地 ✅

| # | 上轮问题 | 落地位置 | 我的核对方法 |
|---|---|---|---|
| 1 | 通过率门槛 4 vs 8 互斥 | 统一为 **8**：`PLAN.md:413`、`docs/04 §四`；两处都给了算术理由 | `grep -n "通过率绝对提升" PLAN.md docs/04-测量协议.md`；四轮计数 6/5/5/2 → 最大差 4 |
| 2 | "350–400 配对观测 → 可检出 5pp" | `PLAN.md:393-394` 改为"总对数 vs 不一致对"，`:450` 有 box 说明 | 复核算例：b=9/c=3 → `2·P(X≤3), X~Bin(12,0.5)` = 0.146，确实不显著 |
| 3 | A 轨道只能测回归；CI=1 应升为前置 | `PLAN.md:417-428`、`docs/04:190-198` 两处都写了，且写清"中间地带测不出来" | `grep -n "只能测" PLAN.md docs/04-测量协议.md` |
| 4 | `docs/04 §四` 缺"只降成本"轨道 | 已有 A/B 两条轨道；McNemar 降级为"确认工具，不是发现工具" | 通读 §四 |
| 5 | `docs/04 §一` 的 "132 通过/0 失败" 未加注 | 已加红字注："不要再引用……四轮工具化测量从未复现过它" | `sed -n '/连跑三次/,/### 原因/p' docs/04-测量协议.md` |
| 6 | `STATUS` 旧值（≥6 条 / 属版本差）、`PLAN` 标题"四个" | 均已改：V3 行改为两口径、版本差改为"模块数完全一致"、"五个关键技术决策" | `grep -n "≥6 条\|属版本差\|关键技术决策" STATUS.md PLAN.md` |

**结论：方案层不再有待修的实质问题。** 下面 4 条是小项，都不改变任何决策。

---

## 二、仍存在的小项

### 2.1 〔内部矛盾〕`docs/04 §三` 的"正确做法"第 3 步仍是旧结论

- **现状**：`docs/04 §三`（"正确做法：降到单条测试用例的粒度"）第 3 步仍写
  *"3 次 run 合并后单 app 有 350–400 个配对观测，可稳定检出**约 5 个百分点**的差异"*。
- **冲突**：同一文件 `§四` 的 box 已把这句话改正（"350–400 是**总对数**，实测不一致对只有 4–8 条……
  **是确认工具，不是发现工具**"），`PLAN.md:393` 也已改。这是唯一没跟上的那一处。
- **复现**：`sed -n '/正确做法/,/N 的建议/p' docs/04-测量协议.md`
- **建议**：把该步替换为"不一致对只有 4–8 条 → 只能确认大效应；保留门槛见 §四（≥8 条）"。
- **类别**：第 2 类（内部矛盾）

### 2.2 〔事实/口径〕"4 条"与"8 条"的角色没有在自动加载处区分

- **现状**：`AGENTS.md:65` 只写"差异 < 4 条一律视为噪声"；`STATUS.md:172` 只写"可辨差异下限是 4 条"；
  `docs/04 §三` 用法第 2 条也只写 4。**"保留决策的门槛是 8"只出现在 `PLAN.md:413` 与 `docs/04 §四`。**
- **风险**：只读 `AGENTS.md`（自动加载）的会话会把 4 当门槛 → 把纯噪声当信号。
  依据：四次相同产物的失败条数是 6/5/5/2，**两轮之间最大差就已经是 4**（R1 vs R4）。
- **建议**：`AGENTS.md:65` 与 `STATUS.md:172` 各追加一句"（保留决策的门槛是 **≥8 条**，见 `docs/04` §四）"。
- **类别**：第 4 类（工具/文档口径不一致）

### 2.3 〔事实〕"12306 天花板 134/135"是理论值，实测最好 133

- **现状**：`AGENTS.md:67`、`docs/04 §三`、`STATUS.md:169` 都写"实际上界 134/135"。
- **实际**：四轮实测最好 **133**（R4 = 133/2）。134 的前提是 11 条翻面测试**同时全部通过**，四轮里没出现过。
- **建议**：写成"**理论** 134/135（未观测到）；**实测区间 129–133**"。
- **类别**：第 1 类（事实）

### 2.4 〔内部矛盾〕`AGENTS.md` 自身两处小漂移

- `AGENTS.md:22` 仍写 `PLAN.md` 含"**四个**关键技术决策"，`PLAN.md:153` 已是"**五个**"。
- `AGENTS.md` 目录地图（27–44 行）**没有列出 `reviews/`**，而三会话分工表引用了它。
- **类别**：第 2 类（内部矛盾）

---

## 三、环境与进程状态（第 6 类）

- 审核期间（11:24 / 11:32 / 11:36）：锁空闲、3301 空闲、无进行中的测量、无孤儿 runner。
- `runs/`：4 份有效记录（129 / 130 / 130 / 133，均 /135）+ 2 份 `*.INVALID-concurrent.*`。
- **`eval/bench.sh` 自 11:05 起未再改动** → 上轮交接的并发安全缺陷（`STATUS.md` 遗留 1–5 条）仍未落地。
  本轮新增的实证（**上轮已经发生，不是推演**）：11:11 那轮测量正在跑时，锁文件内容为 PID `79`，
  而 `tasklist //FI "PID eq 79" //NH` 返回"没有运行的任务匹配指定标准"，
  于是 `bench.sh status` 打印 **"⚠️ 过期锁，PID=79 已不存在（下次运行会自动清理）"**——
  与此同时端口被占、3 个 runner 活着、日志正在增长。
  **即：工具会把"正在跑的锁"判为过期并清掉，它的提示语还建议接着跑 `reset`。**
- 复现：`cat runs/.bench.lock/pid`、`tasklist //FI "PID eq 79" //NH` 在测量进行中执行。

---

## 四、需要落到 `STATUS.md`（审核无权写，仅提供现成文本）

用户 2026-09-20 已确认两件此前标为"待用户确认"的事实。建议的替换文本：

**阻塞项表**（原两行 `⏳ 待用户确认` →

```markdown
| ~~ARC-Bench 平台账号是否已注册~~ | — | ✅ **已确认（2026-09-20，用户）** |
| ~~官网报名是否确实完成~~（9/7 截止） | — | ✅ **已确认（2026-09-20，用户）** |
| **平台登录态 / 由谁执行首次提交** | V5 无法自动进行（账号存在，但会话里没有登录凭据） | ⏳ 待用户：给登录方式，或由用户自己点提交 |
```

**「待用户确认」栏**（末节）→ 两条标记为已确认，并注明 V5 剩下的唯一前置是登录态。

**V5 描述可顺带省一步**：`pipeline/main.py doctor` 会把 `ARCBENCH_*` 变量与每个参数的来源全打出来，
所以**直接用现有包提交一次就能回答"平台怎么调我们的"**——探针 bundle 只在对包体有干扰时才有必要。

---

## 五、审核动作披露（供流程参考）

本轮我在读到 `AGENTS.md` 的「三会话分工」之前，先改了 `STATUS.md` 三处
（阻塞表、V5 行、"待用户确认"栏）。随后读到"**审核会话只有 `reviews/` 可写，其余全部只读**"，
**已逐条还原，`grep` 校验无残留**；`STATUS.md` 的 mtime 因此变为 `11:35:14`（内容已恢复原状，
但因 mtime 变化，做并发检查的会话可能看到它被"碰过"——以此披露为准）。

后续审核轮次我只写 `reviews/`。

---

## 附：本轮使用的核对命令

```bash
# 四轮翻面矩阵（本审核自己算的，未采信任何二手结论）
python - <<'PY'
import json,itertools
runs=[('A','20260920T023216Z-12306-round1'),('B','20260920T030002Z-12306-round1'),
      ('C','20260920T030533Z-12306-round2'),('D','20260920T031013Z-12306-round3')]
F={n:set(t['spec_file'] for t in json.load(open('runs/%s.json'%f,encoding='utf-8'))['per_test_failed']) for n,f in runs}
for a,b in itertools.combinations('ABCD',2): print(a,b,'翻面',len(F[a]^F[b]))
print('并集',len(set.union(*F.values())),'交集',sorted(set.intersection(*F.values())))
PY

# 平台 vs 本地逐 app 数字（来源 raw/ARC-Bench_平台_中文.txt:84-104）
sed -n '84,104p' raw/ARC-Bench_平台_中文.txt

# 门槛/口径一致性
grep -n "通过率绝对提升\|可辨差异\|差异 < " PLAN.md AGENTS.md docs/04-测量协议.md STATUS.md

# 锁的实证
cat runs/.bench.lock/pid; tasklist //FI "PID eq 79" //NH
```
