# 第三十二轮审核：审计文件被误删（已恢复）+ 用户新指令"不看日期"的影响面

> **审核对象**：第三十一轮审核（`976c0c8`）之后的现场状态 + 用户 2026-09-23 的新指令
> **"从现在开始计划不要看日期了，越快完成越好"** 对 `PLAN.md` 的影响面。
> **方法**：逐条复核 31 轮的关键声明（我自己跑）；现场异常查证。

---

## 一、现场异常（已处置）：一份**已提交的审核文件在工作区里被删了**

```
git status  →  D "reviews/2026-09-23-第三十一轮审核.md"
976c0c8 第三十一轮审核：ctrip 0/125（阶梯 4/6）…        ← 提交里有
ls reviews/2026-09-23-第三十一轮审核.md                 ← 工作区没有
```
- **性质**：审计痕迹在工作区丢失（`976c0c8` 提交于 18:52，3 分钟后我查到它已被删；
  **没有任何替代文件**，`git status` 里也没有 `??`）。**原因未知**——两个会话都做过"压缩上下文"，
  不排除是某次清理/move 的副作用。
- **处置**：`git restore` 恢复（`reviews/` 是审核会话的可写范围，恢复已提交的审核产物属正当动作）；
  恢复后 `git status` 干净 ✔。**内容我核过**：它记录了 ctrip 0/125 的读数、③ 三版口径的复跑标定、
  以及三条悬着项（回灌决定 / 复合 pattern 契约 / 9/24 预注册）——与我在下面独立复核到的一致。
- ⚠️ **建议**：以后压缩上下文/清理时，**别碰 `reviews/`**（它是审计线索）；
  若确实要动，用 `git mv` 并提交，别留一个未提交的 `D`。

---

## 二、我复核并**成立**的（31 轮的三条关键声明）

### 2.1 上轮两条 🔴 **按我实测的数字逐字落地**

| 位置 | 内容 |
|---|---|
| `AGENTS.md:266-269` | "**记录里的 `git_commit`：可达 0 / 不可达 21 / 无指纹 15**……两次重排（9/22、9/23）之后，所有旧记录的 `git_commit` 都不可达" ✔ |
| `AGENTS.md:212-219` | 判据改成"**审核里没有标注为「必须先改」的问题**"；打标格式固定为 `## 🔴 必须先改（不满足这条不许合 main）`；"**未被标为「必须先改」的（含 🟡 / 🟢）不挡合并**；审核会话从此轮起照这个格式写" ✔ |

→ **第三十轮的 🔴 已闭合**（那两条不再挡合并）。

### 2.2 `git_subject` 落地并在真记录里生效

`runs/20260923T094815Z-ctrip-score-ctrip1b.json` 的 `pipeline` 块：
`{git_commit: 90d761af…, git_dirty: False, pipeline_dirty: False, git_branch: 'work',
**git_subject: 'STATUS：认领判分队列（prestashop n=2 → ctrip）+ 本轮在飞说明'**, attests: …}` ✔
→ 以后 hash 被重排换掉时，**subject 会成为可复核的那一环** ✔

### 2.3 ctrip 的读数与判读

`tests_total=125 / tests_passed=0 / per_test_passed=[]`；`predicted` 里 `REQ-2.2` 预测 **fail**，
`missing_units: ['cl…']` ✔；`notes` 明写"这是**第一个没拿到非零**的 app"、"**不是「跑了但没分」，
是子集那条自己就挂了**" ✔ → 阶梯停在 **4/6**（keep / bookstack / stackoverflow / prestashop）✔

### 2.4 ③ 三版口径的重构**没把判据改松**（我自己复跑）

```
bookstack 全量 34 spec：覆盖率 451/451（100%）；预测通过 4 / 预测失败 30；
                        矩阵 TP=30 FP=0 / FN=2 TN=2  → 与重构前逐格相同 ✔
```
→ "结果不变" 的声明成立 ✔（这条要紧：③ 是 ctrip/12306 都要用的筛，改松了会把"必挂"放行）

---

## 三、用户新指令的影响面（**"不看日期，越快完成越好"**）

| 受影响处 | 现状 | 该怎么改 |
|---|---|---|
| `PLAN.md` §7 的 **9/24 决策规则**（`904` 起那一整块，含 `B`/`U`/余量/三分支） | 按"9/24 那一刻算两个数"写的 | **整块作废**——没有日期就没有"那一刻"。改成"**完成即止**"的运行规则：**只要还有 app 没非零就继续铺**（成本升序不变），**铺满 6/6 之后再谈深度** |
| §7 的 **阶段表日期列**（`9/26` / `9/30 上午`）与「**日历现实**」块（`885` 起，含"剩 N 天"） | 已与运行方式脱钩 | 删日期或改写成"**闸门驱动，完成即止**"（阶段定义本身不删 ✔） |
| **9/24 预注册**（31 轮列的悬着项 C） | 还没写 | **取消**（不要再写它）——它存在的唯一理由是那个决策点 |
| §1 的"远期 Top20 / 近期 榜上有分" | 不是日期语言 | **保留**（它是目标口径，不是排期） |
| 阶梯 ① 的排序理由 | "若时间不够，已拿到的分最多" | 保留排序（成本升序仍然是"最快到 6/6"的算法），把"时间不够"的措辞换成"**任何时候中断都拿到最多**" |

**这一改其实让 PLAN 更简单**：原先"铺广度 vs 做深度"要按日期触发；现在只剩一条
——**先把 6/6 拿到（那是在榜上的前提），再谈深度**。保底退路（§7）照旧，但不再是"日期到了才启用"。

---

## 🔴 必须先改（不满足这条不许合 main）

**本轮无。**

（31 轮已判"本轮无"；我复核后同意。阶梯 4/6 是**闸门未达**，不是审核挡着——
所以"要不要合 main"此刻的正确回答是：**阶段还没跑完，先别合**，不是"审核有问题"。）

---

## 四、下一步（含"越快"）

**关键路径现在是两条并行的线**，而 ctrip 那条**被方案会话挡着**：

1. **`12306`（未阻塞，最大的一块墙钟）**：② 已做完（子集 `{REQ-1.2}`、**无需注入**）→
   生成 + 判分（135 条 → 最坏 135 分钟）**现在就能开**。它是最后一块"未拿到的非零"里**不需要等任何人**的那个。
2. **ctrip（被挡）**：机制已钉住（判据要**复合 pattern**、产物只写片段）→ 修法就是方案会话要拍的
   "③ 的 unreachable 回灌硬清单"（含表示形态：**一个元素的可访问名必须含这一串、按序**）。
   **拍 → 重跑 → 6/6**。
3. **R1（用户侧）**：与本地判分完全可并行，且 `bundle.zip` 已是今日重打包的一版。

---

**审核者**：第三十二轮 · 2026-09-23 18:55–19:0x（本地）
**本文件与恢复的第三十一轮审核文件是审核会话本轮的全部产物**；未跑任何 bench 子命令，
未动 `repos/` / `pipeline/` / `eval/` / `runs/` / `PLAN.md` / `AGENTS.md`。
