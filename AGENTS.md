# GOSIM 智能体软件工厂黑客松 · 工作区

本文件会在**每个会话开始时自动加载**。它只做一件事：把会话指到正确的文件。

## 一句话

为 2026 OAIC 智能体软件工厂国际黑客松开发参赛 agent（自研薄管线），把多模态需求树编译成可运行的 Web 应用，由锁定的外部 Playwright 测试评分。

---

## 🛑 动手前先看这三条（违反会直接造成损失，细节见后文）

1. **先认领你的角色，再动任何文件。** 三个角色一个会话，写权限不同：
   **方案**写 `PLAN.md` / `AGENTS.md` / 设计类 `docs/`；**执行**写 `pipeline/` / `eval/` / 实测类 `docs/`；
   **审核只写 `reviews/`，其余全部只读。**
   （发生过：审核会话读到这条规则之前先改了 `STATUS.md`，之后自行还原。**规则在文档中段 = 等于没有。**）

2. **动测试之前先 `./eval/bench.sh status`。** 有锁、端口被占、或有 runner 活着 → **不要跑任何 bench 子命令**（包括 `reset`、`stop`）。
   并发会让两轮互相重置数据库，产出两份都不可信的结果——**已经作废过两次 run**。

3. **方案在一个执行阶段内冻结。** 阶段进行中不改 `PLAN.md`；调整只在检查点落地（见 `PLAN.md` §7）。
   唯一例外：发现"照做会白丢分"级别的错误 → 立即修并通知执行会话。

---

## 死线

| 日期 | 事件 |
|---|---|
| **9/21–9/30** | **初赛**（唯一入口，线上） |
| 10/1–10/7 | 决赛（考增量编译） |
| 10/17 | 颁奖（深圳 GOSIM 大会） |

**排行榜 Top 20 进决赛。主赛道计分要求同一份提交跑完全部 6 个任务。**

## 会话开始先读这三个

1. **`STATUS.md`** — 活的进度板。当前做到哪、下一步是什么、卡在哪。**每次会话结束必须更新它。**
2. **`PLAN.md`** — 方案的单一事实来源。架构、五个关键技术决策、复用清单、**里程碑 M1–M6（闸门驱动，不按日期推进）**、风险清单。
3. `README.md` — 赛制与平台信息（从官网抓取，标注了时间）。

需要深入时再按需读 `docs/`（见下）。

## 目录地图

```
AGENTS.md      ← 本文件，自动加载，会话入口
STATUS.md      ← 活的进度板，每次会话结束后更新
PLAN.md        ← 方案，决策的依据
README.md      ← 赛制与平台信息（官网抓取）
ACCOUNT.md     ← 参赛账号信息，凭据位置
.env           ← API key，绝不提交

docs/          ← 调研结论落盘（为什么改、改哪里、有什么坑）
pipeline/      ← 提交物：自研薄管线（要交付的代码）      ← 执行会话可写
eval/          ← 评测脚手架：起参考实现、跑基准、记录结果  ← 执行会话可写
runs/          ← 实验结果记录（每个 arm 一行）
reviews/       ← 审核产出，按轮次命名                    ← 只有审核会话可写

repos/         ← 上游仓库，只读参考，不修改
class/  raw/  screenshots/   ← 存档（转录稿、官网抓取、截图）
```

### docs/ 索引

| 文件 | 内容 | 什么时候读 |
|---|---|---|
| `docs/01-ARC源码解剖.md` | ARC 参考实现架构、三个硬伤、可复用的零件在哪 | 要做设计决策前 |
| `docs/02-提交契约与打包.md` | 提交 zip 的规则 + 生成应用必须满足的 C1–C10 契约 | 打包前、写生成器前 |
| `docs/03-需求树与队列.md` | 需求 DSL 结构、队列状态机、调度分析 | 写需求解析器前 |
| `docs/04-测量协议.md` | **强制操作纪律**：DB 重置、噪声底线、统计方法 | **每次跑实验前必读** |
| `docs/05-成本与模型路由.md` | 成本结构、16 个可用模型、分层路由方案 | 调模型配置时 |
| `docs/06-需求契约实测.md` | **实测出来、与方案原假设不符的事实**（坏 YAML、可访问名句式不统一、标题契约 1:1、六 app 召回抽检） | 写需求解析器前 |
| `docs/07-依赖已知可用版本集.txt` | `pip freeze` 快照（R5 收口）：平台/本地依赖漂移时用来定位，必要时回钉 | 依赖出问题时 |
| `docs/08-V5-提交操作指导.md` | 提交平台的**操作步骤**（点什么、填什么、怎么核） | 每次提交前 |
| `docs/09-平台提交与报错记录.md` | **平台报错原文 + 诊断 + 平台侧事实**（调用 argv/环境变量/输出目录、平台自带模板、构建与起服务、榜单） | 提交失败时、改入口契约前 |
| `docs/10-M3a能力探针.md` | **生成能力有没有？**（1 需求 / 6 判据 / 3→6 通过 / 上游模板 bug / reasoning 占 87% 成本） | 改生成阶段、做闸门前 |
| `docs/11-M3b0自动化闭环.md` | **闭环机器化**：L1 静态闸门 + 模型自检 + 定向修复；零人工 6/6 与 held-out 判据 | 改验证/修复链路前 |
| `docs/12-M3b1模型成本标定.md` | **选型与成本口径**：三个旋钮实测；`reasoning_effort` 与便宜模型都会掉通过率；口径是 token/通过条数 | 铺开到 6 任务前 |

## 硬规则（违反会毁掉实验或触犯赛规）

### 操作纪律

1. **每次测量前必须重置数据库并重启后端**。DB 状态会在运行之间累积，实测能让同一份冻结产物从 0 失败变成 15 失败。见 `docs/04-测量协议.md`。
2. **每轮实验之间用 `--clean` 重建工作区，不要用 `--resume`**。resume 复用队列和已有代码，不是独立重复。
3. **固定 `ARC_TEST_DATE`**。测试是日期相关的。
4. **一次只改一处**，且改动要能用一句可证伪的话描述。
5. **噪声底线 —— 先分组再算，混口径会算错**（权威值在 `docs/04` §三；实测记录 `runs/20260920T-quiet-seq-README.md`）：
   - 🔴 **只在静置条件下比较**：`load_snapshot` 必须 `status=free`（或 `free-leftover`）**且** `runners=0`。
     **含污染轮次的比较一律无效**，不要下结论。
   - 失败**条数**波动，**静置组 0–2（跨度 2）**；污染组（R1–R4，并发事故窗口）2–6（跨度 4）。
     → **可辨差异下限 = 2 条**；**(保留一个改动的门槛) = 4 条**（= 2 × 静置跨度 2）
     ⚠️ 自我修正：静置样本只有 4 轮，偏薄。**跨度若涨到 ≥3 → 门槛退回 6；≥4 → 退回 8。**
   - 会**翻面**的测试有 **11 条**，每轮随机挑 0–2 条失败（静置）→ per-test 对比看**成员变化**，不是条数
   - ⚠️ **`REQ-4.3.7` 不是稳定失败**——R5 通过了。**零稳定失败**、124 条稳定通过。
     **"理论天花板 134/135"已作废**：实测区间 **129–135**，满分**可达但不稳定**。
     → 结论从"满分不存在"改成"满分不稳定"：**仍然不要追 100%**，但**不要再写"天花板是 134"**。

### 赛规红线

6. **不修改 `repos/` 里的任何仓库**。那里是只读参考，改动只发生在 `pipeline/`。
7. **提交包根不能有 `package.json` / `index.js` / `index.ts`**（会触发 runner 的 Node 入口路径）。
8. **zip 第一层必须直接是 `main.py`**，不能多套一层目录。
9. **不得预埋答案**：模板里零业务语义；全仓不能出现题目特定字符串。上传代码会被二次审查。
10. **`.env` 绝不提交**；API key 不写进任何会被提交或被引用的文档。

### 资源纪律

11. **成本是排序口径**（`pass/CNY`），不是附属指标。任何设计决策先问省不省 token。
12. 批量生成用便宜档模型（`glm-5.3-flash` / `deepseek-v4-flash`）；**不要用 `qwen3.8-max` 做批量生成**（基准里最贵一档）。

### 🔴 读数与实验纪律（2026-09-21 加入，每条都对应一次已发生的损失）

13. **「零人工」是唯一读数口径。** 手工补过一个属性、改过一行接线之后的读数**必须标注**，
    **而且不进任何闸门判据**。
    > 本方案有过**两次"6/6"，两次都不是零人工**（一次手工修 3 处接线，一次手工补一个 `aria-label`）。
    > **零人工最好读数 = 5/6。** 一次口径混淆曾经推翻过方案层两个结论。
14. **静态优先：能用零 token 静态判的规则，不许用生成去验。**
    > 9/21 一天约 **97 万 token、零人工满分 0 次**，而三次跑的失败**全部是静态可判的**
    > （`<meter>` 缺 ARIA 名 / ESM-CJS 混用 / 少一个右括号）——**用生成去验静态规则是最贵的浪费形态**。
15. **"判据没报错" ≠ "判据跑过了"。** 每个闸门都要留一条"判据确实执行了"的证据。
    > `gate0_run.sh` 的路径 bug 曾让判据①在三次跑里**静默失败**，而三次都"看起来正常"。
16. **新检查接进闸门之前，先在历史产物语料上跑**（`%TEMP%` 下已有 6+ 份），
    看它是"又宽又松"还是"又紧又假"。
    > `check_db_tables` 的假阳性让闭环**一直在修不存在的问题**（每条假发现 = 一次全量重发，闭环占 65% token）；
    > `check_js_balance.py` 指向装过依赖的目录时**扫 7616 文件、报 322 条误报**。
17. **下"某东西不在文本里"这种结论之前，必须按标准动作查**：
    **`grep -i`（大小写不敏感）+ 同时查 `.yaml` 与 `.md` + 记下行号**。
    > **同型错误已经发生两次**（`Toggle sidebar`、`Note editor`），两次都是拿
    > **大小写敏感**或**只搜引号式**的 grep 下了结论，然后据此排优先级
    > （第一次指向"该投视觉通路"，白排了一轮）。**一句 `grep -i` 就能省掉一整轮。**
    > 反过来也成立：**"抽不到"要用抽取器本身验**（把原句喂给 `_extract_from_text()` 看输出），
    > 不要从 grep 结果推。

## 会话协议

**开始**：读 `STATUS.md` → 按"下一步"开工。**但两个文件各管一半，冲突时按下表判：**

| 冲突的是什么 | 以谁为准 | 为什么 |
|---|---|---|
| **进度**（做到哪、下一步、卡在哪） | **`STATUS.md`** | 它更新 |
| **阶段定义**（有哪些阶段、各阶段的闸门） | **`PLAN.md` §7** | 阶段是方案会话定义的；`STATUS.md` 只是**对着它记进度** |

> ⚠️ **这条改过，原因是踩过一次**：旧规则写"里程碑冲突以 `STATUS.md` 为准"，
> 于是 `PLAN.md` 新增 M3a（能力探针）之后，**只读 `STATUS.md` 的会话会直接跳过它**——
> 而那一步是全计划信息量最高的测量。
> **只读 `STATUS.md` 不足以知道"现在该做哪些阶段"**，必须回到 `PLAN.md` §7 的阶段表。

**结束**：更新 `STATUS.md` 的这几栏——已完成、进行中、下一步、阻塞、最近实验结果。**不更新就等于这次会话的成果丢了。**

**原则**：会话里得出的、非显而易见且不在代码里的结论，写进 `docs/` 或 `STATUS.md`。能重跑的实测数据写进 `runs/`。宁多写几行，不要让下一个会话重新推导。

### 提交纪律（工作区已是 git 仓库）

`2026-09-20` 初始化，根提交 `88f27b6`。

- **每个"一次只改一处"的改动落一个 commit**，message 写**为什么改**（改了什么看 diff）
- 这样 RunRecord 的 `git_commit` / `git_dirty` 才有意义，坏改动才回得去
- 🔴 **`.gitattributes` 强制脚本 LF**：`eval/bench.sh` 的 shebang 一旦带上 `\r` 会失效
  （报 `bad interpreter`，而且看起来像"脚本坏了"，很难一眼归因）。
  **不要把 `core.autocrlf` 设成 `true` 去覆盖它**
- 已被忽略、**不要**加进来：`repos/`（802M 上游克隆）、`.venv/`（145M）、`.env`（凭据）、`output/`（可再生）
- **`runs/` 只跟 RunRecord**（`runs/*.json` + `runs/README.md`）——小、被方案与 `docs/` 引用、且含 `git_commit` 链接。
  **原始日志与逐次用量（`.log`/`.jsonl`/`.txt`/`.out`）不入库**（体量大、只增不减）。
  > ⚠️ **后果要记住**：RunRecord 里的 `log_path` **指向的文件不随 git 走**。
  > 换台机器或 clone 下来之后，**复核者看不到原始输出，要自己重跑**。
- **外围存档不入库**：`raw/`（官网抓取）、`class/`（转录稿）、`screenshots/`——一次抓取后即冻结，无版本历史价值。
  （`README.md` 是 `raw/` 的摘要；`class/` 的转录用前必须核对，见下文 ASR 陷阱）
- ⚠️ **`.gitignore` 不支持行尾注释**：`raw/   # 说明` 会被整行当成模式而**静默失效**。注释必须独立成行

---

## 三会话分工与检查点

**这个项目按三个角色推进，每个角色一个会话。** 分工的理由不是理论——上一轮我同时做方案、执行和自查，
自己的量纲错误一路传了下去；而独立审核抓到了五类我看不见的问题。**关键在审核者不知道方案的推理过程**，
否则会顺着原作者的假设往下想。

| 角色 | 职责 | 可写 | **不可写** |
|---|---|---|---|
| **方案** | `PLAN.md`、`AGENTS.md`、设计类 `docs/` | 这三处 + `STATUS.md` 的认领栏 | `pipeline/`、`eval/`（代码） |
| **执行** | `pipeline/`、`eval/`、实测类 `docs/` | 代码 + 实测记录 + `STATUS.md` 的进度栏 | `PLAN.md`（只在检查点改） |
| **审核** | 复核方案与执行结果 | **只有 `reviews/`** | **其余全部只读** |

审核会话**除自己的 `reviews/` 外一律只读**。这一条最重要：审核的产出是"结论"，
不该和方案、代码混在同一批文件里，否则下一轮谁也说不清哪个数字被谁改过。

> **例外（不算越界）**：`STATUS.md` 的「在飞工作」栏现在由 **`bench.sh` 自动维护**——
> 跑 `test` / `reset` 时自动写认领行（`BENCH_CLAIMED` 防重复），退出时清理。
> 该栏在定义上属**方案**的可写范围，但写它的是工具，所以不要把它读成越界。
> ⚠️ 这个自动认领是对 `STATUS.md` 的**读-改-写**，有并发编辑者时存在极短窗口的覆盖风险；
> **✅ 已补（2026-09-20）**：同时往 `runs/.inflight.log` 追加一行——**只追加、从不修改**，
> 所以没有任何覆盖风险。`tail -5 runs/.inflight.log` 可查"这一轮谁认领过"。

### 分流规则：不是所有事都走完整回路

10 天里走不完太多轮完整回路（执行→审核→用户→方案→调整）。按"正确值是否唯一确定"分两条道：

| 走完整回路（需审核或用户拍板） | 直接修，不等回路 |
|---|---|
| 改动方案里的**决策**：路线、闸门设计、判据、评分口径 | 事实性错误、失效引用、笔误 |
| 改动**测量方法**：噪声底线、统计口径、实验协议 | 文档内部不一致（同一数值多处不同） |
| 任何**不可逆**动作：提交、封包 | 工具与文档不一致（把判据补进工具） |
| 改动**跨阶段排期** | 脚本 bug、进程/端口问题 |

右列的共同点：**正确值是唯一确定的，不需要谁拍板。** 让它们等回路是纯浪费。

### 冻结窗口

**方案在一个执行阶段内冻结，调整只在检查点落地**（检查点定义见 `PLAN.md` §7）。
阶段进行中不改 `PLAN.md`。

> 已发生过一次漂移：方案会话改 `§4.1`（可访问名那节）时，执行会话的 `a11y.py` 已经写完。
> 这次是运气好——实现恰好和修正后的结论一致。若结论反了，那段代码就白写了。

**唯一例外**：发现"照做会白丢分"级别的错误时立即修，不必等检查点（并立刻通知执行会话）。

### 审核清单（固定六类，避免每轮覆盖漂移）

审核会话按这六类走，覆盖才稳定：

1. **事实性错误** —— 数字、口径、引用与实际不符
2. **内部矛盾** —— 同一概念在多处取值不同
3. **前提被证伪** —— 正文结论已被新数据推翻但仍在
4. **工具与文档不一致** —— 判据只写在文档里，没写进工具
5. **纪律照做会坏事** —— 按方案执行会丢分或毁实验
6. **环境与进程状态** —— 残留进程、并发、锁、未完成产物

**给审核会话的输入**：方案全文 + 执行产物 + `runs/` 里的原始证据。
**要求**：每条结论给出可复现的方法（命令、数据、行号），不接受"看起来不对"。

---

## ⚠️ 多会话并发（这个工作区会被多个会话同时操作）

上面的协议只管**顺序**接力。但并行会话是常态，而且已经出过一次事故。

### 已发生的事故（2026-09-20 实测）

两个会话在 21 秒内先后跑了 `bench.sh test`。第二个的 `reset` 按"谁占着端口"把第一个的后端换掉了，而第一个的 runner 不占端口、没被杀，于是**它脚下的数据库被重置、后端被换掉，却继续跑完**。

结果：两份日志都长得像正常结果，两份其实都不可信。已被标记为 `runs/*.INVALID-concurrent.*`。

### 硬规则

1. **动测试之前先看有没有人在跑**
   ```bash
   ./eval/bench.sh status      # 看锁、端口、游离 runner、未完成的 log
   ```
   有锁或端口被占 → **不要跑任何 bench 子命令**（包括 `reset` 和 `stop`）。

2. **`eval/bench.sh test` 是全局独占资源。** 同一时刻只允许一个会话跑。锁会挡住并发，但**不要靠锁兜底**——被挡住是浪费，不是保护。

3. **改文件前先看 mtime** 判断有没有别的会话在动同一片区域：
   ```bash
   find . -newermt "-10 minutes" -type f -not -path "./repos/*" -not -path "*/__pycache__/*"
   ```

4. **认领工作要写进 `STATUS.md` 的「在飞工作」栏**，结束会话时清空。没写就等于没认领，别人会重复做或覆盖。

5. **`runs/` 里的 LogRecord 只有带 summary 才可信。** 没有 `N passed` 汇总行的日志是中断产物，不要拿它下结论。`extract_run.py` 会自动打 `WARNING`。

### 已知的机制缺陷（发现即修，别绕过）

- `status` 报的"游离 runner"**无法区分孤儿和正在跑的**。如果 `status` 显示有游离 runner 而端口也被占用，那很可能是**当前这一轮正在跑**，不是孤儿。此时千万不要"清理"。
- **锁的过期判定是「四条件」，不是"PID 死了就清"**：PID 已死 **且** 无 runner **且** 端口空闲 **且** 锁年龄 > 2h
  （可用 `BENCH_LOCK_STALE_SECS` 覆盖）。**"是否在跑"看心跳（每 20 秒 touch）不看 PID**——
  PID 这条链路单独用会误判（实测：测量正在跑时 `status` 曾打印"过期锁"）。
- 🔴 **崩溃后可能卡死，而且现在只能手工恢复。** 会话被强杀（trap 没跑）时会留下：
  锁残留 + 心跳停 + PID 死，**但 `nohup` 起的后端仍占着 3301** → `port_busy` 成立 → `acquire_lock` **永久拒绝**。
  而 `start` / `stop` / `reset` / `test` **全部要拿锁** → **常规子命令全部不可用**。
  **恢复步骤**（**必须先在 `status` 里确认没有人在跑**）：
  ```bash
  ./eval/bench.sh status                  # 看结论行；确认不是"正在跑"
  netstat -ano | grep ":3301" | grep -i listen    # 找到占用者 PID
  taskkill //F //PID <PID>
  rm -rf runs/.bench.lock
  ```
  → **`bench.sh recover` 子命令待补**（把上面两步收进工具）；见 `STATUS.md` 遗留。

## 非显而易见的事实（跨会话易丢失）

- **环境**：Windows + Git Bash。Node v24.15.0 / npm 11.19.0 / Python 3.13.11 已就位。**本机没有 Docker**，所以 `npm run reference` 不可用，必须用 `eval/` 里的直接启动路径。
- **网关**：`https://api.arc-bench.com/v1`，16 个模型可用。**计费包含 reasoning token**——实测一次极简调用 106 token 里有 16 个是推理。
- 🔴 **平台怎么调我们（V5 实测，2026-09-20；权威记录 `docs/09`）**：
  `python3 /workspace/submission/main.py <需求目录> --output-dir /workspace/template`
  —— 需求是**位置参数**且为**目录**，输出用 **`--output-dir`** 长选项；
  **输出目录 `/workspace/template`** 同时是 project_dir/template_dir，
  `ARCBENCH_RUNNER_EVENTS_PATH` / `ARCBENCH_TRACEABILITY_DIR` 指向它下面的 `.arc/`；
  平台注入 `MODEL` / `VISUAL_MODEL`；容器里是 **Python 3.12**（本地 3.13）。
- 🔴 **平台对每次 run 的第一道闸是"必须产出 `frontend/` + `backend/`"**：
  缺了报 `web template is incomplete: expected frontend/ and backend/ directories`，
  六个任务全挂在这一条上。落地逻辑在 `pipeline/generate/scaffold.py`。
- 🔴 **平台自带模板**（`/opt/arcbench/templates/web-react-express`），并且**平台自己 npm install + build 前端、起服务**
  （实测 frontend 装 290 包/16 秒）。→ **agent 里不要 npm install / build**（agent 跑起来之后没有网络）。
- **看到"六个任务报同一个错"→ 查公共环节**（骨架、构建、起服务、入口契约），
  不要去翻某个 app 的需求。两次提交都是这个形态。
- 🔴 **0 通过 = 不上榜 = 与"没提交"等价。** 提交二把 6 个任务的 86 条测试**全部跑起来**了、
  平台每一步都成功，但 **0/86 通过 → 六任务各 0.0% → 不在榜上**。
  → **地板不是"能跑完"，是"至少有一部分功能可用"。** 这条修正了 `PLAN.md` §4.3 原先写的"测试全红没关系"。
- **榜单分两级**：Senior（平均通过率 **≥80%**，展示全部指标）/ Junior（<80%，只展示排名+用户+通过率+覆盖度）。
  **进 Junior 就是"有分"。**
- ⚠️ **前 6 名里多个队伍 `token ≈ 0` / `runtime ≈ 0` 却拿 80–100%**——计量口径存在我们还不理解的机制。
  **在搞清之前不要按榜单调整成本策略**（见 `PLAN.md` §8 R10）。
- **六个赛题里只有 `12306` 带参考实现**，可作为开发期的 oracle；其余五个只有需求和测试。
- 🔴 **`bookstack` 与 `keep` 的需求文件不是合法 YAML**（`ScannerError`，单个键比同级兄弟多缩进 4 格）。ARC 的加载器是裸 `yaml.safe_load`（`core/files.py:14`），会直接抛异常。**后果：主赛道 6 个任务里有 2 个拿不到完成记录，而计分要求全部完成。** 我们的解析器必须容错（`pipeline/reqcompile/yamlrepair.py`）。详见 `docs/06` §一 与 `PLAN.md` §2。
  ⚠️ **但"保底包 = ARC + 输入修复壳"这个框架已过时**：实测（2026-09-20）跑通平台的是**自研管线**（第 1 阶段 + `pipeline/generate/scaffold.py`），**ARC 至今一次都没跑过**。ARC 降级为防线，不是主线——见 `PLAN.md` §7 M2。
- 平台标注 468 模块 / 484 测试，与本地实测**逐 app 模块数完全一致**，只有测试数差 +6（本地 478）。**不存在"版本差 +8 模块"**——那是把平台的"全部叶子"和本地的"有场景叶子"当成同一个量了。
- **课程转录稿 `class/*.txt` 有大量 ASR 错误**（把 ARC 识别成"二/arc"、Octos 识别成"office/奥克斯"）。引用前必须用 `repos/` 里的真实代码核对。已知被证伪的说法：预算"10 次"（实际 5）、"撞预算进半通过"（实际直接判 FAILED）。
